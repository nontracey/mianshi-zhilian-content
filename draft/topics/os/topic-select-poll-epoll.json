{
  "id": "os.io-model.topic-select-poll-epoll",
  "domain": "os",
  "category": "io-model",
  "group": "io-model",
  "title": "select/poll/epoll",
  "summary": "深入理解三种 IO 多路复用机制的实现原理、性能差异和适用场景，掌握 epoll 的事件驱动模型",
  "tags": [
    "操作系统",
    "IO 多路复用",
    "select",
    "poll",
    "epoll",
    "Reactor"
  ],
  "difficulty": 4,
  "estimatedMinutes": 28,
  "order": 20,
  "recommendWeight": 95,
  "learningCards": [
    {
      "type": "explain",
      "title": "select/poll 的三个老问题",
      "content": "三者都是“一个线程同时盯多个 fd”的多路复用，理解 epoll 先得看清 select/poll 差在哪。\n\n## select\n- 用 `fd_set` **位图**表示关注的 fd，每次调用都要把整个位图从用户态**拷到内核**。\n- 内核**遍历所有 fd** 逐个查就绪状态；返回后用户还要**再遍历一遍**才知道哪个就绪。\n- 受 `FD_SETSIZE` 限制，默认只能盯 **1024** 个 fd；且每次调用都要重设位图。\n\n## poll\n- 用 `pollfd` 数组代替位图，**去掉了 1024 的数量限制**。\n- 但本质没变：仍要**整个数组拷进内核**、内核**仍 O(n) 遍历**、返回后**仍要遍历**找就绪。\n\n## 三个共同痛点\n1. 每次调用都重复**拷贝**全部 fd 进内核；2. 内核每次都 **O(n) 遍历**全部 fd；3. 返回后用户还要 **O(n) 扫一遍**。连接数一大、活跃的又少时，绝大部分遍历都白做。"
    },
    {
      "type": "explain",
      "title": "epoll 为什么高效：红黑树 + 就绪链表 + 回调",
      "content": "epoll 用三个机制一次性解决上面三个痛点：\n\n- **fd 只注册一次**：`epoll_create` 建实例，`epoll_ctl` 把 fd 注册进内核的一棵**红黑树**（增删改查 O(log n)）。之后 `epoll_wait` 不用再重复传入全部 fd——**避免了每次拷贝**。\n- **回调维护就绪链表**：每个注册的 fd 挂一个回调，当网卡数据到达、该 fd 就绪时，回调把它加入内核的**就绪链表**。`epoll_wait` 只需看这个链表空不空——**不再遍历全部 fd**。\n- **只返回就绪的 fd**：`epoll_wait` 直接把就绪链表里的 fd 拷给用户，用户拿到的就是就绪集合，**不用再自己扫一遍**。\n\n所以 `epoll_wait` 的开销只和**活跃连接数**有关，与总连接数无关。这就是它能撑十万级长连接（C10K）的根本原因。"
    },
    {
      "type": "explain",
      "title": "LT vs ET 与性能对比",
      "content": "## 两种触发模式\n- **水平触发 LT（默认）**：只要 fd **还有数据没读完**，每次 `epoll_wait` 都会**重复通知**。编程简单、不容易漏数据，一次没读完下次还会提醒。\n- **边缘触发 ET**：只在 fd **状态发生变化时通知一次**（来了新数据才提醒）。必须**配合非阻塞 IO，并在一次通知里循环读到 `EAGAIN`**（把数据读干净），否则剩下的数据要等下次新数据到达才再被通知，可能“饿死”。性能略高、系统调用更少，但编程更易出错。\n\n## 性能对比一句话\n- select/poll：每次 **O(n)** 遍历全部 fd，n 是监听总数。\n- epoll：`epoll_wait` 约 **O(就绪数)**，注册/删除 fd 是 O(log n)。\n- **结论**：连接多但活跃少（典型长连接服务）时 epoll 碾压；如果只盯少量、且几乎全活跃的 fd，select/poll 差距不大、甚至更省事。这也是 Redis、Nginx 用 epoll 的原因。"
    },
    {
      "type": "diagram",
      "title": "select/poll/epoll：机制分支与风险路径",
      "content": "flowchart TD\nNeo3cp3S[select/poll/epoll 触发入口] --> Neo3cp3M[核心对象或状态: 操作系统]\nNeo3cp3M --> Neo3cp3C{select/poll/epoll 是否走正常路径}\nNeo3cp3C -->|正常| Neo3cp3N[执行 select/poll/epoll 主流程]\nNeo3cp3C -->|异常| Neo3cp3F[处理 select/poll/epoll 边界风险]\nNeo3cp3N --> Neo3cp3O[输出结果或交给下一组件]\nNeo3cp3F --> Neo3cp3R[按 select/poll/epoll 语义降级/重试/报错]\nNeo3cp3O --> Neo3cp3V[验证 select/poll/epoll 日志、指标或测试]\nNeo3cp3R --> Neo3cp3V",
      "fallback": "select/poll/epoll 的图示按本题触发入口、核心状态、正常路径、失败路径和验证手段展开，重点看分支关系。",
      "items": [
        "epoll_create: 创建 eventpoll 结构体（红黑树 + 就绪链表）",
        "epoll_ctl(ADD): 将 fd 插入红黑树，注册回调函数",
        "回调触发: 网络数据到达 -> 回调将 fd 加入就绪链表",
        "epoll_wait: 检查就绪链表，有事件则拷贝到用户空间返回",
        "LT模式: 未读完数据会重复通知",
        "ET模式: 只通知一次，必须用非阻塞fd一次读完"
      ],
      "format": "mermaid",
      "caption": "select/poll/epoll 图中的分支用于区分本题正常机制、异常边界和工程验证路径。",
      "sources": [
        {
          "kind": "mermaid",
          "content": "flowchart TD\nNeo3cp3S[select/poll/epoll 触发入口] --> Neo3cp3M[核心对象或状态: 操作系统]\nNeo3cp3M --> Neo3cp3C{select/poll/epoll 是否走正常路径}\nNeo3cp3C -->|正常| Neo3cp3N[执行 select/poll/epoll 主流程]\nNeo3cp3C -->|异常| Neo3cp3F[处理 select/poll/epoll 边界风险]\nNeo3cp3N --> Neo3cp3O[输出结果或交给下一组件]\nNeo3cp3F --> Neo3cp3R[按 select/poll/epoll 语义降级/重试/报错]\nNeo3cp3O --> Neo3cp3V[验证 select/poll/epoll 日志、指标或测试]\nNeo3cp3R --> Neo3cp3V"
        },
        {
          "kind": "text",
          "content": "select/poll/epoll 的图示按本题触发入口、核心状态、正常路径、失败路径和验证手段展开，重点看分支关系。"
        }
      ]
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | select | poll | epoll |\n| --- | --- | --- | --- |\n| 数据结构 | fd_set 位图 | pollfd 数组 | 红黑树+就绪链表 |\n| fd 数量限制 | FD_SETSIZE(1024) | 无限制 | 无限制 |\n| fd 传递 | 每次拷贝 | 每次拷贝 | 注册一次(epoll_ctl) |\n| 就绪检测 | 遍历 O(n) | 遍历 O(n) | 回调 O(1) |\n| 触发模式 | 水平触发 | 水平触发 | LT 和 ET |\n| 性能(大量连接) | 差 | 差 | 优秀 |\n| 编程复杂度 | 低 | 低 | 中（ET模式高） |"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "// epoll 服务器示例\n#include <sys/epoll.h>\n#include <fcntl.h>\n#include <stdio.h>\n#include <string.h>\n#include <unistd.h>\n\n#define MAX_EVENTS 1024\n\nvoid set_nonblocking(int fd) {\n    int flags = fcntl(fd, F_GETFL, 0);\n    fcntl(fd, F_SETFL, flags | O_NONBLOCK);\n}\n\nint main() {\n    int epfd = epoll_create1(0);\n    struct epoll_event ev, events[MAX_EVENTS];\n    \n    // 监听标准输入\n    ev.events = EPOLLIN;\n    ev.data.fd = STDIN_FILENO;\n    epoll_ctl(epfd, EPOLL_CTL_ADD, STDIN_FILENO, &ev);\n    \n    while (1) {\n        // 等待事件，-1 表示永久阻塞\n        int nfds = epoll_wait(epfd, events, MAX_EVENTS, -1);\n        \n        for (int i = 0; i < nfds; i++) {\n            if (events[i].data.fd == STDIN_FILENO) {\n                char buf[1024];\n                int n = read(STDIN_FILENO, buf, sizeof(buf));\n                if (n > 0) {\n                    buf[n] = '\\0';\n                    printf(\"Read: %s\", buf);\n                }\n            }\n        }\n    }\n    close(epfd);\n    return 0;\n}\n\n// ET 模式设置：EPOLLIN | EPOLLET\n// 必须一次性读完数据\nvoid read_all(int fd) {\n    char buf[4096];\n    while (1) {\n        int n = read(fd, buf, sizeof(buf));\n        if (n < 0) {\n            if (errno == EAGAIN) break; // 数据读完\n            perror(\"read\");\n            break;\n        }\n        if (n == 0) break; // 连接关闭\n        // 处理数据\n    }\n}",
      "language": "c",
      "highlights": [
        {
          "line": 17,
          "note": "`struct epoll_event ev, events[MAX_EVENTS];` 定义了 select/poll/epoll 示例中承载状态和操作的代码位置，复述时从参数和状态字段讲起。"
        },
        {
          "line": 24,
          "note": "`while (1) {` 承担主要遍历过程，时间复杂度通常由这层循环和内部状态更新共同决定。"
        },
        {
          "line": 29,
          "note": "`if (events[i].data.fd == STDIN_FILENO) {` 是分支判断，决定正常路径、边界输入或失败路径走向。"
        },
        {
          "line": 2,
          "note": "`#include <sys/epoll.h>` 更新核心数据结构，是解释状态如何推进的抓手。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "标准面试回答",
      "content": "30 秒结论：select、poll、epoll 都是在一个线程里等待多个 fd 的就绪事件。select/poll 的瓶颈是每轮把关注集合交给内核并线性扫描；epoll 把关注集合注册在内核，fd 状态变化时通过等待队列回调进入就绪队列，所以在大量长连接、少量活跃连接的网络服务里更有优势。\n\n机制要点：\n- select 用 `fd_set` 位图，受 `FD_SETSIZE` 和最大 fd 扫描范围影响，返回后还要重建集合并再次判断哪些 fd 就绪。\n- poll 用 `pollfd` 数组去掉固定 1024 限制，但没有改变每轮拷贝数组、内核线性检查、用户态扫描 `revents` 的模型。\n- epoll 用 `epoll_create1` 创建实例，用 `epoll_ctl` 维护内核中的关注集合，用 `epoll_wait` 只取就绪事件；注册、删除有维护成本，不能简单说所有操作都是 O(1)。\n- LT 是条件满足就重复通知，写法安全；ET 只交付状态变化，必须配合非阻塞 fd 并读到 `EAGAIN`，否则连接可能卡住。\n- Reactor 是把 epoll_wait 所在的事件循环作为分发器：主线程或 IO 线程取事件，再执行 accept、read、write 或投递给 worker。多线程消费同一 fd 时常配 `EPOLLONESHOT`，处理完再 `EPOLL_CTL_MOD` 重新激活。\n- 多个线程或多个 epoll 等同一个 listen socket 时可能出现 accept 惊群，Linux 4.5 后可用 `EPOLLEXCLUSIVE` 缓解部分场景，但它不是普通读写 fd 的万能方案。\n\n边界和排查：\n- epoll 主要适合 socket、pipe、eventfd、timerfd 这类有等待状态变化的 fd；普通文件通常不适合，注册可能返回 `EPERM`，因为普通文件缺少有意义的 poll 等待语义或总是就绪。\n- 如果线上 CPU 高，要用 `strace -tt -T -e epoll_wait,epoll_ctl,accept4,read,write -p <pid>` 看是否空轮询、频繁 `EPOLL_CTL_MOD`、大量 accept 后 `EAGAIN`，再结合 `perf top` 判断时间耗在系统调用、事件循环还是业务处理。\n- 如果 ET 模式连接无响应，优先检查 fd 是否非阻塞、读写循环是否直到 `EAGAIN`、ONESHOT 是否忘了重新激活。",
      "followUpQuestions": [
        {
          "question": "epoll 为什么适合高并发连接？",
          "answer": "关注集合常驻内核，事件就绪后进入就绪队列，用户态不必每次扫描全部 fd。"
        },
        {
          "question": "select 的 fd 限制来自哪里？",
          "answer": "来自 fd_set 位图大小，常见默认 FD_SETSIZE 是 1024。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "select 的 `fd_set` 位图、`FD_SETSIZE` 限制和最大 fd 扫描范围",
        "poll 的 `pollfd` 数组、`events/revents` 语义和仍需线性扫描的原因",
        "epoll 的 `epoll_create1`、`epoll_ctl`、`epoll_wait` 三段调用职责",
        "eventpoll 关注集合、红黑树、等待队列回调和就绪链表之间的关系",
        "LT 与 ET 的触发条件差异，以及 ET 必须使用非阻塞 IO 并读到 `EAGAIN`",
        "不能裸说 epoll 全部 O(1)，要区分 `epoll_wait` 返回成本和 `epoll_ctl` 维护成本",
        "Reactor 模式中事件循环、accept/read/write 分发与业务线程之间的边界",
        "`EPOLLONESHOT` 在多线程处理同一 fd 时避免并发消费的作用",
        "`EPOLLEXCLUSIVE` 缓解共享 listen socket accept 惊群的适用范围",
        "普通文件不适合 epoll 的原因和 `EPERM` 这类错误线索",
        "使用 strace、perf、错误码和事件标志定位空轮询、ET 漏读、ONESHOT 未重激活、accept 惊群"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "os.io-model.topic-select-poll-epoll.recall.1",
      "prompt": "请比较 select、poll、epoll 的数据结构、调用成本和适用场景。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 4
    },
    {
      "id": "os.io-model.topic-select-poll-epoll.recall.2",
      "prompt": "epoll 的 LT 和 ET 模式有什么区别？为什么 ET 要配合非阻塞 IO？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 4
    },
    {
      "id": "os.io-model.topic-select-poll-epoll.recall.3",
      "prompt": "epoll 的 ET 和 LT 模式行为差异是什么？ET 模式为什么必须搭配非阻塞 IO？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 4
    }
  ],
  "rubric": {
    "mustHave": [
      "select fd_set",
      "pollfd 数组",
      "epoll_ctl/epoll_wait",
      "能说明「就绪列表」在「select/poll/epoll」里的作用和判断标准"
    ],
    "goodToHave": [
      "Reactor",
      "能说明「非阻塞 IO」在「select/poll/epoll」里的触发条件、收益和失效边界",
      "EAGAIN"
    ],
    "commonMistakes": [
      "只说 epoll 是 O(1) 不解释条件",
      "ET 模式不读到 EAGAIN",
      "认为 poll 解决了所有 select 问题"
    ],
    "scoreWeights": {
      "coverage": 25,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 25
    }
  },
  "updatedAt": "2026-06-18",
  "status": "draft",
  "prerequisites": [
    "os.io-model.topic-io-models"
  ],
  "interviewFrequency": "high",
  "interviewerFocus": "考察是否能围绕「select/poll/epoll」说清select fd_set、pollfd 数组，并放到操作系统与 Linux 排查场景中解释关键流程、边界条件和出错后的处理思路。"
}
