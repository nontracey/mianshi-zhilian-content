{
  "id": "os.process-thread.topic-deadlock",
  "domain": "os",
  "category": "process-thread",
  "group": "process-thread",
  "title": "死锁的产生与避免",
  "summary": "讲清死锁的定义、四个必要条件、四种处理策略，以及工程上用统一加锁顺序避免、用 jstack 等定位的实战方法。",
  "tags": [
    "操作系统",
    "死锁",
    "资源分配",
    "银行家算法"
  ],
  "difficulty": 3,
  "estimatedMinutes": 20,
  "order": 30,
  "recommendWeight": 93,
  "learningCards": [
    {
      "type": "explain",
      "title": "什么是死锁，四个必要条件",
      "content": "## 什么是死锁\n两个或多个线程/进程**互相等待对方持有的资源**，谁都不肯放手，导致全部卡死、无法推进。最经典的例子：线程 A 持有锁 1 等锁 2，线程 B 持有锁 2 等锁 1。\n\n## 四个必要条件（必须同时满足）\n记住它们是因为——**破坏任意一个就能消除死锁**：\n- **互斥**：资源一次只能被一个线程占用。\n- **持有并等待**：已经拿着资源，又去等别的资源、且不放手。\n- **不可剥夺**：资源只能由持有者主动释放，不能被强抢。\n- **循环等待**：存在一条“A 等 B、B 等 C、C 等 A”的环形等待链。\n\n四个条件里，**互斥**通常没法破坏（资源本身要求独占），所以实践中主要从后三个下手。"
    },
    {
      "type": "explain",
      "title": "四种处理策略与实战定位",
      "content": "## 四种处理策略（理论）\n- **预防**：破坏后三个条件之一——一次性申请所有资源（破持有并等待）、允许抢占（破不可剥夺）、**给资源编号、按固定顺序申请（破循环等待，最实用）**。\n- **避免**：动态判断这次分配后系统是否还处于“安全状态”，代表是**银行家算法**（Need = Max − Allocation，需预知最大需求，开销大、实际少用）。\n- **检测与恢复**：先允许死锁发生，用资源分配图检测环路，再靠终止进程或回滚来恢复（数据库就这么干）。\n- **鸵鸟策略**：假装不会发生——Linux/Windows 对普通进程基本如此，卡住了人工重启。\n\n## 实战里怎么避免和定位（高频）\n- **避免**：约定**统一的加锁顺序**（所有线程都先拿锁 1 再拿锁 2）、用 `tryLock` 设**超时**拿不到就回退、缩小锁粒度/持锁时间。\n- **定位**：Java 用 `jstack` 看线程 dump，JVM 会直接标出 `Found one Java-level deadlock` 并打印环；数据库看 `SHOW ENGINE INNODB STATUS` 的 LATEST DETECTED DEADLOCK；C/Linux 用 gdb 看各线程栈互相等锁。"
    },
    {
      "type": "diagram",
      "format": "mermaid",
      "title": "死锁的循环等待",
      "content": "flowchart LR\nA[\"线程A 持有锁1\"] -->|请求锁2| L2[\"锁2\"]\nL2 --> B[\"线程B 持有锁2\"]\nB -->|请求锁1| L1[\"锁1\"]\nL1 --> A",
      "caption": "死锁的循环等待：A 持有锁1 又请求锁2，B 持有锁2 又请求锁1，形成 A→锁2→B→锁1→A 的环；破坏这个环（统一加锁顺序）即可避免。",
      "fallback": "线程A 持有锁1、请求锁2；锁2 被线程B 持有；线程B 又请求锁1；锁1 被线程A 持有，形成环形等待，谁都拿不到、全卡死。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 策略 | 方法 | 优点 | 缺点 |\n| --- | --- | --- | --- |\n| 预防 | 破坏必要条件 | 彻底消除死锁 | 限制多、资源利用率低 |\n| 避免 | 银行家算法 | 动态检查安全性 | 需预知资源需求，计算开销大 |\n| 检测+恢复 | 资源分配图 | 灵活性高 | 恢复可能丢失数据 |\n| 忽略 | 不处理 | 无开销 | 死锁发生需人工干预 |"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "// 死锁示例：两个线程按不同顺序获取两把锁\n#include <pthread.h>\n#include <stdio.h>\n\npthread_mutex_t lockA = PTHREAD_MUTEX_INITIALIZER;\npthread_mutex_t lockB = PTHREAD_MUTEX_INITIALIZER;\n\n// 线程1：先锁A再锁B\nvoid* thread1(void* arg) {\n    pthread_mutex_lock(&lockA);\n    printf(\"Thread1: holding lockA\\n\");\n    sleep(1); // 模拟处理\n    pthread_mutex_lock(&lockB); // 等待 lockB，但被 thread2 持有\n    printf(\"Thread1: holding lockA and lockB\\n\");\n    pthread_mutex_unlock(&lockB);\n    pthread_mutex_unlock(&lockA);\n    return NULL;\n}\n\n// 线程2：先锁B再锁A（顺序不一致导致死锁）\nvoid* thread2(void* arg) {\n    pthread_mutex_lock(&lockB);\n    printf(\"Thread2: holding lockB\\n\");\n    sleep(1);\n    pthread_mutex_lock(&lockA); // 等待 lockA，但被 thread1 持有\n    printf(\"Thread2: holding lockB and lockA\\n\");\n    pthread_mutex_unlock(&lockA);\n    pthread_mutex_unlock(&lockB);\n    return NULL;\n}\n\n// 解决方案：统一加锁顺序（都先锁A再锁B）\nvoid* thread2_fixed(void* arg) {\n    pthread_mutex_lock(&lockA); // 改为先锁A\n    sleep(1);\n    pthread_mutex_lock(&lockB);\n    // ... 使用资源 ...\n    pthread_mutex_unlock(&lockB);\n    pthread_mutex_unlock(&lockA);\n    return NULL;\n}",
      "language": "c",
      "highlights": [
        {
          "line": 17,
          "note": "这里给出最终结果或提前退出条件，需要补充空值、失败和边界场景。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "结论：死锁是两个或多个线程互相等待对方持有的资源、谁都不放手，导致全都卡死。经典场景是 A 持有锁1 等锁2，B 持有锁2 等锁1。\n\n四个必要条件同时满足才会死锁，破坏任意一个就能消除：互斥、持有并等待、不可剥夺、循环等待。互斥通常没法破坏（资源本身要独占），所以实践从后三个下手。\n\n四种处理策略：① 预防——破坏后三个条件，最实用的是给资源编号、按固定顺序申请（破循环等待）。② 避免——动态判断分配后是否仍处于安全状态，代表是银行家算法，要预知最大需求、开销大、实际少用。③ 检测与恢复——先允许死锁，再用资源分配图检测环路、终止或回滚，数据库就这么干。④ 鸵鸟策略——假装不会发生，卡住人工重启。\n\n工程上避免和定位（高频）。避免靠统一加锁顺序、tryLock 带超时拿不到就回退、缩小锁粒度和持锁时间。定位：Java 用 jstack 看线程 dump，JVM 直接标 Found one Java-level deadlock 并打印环；数据库看 SHOW ENGINE INNODB STATUS 的 LATEST DETECTED DEADLOCK；C/Linux 用 gdb 看各线程栈互相等锁。",
      "followUpQuestions": [
        {
          "question": "线上怀疑发生了死锁，你会用什么手段确认并定位到具体是哪两把锁？",
          "answer": "死锁四个必要条件：互斥、持有并等待、不可剥夺、循环等待。预防：按顺序加锁破坏循环等待。检测：jstack查看线程状态、SHOW ENGINE INNODB STATUS查看InnoDB死锁。应用层：tryLock超时。数据库层：innodb_lock_wait_timeout。"
        },
        {
          "question": "‘统一加锁顺序’为什么能防死锁？它破坏的是哪个必要条件？",
          "answer": "它破坏的是‘循环等待’。死锁的环来自不同线程以不同顺序获取多把锁：A 先拿锁1再等锁2，B 先拿锁2再等锁1，就形成 A→B→A 的环。如果约定所有线程都必须按同一个全局顺序（比如按锁的编号从小到大）申请锁，那么想拿锁2的线程一定已经先拿到了锁1，不可能出现‘持有高序号锁去等低序号锁’的情况，等待关系只能单向、无法成环，循环等待条件被破坏，死锁自然不会发生。代价是要能给所有锁定义一个稳定顺序，业务复杂时不总是容易做到。"
        },
        {
          "question": "死锁、活锁、饥饿有什么区别？",
          "answer": "死锁是多个线程互相等待对方资源、全部卡住不动，线程处于阻塞态、CPU 不忙。活锁是线程没被阻塞、一直在运行，但因为不断响应对方的变化而反复重试、谁也推进不了（比如两人过道互相让路反复同步移动），表现是 CPU 很忙但没进展。饥饿是某个线程长期抢不到资源（如低优先级线程总被高优先级抢占、或写锁被源源不断的读锁饿死），它不是死循环，只是迟迟轮不到。简言之：死锁=互相等死、活锁=瞎忙不前、饥饿=长期排不上队。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能列举死锁的四个必要条件",
        "能解释每种死锁处理策略的原理和优缺点",
        "能说明银行家算法的基本思路",
        "能用资源分配图分析死锁",
        "能通过统一加锁顺序预防死锁",
        "能说明实际项目中的死锁排查方法",
        "能区分死锁、活锁和饥饿的概念"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "os.process-thread.topic-deadlock.recall.1",
      "prompt": "请解释死锁的四个必要条件，并说明破坏其中任意一个条件的常见做法。",
      "mode": "text",
      "expectedMinutes": 4,
      "difficulty": 3
    },
    {
      "id": "os.process-thread.topic-deadlock.recall.2",
      "prompt": "如果面试官问你在实际项目中遇到过死锁吗，你怎么排查和解决的？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "os.process-thread.topic-deadlock.recall.3",
      "prompt": "死锁的四个必要条件是什么？银行家算法属于预防还是避免？工程上更常用哪种思路、为什么？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "能说清死锁定义和四个必要条件，并知道破坏任一即可",
      "能说出按固定顺序加锁破坏循环等待是最实用的预防手段",
      "能区分预防/避免/检测恢复/鸵鸟四种策略",
      "能用 jstack 或 SHOW ENGINE INNODB STATUS 定位死锁"
    ],
    "goodToHave": [
      "能说明互斥条件通常无法破坏",
      "能解释银行家算法的安全状态判断",
      "能区分死锁、活锁、饥饿"
    ],
    "commonMistakes": [
      "只会背四个条件，不知道怎么破坏",
      "用 tryLock 不设超时，仍可能长期等待",
      "加锁顺序不统一导致循环等待"
    ],
    "scoreWeights": {
      "coverage": 25,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 25
    }
  },
  "updatedAt": "2026-06-12",
  "status": "draft",
  "prerequisites": [
    "os.process-thread.topic-process-vs-thread"
  ],
  "interviewFrequency": "high",
  "interviewerFocus": "考察能否说清四个必要条件并知道破坏哪个最实用、区分四种处理策略，并能用 jstack/INNODB STATUS 等手段定位线上死锁。"
}
