{
  "id": "java.java-fundamentals.topic-optional",
  "domain": "java",
  "category": "java-fundamentals",
  "group": "java-fundamentals",
  "title": "Optional 类使用",
  "summary": "讲清 Optional 表达空值语义、创建与函数式取值、应用于返回值的定位，以及 orElse/orElseGet 区别和滥用问题。",
  "tags": [
    "Java",
    "Java基础",
    "Optional 类使用"
  ],
  "difficulty": 2,
  "estimatedMinutes": 12,
  "order": 70,
  "recommendWeight": 72,
  "learningCards": [
    {
      "type": "explain",
      "title": "Optional 类使用：什么是 Optional、创建 Optional",
      "content": "## 一、Optional 的设计意图\n`Optional<T>` 用来显式表达“返回值可能不存在”，让调用方在类型层面看到空值语义。它不是为了把所有 `null` 都包装起来，更不是字段、参数和集合元素的通用替代品。\n\n## 二、创建与取值\n- `Optional.of(value)`：value 必须非空，传 null 立即抛 NPE，适合暴露调用方契约错误。\n- `Optional.ofNullable(value)`：允许 null，常用于包裹外部系统或历史接口返回值。\n- `Optional.empty()`：显式空值。\n\n优先用 `map/flatMap/filter/orElseGet/orElseThrow/ifPresentOrElse` 表达处理链。`isPresent()+get()` 往往只是把判 null 写得更啰嗦。\n\n## 三、边界\n`orElse(x)` 的 x 会立即求值；`orElseGet(supplier)` 只有空时才执行。默认值有副作用、成本高或会访问远程资源时必须用 `orElseGet`。Optional 是 value-based 类，不要用它做锁对象或依赖对象身份。"
    },
    {
      "type": "diagram",
      "format": "mermaid",
      "title": "Optional 空值处理链：返回值语义、惰性默认值与滥用边界",
      "content": "flowchart LR\nA[\"可能为空的返回值\"] --> B[\"Optional.ofNullable(value)\"]\nB --> C{\"值是否存在\"}\nC -->|存在| D[\"map / filter / flatMap\n转换、过滤、避免嵌套\"]\nD --> E[\"得到业务结果\"]\nC -->|不存在| F{\"缺省策略\"}\nF -->|便宜常量| G[\"orElse(default)\"]\nF -->|昂贵或有副作用| H[\"orElseGet(Supplier)\"]\nF -->|必须失败| I[\"orElseThrow()\"]\nB --> J[\"反模式\"]\nJ --> K[\"字段/参数/集合元素过度包装\"]\nJ --> L[\"isPresent 后 get\n等价啰嗦判 null\"]\nJ --> M[\"orElse 调远程/写库\n值存在也会执行\"]",
      "caption": "Optional 的重点不是“没有 null”，而是让返回值空语义可见，并选择惰性或失败策略。",
      "fallback": "图展示 Optional 从返回值包装到 map/flatMap/filter、默认值选择和三类常见滥用。",
      "sources": [
        {
          "kind": "mermaid",
          "content": "flowchart LR\nA[\"可能为空的返回值\"] --> B[\"Optional.ofNullable(value)\"]\nB --> C{\"值是否存在\"}\nC -->|存在| D[\"map / filter / flatMap\n转换、过滤、避免嵌套\"]\nD --> E[\"得到业务结果\"]\nC -->|不存在| F{\"缺省策略\"}\nF -->|便宜常量| G[\"orElse(default)\"]\nF -->|昂贵或有副作用| H[\"orElseGet(Supplier)\"]\nF -->|必须失败| I[\"orElseThrow()\"]\nB --> J[\"反模式\"]\nJ --> K[\"字段/参数/集合元素过度包装\"]\nJ --> L[\"isPresent 后 get\n等价啰嗦判 null\"]\nJ --> M[\"orElse 调远程/写库\n值存在也会执行\"]"
        },
        {
          "kind": "text",
          "content": "图展示 Optional 从返回值包装到 map/flatMap/filter、默认值选择和三类常见滥用。"
        }
      ]
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | 传统 null 检查 | Optional |\n| --- | --- | --- |\n| 空值语义 | 隐式，依赖文档或约定 | 方法签名显式表达可能为空 |\n| 适用位置 | 任意局部判断 | 主要用于返回值 |\n| 处理风格 | if-null 分支 | map/flatMap/filter/orElseGet 链式处理 |\n| 默认值 | 手写分支决定 | orElse/orElseGet/orElseThrow 表达策略 |\n| 成本 | 无包装对象 | 有一层包装，热路径要评估 |\n| 反模式 | 漏判 null | 字段/参数滥用、isPresent+get、orElse 副作用 |"
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "// 1. 创建 Optional\nOptional<String> name = Optional.of(\"Alice\");        // 非空值\nOptional<String> maybeNull = Optional.ofNullable(null); // 允许null\nOptional<String> empty = Optional.empty();              // 空实例\n\n// 2. 安全取值\nString result = name.orElse(\"Default\");           // \"Alice\"\nString result2 = maybeNull.orElse(\"Default\");     // \"Default\"\nString result3 = maybeNull.orElseGet(() -> generateDefault()); // 惰性求值\n\n// 3. 链式处理 - 传统方式 vs Optional\n// 传统方式\nString city = null;\nif (user != null) {\n    Address addr = user.getAddress();\n    if (addr != null) {\n        city = addr.getCity();\n    }\n}\n// Optional 方式\nString city = Optional.ofNullable(user)\n    .map(User::getAddress)\n    .map(Address::getCity)\n    .orElse(\"Unknown\");\n\n// 4. filter 过滤\nOptional<String> filtered = Optional.of(\"hello\")\n    .filter(s -> s.length() > 3);  // Optional[hello]",
      "language": "java",
      "highlights": [
        {
          "line": 14,
          "note": "`if (user != null) {` 是分支判断，决定正常路径、边界输入或失败路径走向。"
        },
        {
          "line": 15,
          "note": "`Address addr = user.getAddress();` 更新核心数据结构，是解释状态如何推进的抓手。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "结论：Optional 用来显式表达“方法返回值可能不存在”，让调用方必须选择默认值、抛异常或继续链式转换。\n\n讲清三点：\n1. 创建契约：`of` 表示非空契约，`ofNullable` 包裹不确定结果，`empty` 表示显式不存在。\n2. 取值方式：优先 `map/flatMap/filter/orElseGet/orElseThrow`，避免 `isPresent()+get()`。\n3. 使用边界：不要作为字段和参数，不要用作锁对象，不要在热路径过度包装。\n\n最容易拉开水平的是 `orElse` 和 `orElseGet`：前者参数会立即求值，后者只有 Optional 为空才执行 Supplier。默认值构造很重、有副作用或访问外部资源时，用错会制造隐藏性能和一致性问题。",
      "followUpQuestions": [
        {
          "question": "Optional 该用在什么地方？为什么不推荐做字段和方法参数？",
          "answer": "Optional 的设计意图是表达「方法返回值可能不存在」，让调用方在编译期就被提醒处理空值。不推荐做字段或方法参数：① 它没实现 Serializable，作为字段会让类不能正常序列化；② 多包了一层对象、有内存和访问开销；③ 方法参数用 Optional 反而让调用更啰嗦，不如直接重载方法或允许 null 加注解。所以最佳实践是只用在返回值，尤其是 Stream.findFirst、Map 查询这类可能空的结果。"
        },
        {
          "question": "orElse 和 orElseGet 有什么区别？什么时候必须用 orElseGet？",
          "answer": "区别在求值时机：orElse(x) 的参数 x 总会被计算，哪怕 Optional 有值、用不上它；orElseGet(supplier) 只在 Optional 为空时才调 supplier。所以当默认值的计算有开销或副作用时（比如 orElse(queryFromDB()) 会无条件查库、orElse(new ExpensiveObject()) 会无条件创建），必须用 orElseGet，否则白白浪费甚至产生不该有的副作用。值是常量时两者无差别。"
        },
        {
          "question": "滥用 Optional 会带来什么问题？",
          "answer": "主要是性能和可读性。每个 Optional 都是一个额外的堆对象，在热点路径或大集合里大量创建会增加 GC 压力；链式 map/flatMap 多了也有调用开销。可读性上，过度嵌套的 Optional 链反而比一个简单的 if (x != null) 难读。还有把 Optional 用在字段上导致序列化问题。所以 Optional 是返回值的空值语义工具、不是到处替代 null 的银弹，内部高频代码该判 null 就判 null。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能解释 Optional 的设计目的和核心价值",
        "能区分 of、ofNullable、empty 三种创建方式的适用场景",
        "能熟练使用 orElse、orElseGet、orElseThrow 等取值方法",
        "能使用 map、flatMap、filter 进行链式空值处理",
        "能说明 Optional 的使用禁忌和最佳实践",
        "能用 Optional 改写传统的 null 检查代码"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "java.java-fundamentals.topic-optional.recall.1",
      "prompt": "Optional 的 map、flatMap、orElse、orElseGet 有什么区别？为什么不建议把 Optional 用作实体字段或方法参数？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.java-fundamentals.topic-optional.recall.2",
      "prompt": "Optional 在 Stream 中如何正确使用？orElse 和 orElseGet 的执行时机有什么区别？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    }
  ],
  "rubric": {
    "mustHave": [
      "能说出 Optional 主要表达返回值可能不存在，而不是替代所有 null",
      "能区分 of/ofNullable/empty 的契约",
      "能用 map/flatMap/filter/orElseGet/orElseThrow 处理空值",
      "能指出字段、参数、isPresent+get、orElse 副作用等滥用"
    ],
    "goodToHave": [
      "能解释 Optional value-based 类的身份边界",
      "能说明 orElse 与 orElseGet 的求值时机",
      "能用 flatMap 避免嵌套 Optional 并保持链式语义"
    ],
    "commonMistakes": [
      "isPresent 后再 get，和判 null 无异",
      "把 Optional 当字段、参数或集合元素的默认类型",
      "用 orElse 包裹有副作用或昂贵默认值",
      "以为 Optional 能提升所有场景性能"
    ],
    "scoreWeights": {
      "coverage": 35,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-06-18",
  "status": "draft",
  "prerequisites": [
    "java.java-fundamentals.topic-028-905d9b58"
  ],
  "interviewFrequency": "medium",
  "interviewerFocus": "考察能否说清 Optional 的设计意图、函数式取值、应用于返回值，并区分 orElse/orElseGet、知道滥用的代价。"
}
