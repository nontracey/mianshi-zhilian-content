{
  "id": "java.concurrency.topic-013-cc70cb0e",
  "domain": "java",
  "category": "concurrency",
  "group": "concurrency",
  "title": "并发理论基础",
  "summary": "讲清并发与并行的区别、并发三性（原子性/可见性/有序性）及保证手段、JMM 主内存模型和 happens-before 规则。",
  "tags": [
    "Java",
    "并发编程",
    "并发理论基础"
  ],
  "difficulty": 3,
  "estimatedMinutes": 20,
  "order": 10,
  "recommendWeight": 87,
  "learningCards": [
    {
      "type": "explain",
      "title": "并发理论基础：并发与并行的区别、JMM 与 happens-before",
      "content": "## 1. 并发与并行的区别\n**并发（Concurrency）**：多个任务在同一个时间段内交替执行，逻辑上的\"同时\"。单核CPU通过时间片轮转实现并发。\n**并行（Parallelism）**：多个任务在同一时刻真正同时执行，物理上的\"同时\"。需要多核CPU支持。\n### 要点\n- 并发是\"交替执行\"，并行是\"同时执行\"\n- 并发解决的是程序的组织结构问题，并行解决的是执行效率问题\n- 单核CPU只能实现并发，不能实现并行\n\n### 示例/代码\n```java\n/**\n* 并发 vs 并行的直观理解\n*/\npublic class ConcurrencyVsParallelism {\n    public static void main(String[] args) {\n        // 并发：两个线程在单核CPU上交替执行（时间片轮转）\n        // 并行：两个线程在多核CPU上真正同时执行\n        Thread t1 = new Thread(() -> {\n            for (int i = 0; i < 5; i++) {\n                System.out.println(\"T1: \" + i + \" [\" + Thread.currentThread().getName() + \"]\");\n            }\n        });\n        Thread t2 = new Thread(() -> {\n            for (int i = 0; i < 5; i++) {\n                System.out.println(\"T2: \" + i + \" [\" + Thread.currentThread().getName() + \"]\");\n            }\n        });\n        t1.start();\n        t2.start();\n    }\n}\n```\n\n## 2. 线程安全的定义\n**线程安全**：当多个线程同时访问一个共享变量时，无论运行时环境如何调度，程序都能表现出正确的行为。\n### 要点\n- 线程安全的本质是保证**原子性、可见性、有序性**\n- 不可变对象（如 String、Integer）天然是线程安全的\n- 线程安全问题的根源在于 CPU 缓存、指令重排序、编译优化\n\n## 3. 原子性、可见性、有序性\n\n### 3.1 原子性（Atomicity）\n一个或多个操作要么全部执行成功，要么全部不执行，中间不会被中断。\n```java\n// 原子性问题演示：count++ 不是原子操作\nprivate static int count = 0;\n// count++ 实际包含三步：\n// 1. 读取 count 的值 (read)\n// 2. 计算 count + 1  (add)\n// 3. 将结果写回 count (write)\n// 线程可能在任意步骤被切换，导致结果错误\n```\n\n### 3.2 可见性（Visibility）\n当一个线程修改了共享变量的值，其他线程能够立即看到修改后的值。\n```java\n// 可见性问题演示\npublic class VisibilityDemo {\n    private static boolean flag = false; // 不加 volatile 可能导致死循环\n    public static void main(String[] args) {\n        new Thread(() -> {\n            while (!flag) {\n                // 可能永远看不到 flag 的变化，陷入死循环\n                // JIT 可能优化为 while(true)\n            }\n            System.out.println(\"Flag changed!\");\n        }).start();\n        new Thread(() -> {\n            try { Thread.sleep(1000); } catch (InterruptedException e) {}\n            flag = true;\n            System.out.println(\"Set flag = true\");\n        }).start();\n    }\n}\n```\n\n### 3.3 有序性（Ordering）\n程序的执行顺序按照代码的先后顺序执行。编译器和CPU可能对指令进行重排序，但单线程结果不变（as-if-serial）。\n```java\n// 有序性问题：指令重排序\nint a = 1;      // (1)\nint b = 2;      // (2)\nint c = a + b;  // (3)\n// (1)和(2)没有依赖关系，可能被重排序为 (2)→(1)→(3)\n// (3)依赖(1)和(2)，不会被重排到它们前面\n```\n\n## 4. Java内存模型（JMM）\nJMM（Java Memory Model）定义了多线程环境下共享变量的访问规则。\n```\n┌──────────────────────────────────────────────┐\n│              主内存（Main Memory）              │\n│         存储所有共享变量的真实值                  │\n│    ┌──────┐   ┌──────┐   ┌──────┐            │\n│    │  x   │   │  y   │   │  z   │            │\n│    └──────┘   └──────┘   └──────┘            │\n└──────┬─────────────┬─────────────┬───────────┘\n│             │             │\n┌────▼────┐  ┌────▼────┐  ┌────▼────┐\n│工作内存1 │  │工作内存2 │  │工作内存3 │\n│ (线程1) │  │ (线程2) │  │ (线程3) │\n│ x的副本  │  │ x的副本  │  │ x的副本  │\n└─────────┘  └─────────┘  └─────────┘\n```\n\n### 要点\n- **主内存**：所有线程共享的内存区域\n- **工作内存**：每个线程私有的内存区域\n- 线程对变量的所有操作都必须在工作内存中进行\n- 线程间变量传递必须通过主内存"
    },
    {
      "type": "explain",
      "title": "并发理论基础：happens-before 原则、指令重排序",
      "content": "## 5. happens-before原则\n如果操作 A happens-before 操作 B，则 A 的结果对 B 可见，且 A 在 B 之前执行。\n### 示例/代码\n```java\n/**\n* happens-before 综合演示\n*/\npublic class HappensBeforeDemo {\n    private int x = 0;\n    private volatile boolean flag = false;\n    // 写线程\n    public void writer() {\n        x = 42;          // (1)\n        flag = true;     // (2) volatile 写\n    }\n    // 读线程\n    public void reader() {\n        if (flag) {                  // (3) volatile 读\n            System.out.println(x);  // (4) 一定能看到 42\n        }\n        // 推导过程：\n        // (1) hb (2)  — 程序顺序规则\n        // (2) hb (3)  — volatile变量规则\n        // (1) hb (3)  — 传递性规则\n        // 所以 x=42 对 (4) 可见\n    }\n}\n```\n\n## 6. 指令重排序\n\n### as-if-serial语义\n不管怎么重排序，单线程程序的执行结果不能被改变。编译器和CPU必须保证数据依赖关系不被破坏。\n```java\n// 有数据依赖的操作不会被重排序\nint a = 1;       // (1)\nint b = a + 2;   // (2) 依赖 (1)，不会被重排到 (1) 前面\n// 无数据依赖的操作可能被重排序\nint x = 1;       // (1)\nint y = 2;       // (2) 与 (1) 无关，可能被重排为 (2)→(1)\n```\n\n## 面试高频问题\n\n### Q1: 并发和并行有什么区别？\n**答**：并发是指多个任务在同一时间段内交替执行（逻辑上的同时），并行是指多个任务在同一时刻真正同时执行（物理上的同时）。单核CPU只能并发，多核CPU才能并行。并发侧重于程序结构设计，并行侧重于执行效率提升。\n### Q2: 什么是线程安全？如何保证？\n**答**：线程安全是指多个线程同时访问共享变量时程序仍能表现正确。需要保证三性：原子性（synchronized/Lock/Atomic）、可见性（volatile/synchronized/final）、有序性（volatile/synchronized/happens-before）。\n### Q3: JMM中主内存和工作内存的关系？\n**答**：主内存是所有线程共享的内存区域，工作内存是每个线程私有的。线程不能直接读写主内存，必须先将变量从主内存 read-load 到工作内存，修改后再 store-write 回主内存。这是并发问题的根源。\n### Q4: happens-before和时间先后有什么区别？\n**答**：happens-before不完全等于时间先后，它强调的是可见性和有序性的保证。两个操作可能时间上A先于B，但如果没有满足happens-before规则，A的结果对B仍不可见。\n### Q5: 为什么DCL单例需要volatile？\n**答**：`new Singleton()` 分为三步：分配内存→初始化→引用赋值。由于指令重排序，引用赋值可能在初始化之前完成。此时另一个线程通过null检查发现instance不为null，返回未初始化的对象。volatile通过内存屏障禁止这种重排序。"
    },
    {
      "type": "diagram",
      "format": "mermaid",
      "title": "并发三性与 JMM",
      "content": "flowchart TD\nA[\"并发问题根源：CPU 缓存/指令重排/编译优化\"] --> B[\"原子性：synchronized/Lock/Atomic\"]\nA --> C[\"可见性：volatile/synchronized/final\"]\nA --> D[\"有序性：volatile/happens-before\"]\nE[\"JMM 主内存 + 线程工作内存\"] --> F[\"happens-before 规则保证可见性\"]\nF --> B\nF --> C\nF --> D",
      "caption": "并发三性（原子性、可见性、有序性）源于 CPU 缓存、指令重排、编译优化；JMM 用主内存加工作内存模型和 happens-before 规则来保证它们。",
      "fallback": "并发问题根源是 CPU 缓存、指令重排、编译优化，对应三性：原子性（synchronized/Lock/Atomic）、可见性（volatile/synchronized/final）、有序性（volatile/happens-before）；JMM 用主内存加工作内存模型和 happens-before 规则保证可见性。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | 原子性 | 可见性 | 有序性 |\n| --- | --- | --- | --- |\n| 定义 | 操作不可中断 | 修改立即可见 | 按代码顺序执行 |\n| 保证方式 | synchronized、CAS | volatile、synchronized | volatile、happens-before |\n| 违反后果 | 数据不一致 | 读到旧数据 | 执行顺序错乱 |\n| 典型问题 | i++非原子性 | 缓存导致不可见 | 指令重排序 |\n| 解决方案 | Atomic类、锁 | volatile、锁 | volatile、内存屏障 |"
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "/**\n * 并发 vs 并行的直观理解\n */\npublic class ConcurrencyVsParallelism {\n    public static void main(String[] args) {\n        // 并发：两个线程在单核CPU上交替执行（时间片轮转）\n        // 并行：两个线程在多核CPU上真正同时执行\n        Thread t1 = new Thread(() -> {\n            for (int i = 0; i < 5; i++) {\n                System.out.println(\"T1: \" + i + \" [\" + Thread.currentThread().getName() + \"]\");\n            }\n        });\n        Thread t2 = new Thread(() -> {\n            for (int i = 0; i < 5; i++) {\n                System.out.println(\"T2: \" + i + \" [\" + Thread.currentThread().getName() + \"]\");\n            }\n        });\n        t1.start();\n        t2.start();\n    }\n}",
      "language": "java",
      "highlights": [
        {
          "line": 9,
          "note": "这里体现状态推进或遍历过程，需要说明终止条件、复杂度和异常输入。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "结论：并发编程的所有问题都绕不开三个核心——原子性、可见性、有序性，而它们的根源是 CPU 缓存、指令重排序和编译优化。JMM（Java 内存模型）就是为屏蔽这些底层差异、给多线程定一套读写规则而生的。\n\n要点：\n\n1. 并发 vs 并行：并发是同一时间段交替执行（逻辑同时，单核也行），并行是同一时刻真正同时（物理同时，要多核）。\n2. 三性：原子性（一组操作不可分割，靠 synchronized、Lock、Atomic 保证）、可见性（一个线程的修改别的线程立即看到，靠 volatile、synchronized、final）、有序性（防指令重排，靠 volatile、happens-before）。\n3. JMM：主内存所有线程共享，每个线程有私有工作内存；线程读写共享变量要先从主内存 load 到工作内存、改完再 store 回去，不一致就出在这里。\n4. happens-before：JMM 定义的一组规则，如果 A happens-before B，A 的结果对 B 可见。常见规则有程序顺序、锁的释放-获取、volatile 写-读、线程 start/join 等，让我们不用关心底层重排、只按规则推导可见性。\n\n边界和坑。① as-if-serial 保证单线程结果不变，但多线程下重排会出问题（DCL 单例要 volatile 就是例子）。② happens-before 是「可见性保证」不是「时间先后」，A 时间上先于 B 不代表对 B 可见，必须有 happens-before 关系。③ 三性要一起看，比如 count++ 有原子性问题。",
      "followUpQuestions": [
        {
          "question": "在高并发场景下，并发编程有哪些最容易踩的坑？能举例吗？",
          "answer": "最经典的坑是把「加了 volatile 就线程安全」当万能——volatile 只保证可见性和有序性、不保证原子性，count++ 这种复合操作照样出错。第二个坑是指令重排导致「看似不可能」的 bug，比如 DCL 单例不加 volatile 会拿到半初始化对象。第三个是误以为 happens-before 等于时间先后，导致以为「我先写的别人就能读到」，其实没有 happens-before 关系就不保证可见。这些坑的共性是：底层（缓存、重排）和直觉不一致，必须靠 JMM 规则推导而不是想当然。"
        },
        {
          "question": "并发程序的性能瓶颈通常在哪里？有哪些优化思路？",
          "answer": "瓶颈通常在锁竞争和缓存一致性。锁竞争激烈时线程频繁阻塞唤醒、上下文切换开销大，优化靠缩小锁粒度、读写锁、无锁结构（CAS、LongAdder）。缓存层面有伪共享（false sharing）——多个线程改同一缓存行的不同变量会互相失效缓存，可以用 @Contended 或字段填充隔离。判断瓶颈用 jstack 看 BLOCKED 线程、用 perf 看缓存未命中。"
        },
        {
          "question": "如果一段并发代码偶发数据不一致，你怎么定位是原子性、可见性还是有序性问题？",
          "answer": "先看操作是不是复合的（读-改-写、check-then-act）——是的话八成是原子性问题，加锁或换 Atomic 验证。如果是单变量的「改了别人读到旧值」，那是可见性，看有没有 volatile 或同步。如果现象诡异到「按代码顺序根本不可能」，怀疑有序性（重排），典型是发布对象时引用先于初始化可见。手段：用 jcstress 这类并发测试工具压出问题，加日志记录线程读到的值和时序，或先用最重的 synchronized 全锁住看问题是否消失来缩小范围。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能解释线程安全的定义",
        "能说出并发编程的三大特性",
        "能解释原子性、可见性、有序性的含义",
        "能说出保证各特性的方法",
        "能解释happens-before规则",
        "能说明volatile的作用和原理",
        "能列举Java中线程安全的类"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "java.concurrency.topic-013-cc70cb0e.recall.1",
      "prompt": "Java 内存模型（JMM）的 happens-before 规则有哪些？它如何保证多线程的可见性和有序性？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "java.concurrency.topic-013-cc70cb0e.recall.2",
      "prompt": "并发理论基础 在什么场景下会导致死锁或性能问题？如何预防？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "java.concurrency.topic-013-cc70cb0e.recall.3",
      "prompt": "并发中的原子性、可见性、有序性分别解决什么问题？请各举一个 Java 例子。",
      "mode": "code",
      "expectedMinutes": 10,
      "difficulty": 3
    },
    {
      "id": "java.concurrency.topic-013-cc70cb0e.recall.4",
      "prompt": "请用两个线程的例子说明可见性问题如何出现，并说明 volatile 或 synchronized 如何建立 happens-before。",
      "mode": "text",
      "expectedMinutes": 4,
      "difficulty": 3
    },
    {
      "id": "java.concurrency.topic-013-cc70cb0e.recall.5",
      "prompt": "给定一段并发代码，你会如何判断它分别违反了原子性、可见性还是有序性，并给出修复方式？",
      "mode": "text",
      "expectedMinutes": 4,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "能区分并发与并行",
      "能说出并发三性（原子性、可见性、有序性）及保证手段",
      "能解释 JMM 主内存与工作内存模型",
      "能说明 happens-before 是可见性保证而非时间先后"
    ],
    "goodToHave": [
      "能举出常见 happens-before 规则",
      "能解释 as-if-serial 与多线程重排的关系",
      "能指出 count++ 的原子性问题"
    ],
    "commonMistakes": [
      "把 happens-before 理解成时间先后",
      "以为单线程 as-if-serial 在多线程也成立",
      "忽略可见性、只关注原子性"
    ],
    "scoreWeights": {
      "coverage": 25,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 25
    }
  },
  "updatedAt": "2026-06-12",
  "status": "draft",
  "interviewFrequency": "high",
  "interviewerFocus": "考察能否区分并发与并行、说清三性及保证手段、解释 JMM 与 happens-before 是可见性保证而非时间先后。",
  "prerequisites": [
    "java.jvm.topic-001-ebcc71cb"
  ]
}
