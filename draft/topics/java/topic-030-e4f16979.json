{
  "id": "java.java-fundamentals.topic-030-e4f16979",
  "domain": "java",
  "category": "java-fundamentals",
  "group": "java-fundamentals",
  "title": "Set、TreeMap 与 Queue 集合",
  "summary": "掌握 HashSet、TreeSet、TreeMap、LinkedHashMap、PriorityQueue 的底层实现、核心特性和适用场景，理解集合去重原理和有序性实现机制。",
  "tags": [
    "Java",
    "Java基础",
    "Set、TreeMap 与 Queue 集合"
  ],
  "difficulty": 2,
  "estimatedMinutes": 25,
  "order": 30,
  "recommendWeight": 82,
  "learningCards": [
    {
      "type": "explain",
      "title": "Set、TreeMap 与 Queue 集合：HashSet、TreeMap 红黑树与 PriorityQueue",
      "content": "## 1. HashSet\nHashSet 底层完全基于 **HashMap** 实现，元素存储在 HashMap 的 key 中，value 统一使用一个 `PRESENT` 哑对象。\n\n### 要点\n- 底层是一个 `HashMap<E, Object>`，元素作为 key，value 固定为 `PRESENT = new Object()`\n- 不允许重复元素（通过 hashCode 和 equals 判断）\n- 允许 null 元素\n- 无序（不保证插入顺序和遍历顺序一致）\n- 线程不安全\n- add 操作时间复杂度 O(1)（无哈希冲突时）\n\n### 示例/代码\n```java\n// HashSet 底层实现\npublic class HashSet<E> extends AbstractSet<E> implements Set<E> {\n    private transient HashMap<E,Object> map;\n    // 所有元素共享的哑 value\n    private static final Object PRESENT = new Object();\n    public HashSet() {\n        map = new HashMap<>();\n    }\npublic boolean add(E e) {\n    return map.put(e, PRESENT) == null; // 利用 HashMap key 的唯一性\n}\npublic boolean remove(Object o) {\n    return map.remove(o) == PRESENT;\n}\npublic boolean contains(Object o) {\n    return map.containsKey(o);\n}\npublic int size() {\n    return map.size();\n}\n}\n// 使用示例\nSet<String> set = new HashSet<>();\nset.add(\"Java\");\nset.add(\"Python\");\nset.add(\"Java\");  // 重复元素，不会被添加\nSystem.out.println(set.size()); // 2\n```\n\n## 2. TreeSet\nTreeSet 底层基于 **TreeMap**（红黑树）实现，元素有序存储。\n\n### 要点\n- 底层是一个 `TreeMap<E, Object>`，元素作为 key，value 固定为 `PRESENT`\n- 元素**有序**（自然排序或自定义 Comparator 排序）\n- 不允许重复元素（通过 Comparable 或 Comparator 的 compareTo 判断）\n- 默认不允许 null 元素；除非自定义 Comparator 明确支持 null 比较\n- 线程不安全\n- add/remove/contains 操作时间复杂度 O(log n)\n\n### 示例/代码\n```java\n// TreeSet 自然排序\nTreeSet<Integer> intSet = new TreeSet<>();\nintSet.add(3);\nintSet.add(1);\nintSet.add(2);\nSystem.out.println(intSet); // [1, 2, 3]，有序\n// 自定义排序\nTreeSet<String> strSet = new TreeSet<>((a, b) -> b.compareTo(a)); // 降序\nstrSet.add(\"Banana\");\nstrSet.add(\"Apple\");\nstrSet.add(\"Cherry\");\nSystem.out.println(strSet); // [Cherry, Banana, Apple]\n// 自定义对象排序\nTreeSet<Student> studentSet = new TreeSet<>(Comparator.comparingInt(Student::getAge));\nstudentSet.add(new Student(\"Alice\", 20));\nstudentSet.add(new Student(\"Bob\", 18));\nstudentSet.add(new Student(\"Charlie\", 22));\n// 遍历结果按年龄升序\n// TreeSet 常用方法\nTreeSet<Integer> set = new TreeSet<>();\nset.add(1); set.add(3); set.add(5); set.add(7);\nset.first();        // 1（最小元素）\nset.last();         // 7（最大元素）\nset.headSet(5);     // [1, 3]（小于5的元素）\nset.tailSet(3);     // [3, 5, 7]（大于等于3的元素）\nset.subSet(3, 7);   // [3, 5]（[3, 7) 范围内的元素）\n```\n\n## 3. TreeMap\nTreeMap 底层是**红黑树**，保证 key 有序。\n\n### 要点\n- 基于红黑树实现，key 按自然顺序或 Comparator 排序\n- put/get/remove 操作时间复杂度 O(log n)\n- 适合需要 key 有序的 Map 场景\n- key 不能为 null（除非提供自定义 Comparator）\n\n### 示例/代码\n```java\n// TreeMap 基本使用\nTreeMap<String, Integer> map = new TreeMap<>();\nmap.put(\"Banana\", 3);\nmap.put(\"Apple\", 5);\nmap.put(\"Cherry\", 2);\n// 遍历顺序：Apple -> Banana -> Cherry（字母序）\n// 常用方法\nmap.firstKey();              // \"Apple\"\nmap.lastKey();               // \"Cherry\"\nmap.lowerKey(\"Banana\");      // \"Apple\"（严格小于）\nmap.higherKey(\"Banana\");     // \"Cherry\"（严格大于）\nmap.floorKey(\"Banana\");      // \"Banana\"（小于等于）\nmap.ceilingKey(\"Banana\");    // \"Banana\"（大于等于）\nmap.subMap(\"Apple\", \"Cherry\"); // [Apple, Cherry) 范围的子Map\n```\n\n## 4. LinkedHashMap\nLinkedHashMap 在 HashMap 的基础上增加了一条**双向链表**，维护元素的插入顺序或访问顺序。\n\n### 要点\n- 继承自 HashMap，底层仍然是数组 + 链表 + 红黑树\n- 额外维护一条双向链表，保证**遍历顺序与插入顺序一致**\n- 可以设置为**访问顺序**模式（`accessOrder = true`），实现 LRU 缓存\n- 遍历性能与元素数量成正比（O(n)），不受容量影响\n- 允许 null key 和 null value\n- 性能略低于 HashMap（维护链表的额外开销）\n\n### 示例/代码\n```java\n// 基本使用：保持插入顺序\nLinkedHashMap<String, Integer> map = new LinkedHashMap<>();\nmap.put(\"Banana\", 3);\nmap.put(\"Apple\", 5);\nmap.put(\"Cherry\", 2);\n// 遍历顺序：Banana -> Apple -> Cherry（与插入顺序一致）\n// 实现 LRU 缓存\npublic class LRUCache<K, V> extends LinkedHashMap<K, V> {\n    private final int maxSize;\n    public LRUCache(int maxSize) {\n        // accessOrder = true 表示按访问顺序排序\n        super(maxSize, 0.75f, true);\n        this.maxSize = maxSize;\n    }\n@Override\nprotected boolean removeEldestEntry(Map.Entry<K, V> eldest) {\n    return size() > maxSize; // 超过容量时移除最老的元素\n}\n}\n// 使用 LRU 缓存\nLRUCache<String, String> cache = new LRUCache<>(3);\ncache.put(\"a\", \"1\");\ncache.put(\"b\", \"2\");\ncache.put(\"c\", \"3\");\ncache.get(\"a\");       // 访问 \"a\"，\"a\" 移到链表末尾\ncache.put(\"d\", \"4\");  // 超过容量，移除最久未访问的 \"b\"\nSystem.out.println(cache); // {c=3, a=1, d=4}\n```\n\n## 5. PriorityQueue\nPriorityQueue 底层是一个**小顶堆**（可通过自定义 Comparator 改为大顶堆），元素按优先级出队。\n\n### 要点\n- 底层使用 Object 数组实现的**完全二叉堆**\n- 默认是小顶堆（最小元素在堆顶），可通过 Comparator 自定义排序\n- 插入和删除操作时间复杂度 O(log n)\n- 不允许 null 元素\n- 非线程安全\n- `peek()` 和 `element()` 获取堆顶元素 O(1)\n- `poll()` 弹出堆顶元素 O(log n)\n\n### 示例/代码\n```java\n// 默认小顶堆\nPriorityQueue<Integer> minHeap = new PriorityQueue<>();\nminHeap.offer(3);\nminHeap.offer(1);\nminHeap.offer(2);\nSystem.out.println(minHeap.peek()); // 1（最小元素）\nSystem.out.println(minHeap.poll()); // 1\nSystem.out.println(minHeap.poll()); // 2\nSystem.out.println(minHeap.poll()); // 3\n// 大顶堆\nPriorityQueue<Integer> maxHeap = new PriorityQueue<>(Collections.reverseOrder());\nmaxHeap.offer(3);\nmaxHeap.offer(1);\nmaxHeap.offer(2);\nSystem.out.println(maxHeap.poll()); // 3\n// 自定义对象排序\nPriorityQueue<int[]> pq = new PriorityQueue<>(Comparator.comparingInt(a -> a[0]));\npq.offer(new int[]{3, 100});\npq.offer(new int[]{1, 200});\npq.offer(new int[]{2, 300});\n// 按数组第一个元素升序\n// 应用：Top K 问题\npublic static int[] topK(int[] nums, int k) {\n    // 小顶堆，维护前K个最大元素\n    PriorityQueue<Integer> heap = new PriorityQueue<>(k);\n    for (int num : nums) {\n        if (heap.size() < k) {\n            heap.offer(num);\n        } else if (num > heap.peek()) {\n            heap.poll();\n            heap.offer(num);\n        }\n}\nreturn heap.stream().mapToInt(Integer::intValue).toArray();\n}\n```\n\n## 6. 集合框架体系图\n```\nIterable\nCollection\n/    |    \\\nList   Set   Queue\n/   \\    |  \\    \\\nArrayList  \\  HashSet  \\  PriorityQueue\nLinkedList  TreeSet  ArrayDeque\nLinkedHashSet\nMap\n/   |    \\\nHashMap  TreeMap  Hashtable\nLinkedHashMap  ConcurrentHashMap\n```\n\n## 8. 面试高频问题\n\n### Q1: HashSet 底层是怎么实现的？\n**答**：HashSet 底层基于 HashMap 实现，元素存储在 HashMap 的 key 中，value 统一使用一个静态的 Object 哑对象（PRESENT）。利用 HashMap key 的唯一性来保证 Set 中元素不重复。add 操作就是调用 `map.put(e, PRESENT)`，返回 null 说明是新元素，返回非 null 说明已存在。\n\n### Q2: TreeSet 和 HashSet 的区别？\n**答**：HashSet 底层是 HashMap，无序，add/remove/contains 时间复杂度 O(1)，允许 null。TreeSet 底层是 TreeMap（红黑树），元素有序，时间复杂度 O(log n)，自然排序下不允许 null；如果自定义 Comparator 明确支持 null，可处理 null。需要有序存储或范围查询用 TreeSet，只需要去重和快速查找用 HashSet。\n\n### Q3: LinkedHashMap 如何实现 LRU 缓存？\n**答**：LinkedHashMap 构造时设置 `accessOrder = true`，使链表按访问顺序排列。每次 get/put 操作会将该元素移到链表末尾。重写 `removeEldestEntry()` 方法，当 size 超过最大容量时返回 true，自动移除链表头部（最久未访问）的元素。\n\n### Q4: PriorityQueue 是怎么实现的？\n**答**：PriorityQueue 底层是一个 Object 数组实现的完全二叉堆，默认是小顶堆（最小元素在堆顶）。插入元素时上浮（siftUp），时间复杂度 O(log n)；弹出堆顶元素时先与末尾交换，再下沉（siftDown），时间复杂度 O(log n）。获取堆顶元素 O(1)。\n\n### Q5: 如何选择合适的集合？\n**答**：根据场景选择：需要随机访问用 ArrayList；需要去重用 HashSet；需要有序用 TreeSet/TreeMap；需要 key-value 用 HashMap；需要保持插入顺序用 LinkedHashMap/LinkedHashSet；需要优先级队列用 PriorityQueue；线程安全场景用 ConcurrentHashMap/CopyOnWriteArrayList。核心原则是：需要下标随机访问选 ArrayList；需要去重选 HashSet；需要范围查询/排序选 TreeSet/TreeMap；需要栈、队列、双端队列优先 ArrayDeque；需要优先级选 PriorityQueue。"
    },
    {
      "type": "diagram",
      "title": "Set、TreeMap、LinkedHashMap、Queue 的底层结构选型",
      "format": "mermaid",
      "content": "flowchart TD\nNeed[集合选型] --> Unique{只需要去重?}\nUnique -->|是| HashSet[HashSet\nHashMap key + PRESENT]\nUnique -->|需要排序/范围| TreeSet[TreeSet\nTreeMap 红黑树]\nNeed --> KV{需要 key-value?}\nKV -->|普通快速查找| HashMap[HashMap\n数组+链表/红黑树]\nKV -->|按 key 有序/范围查询| TreeMap[TreeMap\n红黑树 NavigableMap]\nKV -->|保持插入/访问顺序| Linked[LinkedHashMap\n双向链表\n可做 LRU]\nNeed --> Queue{队列语义?}\nQueue -->|FIFO/Deque| ArrayDeque[ArrayDeque\n循环数组]\nQueue -->|优先级| Priority[PriorityQueue\n二叉堆]\nHashSet --> Risk[依赖 hashCode/equals]\nTreeSet --> Compare[依赖 Comparator/Comparable\n比较相等即去重]\nPriority --> Heap[peek O(1)\noffer/poll O(log n)]",
      "caption": "这篇不是背集合名字，而是按“去重、排序、范围查询、顺序、队列/堆”把底层结构和复杂度对应起来。",
      "fallback": "HashSet 用 HashMap key 去重，TreeSet/TreeMap 用红黑树排序和范围查询，LinkedHashMap 用双向链表保持顺序或实现 LRU，ArrayDeque 适合栈/队列，PriorityQueue 是堆。",
      "items": [
        "HashSet：基于HashMap，元素作为key",
        "TreeSet：基于TreeMap，有序集合",
        "HashSet去重：hashCode() + equals()",
        "HashMap vs Hashtable：线程安全、null值、初始容量",
        "TreeMap：红黑树实现，有序",
        "LinkedHashMap：维护插入顺序",
        "集合选择：根据有序性、线程安全、性能需求选择"
      ],
      "sources": [
        {
          "kind": "mermaid",
          "content": "flowchart TD\nNeed[集合选型] --> Unique{只需要去重?}\nUnique -->|是| HashSet[HashSet\nHashMap key + PRESENT]\nUnique -->|需要排序/范围| TreeSet[TreeSet\nTreeMap 红黑树]\nNeed --> KV{需要 key-value?}\nKV -->|普通快速查找| HashMap[HashMap\n数组+链表/红黑树]\nKV -->|按 key 有序/范围查询| TreeMap[TreeMap\n红黑树 NavigableMap]\nKV -->|保持插入/访问顺序| Linked[LinkedHashMap\n双向链表\n可做 LRU]\nNeed --> Queue{队列语义?}\nQueue -->|FIFO/Deque| ArrayDeque[ArrayDeque\n循环数组]\nQueue -->|优先级| Priority[PriorityQueue\n二叉堆]\nHashSet --> Risk[依赖 hashCode/equals]\nTreeSet --> Compare[依赖 Comparator/Comparable\n比较相等即去重]\nPriority --> Heap[peek O(1)\noffer/poll O(log n)]"
        },
        {
          "kind": "text",
          "content": "HashSet 用 HashMap key 去重，TreeSet/TreeMap 用红黑树排序和范围查询，LinkedHashMap 用双向链表保持顺序或实现 LRU，ArrayDeque 适合栈/队列，PriorityQueue 是堆。"
        }
      ]
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | HashSet | TreeSet | HashMap | TreeMap |\n| --- | --- | --- | --- | --- |\n| 数据结构 | HashMap | TreeMap(红黑树) | 数组+链表/红黑树 | 红黑树 |\n| 有序性 | 无序 | 有序 | 无序 | 有序 |\n| null值 | 允许一个 | 默认不允许，取决于 Comparator | 允许一个键 | 默认不允许，取决于 Comparator |\n| 性能 | O(1) | O(logn) | O(1) | O(logn) |\n| 线程安全 | 不安全（HashSet） | 不安全（TreeSet） | 不安全（HashMap） | 不安全（TreeMap） |\n| 比较方式 | hashCode/equals | Comparable/Comparator | hashCode/equals | Comparable/Comparator |"
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "// HashSet 底层实现\npublic class HashSet<E> extends AbstractSet<E> implements Set<E> {\n    private transient HashMap<E,Object> map;\n    // 所有元素共享的哑 value\n    private static final Object PRESENT = new Object();\n\n    public HashSet() {\n        map = new HashMap<>();\n    }\n\n    public boolean add(E e) {\n        return map.put(e, PRESENT) == null; // 利用 HashMap key 的唯一性\n    }\n\n    public boolean remove(Object o) {\n        return map.remove(o) == PRESENT;\n    }\n\n    public boolean contains(Object o) {\n        return map.containsKey(o);\n    }\n\n    public int size() {\n        return map.size();\n    }\n}\n\n// 使用示例\nSet<String> set = new HashSet<>();\nset.add(\"Java\");\nset.add(\"Python\");\nset.add(\"Java\");  // 重复元素，不会被添加\nSystem.out.println(set.size()); // 2",
      "language": "java",
      "highlights": [
        {
          "line": 12,
          "note": "HashSet 通过 HashMap 的 key 唯一性实现去重；put 返回 null 才表示此前没有该元素，重复插入不会增加集合大小。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "**高频问题1：HashSet的实现原理是什么？**\n\nHashSet基于HashMap实现：\n\n1. 存储结构：内部使用HashMap，元素作为key，value是一个固定的PRESENT对象\n2. 去重原理：依赖元素的hashCode()和equals()方法，先比较hashCode，相同再比较equals\n3. 添加元素：调用HashMap的put方法，如果key已存在则覆盖\n4. 线程安全：HashSet不是线程安全的，可以使用Collections.synchronizedSet()包装。\n\n**高频问题2：TreeSet和HashSet有什么区别？**\n\nTreeSet和HashSet的区别：\n\n1. 数据结构：HashSet基于HashMap，TreeSet基于TreeMap(红黑树)\n2. 有序性：HashSet无序，TreeSet有序(自然排序或自定义排序)\n3. null值：HashSet允许一个null，TreeSet 默认不允许 null；除非自定义 Comparator 明确支持 null 比较\n4. 性能：HashSet O(1)添加删除，TreeSet O(logn)添加删除\n5. 比较方式：HashSet依赖hashCode和equals，TreeSet依赖Comparable或Comparator。\n\n**高频问题3：HashMap和Hashtable有什么区别？**\n\nHashMap和Hashtable的区别：\n\n1. 线程安全：Hashtable线程安全(方法加synchronized)，HashMap非线程安全\n2. null值：Hashtable不允许null键和null值，HashMap允许一个null键和多个null值\n3. 初始容量：Hashtable初始容量11，HashMap初始容量16\n4. 扩容方式：Hashtable扩容2倍+1，HashMap扩容2倍\n5. 遍历方式：Hashtable使用Enumeration，HashMap使用Iterator\n6. 历史原因：Hashtable是JDK 1.0遗留类，不推荐使用。",
      "followUpQuestions": [
        {
          "question": "如果需要一个去重且有序的集合，你会选择 TreeSet 还是先 HashSet 再排序？为什么？",
          "answer": "取决于场景：\n1. 如果需要实时有序，选 TreeSet，O(log n) 插入但始终保持有序\n2. 如果只需最终有序，先 HashSet 去重 O(1)，最后排序 O(n log n)，整体更快\n3. 如果数据量小（<1000），差异可忽略，优先代码简洁性。"
        },
        {
          "question": "LinkedHashMap 的 accessOrder 模式在生产中有哪些典型应用？",
          "answer": "典型应用：\n1. LRU 缓存实现，继承 LinkedHashMap 并重写 removeEldestEntry\n2. 访问频率统计，结合 accessOrder 和计数器\n3. 会话超时管理，按最后访问时间排序清理过期会话。注意多线程场景需要额外同步。"
        },
        {
          "question": "PriorityQueue 在什么场景下会退化？如何避免？",
          "answer": "退化场景：\n1. 大量重复元素导致堆不平衡\n2. 自定义 Comparator 逻辑复杂影响性能\n3. 频繁扩容导致数组复制开销。避免方法：预估容量初始化、使用简单 Comparator、考虑用 TreeMap 替代（当需要去重时）。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能解释 HashSet 的去重原理（hashCode + equals）",
        "能比较 TreeSet 和 HashSet 的底层结构和性能差异",
        "能说明 TreeMap 的红黑树实现和排序机制",
        "能描述 LinkedHashMap 的双向链表和 accessOrder 模式",
        "能解释 PriorityQueue 的堆结构和 offer/poll 操作",
        "能根据场景选择合适的集合类"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "java.java-fundamentals.topic-030-e4f16979.recall.1",
      "prompt": "HashSet 为什么依赖 hashCode 和 equals？如果只重写 equals 会怎样？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.java-fundamentals.topic-030-e4f16979.recall.2",
      "prompt": "TreeSet 与 HashSet 的底层结构、复杂度和适用场景有什么区别？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.java-fundamentals.topic-030-e4f16979.recall.3",
      "prompt": "LinkedHashMap 如何基于访问顺序实现 LRU？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.java-fundamentals.topic-030-e4f16979.recall.4",
      "prompt": "PriorityQueue 的堆结构如何保证 offer/poll 的复杂度？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    }
  ],
  "rubric": {
    "mustHave": [
      "能解释 HashSet 基于 HashMap 的实现原理",
      "能比较 TreeSet 和 HashSet 的区别和适用场景",
      "能说明 TreeMap 的红黑树实现和有序性",
      "能解释 LinkedHashMap 如何实现 LRU 缓存"
    ],
    "goodToHave": [
      "能说出各集合的源码细节（如 HashMap 的扰动函数）",
      "能解释集合扩容机制和性能权衡",
      "能结合实际场景说明集合选择策略"
    ],
    "commonMistakes": [
      "不清楚 HashSet 去重依赖 hashCode 和 equals 的协作",
      "混淆 TreeMap 和 HashMap 的有序性",
      "不知道 PriorityQueue 默认是小顶堆"
    ],
    "scoreWeights": {
      "coverage": 35,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-06-18",
  "status": "draft",
  "prerequisites": [
    "java.java-fundamentals.topic-028-905d9b58"
  ],
  "interviewFrequency": "medium",
  "interviewerFocus": "考察是否能围绕「Set、TreeMap 与 Queue 集合」说清能解释 HashSet 基于 HashMap 的实现原理、能比较 TreeSet 和 HashSet 的区别和适用场景，并放到Java 工程场景中解释关键流程、边界条件和出错后的处理思路。"
}
