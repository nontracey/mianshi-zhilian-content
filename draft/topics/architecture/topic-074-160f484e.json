{
  "id": "architecture.system-design.topic-074-160f484e",
  "domain": "architecture",
  "category": "system-design",
  "group": "system-design",
  "title": "Redis 高可用架构",
  "summary": "理解 Redis 主从复制、哨兵和 Cluster 的高可用机制，能说明故障切换、复制延迟、槽位分片和一致性边界。",
  "tags": [
    "架构设计",
    "Redis",
    "高可用",
    "主从复制",
    "Cluster"
  ],
  "difficulty": 4,
  "estimatedMinutes": 40,
  "order": 55,
  "recommendWeight": 85,
  "learningCards": [
    {
      "type": "explain",
      "title": "Redis 高可用架构：主从复制、哨兵模式（Sentinel）",
      "content": "## 一、主从复制\n### 1.1 概述\n主从复制（Master-Slave Replication）是Redis高可用的基础。主节点负责写操作，从节点负责读操作，实现读写分离和数据备份。\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   主从复制架构                                │\n├──────────────────────────────────────────────────────────────┤\n│                                                              │\n│    ┌─────────┐     复制      ┌─────────┐                    │\n│    │ Master  │ ──────────→  │ Slave1  │                     │\n│    │ (写)    │              │ (只读)   │                     │\n│    └─────────┘              └─────────┘                     │\n│         │                                                  │\n│         │         复制                                      │\n│         └──────────────→  ┌─────────┐                      │\n│                           │ Slave2  │                      │\n│                           │ (只读)   │                      │\n│                           └─────────┘                      │\n└──────────────────────────────────────────────────────────────┘\n```\n### 1.2 全量同步（Full Sync）\n第一次连接或无法增量同步时，从节点执行全量同步：\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   全量同步流程                                │\n├──────────────────────────────────────────────────────────────┤\n│  1. 从节点发送 PSYNC ? -1（首次连接）                        │\n│     ↓                                                        │\n│  2. 主节点返回 +FULLRESYNC <runid> <offset>                 │\n│     ↓                                                        │\n│  3. 主节点执行 BGSAVE 生成 RDB 文件                          │\n│     ↓                                                        │\n│  4. 主节点将 RDB 文件发送给从节点                             │\n│     ↓                                                        │\n│  5. 从节点清空旧数据，加载 RDB                               │\n│     ↓                                                        │\n│  6. 主节点将 RDB 期间的新命令发送给从节点（repl_backlog）     │\n│     ↓                                                        │\n│  7. 同步完成，后续进入增量同步                                │\n└──────────────────────────────────────────────────────────────┘\n```\n**全量同步的开销**：\n- 主节点fork子进程生成RDB，可能阻塞\n- RDB文件传输占用网络带宽\n- 从节点加载RDB期间无法对外服务\n- 应尽量避免频繁全量同步\n### 1.3 增量同步（Partial Sync）\nRedis 2.8+支持增量同步（PSYNC），基于repl_backlog_buffer：\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   增量同步原理                                │\n├──────────────────────────────────────────────────────────────┤\n│  repl_backlog_buffer：一个环形缓冲区                         │\n│  ┌──────────────────────────────────────────┐               │\n│  │ cmd1│cmd2│cmd3│cmd4│cmd5│cmd6│cmd7│cmd8  │               │\n│  └──────────────────────────────────────────┘               │\n│       ↑                 ↑                                    │\n│    offset=1          offset=5                                │\n│    (从节点位置)       (主节点位置)                            │\n│                                                              │\n│  增量同步：                                                    │\n│  1. 从节点断线重连后发送 PSYNC <runid> <offset>              │\n│  2. 主节点检查 offset 是否在 backlog 中                      │\n│  3. 在 → 发送 offset 之后的命令（增量同步）                  │\n│  4. 不在 → 执行全量同步                                      │\n└──────────────────────────────────────────────────────────────┘\n```\n**配置参数**：\n```bash\n# redis.conf\nrepl-backlog-size 256mb       # backlog缓冲区大小（建议调大）\nrepl-backlog-ttl 3600         # 没有从节点连接时，backlog保留时间\nrepl-diskless-sync yes        # 是否使用无磁盘复制（RDB直接通过socket传输）\nrepl-diskless-sync-delay 5    # 无磁盘复制延迟（等待更多从节点加入）\n```\n### 1.4 主从配置\n```bash\n# 从节点配置（redis.conf）\nreplicaof <masterip> <masterport>    # 设置主节点地址\nmasterauth <password>                # 主节点密码\nreplica-read-only yes                # 从节点只读（默认）\n# 也可以在运行时设置\nredis-cli> REPLICAOF 192.168.1.100 6379\nredis-cli> CONFIG SET masterauth 123456\n```\n\n## 二、哨兵模式（Sentinel）\n### 2.1 概述\n哨兵模式在主从复制基础上增加了自动故障检测和故障转移能力。\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   哨兵模式架构                                │\n├──────────────────────────────────────────────────────────────┤\n│                                                              │\n│    ┌──────────┐  ┌──────────┐  ┌──────────┐                │\n│    │Sentinel1│  │Sentinel2│  │Sentinel3│                    │\n│    └──────────┘  └──────────┘  └──────────┘                │\n│         │              │              │                     │\n│         └──────────────┼──────────────┘                     │\n│                        │ 监控                                │\n│                        ↓                                    │\n│    ┌─────────┐    ┌─────────┐    ┌─────────┐               │\n│    │ Master  │───→│ Slave1  │───→│ Slave2  │               │\n│    └─────────┘    └─────────┘    └─────────┘               │\n│                                                              │\n│  哨兵负责：监控、通知、自动故障转移、配置中心                 │\n└──────────────────────────────────────────────────────────────┘\n```\n### 2.2 故障检测\n哨兵使用**主观下线（SDOWN）**和**客观下线（ODOWN）**两种机制：\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   故障检测流程                                │\n├──────────────────────────────────────────────────────────────┤\n│  1. 每个哨兵每秒向主节点发送PING                             │\n│     ↓                                                        │\n│  2. 主节点超过down-after-milliseconds未响应 → SDOWN（主观）  │\n│     ↓                                                        │\n│  3. 该哨兵询问其他哨兵：\"主节点是否下线？\"                   │\n│     ↓                                                        │\n│  4. 当 >= quorum个哨兵认为主节点下线 → ODOWN（客观）         │\n│     ↓                                                        │\n│  5. 触发故障转移流程                                         │\n└──────────────────────────────────────────────────────────────┘\n```\n### 2.3 故障转移（Failover）\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   故障转移流程                                │\n├──────────────────────────────────────────────────────────────┤\n│  1. 哨兵之间选举Leader（Raft算法）                           │\n│     ↓                                                        │\n│  2. Leader哨兵从从节点中选择新Master                          │\n│     选择规则：                                               │\n│     - 排除已下线的节点                                       │\n│     - 排除最近与主节点断线时间过长的节点                      │\n│     - 优先选择 replica-priority 最小的                       │\n│     - 优先选择复制偏移量最大的（数据最新）                    │\n│     - 优先选择 runid 最小的                                  │\n│     ↓                                                        │\n│  3. 向选中的从节点发送 SLAVEOF NO ONE（升级为Master）        │\n│     ↓                                                        │\n│  4. 向其他从节点发送 SLAVEOF <new_master_ip> <port>          │\n│     ↓                                                        │\n│  5. 将旧Master设置为新Master的从节点（恢复后自动成为从节点）  │\n└──────────────────────────────────────────────────────────────┘\n```\n### 2.4 哨兵配置\n```bash\n# sentinel.conf\nport 26379\nsentinel monitor mymaster 127.0.0.1 6379 2    # 监控主节点，quorum=2\nsentinel down-after-milliseconds mymaster 30000  # 30秒无响应判定SDOWN\nsentinel failover-timeout mymaster 180000        # 故障转移超时180秒\nsentinel parallel-syncs mymaster 1               # 同时同步的从节点数\nsentinel auth-pass mymaster 123456               # 主节点密码\n```"
    },
    {
      "type": "explain",
      "title": "Redis 高可用架构：Cluster集群、数据分片策略",
      "content": "## 三、Cluster集群\n### 3.1 概述\nRedis Cluster是Redis 3.0+的分布式集群方案，支持数据分片和高可用。\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   Cluster集群架构                             │\n├──────────────────────────────────────────────────────────────┤\n│                                                              │\n│  ┌────────────┐  ┌────────────┐  ┌────────────┐            │\n│  │  Node A    │  │  Node B    │  │  Node C    │            │\n│  │ Slot 0-5460│  │Slot 5461-  │  │Slot 10923- │            │\n│  │ (Master)   │  │10922(Master)│ │16383(Master)│            │\n│  └─────┬──────┘  └─────┬──────┘  └─────┬──────┘            │\n│        │               │               │                    │\n│  ┌─────┴──────┐  ┌─────┴──────┐  ┌─────┴──────┐            │\n│  │  Node A'   │  │  Node B'   │  │  Node C'   │            │\n│  │ (Slave)    │  │ (Slave)    │  │ (Slave)    │            │\n│  └────────────┘  └────────────┘  └────────────┘            │\n│                                                              │\n│  16384个哈希槽分配到3个主节点                                │\n│  每个主节点至少1个从节点（高可用）                            │\n└──────────────────────────────────────────────────────────────┘\n```\n### 3.2 哈希槽（Hash Slot）\nRedis Cluster将所有数据划分为**16384个哈希槽**：\n```bash\n# 计算key属于哪个槽\nHASH_SLOT = CRC16(key) mod 16384\n# 例如\nCRC16(\"user:1001\") mod 16384 = 8888   → Node B\nCRC16(\"order:2001\") mod 16384 = 3333  → Node A\n```\n**Hash Tag**：可以强制多个key分配到同一个槽\n```bash\n# {user:1001} 中的 {user:1001} 是hash tag\n# 只对 {} 内的内容计算CRC16\nSET {user:1001}:name \"张三\"    # 计算CRC16(\"user:1001\")\nSET {user:1001}:age 25         # 同一个槽，可以执行事务/Lua脚本\n```\n### 3.3 节点通信（Gossip协议）\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   Gossip协议通信                              │\n├──────────────────────────────────────────────────────────────┤\n│  每个节点每秒随机选择几个节点发送PING消息                    │\n│  消息内容：自己的状态 + 已知的其他节点状态                    │\n│                                                              │\n│  消息类型：                                                   │\n│  MEET    - 新节点加入集群                                    │\n│  PING    - 心跳检测，携带自己和部分节点信息                   │\n│  PONG    - 对PING/MEET的响应                                 │\n│  FAIL    - 广播节点故障信息                                  │\n│                                                              │\n│  特点：                                                       │\n│  - 去中心化，每个节点都知道整个集群的拓扑                     │\n│  - 最终一致性，新状态需要几秒才能传播到所有节点               │\n│  - 故障检测：cluster-node-timeout 毫秒内无响应 → 标记PFAIL   │\n└──────────────────────────────────────────────────────────────┘\n```\n### 3.4 Cluster配置与操作\n```bash\n# 创建集群（Redis 5.0+）\nredis-cli --cluster create \\\n192.168.1.101:6379 192.168.1.102:6379 192.168.1.103:6379 \\\n192.168.1.104:6379 192.168.1.105:6379 192.168.1.106:6379 \\\n--cluster-replicas 1\n# redis.conf 集群配置\ncluster-enabled yes\ncluster-config-file nodes.conf\ncluster-node-timeout 15000\n# 查看集群信息\nredis-cli> CLUSTER INFO\nredis-cli> CLUSTER NODES\n# 添加节点\nredis-cli --cluster add-node <new_node_ip:port> <existing_node_ip:port>\n# 迁移槽位\nredis-cli --cluster reshard <node_ip:port>\n# 删除节点\nredis-cli --cluster del-node <node_ip:port> <node_id>\n```\n### 3.5 客户端重定向\n```bash\n# 当客户端发送的key不在当前节点时\nredis-cli> SET user:1001 \"张三\"\n(error) MOVED 8888 192.168.1.102:6379\n# 客户端需要重定向到 192.168.1.102:6379\n# 使用 -c 参数自动重定向\nredis-cli -c -h 192.168.1.101 -p 6379\n```\n\n## 五、数据分片策略\n### 5.1 Redis Cluster的分片策略\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   数据分片策略对比                            │\n├──────────────────────────────────────────────────────────────┤\n│  客户端分片：                                                │\n│  - 客户端自己决定key发往哪个节点                             │\n│  - 优点：简单，无需代理                                      │\n│  - 缺点：扩缩容需要客户端配合                                │\n│                                                              │\n│  代理分片（如Twemproxy/Codis）：                             │\n│  - 请求先到代理，代理转发到对应节点                           │\n│  - 优点：客户端透明                                          │\n│  - 缺点：增加一跳延迟，代理成为瓶颈                          │\n│                                                              │\n│  Redis Cluster分片：                                         │\n│  - 节点自身维护槽位映射                                      │\n│  - 客户端缓存槽位映射，直连目标节点                          │\n│  - 优点：去中心化，性能好                                    │\n│  - 缺点：客户端需要支持Cluster协议                           │\n└──────────────────────────────────────────────────────────────┘\n```"
    },
    {
      "type": "explain",
      "title": "Redis 高可用架构：故障切换与一致性边界",
      "content": "Redis 高可用不是“部署多个节点”这么简单，关键要同时回答复制、故障检测、主从切换和分片扩容。主从复制解决读扩展和数据备份，但写入仍在主节点且复制是异步的；哨兵负责主观下线、客观下线和故障转移，但切换期间可能出现短暂不可用和少量数据丢失；Cluster 通过 16384 个槽位做分片，并让每个 master 配 slave 承担故障转移。面试时要说明业务能接受的 RPO/RTO、是否需要强一致、热点 key 和大 key 是否会破坏分片均衡。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | 主从复制 | 哨兵模式 | Cluster模式 |\n| --- | --- | --- | --- |\n| 数据分片 | 不支持 | 不支持 | 支持（16384槽位） |\n| 自动故障转移 | 不支持 | 支持 | 支持 |\n| 写扩展 | 不支持 | 不支持 | 支持多主写入 |\n| 实现复杂度 | 低 | 中 | 高 |\n| 需要Sentinel | 不需要 | 需要 | 不需要 |\n| 数据一致性 | 异步复制，有延迟 | 异步复制 | 异步复制 |\n| 适用场景 | 读多写少 | 高可用要求 | 大数据量+高并发 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "Redis 高可用架构的核心结构图解。",
      "items": [
        "主从复制：Master写 → 全量/增量同步 → Slave读",
        "全量同步：PSYNC → BGSAVE → RDB传输 → 加载",
        "增量同步：repl_backlog缓冲区同步增量命令",
        "哨兵监控：主观下线(SDOWN) → 客观下线(ODOWN)",
        "故障转移：选举Leader Sentinel → 提升最优Slave",
        "Cluster槽位：CRC16(key) % 16384分配到节点",
        "Smart Client：缓存槽位映射，MOVED/ASK重定向"
      ]
    },
    {
      "type": "code",
      "title": "Redis 主从与哨兵配置片段",
      "content": "redis-cli> REPLICAOF 10.0.0.1 6379\nredis-cli> CONFIG SET masterauth strong-password\n\n# sentinel.conf\nsentinel monitor mymaster 10.0.0.1 6379 2\nsentinel down-after-milliseconds mymaster 5000\nsentinel failover-timeout mymaster 60000\nsentinel parallel-syncs mymaster 1",
      "language": "bash"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "**问题1：Redis主从复制、哨兵、Cluster三种模式有什么区别？**\n\n主从复制是基础，Master负责写，Slave负责读，实现读写分离。但不能自动故障转移，Master宕机需要手动切换。\n\n哨兵模式在主从基础上增加了Sentinel进程，负责监控Master/Slave状态。Master宕机后自动选举新Master，实现故障转移。但数据还是存在一个节点上，写能力受限。\n\nCluster模式是真正的分布式集群，数据通过16384个槽位分片存储在多个节点上。每个节点负责一部分槽位，支持水平扩展。不需要Sentinel，Cluster自己有故障检测和转移能力。\n\n简单说：主从解决读扩展，哨兵解决高可用，Cluster解决写扩展和数据分片。\n\n**问题2：Redis Cluster的槽位分配和路由机制是怎样的？**\n\nRedis Cluster有16384个槽位，通过CRC16(key) % 16384计算key属于哪个槽位。每个Master节点负责一部分槽位。\n\n客户端使用Smart Client，缓存了槽位和节点的映射关系。发送命令时直接定位到正确的节点。如果节点返回MOVED重定向，客户端会更新本地路由表。如果正在迁移槽位，返回ASK重定向，客户端需要发送ASKING命令到目标节点。",
      "followUpQuestions": [
        {
          "question": "高可用架构的核心原则？",
          "answer": "核心原则：1. 冗余（多副本）；2. 故障检测（心跳、超时）；3. 故障转移（主从切换）；4. 限流降级（保护系统）；5. 监控告警（快速发现）。实现方式：集群、负载均衡、熔断、降级、限流、异步、缓存。"
        },
        {
          "question": "如何设计高可用系统？",
          "answer": "设计要点：1. 无状态设计（便于扩展）；2. 服务拆分（微服务）；3. 数据冗余（主从、分片）；4. 故障隔离（舱壁模式）；5. 异步解耦（消息队列）；6. 缓存策略（多级缓存）；7. 监控告警（全链路追踪）。"
        },
        {
          "question": "限流算法有哪些？",
          "answer": "限流算法：1. 计数器（固定窗口）；2. 滑动窗口；3. 漏桶（恒定速率）；4. 令牌桶（允许突发）。实现：Guava RateLimiter（令牌桶）、Sentinel（滑动窗口）、Nginx（漏桶）。选择：突发流量→令牌桶；恒定速率→漏桶。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能说出主从复制的全量同步和增量同步流程",
        "能解释哨兵模式的故障转移流程",
        "能说出Cluster的16384槽位分配和路由机制",
        "能区分三种模式的适用场景",
        "能解释Smart Client的工作原理和MOVED/ASK重定向",
        "能说出全量同步的触发条件和性能开销",
        "能说明Cluster的扩缩容方式"
      ]
    }
  ],
  "recallPrompts": [
    {
      "prompt": "高可用架构中限流、熔断、降级三者各自解决什么问题？如何配合使用？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2,
      "id": "architecture.system-design.topic-074-160f484e.recall.1"
    },
    {
      "prompt": "限流算法有哪些？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2,
      "id": "architecture.system-design.topic-074-160f484e.recall.2"
    }
  ],
  "rubric": {
    "mustHave": [
      "能区分主从复制、哨兵和 Cluster 的职责",
      "能解释异步复制导致的延迟和数据丢失窗口",
      "能说明哨兵故障转移流程和 quorum 作用",
      "能描述 Cluster 槽位分片和主从故障转移"
    ],
    "goodToHave": [
      "能结合 RPO/RTO 选择部署方案",
      "能说明热点 key、大 key 和脑裂风险",
      "能给出监控指标和演练方案"
    ],
    "commonMistakes": [
      "把主从复制误认为自动故障转移方案",
      "忽略 Redis 复制是异步的，承诺强一致",
      "只背哨兵/Cluster 名词，说不清故障切换窗口"
    ],
    "scoreWeights": {
      "coverage": 30,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 20
    }
  },
  "updatedAt": "2026-06-11",
  "status": "draft",
  "prerequisites": [],
  "interviewFrequency": "high",
  "interviewerFocus": "考察是否能围绕「Redis 高可用架构」说清主从、哨兵、Cluster 的职责边界，并解释复制延迟、故障切换和槽位分片对一致性与可用性的影响。"
}
