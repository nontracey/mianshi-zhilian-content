{
  "id": "algorithm.array.topic-leetcode-56",
  "domain": "algorithm",
  "category": "array",
  "group": "array",
  "title": "LeetCode 56: 合并区间",
  "summary": "合并所有重叠区间，核心是按起点排序后线性扫描合并。",
  "tags": [
    "算法与数据结构",
    "数组",
    "排序",
    "区间",
    "LeetCode"
  ],
  "difficulty": 2,
  "estimatedMinutes": 12,
  "order": 20,
  "recommendWeight": 85,
  "leetcodeUrl": "https://leetcode.cn/problems/merge-intervals/",
  "learningCards": [
    {
      "type": "explain",
      "title": "题目描述",
      "content": "# LeetCode 56: 合并区间\n\n## 题目描述\n以数组 `intervals` 表示若干区间，`intervals[i] = [start_i, end_i]`。合并所有重叠的区间，返回不重叠的区间数组。\n\n## 示例\n```\n输入：intervals = [[1,3],[2,6],[8,10],[15,18]]\n输出：[[1,6],[8,10],[15,18]]\n解释：[1,3] 和 [2,6] 重叠，合并为 [1,6]。\n```"
    },
    {
      "type": "explain",
      "title": "解法：按起点排序 + 线性合并",
      "content": "## 思路\n1. **按区间起点升序排序**。\n2. 线性遍历，维护结果列表的最后一个区间 `last`：\n   - 若当前区间起点 `cur[0] <= last[1]`（与 last 重叠或相接），合并：`last[1] = max(last[1], cur[1])`。\n   - 否则不重叠，把 cur 加入结果。\n\n## 为什么按起点排序就够\n排序后，任何能与某区间合并的区间一定**紧邻其后**；只要当前起点不超过已合并区间的右端，就一定重叠，故一次线性扫描即可。\n\n## 复杂度\n- 时间 O(n log n)（排序主导）；空间 O(n) 结果（或 O(log n) 排序栈）。\n\n## 边界\n- `[1,4],[4,5]` 端点相接算重叠（用 <=）。\n- 合并时取右端 max，避免 `[1,5],[2,3]` 被错误截短。"
    },
    {
      "type": "code",
      "title": "完整代码实现",
      "content": "import java.util.Arrays;\nimport java.util.ArrayList;\nimport java.util.List;\n\npublic class MergeIntervals {\n    public int[][] merge(int[][] intervals) {\n        Arrays.sort(intervals, (a, b) -> a[0] - b[0]);     // 按起点排序\n        List<int[]> res = new ArrayList<>();\n        for (int[] cur : intervals) {\n            if (res.isEmpty() || cur[0] > res.get(res.size() - 1)[1]) {\n                res.add(cur);                               // 不重叠，新开\n            } else {\n                int[] last = res.get(res.size() - 1);\n                last[1] = Math.max(last[1], cur[1]);         // 重叠，扩右端\n            }\n        }\n        return res.toArray(new int[res.size()][]);\n    }\n}",
      "language": "java"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "**Q1: 合并区间怎么做？**\n先按区间起点升序排序，再线性扫描：当前起点 <= 结果中最后一个区间的右端就合并（右端取 max），否则新开一个区间。时间 O(n log n)。\n\n**Q2: 为什么排序后一次扫描就够？**\n排序后能与某区间合并的都紧邻其后；只要当前起点不超过已合并区间的右端就一定重叠，不会有“跳过去又能合并”的情况。\n\n**Q3: 端点相接算不算重叠？**\n看题意，本题 [1,4] 与 [4,5] 通常算重叠，用 cur[0] <= last[1] 判断（取等合并）。",
      "followUpQuestions": [
        {
          "question": "合并时为什么右端取 max 而不是直接用 cur[1]？",
          "answer": "可能出现 [1,5] 之后是 [2,3] 这种被包含的区间，若直接用 cur[1] 会把右端从 5 截短到 3，必须取 max(last[1], cur[1])。"
        },
        {
          "question": "不排序能做吗？",
          "answer": "不排序就无法保证可合并区间相邻，需要反复两两比较合并直到稳定，复杂度退化到 O(n²)；排序把它降到 O(n log n)。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能写出按起点排序 + 线性合并的解法",
        "能解释排序后一次扫描即可的原因",
        "能说明右端取 max 的必要性",
        "能处理端点相接是否重叠的判断",
        "能分析 O(n log n) 复杂度"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "algorithm.array.topic-leetcode-56.recall.1",
      "prompt": "请手写 LeetCode 56 合并区间的代码。",
      "mode": "code",
      "expectedMinutes": 4,
      "difficulty": 2
    },
    {
      "id": "algorithm.array.topic-leetcode-56.recall.2",
      "prompt": "解释为什么按起点排序后一次线性扫描就能合并所有重叠区间，且右端要取 max。",
      "mode": "text",
      "expectedMinutes": 2,
      "difficulty": 2
    }
  ],
  "rubric": {
    "mustHave": [
      "按起点排序",
      "线性扫描合并逻辑",
      "右端取 max",
      "O(n log n) 复杂度"
    ],
    "goodToHave": [
      "排序后一次扫描的正确性论证",
      "端点相接的处理",
      "不排序的退化复杂度"
    ],
    "commonMistakes": [
      "忘记排序直接扫描",
      "合并时右端直接用 cur[1] 截短区间",
      "重叠判断符号写反",
      "把端点相接漏判"
    ],
    "scoreWeights": {
      "coverage": 25,
      "accuracy": 40,
      "interviewExpression": 15,
      "depth": 20
    }
  },
  "updatedAt": "2026-06-11",
  "status": "draft",
  "interviewFrequency": "high",
  "interviewerFocus": "考察按起点排序 + 线性合并的标准区间套路，以及右端取 max、排序正确性的理解。"
}
