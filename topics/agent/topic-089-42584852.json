{
  "id": "agent.llm.topic-089-42584852",
  "domain": "agent",
  "category": "llm",
  "group": "llm",
  "title": "RAG进阶",
  "summary": "1.1 Naive RAG的局限",
  "tags": [
    "Agent",
    "LLM 基础",
    "RAG进阶"
  ],
  "difficulty": 2,
  "estimatedMinutes": 20,
  "order": 890,
  "recommendWeight": 91,
  "learningCards": [
    {
      "type": "explain",
      "title": "知识全景",
      "content": "# RAG进阶\n## 一、RAG进阶架构\n### 1.1 Naive RAG的局限\n```\nNaive RAG流程：\n用户问题 → Embedding → 向量检索Top-K → 注入Prompt → LLM生成\n问题：\n1. 检索不精准——单一Embedding可能遗漏关键信息\n2. 上下文质量差——Top-K中可能混入不相关文档\n3. 无法处理复杂问题——多跳推理、跨文档综合困难\n4. 信息过时——知识库更新后需要重建索引\n```\n### 1.2 Advanced RAG架构\n```\nAdvanced RAG三层优化：\n第一层：索引优化\n├── 结构感知分块\n├── 元数据标注\n├── 父子文档关系\n└── 多粒度Embedding\n第二层：检索优化\n├── 混合检索（向量+BM25）\n├── 重排序（Cross-Encoder）\n├── 查询改写（Multi-Query）\n└── 路由（按问题类型选检索策略）\n第三层：生成优化\n├── 引用溯源\n├── 答案校验\n├── 自检与重试\n└── 流式输出\n```\n## 二、高级检索策略\n### 2.1 查询改写（Query Transformation）\n#### Multi-Query\n```python\nfrom langchain.retrievers.multi_query import MultiQueryRetriever\n# LLM将原始问题改写为多个不同角度的查询\nprompt = \"\"\"\n你是一位查询优化专家。请将用户问题改写为3个不同角度的搜索查询，\n以便从知识库中检索到最相关的信息。\n原始问题：{question}\n请输出3个改写后的查询，每行一个：\n\"\"\"\n# 效果：原始问题\"系统性能差怎么办\"\n# 改写1：\"系统性能优化的方法\"\n# 改写2：\"如何排查系统性能问题\"\n# 改写3：\"系统响应慢的原因分析\"\n```\n#### HyDE（Hypothetical Document Embeddings）\n```\nHyDE流程：\n1. 用户问题 → LLM生成假设性答案\n2. 假设性答案 → Embedding\n3. 用假设性答案的Embedding去检索\n4. 检索到的真实文档 → LLM生成最终答案\n为什么有效：\n- 问题和文档的Embedding空间存在鸿沟\n- 假设性答案的Embedding更接近真实文档\n- 特别适合需要推理的复杂问题\n```\n```python\ndef hyde_retrieval(question, llm, vectorstore, k=5):\n    # 步骤1：生成假设性答案\n    hyde_prompt = f\"请回答以下问题（即使你不确定，也请给出一个假设性答案）：\\n{question}\"\n    hypothetical_answer = llm.invoke(hyde_prompt).content\n    # 步骤2：用假设性答案检索\n    hyde_embedding = embedding_model.embed(hypothetical_answer)\n    docs = vectorstore.similarity_search_by_vector(hyde_embedding, k=k)\n    # 步骤3：用真实文档生成最终答案\n    final_prompt = f\"基于以下上下文回答问题：\\n{question}\\n\\n上下文：\\n{format_docs(docs)}\"\n    return llm.invoke(final_prompt).content\n```\n#### Step-Back Prompting\n```\n原理：将具体问题抽象为更通用的\"后退\"问题，先获取背景知识\n示例：\n原始问题：\"2025年Q3我们的营收比Q2增长了多少？\"\n后退问题：\"公司季度营收数据的计算方法和数据来源是什么？\"\n流程：\n1. 生成后退问题\n2. 用后退问题检索背景知识\n3. 用原始问题+背景知识生成答案\n```\n### 2.2 重排序（Reranking）\n#### Cross-Encoder vs Bi-Encoder\n```\nBi-Encoder（向量检索用的）：\n- 查询和文档分别独立编码为向量\n- 用向量相似度计算相关性\n- 优点：可以预计算文档向量，检索极快\n- 缺点：无法建模查询-文档的细粒度交互\nCross-Encoder（重排序用的）：\n- 查询和文档拼接后一起编码\n- 直接输出相关性分数\n- 优点：精度高，能捕获细粒度语义\n- 缺点：每对查询-文档都要重新计算，慢\n最佳实践：\nBi-Encoder召回Top-50 → Cross-Encoder重排序 → 取Top-5\n```\n#### 主流Reranker模型\n```python\n# 使用BGE-Reranker\nfrom FlagEmbedding import FlagReranker\nreranker = FlagReranker('BAAI/bge-reranker-v2-m3', use_fp16=True)\n# 重排序\npairs = [(query, doc.page_content) for doc in candidate_docs]\nscores = reranker.compute_score(pairs)\n# 按分数排序\nranked = sorted(zip(candidate_docs, scores), key=lambda x: x[1], reverse=True)\ntop_docs = [doc for doc, score in ranked[:5]]\n```\n### 2.3 路由检索（Routing）\n```python\nfrom langchain_core.output_parsers import StrOutputParser\n# 路由器：根据问题类型选择检索策略\nrouter_prompt = \"\"\"\n根据用户问题，选择最合适的检索策略：\n- 如果是精确的事实查询（如\"XX的定义是什么\"），返回 \"exact\"\n- 如果是探索性问题（如\"如何实现XX\"），返回 \"explore\"\n- 如果是比较性问题（如\"XX和YY的区别\"），返回 \"compare\"\n- 如果是多跳推理问题，返回 \"multi_hop\"\n问题：{question}\n返回策略名称（只返回一个词）：\n\"\"\"\nrouter = router_prompt | llm | StrOutputParser()\ndef route_and_retrieve(question):\n    strategy = router.invoke({\"question\": question})\n    if strategy == \"exact\":\n        return exact_search(question)        # 精确匹配\n    elif strategy == \"explore\":\n        return multi_query_search(question)  # 多查询检索\n    elif strategy == \"compare\":\n        return compare_search(question)      # 分别检索再综合\n    elif strategy == \"multi_hop\":\n        return multi_hop_search(question)    # 多跳检索\n    else:\n        return default_search(question)\n```\n## 三、高级分块策略\n### 3.1 父子分块（Parent-Child Chunking）\n```\n原理：\n- 将文档切分为小块（Child，用于检索）\n- 每个小块关联其所属的大块（Parent，用于生成）\n- 检索时匹配Child，返回时注入Parent的完整上下文\n优势：\n- 检索精度高（小块更精准匹配查询）\n- 生成质量好（大块提供完整上下文）\n```\n```python\n# 实现父子分块\nfrom langchain.text_splitter import RecursiveCharacterTextSplitter\n# 父块（大块，用于生成）\nparent_splitter = RecursiveCharacterTextSplitter(\nchunk_size=2000,\nchunk_overlap=200\n)\n# 子块（小块，用于检索）\nchild_splitter = RecursiveCharacterTextSplitter(\nchunk_size=500,\nchunk_overlap=50\n)\n# 创建父子关系\nparent_docs = parent_splitter.split_documents(docs)\nchild_docs = []\nfor i, parent in enumerate(parent_docs):\n    children = child_splitter.split_documents([parent])\n    for child in children:\n        child.metadata[\"parent_id\"] = parent.metadata[\"doc_id\"]\n        child.metadata[\"parent_content\"] = parent.page_content\n        child_docs.extend(children)\n        # 检索时\n        def retrieve_with_parent(query, vectorstore, k=5):\n            # 检索子块\n            child_results = vectorstore.similarity_search(query, k=k)\n            # 返回父块内容\n            parent_contents = []\n            seen_parents = set()\n            for child in child_results:\n                parent_id = child.metadata.get(\"parent_id\")\n                if parent_id and parent_id not in seen_parents:\n                    parent_contents.append(child.metadata[\"parent_content\"])\n                    seen_parents.add(parent_id)\n                    return parent_contents\n```\n### 3.2 语义分块（Semantic Chunking）\n```\n原理：按语义边界而非固定长度切分\n方法：\n1. 计算相邻句子之间的Embedding相似度\n2. 相似度突降的位置就是切分点\n3. 将连续的高相似度句子合并为一个块\n优势：保持语义连贯性，避免在句子中间截断\n```\n```python\nfrom langchain_experimental.text_splitter import SemanticChunker\nsplitter = SemanticChunker(\nembeddings=embedding_model,\nbreakpoint_threshold_type=\"percentile\",  # 按百分位确定阈值\nbreakpoint_threshold_amount=90           # 相似度低于90%时切分\n)\nchunks = splitter.create_documents(text)\n```\n### 3.3 分块策略选择指南\n## 四、GraphRAG\n### 4.1 什么是GraphRAG\n```\n传统RAG的局限：\n- 文档块之间是独立的，无法捕获实体关系\n- 无法回答\"全局性\"问题（如\"文档中提到了哪些主要人物及其关系\"）\nGraphRAG的解决：\n- 从文档中提取实体和关系，构建知识图谱\n- 将图谱信息融入检索过程\n- 支持全局性查询和多跳推理\n```\n### 4.2 GraphRAG流程\n```\n1. 文档处理\n↓\n2. 实体提取（人名、组织、地点、概念等）\n↓\n3. 关系提取（A是B的CEO，A公司在B城市等）\n↓\n4. 构建知识图谱（实体为节点，关系为边）\n↓\n5. 社区发现（将图谱划分为多个社区）\n↓\n6. 为每个社区生成摘要\n↓\n7. 检索时：\n- 全局查询 → 使用社区摘要\n- 具体查询 → 使用实体/关系 + 原文档\n```\n### 4.3 Microsoft GraphRAG\n```python\n# 使用Microsoft GraphRAG\nfrom graphrag import GraphRAG\n# 索引\ngraphrag.index(\ninput_dir=\"./documents\",\noutput_dir=\"./graphrag_output\",\nentity_types=[\"person\", \"organization\", \"location\", \"concept\"]\n)\n# 查询\n# 全局查询（全局性问题）\nresult = graphrag.query(\n\"文档中讨论了哪些主要主题？\",\nmethod=\"global\"\n)\n# 局部查询（具体事实）\nresult = graphrag.query(\n\"张三在项目中担任什么角色？\",\nmethod=\"local\"\n)\n```\n### 4.4 GraphRAG vs 传统RAG\n## 五、面试高频问题\n**Q1: RAG系统如何评估效果？**\n> 分层评估：1）检索层——Recall@K、MRR、NDCG，评估检索质量；2）重排序层——Precision@K，评估排序质量；3）生成层——Faithfulness（是否忠实于上下文）、Answer Relevancy（答案是否切题）、Context Precision（上下文是否精准），用RAGAS框架自动评估。关键：建立Golden Dataset（500+标注样本），每次迭代后自动跑评估Pipeline。\n**Q2: 重排序有必要吗？性能开销大不大？**\n> 有必要。实测表明，向量检索+重排序比纯向量检索的NDCG提升15-25%。性能方面：Bi-Encoder检索Top-50约10ms，Cross-Encoder重排序50对约50ms，总延迟60ms，对用户体验影响不大。如果延迟敏感，可以用更轻量的Reranker模型或减少候选集大小。\n**Q3: GraphRAG和传统RAG怎么选？**\n> 判断标准：1）如果主要是事实查询（\"XX是什么\"），传统RAG足够；2）如果需要全局分析（\"文档中有哪些主题\"）、关系查询（\"A和B的关系\"）、多跳推理，GraphRAG效果更好；3）GraphRAG构建成本高（需要实体/关系提取），适合文档量稳定、查询复杂的场景；4）实际项目中，可以先用传统RAG验证需求，再根据查询类型决定是否引入GraphRAG。\n**Q4: 如何处理RAG中的多语言场景？**\n> 1）Embedding模型选多语言的（BGE-M3支持100+语言）；2）分块时注意不同语言的标点符号（中文句号vs英文句号）；3）检索时查询和文档用同一个Embedding模型；4）如果知识库是混合语言，可以分别建索引，检索时根据查询语言路由到对应索引。\n## 六、今日笔记\n```\n日期：_______________\n今日重点掌握：\n□ Advanced RAG三层优化架构\n□ 查询改写（Multi-Query/HyDE/Step-Back）\n□ 重排序（Cross-Encoder vs Bi-Encoder）\n□ 路由检索\n□ 高级分块策略（父子/语义）\n□ GraphRAG原理\n最深印象的知识点：\n_______________________________________________\n需要进一步研究的内容：\n_______________________________________________\n面试中需要注意的话术要点：\n_______________________________________________\n```\n## 七、参考资料\n- [Advanced RAG论文](https://arxiv.org/abs/2312.10997)\n- [HyDE论文](https://arxiv.org/abs/2212.10496)\n- [Step-Back Prompting论文](https://arxiv.org/abs/2310.06117)\n- [Microsoft GraphRAG](https://microsoft.github.io/graphrag/)\n- [RAGAS评估框架](https://docs.ragas.io/)\n- [BGE-Reranker](https://huggingface.co/BAAI/bge-reranker-v2-m3)\n- [LangChain MultiQueryRetriever](https://python.langchain.com/docs/modules/data_connection/retrievers/multi-query-retriever)"
    },
    {
      "type": "explain",
      "title": "关键机制拆解",
      "content": "先把 RAG进阶 当成一个可讲清楚的系统来理解：\n\n1. 一、RAG进阶架构\n2. 1 Naive RAG的局限\n3. Naive RAG流程：\n4. 用户问题 → Embedding → 向量检索Top-K → 注入Prompt → LLM生成\n5. 检索不精准——单一Embedding可能遗漏关键信息\n\n学习时重点关注“为什么需要它、它如何工作、什么时候会出问题、面试时怎么结构化表达”。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | RAG进阶 | 相邻知识/常见误解 |\n| --- | --- | --- |\n| 解决的问题 | 说明核心目标和使用场景 | 只背定义，忽略为什么需要它 |\n| 核心机制 | 拆成关键角色、流程、边界条件 | 把实现细节和概念边界混在一起 |\n| 面试表达 | 先结论，再机制，再场景和取舍 | 直接罗列术语，缺少层次 |\n| 关联分类 | LLM 基础 | 需要能和同类知识做横向比较 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "RAG进阶 的学习路径图，用于替代原始 Markdown 中容易变形的 ASCII 图。",
      "fallback": "建议用“输入/触发 → 核心流程 → 关键状态 → 输出/风险”的流程图复述 RAG进阶，并标出它在 LLM 基础 中的位置。",
      "items": [
        "定位：LLM 基础",
        "输入：问题场景或触发条件",
        "机制：1 Naive RAG的局限",
        "输出：面试可讲的结论与取舍"
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "Naive RAG流程：\n用户问题 → Embedding → 向量检索Top-K → 注入Prompt → LLM生成\n问题：\n1. 检索不精准——单一Embedding可能遗漏关键信息\n2. 上下文质量差——Top-K中可能混入不相关文档\n3. 无法处理复杂问题——多跳推理、跨文档综合困难\n4. 信息过时——知识库更新后需要重建索引"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "可以按“四段式”回答 RAG进阶：先用一句话给定义和价值；再拆 LLM 基础 里的关键机制；接着补充适用场景、性能或一致性等取舍；最后用项目排查、优化或设计经验收束。"
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "RAG进阶 的定义和边界",
        "RAG进阶 的关键机制",
        "RAG进阶 的典型场景和常见追问",
        "RAG进阶 在面试中的结构化表达方式"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "agent.llm.topic-089-42584852.recall.1",
      "prompt": "请用自己的话解释 RAG进阶。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "agent.llm.topic-089-42584852.recall.2",
      "prompt": "如果面试官追问 RAG进阶 的应用场景或常见误区，你会怎么回答？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "定义准确",
      "关键机制",
      "适用场景",
      "常见误区",
      "面试表达结构"
    ],
    "goodToHave": [
      "结合项目经验",
      "能做对比",
      "能说明取舍"
    ],
    "commonMistakes": [
      "只背概念不讲原因",
      "忽略边界条件",
      "缺少面试化表达"
    ],
    "scoreWeights": {
      "coverage": 40,
      "accuracy": 25,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-05-27"
}
