{
  "id": "python.python-advanced.decorators",
  "domain": "python",
  "category": "python-advanced",
  "group": "python-advanced",
  "title": "装饰器",
  "summary": "装饰器原理、functools.wraps、带参数装饰器、类装饰器、装饰器执行时机",
  "tags": [
    "Python",
    "装饰器",
    "高阶函数",
    "闭包"
  ],
  "difficulty": 3,
  "estimatedMinutes": 20,
  "order": 50,
  "recommendWeight": 90,
  "learningCards": [
    {
      "type": "explain",
      "title": "装饰器：本质、基础装饰器",
      "content": "## 一、本质\n\n装饰器本质是**接受一个函数并返回一个新函数的高阶函数**。语法糖 `@decorator` 等价于 `func = decorator(func)`。\n\n```python\n@decorator\ndef func():\n    pass\n# 等价于 func = decorator(func)\n```\n\n## 二、基础装饰器\n\n### 最简单的装饰器\n\n```python\ndef timer(func):\n    def wrapper(*args, **kwargs):\n        start = time.time()\n        result = func(*args, **kwargs)\n        print(f\"耗时: {time.time() - start:.3f}s\")\n        return result\n    return wrapper\n\n@timer\ndef heavy_work():\n    time.sleep(1)\n```\n\n### functools.wraps\n\n装饰器会丢失原函数的元信息（`__name__`、`__doc__`、签名等）。用 `functools.wraps` 修复：\n\n```python\nimport functools\n\ndef timer(func):\n    @functools.wraps(func)  # 保留原函数元信息\n    def wrapper(*args, **kwargs):\n        ...\n        return result\n    return wrapper\n```\n\n**不加 wraps 的影响**：Python 官方装饰器指南指出 `@wraps` 是「唯一正确写法」，否则 help()、自动文档生成、签名保留等都会出错。\n\n## 三、带参数的装饰器\n\n```python\ndef repeat(n):\n    def decorator(func):\n        @functools.wraps(func)\n        def wrapper(*args, **kwargs):\n            for _ in range(n):\n                func(*args, **kwargs)\n        return wrapper\n    return decorator\n\n@repeat(n=3)\ndef greet(name):\n    print(f\"Hello {name}\")\n```\n\n理解时可以优先抓住「能准确说出装饰器的定义和本质；能解释 functools.wraps 的用途；能写出带参数装饰器的三层结构」。这些点分别对应定义、执行流程和边界条件；要把触发条件、状态变化和结果影响串起来，而不是孤立背词。"
    },
    {
      "type": "explain",
      "title": "装饰器：类装饰器、多个装饰器的执行顺序",
      "content": "## 四、类装饰器\n\n通过实现 `__call__` 方法，用类做装饰器：\n\n```python\nclass CountCalls:\n    def __init__(self, func):\n        functools.update_wrapper(self, func)\n        self.func = func\n        self.count = 0\n\n    def __call__(self, *args, **kwargs):\n        self.count += 1\n        print(f\"Call {self.count}\")\n        return self.func(*args, **kwargs)\n```\n\n## 五、多个装饰器的执行顺序\n\n```python\n@decorator_a\n@decorator_b\ndef func():\n    pass\n\n# 等价于 func = decorator_a(decorator_b(func))\n# 装饰时：先应用 decorator_b，再应用 decorator_a\n# 执行时：先执行 decorator_a 的 wrapper，再执行 decorator_b 的 wrapper\n```\n\n## 六、实际应用场景\n\n- **计时/日志**：`@timer`、`@log_call`\n- **权限校验**：`@login_required`、`@has_permission('admin')`\n- **缓存**：`@lru_cache`、`@cache`\n- **重试**：`@retry(max_attempts=3)`\n- **输入验证**：`@validate_args`\n- **注册/路由**：如 Flask 的 `@app.route()`"
    },
    {
      "type": "diagram",
      "title": "装饰器 学习主线图",
      "format": "mermaid",
      "content": "flowchart LR\nA[装饰器] --> B[Python]\nB --> C[高阶函数]\nC --> D[闭包]\nD --> E[能准确说出装饰器的定义和本质]\nE --> F[functools.wraps 的用途]",
      "caption": "从 Python 到 functools.wraps 的用途，这张图帮助区分 装饰器 的核心机制、边界条件和常见误区。",
      "fallback": "装饰器 -> Python -> 高阶函数 -> 闭包 -> 能准确说出装饰器的定义和本质 -> functools.wraps 的用途"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | 函数装饰器 | 类装饰器 | functools.wraps |\n| --- | --- | --- | --- |\n| 实现 | 外层函数返回内层函数 | 类实现 __call__ | 包装器函数 |\n| 元信息保留 | ❌ 丢失 | ❌ 需 update_wrapper | ✅ 保留 |\n| 状态维护 | 外层闭包变量 | 实例属性 | 无 |\n| 复杂度 | 低 | 中 | — |\n| 适用场景 | 简单包装（日志、计时） | 需要状态的场景（计数） | 所有装饰器 |\n| 是否是装饰器 | ✅ | ✅ | 辅助工具 |"
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "# 1. 实际生产级装饰器：重试\nimport functools\nimport time\nimport random\n\ndef retry(max_attempts=3, delay=1, backoff=2, exceptions=(Exception,)):\n    def decorator(func):\n        @functools.wraps(func)\n        def wrapper(*args, **kwargs):\n            last_exception = None\n            for attempt in range(1, max_attempts + 1):\n                try:\n                    return func(*args, **kwargs)\n                except exceptions as e:\n                    last_exception = e\n                    if attempt == max_attempts:\n                        raise\n                    time.sleep(delay * (backoff ** (attempt - 1)))\n            return None\n        return wrapper\n    return decorator\n\n@retry(max_attempts=5, exceptions=(ConnectionError, TimeoutError))\ndef fetch_data(url):\n    # 可能失败的网络请求\n    ...\n\n# 2. 装饰器的叠放顺序\n@timer\n@retry(max_attempts=3)\ndef unstable_api():\n    ...\n# 执行顺序：timer → retry → unstable_api\n\n# 3. @property 是内置类装饰器\nclass Person:\n    def __init__(self, name):\n        self._name = name\n\n    @property\n    def name(self):\n        return self._name\n\n    @name.setter\n    def name(self, value):\n        if not isinstance(value, str):\n            raise TypeError(\"name must be str\")\n        self._name = value",
      "language": "python",
      "highlights": [
        {
          "line": 16,
          "note": "这里是边界条件分支，回答 装饰器 时要说明它为什么会改变执行路径。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "结论：装饰器 要先说清它解决什么问题，再展开机制、边界和验证方式。装饰器原理、functools.wraps、带参数装饰器、类装饰器、装饰器执行时机\n\n我会这样回答：\n1. 先定位核心问题：能准确说出装饰器的定义和本质。\n2. 再串起关键机制：functools.wraps 的用途，并说明它和 带参数装饰器的三层结构 的关系。\n3. 接着补充边界和风险：忘记 @wraps 导致函数元信息丢失。\n4. 最后落到验证方式：结合执行时机、对象关系、异常栈、测试或 profiling 结果验证判断。\n\n如果继续追问，我会用一个具体场景串起输入、状态变化和失败处理，避免只停留在概念定义。",
      "followUpQuestions": [
        {
          "question": "装饰器的性能开销主要在哪里？大量使用装饰器有什么影响？",
          "answer": "主要开销在调用时的额外函数调用链。每个装饰器在每次函数调用时都增加了一层 wrapper 调用。如果有 10 个装饰器叠加，就有 10 层额外调用。对于高频调用的函数，可以考虑：1) 用偏函数替代简单装饰器；2) 在非生产环境禁用某些装饰器；3) 用 __wrapped__ 属性暴露原始函数。"
        },
        {
          "question": "@property 和描述符有什么关系？自己实现一个类似 @property 的装饰器怎么做？",
          "answer": "@property 是 Python 内置的描述符形式，本质是创建一个 property 对象（实现了 __get__、__set__、__delete__ 的类）。自己实现需要定义一个类，实现 __get__ 方法，然后用它装饰函数。面向对象基础和进阶主题中的描述符详细讲解了这个机制。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能解释装饰器本质是高阶函数",
        "能写出使用 functools.wraps 的正确装饰器",
        "能解释带参数装饰器的三层嵌套结构",
        "能说出多个装饰器的执行顺序",
        "能对比类装饰器和函数装饰器的差异",
        "能说出装饰器的典型使用场景（计时、重试、缓存）",
        "能理解装饰器在模块加载时执行的原理"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "python.python-advanced.decorators.recall.1",
      "prompt": "多个装饰器叠加时，执行顺序是怎样的？请用代码示例说明装饰和执行两个阶段。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "python.python-advanced.decorators.recall.2",
      "prompt": "请实现一个带参数的重试装饰器 @retry(max_attempts=3, delay=1, backoff=2)。",
      "mode": "code",
      "expectedMinutes": 8,
      "difficulty": 3
    },
    {
      "id": "python.python-advanced.decorators.recall.3",
      "prompt": "带参数的装饰器是几层函数嵌套？不加 functools.wraps 会丢失什么？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "能准确说出装饰器的定义和本质",
      "能解释 functools.wraps 的用途",
      "能写出带参数装饰器的三层结构",
      "能解释多个装饰器的执行顺序"
    ],
    "goodToHave": [
      "能实现类装饰器",
      "能解释装饰器的性能开销和优化策略",
      "能说明 @property 底层是描述符机制"
    ],
    "commonMistakes": [
      "忘记 @wraps 导致函数元信息丢失",
      "混淆装饰时间和调用时间的顺序",
      "带参数装饰器的嵌套层数搞错"
    ],
    "scoreWeights": {
      "coverage": 25,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 25
    }
  },
  "status": "production",
  "interviewFrequency": "high",
  "interviewerFocus": "考察是否能围绕「装饰器」说清能准确说出装饰器的定义和本质、能解释 functools.wraps 的用途，并放到Python 工程场景中解释关键流程、边界条件和出错后的处理思路。",
  "updatedAt": "2026-06-12",
  "prerequisites": [
    "python.python-basics.data-types-variables"
  ]
}
