{
  "id": "python.python-advanced.topic-exceptions",
  "domain": "python",
  "category": "python-advanced",
  "group": "python-advanced",
  "title": "异常处理与 ExceptionGroup",
  "summary": "掌握异常体系、try/except/else/finally 执行时机、raise from，以及 3.11 的 ExceptionGroup 与 except*。",
  "tags": [
    "Python",
    "异常",
    "ExceptionGroup",
    "3.11"
  ],
  "difficulty": 3,
  "estimatedMinutes": 16,
  "order": 90,
  "recommendWeight": 82,
  "learningCards": [
    {
      "type": "explain",
      "title": "异常体系与 try 的四个子句",
      "content": "## 异常层级\n所有异常继承自 `BaseException`；用户一般捕获 `Exception` 及其子类。`KeyboardInterrupt`、`SystemExit` 直接继承 `BaseException`，所以 `except Exception` **不会**误吞它们。\n\n## try/except/else/finally 执行时机\n- `try`：可能抛异常的代码。\n- `except`：匹配到异常时执行（从上到下第一个匹配）。\n- `else`：`try` **没有**异常时执行（适合放“成功才做”的逻辑，缩小 try 范围）。\n- `finally`：**无论是否异常都执行**，常用于释放资源；即使 try/except 里有 return，finally 也会先执行。\n\n## raise 与异常链\n- `raise` 重新抛出当前异常（保留栈）。\n- `raise NewError() from e`：显式标注**异常链**（`__cause__`），排查时能看到根因；裸 `raise NewError()` 会隐式链（`__context__`）。\n- 自定义异常：继承 `Exception`，按业务建立异常体系便于分层捕获。"
    },
    {
      "type": "explain",
      "title": "ExceptionGroup 与 except*（3.11+）",
      "content": "## 解决什么问题\n并发场景（如 `asyncio.TaskGroup`）里**多个任务可能同时失败**，传统单异常模型只能抛一个。3.11 引入 **ExceptionGroup** 把多个异常聚合成一个，配合 **`except*`** 按类型分别处理。\n\n## 用法\n```python\ntry:\n    raise ExceptionGroup(\"errs\", [ValueError(\"a\"), TypeError(\"b\")])\nexcept* ValueError as eg:\n    ...  # 只处理组里的 ValueError 子组\nexcept* TypeError as eg:\n    ...  # 只处理 TypeError 子组\n```\n- `except*` 会把匹配的子异常拆成子 ExceptionGroup 处理，未匹配的继续向上传播。\n- `asyncio.TaskGroup`（3.11+）中任一任务失败，会以 ExceptionGroup 抛出所有失败。\n\n## 配套\n- `contextlib.suppress(Exc)` 优雅忽略特定异常；`add_note()`（3.11）给异常附加上下文信息。"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "import asyncio\n\nasync def worker(n):\n    if n == 2:\n        raise ValueError(f\"bad {n}\")\n    await asyncio.sleep(0.1)\n    return n\n\nasync def main():\n    try:\n        async with asyncio.TaskGroup() as tg:   # 3.11+\n            for i in range(4):\n                tg.create_task(worker(i))\n    except* ValueError as eg:                    # 处理聚合里的 ValueError\n        print(\"caught\", [str(e) for e in eg.exceptions])\n\n# try/except/else/finally 时机\ndef divide(a, b):\n    try:\n        r = a / b\n    except ZeroDivisionError as e:\n        raise ValueError(\"b must be non-zero\") from e   # 异常链\n    else:\n        return r            # 仅无异常时\n    finally:\n        print(\"always runs\")  # 总会执行\n\nasyncio.run(main())",
      "language": "python"
    },
    {
      "type": "interviewAnswer",
      "title": "标准面试回答",
      "content": "结论：Python 异常都继承 BaseException，业务一般捕获 Exception；try/except/else/finally 中 else 在无异常时执行、finally 总会执行。raise ... from e 用来保留异常链便于排查。\n\n3.11 引入 ExceptionGroup + except*，解决并发下多个异常只能抛一个的问题：asyncio.TaskGroup 里多个任务失败会聚合成 ExceptionGroup，用 except* 按类型分别处理未匹配的继续传播。\n\n追问边界：except Exception 不会捕获 KeyboardInterrupt/SystemExit（它们直接继承 BaseException）；不要用裸 except 吞掉所有异常；finally 里 return 会覆盖 try 的返回值和异常，应避免。",
      "followUpQuestions": [
        {
          "question": "except Exception 能捕获 KeyboardInterrupt 吗？",
          "answer": "不能。KeyboardInterrupt 和 SystemExit 直接继承 BaseException 而非 Exception，所以 except Exception 不会吞掉它们，保证 Ctrl+C、sys.exit 能正常中断/退出。要捕获它们得显式 except BaseException（一般不建议）。"
        },
        {
          "question": "raise NewError() 和 raise NewError() from e 有什么区别？",
          "answer": "from e 显式设置 __cause__，traceback 会明确显示“由 e 引起”，根因清晰；不带 from 时若在 except 块里抛出，会隐式设置 __context__（“处理 e 时又发生了”），语义较弱。排查链路问题时优先用 from。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能画出 BaseException/Exception 层级并解释为何 except Exception 不吞 KeyboardInterrupt",
        "能说清 try/except/else/finally 四个子句的执行时机",
        "能用 raise from 维护异常链并解释作用",
        "能用 ExceptionGroup + except* 处理并发多异常",
        "能说明 asyncio.TaskGroup 失败时的异常聚合行为"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "python.python-advanced.topic-exceptions.recall.1",
      "prompt": "解释 try/except/else/finally 各自的执行时机，并说明 finally 里有 return 的风险。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "python.python-advanced.topic-exceptions.recall.2",
      "prompt": "ExceptionGroup 和 except* 解决了什么问题？写一段用 asyncio.TaskGroup 触发并按类型处理的代码思路。",
      "mode": "code",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "BaseException/Exception 层级",
      "else/finally 执行时机",
      "raise from 异常链",
      "ExceptionGroup + except*"
    ],
    "goodToHave": [
      "asyncio.TaskGroup 聚合异常",
      "contextlib.suppress / add_note",
      "为何 except Exception 不吞 KeyboardInterrupt"
    ],
    "commonMistakes": [
      "用裸 except 吞掉所有异常",
      "以为 except Exception 能捕获 SystemExit",
      "finally 里 return 覆盖异常",
      "并发多异常时只处理第一个"
    ],
    "scoreWeights": {
      "coverage": 30,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 20
    }
  },
  "updatedAt": "2026-06-11",
  "status": "production",
  "interviewFrequency": "high",
  "interviewerFocus": "考察异常体系、try 四子句时机、异常链，以及 3.11 ExceptionGroup/except* 在并发下的应用。"
}
