{
  "id": "network.http-https.topic-https",
  "domain": "network",
  "category": "http-https",
  "group": "http-https",
  "title": "HTTPS 加密原理",
  "summary": "深入理解HTTPS的加密体系，包括对称加密、非对称加密、数字证书和TLS握手流程，掌握HTTPS如何保证数据传输的安全性。",
  "tags": [
    "网络",
    "HTTPS",
    "TLS",
    "证书",
    "对称加密"
  ],
  "difficulty": 3,
  "estimatedMinutes": 20,
  "order": 30,
  "recommendWeight": 92,
  "learningCards": [
    {
      "type": "explain",
      "title": "核心概念",
      "content": "# HTTPS 加密原理\n\n## 一、为什么需要HTTPS\nHTTP是明文传输，存在三大风险：**窃听**(第三方截获数据)、**篡改**(中间人修改数据)、**冒充**(伪装成目标服务器)。HTTPS = HTTP + TLS/SSL，通过加密解决这些问题。"
    },
    {
      "type": "explain",
      "title": "深入理解",
      "content": "## 二、加密方式\n\n### 2.1 对称加密\n- 加密和解密使用同一个密钥\n- 算法：AES、DES、ChaCha20\n- 优点：速度快\n- 缺点：密钥分发困难\n\n### 2.2 非对称加密\n- 公钥加密，私钥解密(或私钥签名，公钥验证)\n- 算法：RSA、ECC、DSA\n- 优点：解决了密钥分发问题\n- 缺点：速度慢(比对称加密慢100-1000倍)\n\n### 2.3 混合加密\nHTTPS采用混合加密：非对称加密交换对称密钥，之后用对称密钥加密数据。\n\n## 三、数字证书\n\n### 3.1 证书的作用\n验证服务器身份，防止中间人攻击。\n\n### 3.2 证书链\n- 根证书(Root CA)：内置在操作系统/浏览器中\n- 中间证书(Intermediate CA)：由根证书签发\n- 服务器证书：由中间证书签发\n\n### 3.3 证书验证过程\n浏览器检查：证书是否过期 → 域名是否匹配 → 证书链是否可信 → 证书是否被吊销(CRL/OCSP)"
    },
    {
      "type": "explain",
      "title": "深入理解（续）",
      "content": "## 四、TLS握手流程(TLS 1.2)\n```\n客户端                              服务器\n  |-- ClientHello(支持的加密套件) -->|\n  |<-- ServerHello(选定加密套件) ---|\n  |<-- Certificate(服务器证书) ----|\n  |<-- ServerHelloDone -------------|\n  |-- 验证证书                      |\n  |-- ClientKeyExchange(预主密钥) ->|\n  |-- ChangeCipherSpec ------------>|\n  |-- Finished -------------------->|\n  |<-- ChangeCipherSpec ------------|\n  |<-- Finished --------------------|\n  [握手完成，开始加密通信]\n```\n\n## 五、TLS 1.3改进\n- 握手从2-RTT减少到1-RTT\n- 支持0-RTT恢复连接\n- 移除不安全的加密算法(RSA密钥交换、CBC模式等)\n- 只保留AEAD加密(AES-GCM、ChaCha20-Poly1305)"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "TLS握手流程图",
      "fallback": "展示客户端和服务器在TLS握手过程中的消息交换序列",
      "items": [
        "ClientHello: 客户端发送支持的TLS版本、加密套件列表、随机数Client Random",
        "ServerHello: 服务器选定TLS版本和加密套件，发送随机数Server Random",
        "Certificate: 服务器发送证书链(服务器证书+中间证书)",
        "ServerKeyExchange: 服务器发送密钥交换参数(如ECDHE的公钥)",
        "ClientKeyExchange: 客户端发送密钥交换参数，双方计算预主密钥",
        "ChangeCipherSpec: 通知对方后续消息使用协商的密钥加密",
        "Finished: 握手完成消息，验证握手过程未被篡改"
      ],
      "asset": "assets/diagrams/tls-handshake.svg",
      "svgPath": "assets/diagrams/03-https-tls-handshake.svg"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "# 使用openssl命令查看和验证HTTPS证书\n\n# 1. 查看远程服务器证书信息\n$ openssl s_client -connect example.com:443 -servername example.com\n# 输出包含证书链、加密套件、TLS版本等信息\n\n# 2. 查看证书详情\n$ echo | openssl s_client -connect example.com:443 2>/dev/null | \\\n    openssl x509 -noout -text\n# 显示证书的颁发者、有效期、公钥信息、SAN等\n\n# 3. 验证证书链\n$ openssl verify -CAfile /etc/ssl/certs/ca-certificates.crt server.crt\n\n# 4. 检查证书是否匹配私钥\n$ openssl x509 -noout -modulus server.crt | openssl md5\n$ openssl rsa -noout -modulus server.key | openssl md5\n# 两个MD5值相同则匹配\n\n# 5. 生成自签名证书(开发测试用)\n$ openssl req -x509 -newkey rsa:2048 -keyout key.pem -out cert.pem \\\n    -days 365 -nodes -subj \"/CN=localhost\"\n\n# 6. 查看支持的加密套件\n$ openssl ciphers -v 'HIGH:!aNULL:!MD5'\n\n# Python: 验证HTTPS证书\nimport ssl\nimport socket\n\ndef check_cert(hostname, port=443):\n    ctx = ssl.create_default_context()\n    with ctx.wrap_socket(socket.socket(), server_hostname=hostname) as s:\n        s.connect((hostname, port))\n        cert = s.getpeercert()\n        print(f\"Subject: {cert['subject']}\")\n        print(f\"Issuer: {cert['issuer']}\")\n        print(f\"Valid from: {cert['notBefore']}\")\n        print(f\"Valid until: {cert['notAfter']}\")\n        print(f\"SANs: {cert.get('subjectAltName', [])}\")\n\ncheck_cert('example.com')",
      "language": "python"
    },
    {
      "type": "interviewAnswer",
      "title": "标准面试回答",
      "content": "结论：HTTPS 是 HTTP 加 TLS。TLS 负责服务器身份认证、密钥协商、数据加密和完整性保护；真正传输业务数据时通常使用对称加密，非对称能力主要用于认证和协商密钥。\n\n机制上，TLS 握手可以分成：\n1. 客户端发送支持的 TLS 版本、随机数和加密套件。\n2. 服务端返回证书、随机数和选定套件，客户端验证证书链、域名和有效期。\n3. 双方通过密钥交换算法协商会话密钥。\n4. 后续 HTTP 数据用会话密钥做对称加密，并用 MAC/AEAD 保护完整性。\n\n追问边界：TLS 1.2 和 TLS 1.3 细节不同，回答时抓稳定主线即可。证书只能证明域名对应的公钥可信，不代表业务一定安全；HTTPS 也不能防止服务端自身泄露或前端 XSS。",
      "followUpQuestions": [
        {
          "question": "证书链验证验证什么？",
          "answer": "验证证书是否由可信 CA 签发、域名是否匹配、证书是否过期或被吊销，以及签名是否能用上级公钥验证。"
        },
        {
          "question": "为什么不用非对称加密传所有数据？",
          "answer": "非对称加密计算成本高，适合认证和协商；对称加密速度快，适合大量业务数据传输。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能解释对称加密和非对称加密的区别及各自优缺点",
        "能说明HTTPS混合加密的工作流程",
        "能解释数字证书的结构和验证过程(证书链)",
        "能描述TLS 1.2握手的完整消息交互过程",
        "能说明TLS 1.3相比1.2的改进(1-RTT/0-RTT/前向保密)",
        "能用openssl命令查看和验证HTTPS证书"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "network.http-https.topic-https.recall.1",
      "prompt": "请说明 HTTPS/TLS 握手如何完成身份认证、密钥协商和加密通信。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "network.http-https.topic-https.recall.2",
      "prompt": "为什么 HTTPS 同时使用非对称加密和对称加密？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "TLS",
      "证书校验",
      "密钥协商",
      "对称加密",
      "完整性保护"
    ],
    "goodToHave": [
      "TLS 1.3 简化握手",
      "证书链",
      "前向安全",
      "中间人攻击"
    ],
    "commonMistakes": [
      "认为 HTTPS 只是在 HTTP 外面套公钥加密",
      "忽略证书校验",
      "认为 HTTPS 能解决所有安全问题"
    ],
    "scoreWeights": {
      "coverage": 25,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 25
    }
  },
  "updatedAt": "2026-05-29",
  "status": "production",
  "prerequisites": [
    "network.tcp-udp.topic-tcp-handshake"
  ],
  "interviewFrequency": "high",
  "interviewerFocus": "考察对网络协议的理解深度，是否了解协议的设计动机和实际排查方法"
}
