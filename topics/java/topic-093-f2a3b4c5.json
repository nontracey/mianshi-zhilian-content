{
  "id": "java.middleware.topic-093-f2a3b4c5",
  "domain": "java",
  "category": "middleware",
  "group": "middleware",
  "title": "设计模式在并发中的应用",
  "summary": "AQS模板方法、线程池策略模式、并发单例",
  "tags": [
    "Java",
    "并发编程",
    "设计模式",
    "AQS"
  ],
  "difficulty": 2,
  "estimatedMinutes": 20,
  "order": 310,
  "recommendWeight": 70,
  "learningCards": [
    {
      "type": "explain",
      "title": "知识全景",
      "content": "# 设计模式在并发中的应用\n## 一、AQS模板方法模式\n### 1.1 AQS核心结构\n```\n┌─────────────────────────────────────────────────────────────────┐\n│                    AQS（AbstractQueuedSynchronizer）              │\n├─────────────────────────────────────────────────────────────────┤\n│  state: volatile int（同步状态）                                 │\n│  CLH队列: 双向链表，存放等待获取锁的线程                         │\n│                                                                  │\n│  模板方法：                                                       │\n│  - acquire(int arg) → 获取锁                                    │\n│  - release(int arg) → 释放锁                                    │\n│                                                                  │\n│  需要子类实现的方法：                                             │\n│  - tryAcquire(int arg) → 尝试获取锁                             │\n│  - tryRelease(int arg) → 尝试释放锁                             │\n│  - tryAcquireShared(int arg) → 共享模式获取                     │\n│  - tryReleaseShared(int arg) → 共享模式释放                     │\n└─────────────────────────────────────────────────────────────────┘\n```\n### 1.2 基于AQS的实现\n| 组件 | 模式 | state含义 |\n| --- | --- | --- |\n| ReentrantLock | 独占 | 重入次数 |\n| ReentrantReadWriteLock | 共享+独占 | 高16位读锁计数，低16位写锁计数 |\n| CountDownLatch | 共享 | 计数值 |\n| Semaphore | 共享 | 许可数 |\n\n## 二、线程池策略模式\n### 2.1 拒绝策略\n```java\n// JDK内置四种拒绝策略\npublic interface RejectedExecutionHandler {\n    void rejectedExecution(Runnable r, ThreadPoolExecutor executor);\n}\n\n// AbortPolicy: 抛出RejectedExecutionException（默认）\n// CallerRunsPolicy: 调用者线程执行\n// DiscardPolicy: 静默丢弃\n// DiscardOldestPolicy: 丢弃队列最老的任务\n```\n### 2.2 自定义拒绝策略\n```java\n// 记录日志 + 持久化到数据库\npublic class LogRejectedHandler implements RejectedExecutionHandler {\n    @Override\n    public void rejectedExecution(Runnable r, ThreadPoolExecutor executor) {\n        log.warn(\"任务被拒绝，队列已满\");\n        // 持久化到数据库，后续补偿\n        db.saveTask(r);\n    }\n}\n```\n\n## 三、并发单例模式\n### 3.1 双重检查锁（DCL）\n```java\npublic class Singleton {\n    private static volatile Singleton instance; // volatile禁止指令重排\n    \n    private Singleton() {}\n    \n    public static Singleton getInstance() {\n        if (instance == null) {                    // 第一次检查\n            synchronized (Singleton.class) {\n                if (instance == null) {            // 第二次检查\n                    instance = new Singleton();\n                }\n            }\n        }\n        return instance;\n    }\n}\n```\n### 3.2 为什么需要volatile\n- `new Singleton()` 分三步：分配内存 → 初始化 → 引用赋值\n- 没有volatile，可能发生指令重排：分配内存 → 引用赋值 → 初始化\n- 其他线程可能拿到未初始化的对象\n\n### 3.3 其他并发安全的单例\n- 枚举单例（推荐）：JVM保证线程安全和防序列化\n- 静态内部类：利用类加载机制保证线程安全\n\n## 四、观察者模式：事件监听\n```java\n// Spring Event\n@Service\npublic class OrderService {\n    @Autowired\n    private ApplicationEventPublisher publisher;\n    \n    public void createOrder(Order order) {\n        // 创建订单\n        publisher.publishEvent(new OrderCreatedEvent(order));\n    }\n}\n\n@Component\npublic class OrderEventListener {\n    @EventListener\n    public void onOrderCreated(OrderCreatedEvent event) {\n        // 异步处理：发送通知、更新库存等\n    }\n}\n```"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | ReentrantLock | synchronized |\n| --- | --- | --- |\n| 实现方式 | AQS（Java层面） | JVM内置（字节码） |\n| 可中断 | 支持（lockInterruptibly） | 不支持 |\n| 超时获取 | 支持（tryLock） | 不支持 |\n| 公平锁 | 支持（FairSync） | 不支持 |\n| 条件变量 | 支持（多个Condition） | 只有wait/notify |\n| 性能 | 高（JDK6+优化后接近） | 高（JDK6+偏向锁优化） |\n| 释放方式 | 手动unlock（finally中） | 自动释放（出同步块） |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "设计模式在并发中的应用的核心结构图解。",
      "items": [
        "AQS核心：volatile int state + CLH队列（双向链表）",
        "模板方法：acquire/release定义流程，子类实现tryAcquire/tryRelease",
        "ReentrantLock：独占模式，state=重入次数",
        "CountDownLatch：共享模式，state=计数值",
        "线程池策略：AbortPolicy/CallerRunsPolicy/DiscardPolicy/DiscardOldestPolicy",
        "DCL单例：外层检查 → synchronized → 内层检查 → 创建实例",
        "volatile作用：禁止指令重排，保证可见性"
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "// AQS实现一个简单的互斥锁\npublic class MutexLock extends AbstractQueuedSynchronizer {\n    @Override\n    protected boolean tryAcquire(int arg) {\n        if (compareAndSetState(0, 1)) {\n            setExclusiveOwnerThread(Thread.currentThread());\n            return true;\n        }\n        return false;\n    }\n    \n    @Override\n    protected boolean tryRelease(int arg) {\n        setExclusiveOwnerThread(null);\n        setState(0);\n        return true;\n    }\n}\n\n// 线程池自定义拒绝策略\nThreadPoolExecutor executor = new ThreadPoolExecutor(\n    10, 20, 60, TimeUnit.SECONDS,\n    new LinkedBlockingQueue<>(1000),\n    new LogRejectedHandler() // 自定义策略\n);\n\n// 枚举单例（最安全）\npublic enum Singleton {\n    INSTANCE;\n    public void doSomething() { }\n}"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "**问题1：AQS的模板方法模式是什么？**\n\nAQS（AbstractQueuedSynchronizer）是并发包的核心框架。它用模板方法模式定义了获取和释放锁的流程（acquire/release），子类只需实现tryAcquire/tryRelease。\n\nAQS的核心是volatile int state（同步状态）和CLH队列（双向链表，存放等待线程）。基于AQS的组件：ReentrantLock（独占，state=重入次数）、ReentrantReadWriteLock（共享+独占，高16位读锁计数，低16位写锁计数）、CountDownLatch（共享，state=计数值）、Semaphore（共享，state=许可数）。\n\n**问题2：DCL单例为什么需要volatile？**\n\nDCL（双重检查锁）单例中，`instance = new Singleton()`不是原子操作，分为三步：分配内存、初始化对象、引用指向内存。\n\nJVM可能指令重排为：分配内存 → 引用指向内存 → 初始化对象。如果线程A执行到第二步（引用已指向内存但对象未初始化），线程B进入外层检查发现instance不为null，直接返回一个未初始化的对象，导致错误。\n\nvolatile禁止指令重排，保证对象初始化完成后才对其他线程可见。枚举单例更安全，天然防止反射和序列化破坏。",
      "followUpQuestions": [
        {
          "question": "能详细说说1.1 AQS核心结构的具体原理吗？",
          "answer": "关于1.1 AQS核心结构，关键要理解它的底层机制。同时要注意和1.2 基于AQS的实现的区别。在实际开发中，这直接影响到我们如何选择合适的方案。"
        },
        {
          "question": "在实际项目中，你一般怎么选择？有什么经验？",
          "answer": "在项目中做选择时，我通常会考虑几个维度：性能需求、团队熟悉度、维护成本。没有绝对的最优解，关键是能说清楚为什么选了当前方案，以及它的局限性在哪里。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能说出AQS的核心结构和模板方法模式",
        "能列举基于AQS的并发组件及其state含义",
        "能说出线程池的四种拒绝策略",
        "能解释DCL单例为什么需要volatile",
        "能说出枚举单例的优势",
        "能区分模板方法模式和策略模式",
        "能解释ReentrantLock和synchronized的区别"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "java.middleware.topic-093-f2a3b4c5.recall.1",
      "prompt": "请用自己的话解释AQS的模板方法模式和基于AQS的并发组件。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.middleware.topic-093-f2a3b4c5.recall.2",
      "prompt": "如果面试官问\"DCL单例为什么需要volatile\"，你会怎么回答？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    }
  ],
  "rubric": {
    "mustHave": [
      "AQS的核心结构和模板方法",
      "基于AQS的并发组件",
      "线程池拒绝策略",
      "DCL单例的volatile原因",
      "枚举单例的优势"
    ],
    "goodToHave": [
      "能自定义AQS实现",
      "能说明自定义拒绝策略的场景",
      "能说明Spring Event的异步处理"
    ],
    "commonMistakes": [
      "只知道DCL不了解volatile",
      "混淆模板方法和策略模式",
      "忽略枚举单例的优势"
    ],
    "scoreWeights": {
      "coverage": 40,
      "accuracy": 25,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-05-28",
  "status": "production",
  "prerequisites": [
    "java.middleware.topic-090-c9d0e1f2"
  ]
}
