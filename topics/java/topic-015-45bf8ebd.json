{
  "id": "java.concurrency.topic-015-45bf8ebd",
  "domain": "java",
  "category": "concurrency",
  "group": "concurrency",
  "title": "volatile 原理",
  "summary": "讲清 volatile 在 JMM 中的可见性和有序性语义、volatile 写/读的 happens-before 链、release/acquire 屏障效果、DCL 安全发布，以及为什么读-改-写仍然不具备原子性。",
  "tags": [
    "Java",
    "并发编程",
    "volatile"
  ],
  "difficulty": 4,
  "estimatedMinutes": 35,
  "order": 100,
  "recommendWeight": 84,
  "learningCards": [
    {
      "type": "explain",
      "title": "volatile 的核心是同步顺序",
      "content": "volatile 是 Java 内存模型里的轻量同步机制。对同一个 volatile 变量，一次写入会 synchronizes-with 后续读到该写或更新值的 volatile 读取，从而建立 happens-before：写线程在 volatile 写之前做的普通写，对读线程在 volatile 读之后的普通读可见。\n\n更精确地说，volatile 不是“把所有东西马上刷到主内存”的魔法，而是给编译器和 CPU 加约束：volatile 写具备 release 语义，写之前的内存操作不能被重排到它之后；volatile 读具备 acquire 语义，读之后的内存操作不能被重排到它之前。JVM 会在不同硬件上选择相应的内存屏障或指令组合来实现这个语义。\n\n所以 volatile 适合表达单变量状态发布：停止标志、配置引用整体替换、DCL 单例引用发布、读多写少的只读快照。它不适合保护“多个变量共同保持一致”或“读取后再修改”的复合逻辑。"
    },
    {
      "type": "explain",
      "title": "可见性和有序性如何串起来",
      "content": "典型模式是：线程 A 先写普通字段 `data`，再写 volatile 标志 `ready=true`；线程 B 读到 `ready=true` 后，再读取 `data`。由于 volatile 写和读建立 happens-before，A 在 volatile 写前的普通写，对 B 在 volatile 读后的普通读可见。这也是很多“发布配置引用”“关闭标志”的基础。\n\nvolatile 对重排序的约束可以按屏障理解：volatile 写前通常需要 StoreStore，写后需要 StoreLoad；volatile 读后需要 LoadLoad 和 LoadStore。不同 CPU 的具体指令不完全相同，面试中不必把硬件细节背成绝对映射，关键是讲清写是 release、读是 acquire，以及它们只围绕同一个 volatile 变量建立同步关系。\n\n如果读写的是不同 volatile 变量，不能自动把它们当作同一条同步链。多个字段要一起满足不变式时，应使用锁、不可变对象整体替换、AtomicReference 或 VarHandle/原子类提供的更明确语义。"
    },
    {
      "type": "explain",
      "title": "volatile 不提供读-改-写原子性",
      "content": "`count++` 包含读取、计算、写回三步。把 count 声明成 volatile，只能保证每次读和写各自有 volatile 语义，不能把三步合成一个不可打断的事务。两个线程都先读到 0，然后分别写回 1，最终结果就是 1，而不是 2。\n\n这也是 volatile 和 synchronized/Atomic 的边界。volatile 解决“我能不能看到另一个线程发布的状态”；AtomicInteger、LongAdder、synchronized、Lock 解决“多个线程能不能安全地修改同一个状态”。高并发计数优先考虑 LongAdder，低竞争计数用 AtomicInteger，涉及多个字段一致性时用锁。\n\n另一个常见误区是 volatile 数组。`volatile int[] arr` 只保证数组引用本身的读写有 volatile 语义，不保证 `arr[0]` 这类元素读写具备 volatile 语义。元素级并发更新要用 AtomicIntegerArray、锁或不可变快照替换。"
    },
    {
      "type": "explain",
      "title": "DCL 需要 volatile 来安全发布对象",
      "content": "Double-Checked Locking 的目标是：第一次创建对象时加锁，之后读取实例时走无锁快路径。问题在于 `new Singleton()` 不是单步操作，至少包含分配内存、执行构造初始化、把引用赋给 instance。没有 volatile 时，引用赋值可能被观察到早于初始化完成，另一个线程在外层 null 检查通过后可能拿到半初始化对象。\n\n把 `instance` 声明为 volatile 后，构造函数里对对象字段的写发生在 volatile 写 instance 之前；其他线程 volatile 读到非 null instance 后，就能看到初始化完成后的字段状态。这里 volatile 保护的是“引用发布顺序和可见性”，不是让构造过程变成互斥；第一次创建仍依赖 synchronized 保证只创建一个实例。\n\n更简单的单例可以用静态内部类或枚举；DCL 的价值主要在需要懒加载、又想避免后续每次加锁的场景。"
    },
    {
      "type": "diagram",
      "format": "svg",
      "title": "volatile happens-before、屏障与原子性边界",
      "content": "assets/diagrams/java-volatile-happens-before.svg"
    },
    {
      "type": "compareTable",
      "title": "volatile、Atomic 和锁的边界",
      "content": "| 能力 | volatile | Atomic/LongAdder | synchronized/Lock |\n| --- | --- | --- | --- |\n| 单变量可见性 | 支持 | 支持 | 支持 |\n| 有序性/安全发布 | 支持 release/acquire 语义 | 取决于具体原子操作语义 | 临界区进入/退出建立 happens-before |\n| 读-改-写原子性 | 不支持 | 支持单变量原子更新 | 支持任意临界区 |\n| 多变量一致性 | 不适合 | AtomicReference 可整体替换 | 适合保护不变式 |\n| 阻塞 | 不阻塞 | 不阻塞或低阻塞 | 竞争时可能阻塞 |\n| 典型场景 | 停止标志、配置引用、DCL instance | 计数器、状态机、CAS 更新 | 复合操作、跨字段一致性、条件等待 |\n\n| 易错点 | 正确理解 |\n| --- | --- |\n| `volatile int count; count++` | volatile 不把三步合成原子操作 |\n| `volatile int[] arr` | 只保护数组引用，不保护元素 |\n| 多个 volatile 字段 | 不自动形成跨字段一致性 |\n| DCL 只有 volatile | 仍需要 synchronized 保护首次创建 |\n| 把 volatile 当性能优化 | 先确认语义匹配，再谈开销 |"
    },
    {
      "type": "code",
      "title": "可运行代码：停止标志、非原子自增和 DCL",
      "language": "java",
      "content": "import java.util.Set;\nimport java.util.concurrent.ConcurrentHashMap;\nimport java.util.concurrent.CountDownLatch;\nimport java.util.concurrent.CyclicBarrier;\nimport java.util.concurrent.atomic.AtomicInteger;\n\npublic class Main {\n    private static volatile boolean running = true;\n    private static volatile int unsafeCounter = 0;\n    private static final AtomicInteger safeCounter = new AtomicInteger();\n    private static volatile Singleton singleton;\n\n    public static void main(String[] args) throws Exception {\n        demonstrateStopFlag();\n        demonstrateNonAtomicIncrement();\n        demonstrateDclPublication();\n    }\n\n    static void demonstrateStopFlag() throws InterruptedException {\n        AtomicInteger loops = new AtomicInteger();\n        Thread worker = new Thread(() -> {\n            while (running) {\n                loops.incrementAndGet();\n            }\n        }, \"volatile-stop-worker\");\n        worker.start();\n        Thread.sleep(30);\n        running = false;\n        worker.join(1000);\n        System.out.println(\"stopFlag workerAlive=\" + worker.isAlive()\n                + \", loops=\" + loops.get());\n        assert !worker.isAlive();\n    }\n\n    static void demonstrateNonAtomicIncrement() throws Exception {\n        unsafeCounter = 0;\n        CyclicBarrier beforeRead = new CyclicBarrier(2);\n        CyclicBarrier beforeWrite = new CyclicBarrier(2);\n        Runnable lostUpdate = () -> {\n            await(beforeRead);\n            int snapshot = unsafeCounter;\n            await(beforeWrite);\n            unsafeCounter = snapshot + 1;\n        };\n        Thread t1 = new Thread(lostUpdate, \"volatile-race-1\");\n        Thread t2 = new Thread(lostUpdate, \"volatile-race-2\");\n        t1.start();\n        t2.start();\n        t1.join();\n        t2.join();\n        System.out.println(\"unsafeCounter=\" + unsafeCounter);\n        assert unsafeCounter == 1;\n\n        safeCounter.set(0);\n        CountDownLatch done = new CountDownLatch(2);\n        Runnable atomicUpdate = () -> {\n            safeCounter.incrementAndGet();\n            done.countDown();\n        };\n        new Thread(atomicUpdate, \"atomic-1\").start();\n        new Thread(atomicUpdate, \"atomic-2\").start();\n        done.await();\n        System.out.println(\"safeCounter=\" + safeCounter.get());\n        assert safeCounter.get() == 2;\n    }\n\n    static void demonstrateDclPublication() throws InterruptedException {\n        Set<Integer> identities = ConcurrentHashMap.newKeySet();\n        CountDownLatch done = new CountDownLatch(8);\n        for (int i = 0; i < 8; i++) {\n            new Thread(() -> {\n                Singleton value = getSingleton();\n                identities.add(System.identityHashCode(value));\n                assert value.answer == 42;\n                done.countDown();\n            }, \"dcl-reader-\" + i).start();\n        }\n        done.await();\n        System.out.println(\"singletonInstances=\" + identities.size());\n        assert identities.size() == 1;\n    }\n\n    static Singleton getSingleton() {\n        Singleton local = singleton;\n        if (local == null) {\n            synchronized (Main.class) {\n                local = singleton;\n                if (local == null) {\n                    local = new Singleton(42);\n                    singleton = local;\n                }\n            }\n        }\n        return local;\n    }\n\n    static void await(CyclicBarrier barrier) {\n        try {\n            barrier.await();\n        } catch (Exception e) {\n            throw new RuntimeException(e);\n        }\n    }\n\n    static final class Singleton {\n        final int answer;\n\n        Singleton(int answer) {\n            this.answer = answer;\n        }\n    }\n}",
      "highlights": [
        {
          "line": 27,
          "note": "volatile 写 running=false 发布停止信号，worker 的 volatile 读能观察到该变化。"
        },
        {
          "line": 40,
          "note": "两个线程故意先读同一个快照，再写回，证明 volatile 不提供读-改-写原子性。"
        },
        {
          "line": 55,
          "note": "AtomicInteger 把自增封装成原子更新，语义不同于 volatile int++。"
        },
        {
          "line": 87,
          "note": "DCL 中 synchronized 负责唯一创建，volatile 负责 instance 的安全发布。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "我会先给结论：volatile 保证单个变量的可见性和有序性，不保证复合操作原子性。JMM 规定，对同一个 volatile 变量的写，happens-before 后续读到该写或更新值的 volatile 读；因此写前的普通写，对读后的普通读可见。\n\n底层可以用 release/acquire 解释：volatile 写是 release，写之前的操作不能被重排到写之后；volatile 读是 acquire，读之后的操作不能被重排到读之前。JVM 会通过内存屏障或硬件指令实现这个约束，不同 CPU 指令细节不同，但语义一致。\n\n边界要主动说明：`count++` 是读、加、写三步，volatile 只保证读和写的可见性，不把三步变成原子事务；计数用 AtomicInteger 或 LongAdder，多个字段一致性用锁。volatile 数组只保护引用，不保护元素。\n\nDCL 是经典应用：instance 要用 volatile，因为 new 对象包含分配、初始化、引用赋值。volatile 禁止引用发布早于初始化被其他线程观察到；synchronized 仍负责首次创建的互斥。最后给场景：停止标志、配置快照引用、安全发布不可变对象适合 volatile；复合修改和条件等待不适合。",
      "followUpQuestions": [
        {
          "question": "volatile 为什么不能保证 count++ 的原子性？",
          "answer": "count++ 会先读旧值，再计算新值，再写回。volatile 让每次读写都有可见性和顺序约束，但两个线程仍可能都读到同一个旧值，然后各自写回相同的新值，导致丢失更新。要把读-改-写作为一个整体，需要 AtomicInteger、LongAdder、synchronized 或 Lock。"
        },
        {
          "question": "DCL 中 volatile 防止的是什么问题？",
          "answer": "它防止 instance 引用在对象初始化完成前被其他线程观察到。new 对象不是单步操作，引用赋值可能被重排到初始化之前；volatile 写 instance 具备 release 语义，构造中的字段写不能被发布到它之后，其他线程 volatile 读到非 null 后能看到初始化完成的状态。"
        },
        {
          "question": "volatile 和 synchronized 怎么选？",
          "answer": "如果只是发布一个状态或引用，且写入是单变量赋值，volatile 通常足够；如果需要保护多个变量的一致性、执行 check-then-act、计数自增、条件等待或临界区互斥，就用 synchronized、Lock 或原子类。volatile 是可见性和有序性工具，不是通用互斥工具。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能说明 volatile 写/读如何建立 happens-before",
        "能用 release/acquire 解释 volatile 的有序性边界",
        "能说出 volatile 写和读对应的典型屏障效果",
        "能解释 volatile 为什么不保证 count++ 原子性",
        "能说明 DCL 中 volatile 和 synchronized 分别承担什么职责",
        "能识别 volatile 数组和多个 volatile 字段的一致性误区",
        "能在 volatile、Atomic/LongAdder、synchronized/Lock 之间做正确选型"
      ]
    }
  ],
  "quiz": null,
  "recallPrompts": [
    {
      "id": "java.concurrency.topic-015-45bf8ebd.recall.1",
      "prompt": "volatile 的可见性和禁止重排序分别是如何实现的？DCL 单例中为什么要加 volatile？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 4
    },
    {
      "id": "java.concurrency.topic-015-45bf8ebd.recall.2",
      "prompt": "volatile 原理在什么场景下会导致数据不一致或性能问题？如何预防？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 4
    },
    {
      "id": "java.concurrency.topic-015-45bf8ebd.recall.3",
      "prompt": "请写一个 volatile 状态标志或安全发布示例，并说明为什么 volatile 不能保证 count++ 的原子性。",
      "mode": "code",
      "expectedMinutes": 10,
      "difficulty": 4
    },
    {
      "id": "java.concurrency.topic-015-45bf8ebd.recall.4",
      "prompt": "请手写 DCL 单例，并说明 volatile 防止的是哪一步重排序。",
      "mode": "code",
      "expectedMinutes": 6,
      "difficulty": 4
    },
    {
      "id": "java.concurrency.topic-015-45bf8ebd.recall.5",
      "prompt": "请写一个 volatile 状态标志停止线程的示例，并说明为什么 volatile 不能替代锁。",
      "mode": "code",
      "expectedMinutes": 6,
      "difficulty": 4
    }
  ],
  "rubric": {
    "mustHave": [
      "能说出 volatile 保证可见性和有序性、但不保证原子性",
      "能解释 volatile 写/读基于 happens-before 建立可见性",
      "能讲清 DCL 单例为什么给 instance 加 volatile",
      "能说明复合操作要改用 Atomic、LongAdder、synchronized 或 Lock"
    ],
    "goodToHave": [
      "能用 release/acquire 和屏障解释 volatile 读写语义",
      "能区分 volatile 数组的引用可见与元素不可见",
      "能说明多个 volatile 字段不自动形成跨字段一致性",
      "能用 LongAdder / AtomicInteger 对比 volatile 在高频写下的取舍"
    ],
    "commonMistakes": [
      "以为 volatile 能保证 count++ 这类复合操作的原子性",
      "DCL 单例忘记给 instance 加 volatile，可能读到未初始化对象",
      "以为给数组加 volatile 能让数组元素的写可见",
      "把 volatile 当作 synchronized 的轻量替代品"
    ],
    "scoreWeights": {
      "coverage": 20,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 30
    }
  },
  "updatedAt": "2026-06-19",
  "status": "production",
  "prerequisites": [
    "java.concurrency.topic-013-cc70cb0e"
  ],
  "interviewFrequency": "high",
  "interviewerFocus": "考察能否从 JMM happens-before、release/acquire、DCL 安全发布和非原子复合操作边界解释 volatile。"
}
