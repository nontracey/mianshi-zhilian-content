{
  "id": "java.concurrency.topic-virtual-threads",
  "domain": "java",
  "category": "concurrency",
  "group": "concurrency",
  "title": "Virtual Threads（Java 21+）",
  "summary": "讲清 Java 21 虚拟线程的线程-per-task 模型、mount/unmount 到 carrier 的调度机制、适合阻塞 I/O 而非 CPU 加速、JDK 21-23 与 JDK 24 pinning 差异，以及资源限流和观测迁移边界。",
  "tags": [
    "Java",
    "并发编程",
    "Virtual Threads",
    "Project Loom"
  ],
  "difficulty": 4,
  "estimatedMinutes": 35,
  "order": 110,
  "recommendWeight": 84,
  "learningCards": [
    {
      "type": "explain",
      "title": "虚拟线程让 thread-per-task 回来",
      "content": "Virtual Threads 是 JDK 21 正式特性（OpenJDK JEP 444）。它仍然是 `java.lang.Thread`，但由 JVM 调度，运行时挂载到少量平台线程（carrier）上。它的目标不是让 CPU 变快，而是让“一请求/一任务/一线程”的同步阻塞写法在大量 I/O 等待场景下具备更好的伸缩性。\n\n平台线程直接映射到 OS 线程，创建、栈内存和上下文切换成本较高，所以传统服务常用固定线程池复用平台线程。虚拟线程很便宜，推荐每个任务创建一个：`Executors.newVirtualThreadPerTaskExecutor()`、`Thread.startVirtualThread(...)` 或 `Thread.ofVirtual()`。不要把虚拟线程再池化；如果需要限制并发，限制的是数据库连接、下游 QPS、文件句柄、队列长度等外部资源。\n\n虚拟线程对迁移很友好，因为大量同步阻塞代码可以保留形态；但它不会自动修复慢 SQL、下游限流缺失、ThreadLocal 大对象、锁粒度过大或 CPU 密集计算。"
    },
    {
      "type": "explain",
      "title": "mount/unmount：等待时释放 carrier",
      "content": "虚拟线程执行 Java 代码时会 mount 到某个 carrier 平台线程上。遇到 JDK 已适配的阻塞点，例如网络 I/O、`LockSupport.park`、`Thread.sleep`、部分阻塞队列等待，虚拟线程可以 unmount，carrier 被释放去运行其他虚拟线程；等待条件满足后，虚拟线程再 remount 到某个 carrier 继续执行。\n\n这解释了它适合阻塞 I/O 的原因：平台线程不再被大量等待中的请求长期占住。对 CPU 密集任务，虚拟线程仍要消耗真实 CPU 时间，创建再多也不会突破核心数；CPU 密集任务仍要用固定大小执行器、ForkJoin 或并行流等方式控制并行度。\n\n阻塞能否卸载取决于阻塞点是否与虚拟线程协作。老旧 native/foreign 调用、某些不可中断阻塞和长时间持有临界区，仍可能限制伸缩性。"
    },
    {
      "type": "explain",
      "title": "pinning 的版本边界要讲准",
      "content": "JDK 21-23 中，一个虚拟线程在 `synchronized` 方法或代码块里执行阻塞操作时，可能 pin 住 carrier，导致 carrier 不能去运行其他虚拟线程。JFR 可观察到 `jdk.VirtualThreadPinned` 事件，线程 dump 也能帮助定位长期占用点。\n\nOpenJDK JEP 491 在 JDK 24 改进了 synchronized 与虚拟线程的协作，目标是让虚拟线程在 synchronized 中阻塞时也能释放底层平台线程，消除几乎所有这类 pinning。这个变化意味着 2026 年不能再简单说“虚拟线程场景要把 synchronized 全换成 ReentrantLock”。正确说法是：JDK 21-23 要重点规避 synchronized 内慢阻塞；JDK 24+ 仍要保持临界区短，锁选择回到语义、可中断、超时、多条件队列等需求。\n\nnative/foreign 调用、长时间 CPU 占用、下游资源耗尽和粗粒度锁仍然是边界。"
    },
    {
      "type": "explain",
      "title": "上线评估看外部资源和观测",
      "content": "迁移到虚拟线程后，最常见的错觉是“线程更多，所以并发更高”。真实瓶颈往往转移到数据库连接池、HTTP 连接池、Redis 连接、第三方接口 QPS、消息队列消费速率或本机文件句柄。虚拟线程让等待便宜，但不会让下游容量变大。\n\n因此每任务虚拟线程通常要配合 Semaphore、连接池、bulkhead、限流器和超时。不要用固定虚拟线程池大小表达资源保护；那会把便宜的线程又变成稀缺资源。应该让虚拟线程多，但让外部资源有明确预算。\n\n观测上要关注：JFR pinning 事件、虚拟线程 thread dump、carrier CPU、下游连接池等待、请求延迟分布、ThreadLocal 内存占用、MDC/trace 上下文传播。JEP 444 说明虚拟线程支持 ThreadLocal，但大量虚拟线程持有大对象会放大内存压力；只读上下文可以评估 ScopedValue。"
    },
    {
      "type": "diagram",
      "format": "svg",
      "title": "虚拟线程调度、pinning 与资源边界",
      "content": "assets/diagrams/java-virtual-threads-scheduling.svg"
    },
    {
      "type": "compareTable",
      "title": "平台线程、虚拟线程和响应式对比",
      "content": "| 维度 | 平台线程池 | 虚拟线程 | 响应式/事件驱动 |\n| --- | --- | --- | --- |\n| 编程模型 | 同步阻塞，线程复用 | 同步阻塞，每任务一个线程 | 非阻塞回调/流式链 |\n| 调度方 | OS + JVM | JVM 调度，挂载到 carrier | 事件循环和回调调度 |\n| 适合场景 | CPU 任务、固定资源池 | 大量阻塞 I/O、请求型服务 | 高吞吐流处理、显式背压链路 |\n| 并发保护 | 线程池大小、队列、拒绝策略 | 下游资源预算、Semaphore、连接池 | 背压协议、限流、事件循环保护 |\n| CPU 密集 | 按核心数控制 | 不提升算力 | 不提升算力 |\n| ThreadLocal | 常用但要清理 | 支持，但数量大时谨慎 | 通常依赖上下文传播机制 |\n| 主要风险 | 线程数不足、队列堆积 | 下游耗尽、pinning、ThreadLocal 内存 | 心智负担、阻塞污染事件循环 |\n\n| 版本边界 | 说明 |\n| --- | --- |\n| JDK 21 | 虚拟线程正式特性，ThreadLocal 始终支持 |\n| JDK 21-23 | synchronized 内阻塞可能 pin carrier |\n| JDK 24+ | JEP 491 改进 synchronized pinning，但仍要控制临界区和外部资源 |\n| JDK 25+ | ScopedValue 正式化后更适合只读上下文作用域传递 |"
    },
    {
      "type": "code",
      "title": "可运行代码：检测并演示虚拟线程 per-task 模型",
      "language": "java",
      "content": "import java.lang.reflect.Method;\nimport java.util.ArrayList;\nimport java.util.List;\nimport java.util.concurrent.Callable;\nimport java.util.concurrent.ExecutorService;\nimport java.util.concurrent.Executors;\nimport java.util.concurrent.Future;\nimport java.util.concurrent.Semaphore;\nimport java.util.concurrent.TimeUnit;\nimport java.util.concurrent.atomic.AtomicInteger;\n\npublic class Main {\n    public static void main(String[] args) throws Exception {\n        Method factory;\n        try {\n            factory = Executors.class.getMethod(\"newVirtualThreadPerTaskExecutor\");\n        } catch (NoSuchMethodException notOnJdk21) {\n            System.out.println(\"virtualThreadsAvailable=false, java=\"\n                    + System.getProperty(\"java.version\"));\n            return;\n        }\n\n        ExecutorService executor = (ExecutorService) factory.invoke(null);\n        Semaphore dbPermits = new Semaphore(2);\n        AtomicInteger maxInDb = new AtomicInteger();\n        AtomicInteger activeInDb = new AtomicInteger();\n        List<Future<String>> futures = new ArrayList<>();\n        try {\n            for (int i = 0; i < 6; i++) {\n                int taskId = i;\n                Callable<String> task = () -> {\n                    boolean virtual = isVirtual(Thread.currentThread());\n                    dbPermits.acquire();\n                    int now = activeInDb.incrementAndGet();\n                    maxInDb.accumulateAndGet(now, Math::max);\n                    try {\n                        Thread.sleep(30);\n                        return \"task-\" + taskId + \":virtual=\" + virtual;\n                    } finally {\n                        activeInDb.decrementAndGet();\n                        dbPermits.release();\n                    }\n                };\n                futures.add(executor.submit(task));\n            }\n            for (Future<String> future : futures) {\n                System.out.println(future.get());\n            }\n            System.out.println(\"maxInDb=\" + maxInDb.get());\n            assert maxInDb.get() <= 2;\n        } finally {\n            executor.shutdown();\n            executor.awaitTermination(1, TimeUnit.SECONDS);\n        }\n    }\n\n    static boolean isVirtual(Thread thread) {\n        try {\n            Method method = Thread.class.getMethod(\"isVirtual\");\n            return (Boolean) method.invoke(thread);\n        } catch (ReflectiveOperationException unavailableBeforeJdk21) {\n            return false;\n        }\n    }\n}",
      "highlights": [
        {
          "line": 15,
          "note": "用反射检测 JDK 21 API，代码在 JDK 17 可运行，在 JDK 21+ 会真实使用虚拟线程执行器。"
        },
        {
          "line": 23,
          "note": "虚拟线程不再池化，外部资源用 Semaphore 或连接池限制并发。"
        },
        {
          "line": 33,
          "note": "每个任务提交后由虚拟线程执行；是否虚拟线程通过 Thread.isVirtual 反射验证。"
        },
        {
          "line": 48,
          "note": "运行结果验证数据库类资源并发没有超过许可数。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "我会先定义：虚拟线程是 JDK 21 正式引入的轻量级 `java.lang.Thread`，由 JVM 调度，运行时挂载到 carrier 平台线程上。它让高并发 I/O 服务可以继续使用同步阻塞代码，每个请求一个虚拟线程，而不是把业务拆成复杂回调链。\n\n底层抓手是 mount/unmount：虚拟线程运行 Java 代码时占用 carrier；遇到适配的阻塞 I/O、sleep、park 等等待点可以卸载，carrier 去跑其他虚拟线程；等待结束后再挂载继续。它降低的是等待期间占用平台线程的成本，不提升 CPU 算力。\n\n边界要讲三类。第一，不要池化虚拟线程，直接每任务创建；真正要限的是数据库连接、下游 QPS、文件句柄和队列。第二，JDK 21-23 中 synchronized 内阻塞可能 pin carrier；JDK 24 的 JEP 491 已改善这类 pinning，但 native/foreign 调用、长时间持锁和下游耗尽仍要排查。第三，ThreadLocal 虽被支持，但大量虚拟线程持有大对象会增加内存压力，只读上下文优先考虑作用域化传递。\n\n与响应式相比，虚拟线程保留同步代码和调试体验，适合大量请求型阻塞 I/O；响应式仍适合复杂流处理和显式背压链路。上线验证看 JFR pinning、线程 dump、carrier CPU、下游连接池等待和端到端延迟。",
      "followUpQuestions": [
        {
          "question": "为什么不要池化虚拟线程？",
          "answer": "平台线程昂贵，所以需要池化复用；虚拟线程很便宜，设计目标就是每个任务创建一个并在任务结束后丢弃。池化虚拟线程会重新制造稀缺线程，并掩盖真正需要保护的资源。并发控制应放在数据库连接池、HTTP 连接池、Semaphore、限流器或队列上。"
        },
        {
          "question": "虚拟线程适合 CPU 密集任务吗？",
          "answer": "不适合用来提升 CPU 密集任务吞吐。虚拟线程只降低等待型任务占用平台线程的成本，CPU 计算仍受核心数限制。CPU 密集任务要限制并行度，用固定大小执行器、ForkJoin 或并行算法，避免创建大量同时争抢 CPU 的任务。"
        },
        {
          "question": "如何排查虚拟线程上线后效果不达预期？",
          "answer": "先看瓶颈是否转移到下游：数据库连接池等待、HTTP 连接池、第三方限流、文件句柄。再看 JFR 的 pinning 事件和线程 dump，定位 synchronized、native 或长临界区。还要看 ThreadLocal/MDC 内存占用、carrier CPU、请求延迟分布和超时配置，确认不是把等待从线程池转移到了资源池。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能解释虚拟线程、平台线程和 carrier 的关系",
        "能说明 mount/unmount 如何降低阻塞 I/O 对平台线程的占用",
        "能写出每任务一个虚拟线程的创建方式，并说明不要池化虚拟线程",
        "能指出 CPU 密集任务、下游资源耗尽、ThreadLocal 大对象不是虚拟线程能自动解决的问题",
        "能说明 JDK 21-23 synchronized pinning 与 JDK 24+ JEP 491 的版本差异",
        "能用 JFR、线程 dump、连接池等待和延迟指标排查迁移问题",
        "能对比虚拟线程、传统平台线程池和响应式模型的适用边界"
      ]
    }
  ],
  "quiz": null,
  "recallPrompts": [
    {
      "id": "java.concurrency.topic-virtual-threads.recall.1",
      "prompt": "Virtual Threads（Java 21+） 的实现原理是什么？和平台线程池相比有什么优劣？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 4
    },
    {
      "id": "java.concurrency.topic-virtual-threads.recall.2",
      "prompt": "Virtual Threads 在 JDK 21-23 中为什么可能发生 pinning？JDK 24+ 的 JEP 491 改变了什么？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 4
    },
    {
      "id": "java.concurrency.topic-virtual-threads.recall.3",
      "prompt": "Virtual Threads、传统线程池和响应式编程有什么关键区别？",
      "mode": "text",
      "expectedMinutes": 4,
      "difficulty": 4
    },
    {
      "id": "java.concurrency.topic-virtual-threads.recall.scenario",
      "prompt": "生产环境里 Virtual Threads 出现效果不达预期时，你会如何定位原因、选择修复方案并验证结果？",
      "mode": "text",
      "expectedMinutes": 4,
      "difficulty": 4
    }
  ],
  "rubric": {
    "mustHave": [
      "能准确描述虚拟线程、平台线程、carrier 的关系",
      "能解释阻塞 I/O 时卸载/重新挂载机制",
      "能展示每任务一个虚拟线程的代码并说明不要池化",
      "能说明 Java 21-23 pinning 与 Java 24 JEP 491 的版本差异"
    ],
    "goodToHave": [
      "能指出 DB 连接池、下游限流、ThreadLocal 大对象、监控兼容性等迁移风险",
      "能比较虚拟线程、传统线程池、响应式模型的取舍",
      "能用 JFR/线程 dump/pinning 诊断定位问题",
      "能说明 ScopedValue 对只读上下文的适用边界"
    ],
    "commonMistakes": [
      "以为虚拟线程适合 CPU 密集任务或能提升 CPU 算力",
      "继续用固定虚拟线程池控制并发，忽略外部资源限流",
      "忽略 JDK 版本下 synchronized/native pinning 差异",
      "把虚拟线程和响应式简单说成谁完全替代谁"
    ],
    "scoreWeights": {
      "coverage": 25,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 25
    }
  },
  "updatedAt": "2026-06-19",
  "status": "production",
  "prerequisites": [
    "java.concurrency.topic-021-d2222a23",
    "java.concurrency.topic-025-a8370d9d"
  ],
  "interviewFrequency": "medium",
  "interviewerFocus": "考察能否基于 JDK 21/24 版本事实解释虚拟线程调度、pinning、每任务模型、资源限流和生产迁移排查。"
}
