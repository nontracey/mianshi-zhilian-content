{
  "id": "java.concurrency.topic-029-2b936689",
  "domain": "java",
  "category": "concurrency",
  "group": "concurrency",
  "title": "ArrayList与LinkedList",
  "summary": "ArrayList与LinkedList",
  "tags": [
    "Java",
    "并发编程",
    "ArrayList与LinkedList"
  ],
  "difficulty": 2,
  "estimatedMinutes": 20,
  "order": 290,
  "recommendWeight": 71,
  "learningCards": [
    {
      "type": "explain",
      "title": "知识全景",
      "content": "# ArrayList与LinkedList\n## 1. ArrayList 底层实现\nArrayList 底层是一个 **Object 数组**，通过动态扩容来支持元素的增长。\n### 要点\n- 底层使用 `Object[] elementData` 存储元素\n- 默认初始容量为 10（首次 add 时才真正分配）\n- 支持随机访问（通过下标直接访问），时间复杂度 O(1)\n- 是线程不安全的，适合单线程读多写少的场景\n- 实现了 `RandomAccess` 接口，表明支持快速随机访问\n- 使用 `transient` 修饰 elementData，自定义了序列化逻辑（只序列化有效元素）\n### 示例/代码\n```java\n// ArrayList 核心字段\npublic class ArrayList<E> extends AbstractList<E> implements List<E> {\n    // 默认初始容量\n    private static final int DEFAULT_CAPACITY = 10;\n    // 空数组（指定容量为0时使用）\n    private static final Object[] EMPTY_ELEMENTDATA = {};\n    // 默认空数组（无参构造时使用，延迟初始化）\n    private static final Object[] DEFAULTCAPACITY_EMPTY_ELEMENTDATA = {};\n    // 实际存储数组\n    transient Object[] elementData;\n    // 实际元素个数\n    private int size;\n    // 无参构造：使用空数组，首次 add 时才扩容到 10\n    public ArrayList() {\n        this.elementData = DEFAULTCAPACITY_EMPTY_ELEMENTDATA;\n    }\n// 指定初始容量\npublic ArrayList(int initialCapacity) {\n    if (initialCapacity > 0) {\n        this.elementData = new Object[initialCapacity];\n    } else if (initialCapacity == 0) {\n        this.elementData = EMPTY_ELEMENTDATA;\n    } else {\n        throw new IllegalArgumentException(\"Illegal Capacity: \" + initialCapacity);\n    }\n}\n// 从 Collection 构造\npublic ArrayList(Collection<? extends E> c) {\n    elementData = c.toArray();\n    if ((size = elementData.length) != 0) {\n        if (elementData.getClass() != Object[].class)\n        elementData = Arrays.copyOf(elementData, size, Object[].class);\n    } else {\n        this.elementData = EMPTY_ELEMENTDATA;\n    }\n}\n}\n```\n## 2. ArrayList 扩容机制\n### 要点\n- 新容量 = 旧容量 + 旧容量 >> 1（即 **1.5 倍**扩容）\n- 扩容时使用 `Arrays.copyOf()` 进行数组复制，创建新数组并拷贝旧数据\n- 无参构造时，首次 add 分配容量为 10，之后按 1.5 倍递增：10 → 15 → 22 → 33 → ...\n- `ensureCapacity()` 方法可以预分配空间，避免多次扩容（性能优化手段）\n- 扩容涉及数组拷贝，时间复杂度 O(n)，因此如果能预估元素数量，建议指定初始容量\n### 示例/代码\n```java\n// ArrayList 扩容核心代码\nprivate void grow(int minCapacity) {\n    int oldCapacity = elementData.length;\n    int newCapacity = oldCapacity + (oldCapacity >> 1); // 1.5倍\n    if (newCapacity - minCapacity < 0)\n    newCapacity = minCapacity;\n    if (newCapacity - MAX_ARRAY_SIZE > 0)\n    newCapacity = hugeCapacity(minCapacity);\n    // 拷贝到新数组\n    elementData = Arrays.copyOf(elementData, newCapacity);\n}\n// 扩容触发点：add 方法\npublic boolean add(E e) {\n    ensureCapacityInternal(size + 1);\n    elementData[size++] = e;\n    return true;\n}\nprivate void ensureCapacityInternal(int minCapacity) {\n    if (elementData == DEFAULTCAPACITY_EMPTY_ELEMENTDATA) {\n        minCapacity = Math.max(DEFAULT_CAPACITY, minCapacity); // 首次最少分配10\n    }\nensureExplicitCapacity(minCapacity);\n}\n// 性能优化：预分配空间\nArrayList<String> list = new ArrayList<>(1000); // 直接分配1000，避免多次扩容\nlist.ensureCapacity(5000); // 后续预扩容\n```\n## 3. ArrayList 的 add、get、remove 时间复杂度\n### 要点\n### 示例/代码\n```java\n// add(index, e) 需要移动元素\npublic void add(int index, E element) {\n    rangeCheckForAdd(index);\n    ensureCapacityInternal(size + 1);\n    // 将 index 及之后的元素后移一位\n    System.arraycopy(elementData, index, elementData, index + 1, size - index);\n    elementData[index] = element;\n    size++;\n}\n// remove(index) 需要移动元素\npublic E remove(int index) {\n    rangeCheck(index);\n    E oldValue = elementData(index);\n    int numMoved = size - index - 1;\n    if (numMoved > 0)\n    // 将 index 之后的元素前移一位\n    System.arraycopy(elementData, index+1, elementData, index, numMoved);\n    elementData[--size] = null; // 帮助GC\n    return oldValue;\n}\n```\n## 4. LinkedList 底层实现\nLinkedList 底层是一个 **双向链表**，每个节点包含前驱指针和后继指针。\n### 要点\n- 每个节点是 `Node` 对象，包含 `item`（数据）、`prev`（前驱）、`next`（后继）\n- 实现了 `List` 和 `Deque` 接口，既可以当列表用，也可以当队列/双端队列用\n- 不支持随机访问，按索引访问需要从头/尾遍历\n- 插入和删除操作只需要修改指针，不需要移动元素\n- 非线程安全，多线程环境下需额外同步\n### 示例/代码\n```java\n// LinkedList 核心结构\npublic class LinkedList<E> extends AbstractSequentialList<E> implements List<E>, Deque<E> {\n    transient int size = 0;\n    transient Node<E> first; // 头节点\n    transient Node<E> last;  // 尾节点\n    // 内部节点类\n    private static class Node<E> {\n        E item;\n        Node<E> next;\n        Node<E> prev;\n        Node(Node<E> prev, E element, Node<E> next) {\n            this.item = element;\n            this.next = next;\n            this.prev = prev;\n        }\n}\n// 头部插入 O(1)\nprivate void linkFirst(E e) {\n    final Node<E> f = first;\n    final Node<E> newNode = new Node<>(null, e, f);\n    first = newNode;\n    if (f == null)\n    last = newNode;\n    else\n    f.prev = newNode;\n    size++;\n}\n// 尾部插入 O(1)\nvoid linkLast(E e) {\n    final Node<E> l = last;\n    final Node<E> newNode = new Node<>(l, e, null);\n    last = newNode;\n    if (l == null)\n    first = newNode;\n    else\n    l.next = newNode;\n    size++;\n}\n}\n```\n## 5. ArrayList 与 LinkedList 对比\n### 要点\n- **实际开发中 ArrayList 几乎总是优于 LinkedList**：即使频繁在中间插入删除，ArrayList 的数组拷贝由于 CPU 缓存友好，实际性能也往往优于 LinkedList\n- LinkedList 的优势在于：头部插入/删除频繁、需要实现队列/双端队列的场景\n- 如果需要频繁在头部操作，可以考虑 `ArrayDeque`（基于循环数组，比 LinkedList 更快）\n### 示例/代码\n```java\n// 性能对比测试\nList<Integer> arrayList = new ArrayList<>();\nList<Integer> linkedList = new LinkedList<>();\n// 尾部插入：两者差异不大\nfor (int i = 0; i < 100000; i++) {\n    arrayList.add(i);\n    linkedList.add(i);\n}\n// 随机访问：ArrayList 远快于 LinkedList\nfor (int i = 0; i < 10000; i++) {\n    arrayList.get(i);       // O(1)\n    linkedList.get(i);      // O(n)，每次从头遍历\n}\n// 头部插入：LinkedList 更快\nfor (int i = 0; i < 10000; i++) {\n    arrayList.add(0, i);    // O(n)，需要移动所有元素\n    linkedList.addFirst(i); // O(1)，直接修改指针\n}\n```\n## 6. 线程安全的 List\n### 6.1 Vector\n- JDK1.0 就存在的古老集合类\n- 所有方法都加了 `synchronized`，线程安全但性能差\n- 扩容时默认 2 倍（可指定增量），ArrayList 是 1.5 倍\n- **已不推荐使用**\n### 6.2 Collections.synchronizedList\n- 通过包装器模式，将普通 List 包装为线程安全的 List\n- 内部使用 `synchronized(mutex)` 同步，但迭代时仍需手动加锁\n- 适合写多读少的场景\n### 6.3 CopyOnWriteArrayList（重点）\n- **写时复制**：每次写操作都会复制整个底层数组，在新数组上修改后替换引用\n- 读操作完全无锁，性能极高\n- 适合**读多写少**的场景（如监听器列表、配置缓存）\n- 缺点：写操作成本高（数组拷贝），数据实时性不强（读的是快照）\n### 示例/代码\n```java\n// Collections.synchronizedList\nList<String> syncList = Collections.synchronizedList(new ArrayList<>());\nsyncList.add(\"hello\");\n// 迭代时必须手动加锁\nsynchronized (syncList) {\n    for (String s : syncList) {\n        System.out.println(s);\n    }\n}\n// CopyOnWriteArrayList\nCopyOnWriteArrayList<String> cowList = new CopyOnWriteArrayList<>();\ncowList.add(\"hello\");\n// 迭代无需加锁，读的是快照\nfor (String s : cowList) {\n    System.out.println(s);\n}\n// CopyOnWriteArrayList 写操作源码（简化）\npublic boolean add(E e) {\n    synchronized (lock) {\n        Object[] elements = getArray();\n        int len = elements.length;\n        Object[] newElements = Arrays.copyOf(elements, len + 1); // 复制新数组\n        newElements[len] = e;\n        setArray(newElements); // 替换引用\n        return true;\n    }\n}\n```\n## 7. 面试高频问题\n### Q1: ArrayList 和 LinkedList 的区别？\n**答**：ArrayList 底层是动态数组，支持 O(1) 随机访问，尾部插入 O(1) 均摊，中间插入/删除 O(n) 需要移动元素。LinkedList 底层是双向链表，不支持随机访问（按索引访问 O(n)），头部/尾部插入删除 O(1)，但每个节点额外存储两个指针，内存开销更大。实际开发中，由于 CPU 缓存友好性，ArrayList 在绝大多数场景下性能优于 LinkedList。\n### Q2: ArrayList 的扩容机制是怎样的？\n**答**：ArrayList 默认初始容量为 10（首次 add 时分配），每次扩容为原来的 1.5 倍（旧容量 + 旧容量 >> 1）。扩容时通过 `Arrays.copyOf()` 创建新数组并拷贝旧数据。建议在已知元素数量时使用 `new ArrayList<>(capacity)` 指定初始容量，避免频繁扩容。\n### Q3: 为什么 ArrayList 的 elementData 用 transient 修饰？\n**答**：因为 elementData 数组通常会有未使用的空间（length > size），直接序列化会浪费空间且传输无效数据。ArrayList 自定义了 `writeObject()` 和 `readObject()` 方法，只序列化 size 个有效元素，反序列化后再重建数组。\n### Q4: 什么场景下使用 LinkedList？\n**答**：① 需要频繁在列表头部插入/删除元素；② 需要实现队列或双端队列（LinkedList 实现了 Deque 接口）；③ 不需要随机访问。但在实践中，ArrayDeque（基于循环数组）在队列场景下通常比 LinkedList 性能更好。\n### Q5: CopyOnWriteArrayList 的原理和适用场景？\n**答**：CopyOnWriteArrayList 采用写时复制策略，每次写操作都会加锁并复制整个底层数组，在新数组上完成修改后替换引用。读操作完全无锁，读的是快照。适合读多写少的场景，如事件监听器列表、配置缓存等。缺点是写操作成本高（数组拷贝），且读到的数据可能不是最新的。"
    },
    {
      "type": "explain",
      "title": "关键机制拆解",
      "content": "先把 ArrayList与LinkedList 当成一个可讲清楚的系统来理解：\n\n1. ArrayList与LinkedList\n2. ArrayList 底层实现\n3. ArrayList 底层是一个 **Object 数组**，通过动态扩容来支持元素的增长。\n4. 底层使用 `Object[] elementData` 存储元素\n5. 默认初始容量为 10（首次 add 时才真正分配）\n\n学习时重点关注“为什么需要它、它如何工作、什么时候会出问题、面试时怎么结构化表达”。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | ArrayList与LinkedList | 相邻知识/常见误解 |\n| --- | --- | --- |\n| 解决的问题 | 说明核心目标和使用场景 | 只背定义，忽略为什么需要它 |\n| 核心机制 | 拆成关键角色、流程、边界条件 | 把实现细节和概念边界混在一起 |\n| 面试表达 | 先结论，再机制，再场景和取舍 | 直接罗列术语，缺少层次 |\n| 关联分类 | 并发编程 | 需要能和同类知识做横向比较 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "ArrayList与LinkedList 的学习路径图，用于替代原始 Markdown 中容易变形的 ASCII 图。",
      "fallback": "建议用“输入/触发 → 核心流程 → 关键状态 → 输出/风险”的流程图复述 ArrayList与LinkedList，并标出它在 并发编程 中的位置。",
      "items": [
        "定位：并发编程",
        "输入：问题场景或触发条件",
        "机制：ArrayList 底层实现",
        "输出：面试可讲的结论与取舍"
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "// ArrayList 核心字段\npublic class ArrayList<E> extends AbstractList<E> implements List<E> {\n    // 默认初始容量\n    private static final int DEFAULT_CAPACITY = 10;\n    // 空数组（指定容量为0时使用）\n    private static final Object[] EMPTY_ELEMENTDATA = {};\n    // 默认空数组（无参构造时使用，延迟初始化）\n    private static final Object[] DEFAULTCAPACITY_EMPTY_ELEMENTDATA = {};\n    // 实际存储数组\n    transient Object[] elementData;\n    // 实际元素个数\n    private int size;\n\n    // 无参构造：使用空数组，首次 add 时才扩容到 10\n    public ArrayList() {\n        this.elementData = DEFAULTCAPACITY_EMPTY_ELEMENTDATA;\n    }\n\n    // 指定初始容量\n    public ArrayList(int initialCapacity) {\n        if (initialCapacity > 0) {\n            this.elementData = new Object[initialCapacity];\n        } else if (initialCapacity == 0) {\n            this.elementData = EMPTY_ELEMENTDATA;\n        } else {\n            throw new IllegalArgumentException(\"Illegal Capacity: \" + initialCapacity);\n        }\n    }\n\n    // 从 Collection 构造\n    public ArrayList(Collection<? extends E> c) {\n        elementData = c.toArray();\n        if ((size = elementData.length) != 0) {\n            if (elementData.getClass() != Object[].class)\n                elementData = Arrays.copyOf(elementData, size, Obj"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "可以按“四段式”回答 ArrayList与LinkedList：先用一句话给定义和价值；再拆 并发编程 里的关键机制；接着补充适用场景、性能或一致性等取舍；最后用项目排查、优化或设计经验收束。"
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "ArrayList与LinkedList 的定义和边界",
        "ArrayList与LinkedList 的关键机制",
        "ArrayList与LinkedList 的典型场景和常见追问",
        "ArrayList与LinkedList 在面试中的结构化表达方式"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "java.concurrency.topic-029-2b936689.recall.1",
      "prompt": "请用自己的话解释 ArrayList与LinkedList。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.concurrency.topic-029-2b936689.recall.2",
      "prompt": "如果面试官追问 ArrayList与LinkedList 的应用场景或常见误区，你会怎么回答？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "定义准确",
      "关键机制",
      "适用场景",
      "常见误区",
      "面试表达结构"
    ],
    "goodToHave": [
      "结合项目经验",
      "能做对比",
      "能说明取舍"
    ],
    "commonMistakes": [
      "只背概念不讲原因",
      "忽略边界条件",
      "缺少面试化表达"
    ],
    "scoreWeights": {
      "coverage": 40,
      "accuracy": 25,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-05-27"
}
