{
  "id": "java.spring.topic-078-0195276a",
  "domain": "java",
  "category": "spring",
  "group": "spring",
  "title": "可靠性与实战",
  "summary": "消息从生产者到消费者，可能在三个环节丢失：",
  "tags": [
    "Java",
    "Spring 生态",
    "可靠性与实战"
  ],
  "difficulty": 2,
  "estimatedMinutes": 20,
  "order": 780,
  "recommendWeight": 62,
  "learningCards": [
    {
      "type": "explain",
      "title": "知识全景",
      "content": "# 可靠性与实战\n## 一、消息丢失问题\n消息从生产者到消费者，可能在三个环节丢失：\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   消息丢失的三个环节                          │\n├──────────────────────────────────────────────────────────────┤\n│                                                              │\n│  Producer ──①──→ Broker ──②──→ Queue ──③──→ Consumer        │\n│                                                              │\n│  ① 生产端丢失：网络故障、Broker未收到消息                   │\n│  ② Broker丢失：消息在Broker宕机时未持久化                   │\n│  ③ 消费端丢失：消费者收到消息后宕机，消息未处理完            │\n└──────────────────────────────────────────────────────────────┘\n```\n### 1.1 生产端防丢失：Publisher Confirm\n```java\n@Configuration\npublic class RabbitMQConfig {\n@Bean\npublic RabbitTemplate rabbitTemplate(ConnectionFactory connectionFactory) {\nRabbitTemplate template = new RabbitTemplate(connectionFactory);\n// 开启publisher confirm\ntemplate.setConfirmCallback((correlationData, ack, cause) -> {\nif (!ack) {\nlog.error(\"消息发送失败，correlationId={}, cause={}\",\ncorrelationData.getId(), cause);\n// 重试或记录到数据库\nretryService.saveFailedMessage(correlationData);\n}\n});\n// 开启publisher return（消息到达队列失败时回调）\ntemplate.setReturnsCallback(returned -> {\nlog.error(\"消息路由到队列失败，message={}, exchange={}, routingKey={}\",\nnew String(returned.getMessage().getBody()),\nreturned.getExchange(),\nreturned.getRoutingKey());\n});\n// 设置mandatory=true（配合return使用）\ntemplate.setMandatory(true);\nreturn template;\n}\n}\n```\n### 1.2 Broker防丢失：持久化\n```java\n// 1. Exchange持久化\nchannel.exchangeDeclare(\"exchange_name\", BuiltinExchangeType.DIRECT, true);\n// 2. Queue持久化\nchannel.queueDeclare(\"queue_name\", true, false, false, null);\n// 3. Message持久化\nAMQP.BasicProperties props = new AMQP.BasicProperties.Builder()\n.deliveryMode(2)  // 2=持久化，1=非持久化\n.build();\nchannel.basicPublish(\"exchange_name\", \"routing_key\", props, message.getBytes());\n```\n### 1.3 消费端防丢失：手动Ack\n```java\n@Component\npublic class OrderMessageListener {\n@RabbitListener(queues = \"order_queue\")\npublic void handleOrderMessage(Message message, Channel channel) throws IOException {\nlong deliveryTag = message.getMessageProperties().getDeliveryTag();\ntry {\n// 处理消息\nString body = new String(message.getBody());\norderService.processOrder(body);\n// 处理成功，手动确认\nchannel.basicAck(deliveryTag, false);\n} catch (Exception e) {\nlog.error(\"消息处理失败\", e);\n// 处理失败，拒绝消息\n// 第三个参数：requeue=true重新入队，false进入死信队列\nchannel.basicNack(deliveryTag, false, false);\n// 注意：不要设置requeue=true，否则会反复失败\n}\n}\n}\n```\n### 1.4 完整的消息可靠性保证\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   消息可靠性保证方案                          │\n├──────────────────────────────────────────────────────────────┤\n│  生产端：                                                     │\n│  ✅ 开启Publisher Confirm                                    │\n│  ✅ 记录消息发送日志，失败时重试                             │\n│  ✅ 设置mandatory=true，路由失败时回调                       │\n│                                                              │\n│  Broker：                                                     │\n│  ✅ Exchange持久化                                           │\n│  ✅ Queue持久化                                              │\n│  ✅ Message持久化（deliveryMode=2）                          │\n│  ✅ 配置镜像队列或Quorum Queue（集群高可用）                 │\n│                                                              │\n│  消费端：                                                     │\n│  ✅ 关闭自动确认，使用手动Ack                                │\n│  ✅ 处理成功后Ack，处理失败后Nack                            │\n│  ✅ 失败消息进入死信队列，后续人工/自动处理                  │\n└──────────────────────────────────────────────────────────────┘\n```\n## 二、消息幂等性（去重）\n### 2.1 为什么需要幂等性\n消息可能被重复消费，原因包括：\n- 消费者处理成功但Ack失败，Broker重新投递\n- 网络抖动导致重复发送\n- 消费者超时，Broker认为消费失败并重新投递\n### 2.2 幂等性设计\n#### 方案一：唯一ID + 去重表\n```java\n@Component\npublic class IdempotentMessageListener {\n@Autowired\nprivate MessageDedupMapper dedupMapper;\n@RabbitListener(queues = \"order_queue\")\npublic void handleMessage(Message message, Channel channel) throws IOException {\nString messageId = message.getMessageProperties().getMessageId();\n// 1. 检查是否已处理\nif (dedupMapper.exists(messageId)) {\nlog.info(\"消息已处理，跳过：{}\", messageId);\nchannel.basicAck(message.getMessageProperties().getDeliveryTag(), false);\nreturn;\n}\ntry {\n// 2. 处理消息\norderService.processOrder(new String(message.getBody()));\n// 3. 记录已处理\ndedupMapper.insert(messageId, LocalDateTime.now());\n// 4. 确认消息\nchannel.basicAck(message.getMessageProperties().getDeliveryTag(), false);\n} catch (Exception e) {\nchannel.basicNack(message.getMessageProperties().getDeliveryTag(), false, false);\n}\n}\n}\n// 去重表\n// CREATE TABLE message_dedup (\n//     message_id VARCHAR(64) PRIMARY KEY,\n//     processed_at DATETIME,\n//     INDEX idx_processed_at (processed_at)\n// );\n```\n#### 方案二：业务状态机\n```java\n// 订单状态机：待支付 → 已支付 → 已发货 → 已完成\n// 重复收到\"支付成功\"消息时，检查订单状态\npublic void handlePaySuccess(String orderId) {\nOrder order = orderMapper.selectById(orderId);\nif (order.getStatus() == OrderStatus.PENDING) {\n// 待支付 → 已支付（正常处理）\norder.setStatus(OrderStatus.PAID);\norderMapper.updateById(order);\n} else if (order.getStatus() == OrderStatus.PAID) {\n// 已支付 → 收到重复消息，忽略\nlog.warn(\"重复的支付成功消息，orderId={}\", orderId);\n} else {\n// 其他状态，记录异常\nlog.error(\"异常的订单状态，orderId={}, status={}\", orderId, order.getStatus());\n}\n}\n```\n#### 方案三：Redis原子操作\n```java\npublic boolean tryProcess(String messageId) {\n// SETNX保证只有一个线程能处理该消息\nBoolean success = redisTemplate.opsForValue()\n.setIfAbsent(\"msg:processed:\" + messageId, \"1\", 24, TimeUnit.HOURS);\nreturn Boolean.TRUE.equals(success);\n}\n// 使用\npublic void handleMessage(String messageId, String body) {\nif (!tryProcess(messageId)) {\nlog.info(\"消息已处理，跳过：{}\", messageId);\nreturn;\n}\ntry {\nprocessBusiness(body);\n} catch (Exception e) {\n// 处理失败，删除标记（允许重试）\nredisTemplate.delete(\"msg:processed:\" + messageId);\nthrow e;\n}\n}\n```\n## 三、消息顺序性\n### 3.1 问题场景\n```\n┌──────────────────────────────────────────────────────────────┐\n│                   消息顺序性问题                              │\n├──────────────────────────────────────────────────────────────┤\n│  场景：订单操作需要有序                                       │\n│  1. 创建订单（order_id=1001）                                │\n│  2. 支付订单（order_id=1001）                                │\n│  3. 发货订单（order_id=1001）                                │\n│                                                              │\n│  如果这三个消息被不同消费者并发消费，可能出现：               │\n│  - 消费者A先处理\"发货\"，消费者B后处理\"创建\"                  │\n│  - 导致业务异常                                              │\n└──────────────────────────────────────────────────────────────┘\n```\n### 3.2 保证顺序的方案\n```java\n// 方案：相同业务ID的消息路由到同一个队列，单线程消费\n// 生产端：根据订单ID选择队列\npublic void sendOrderMessage(Long orderId, String message) {\n// 使用订单ID的hash值选择队列\nint queueIndex = Math.abs(orderId.hashCode()) % QUEUE_COUNT;\nString queueName = \"order_queue_\" + queueIndex;\nrabbitTemplate.convertAndSend(\"order_exchange\", queueName, message);\n}\n// 消费端：每个队列单线程消费\n@RabbitListener(queues = \"order_queue_0\", concurrency = \"1\")\npublic void handleQueue0(Message message, Channel channel) {\n// 单线程顺序消费\nprocessMessage(message);\n}\n@RabbitListener(queues = \"order_queue_1\", concurrency = \"1\")\npublic void handleQueue1(Message message, Channel channel) {\nprocessMessage(message);\n}\n```\n## 四、Spring AMQP使用\n### 4.1 依赖配置\n```xml\n<!-- pom.xml -->\n<dependency>\n<groupId>org.springframework.boot</groupId>\n<artifactId>spring-boot-starter-amqp</artifactId>\n</dependency>\n```\n```yaml\n# application.yml\nspring:\nrabbitmq:\nhost: localhost\nport: 5672\nusername: guest\npassword: guest\nvirtual-host: /\n# 生产者确认\npublisher-confirm-type: correlated  # none/simple/correlated\npublisher-returns: true\n# 消费者手动确认\nlistener:\nsimple:\nacknowledge-mode: manual  # auto/manual/none\nprefetch: 1               # 预取消息数量\nretry:\nenabled: true\nmax-attempts: 3\ninitial-interval: 3000\n```\n### 4.2 配置类\n```java\n@Configuration\npublic class RabbitMQConfig {\n// 声明交换机\n@Bean\npublic DirectExchange orderExchange() {\nreturn ExchangeBuilder.directExchange(\"order.exchange\")\n.durable(true)\n.build();\n}\n// 声明队列\n@Bean\npublic Queue orderQueue() {\nreturn QueueBuilder.dur"
    },
    {
      "type": "explain",
      "title": "关键机制拆解",
      "content": "先把 可靠性与实战 当成一个可讲清楚的系统来理解：\n\n1. 一、消息丢失问题\n2. 消息从生产者到消费者，可能在三个环节丢失：\n3. 1 生产端防丢失：Publisher Confirm\n4. @Configuration\n5. public class RabbitMQConfig {\n\n学习时重点关注“为什么需要它、它如何工作、什么时候会出问题、面试时怎么结构化表达”。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | 可靠性与实战 | 相邻知识/常见误解 |\n| --- | --- | --- |\n| 解决的问题 | 说明核心目标和使用场景 | 只背定义，忽略为什么需要它 |\n| 核心机制 | 拆成关键角色、流程、边界条件 | 把实现细节和概念边界混在一起 |\n| 面试表达 | 先结论，再机制，再场景和取舍 | 直接罗列术语，缺少层次 |\n| 关联分类 | Spring 生态 | 需要能和同类知识做横向比较 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "可靠性与实战 的学习路径图，用于替代原始 Markdown 中容易变形的 ASCII 图。",
      "fallback": "建议用“输入/触发 → 核心流程 → 关键状态 → 输出/风险”的流程图复述 可靠性与实战，并标出它在 Spring 生态 中的位置。",
      "items": [
        "定位：Spring 生态",
        "输入：问题场景或触发条件",
        "机制：一、消息丢失问题",
        "输出：面试可讲的结论与取舍"
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "@Configuration\npublic class RabbitMQConfig {\n@Bean\npublic RabbitTemplate rabbitTemplate(ConnectionFactory connectionFactory) {\nRabbitTemplate template = new RabbitTemplate(connectionFactory);\n// 开启publisher confirm\ntemplate.setConfirmCallback((correlationData, ack, cause) -> {\nif (!ack) {\nlog.error(\"消息发送失败，correlationId={\n}\n, cause={\n}\n\",\ncorrelationData.getId(), cause);\n// 重试或记录到数据库\nretryService.saveFailedMessage(correlationData);\n}\n}\n);\n// 开启publisher return（消息到达队列失败时回调）"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "可以按“四段式”回答 可靠性与实战：先用一句话给定义和价值；再拆 Spring 生态 里的关键机制；接着补充适用场景、性能或一致性等取舍；最后用项目排查、优化或设计经验收束。"
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "可靠性与实战 的定义和边界",
        "可靠性与实战 的关键机制",
        "可靠性与实战 的典型场景和常见追问",
        "可靠性与实战 在面试中的结构化表达方式"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "java.spring.topic-078-0195276a.recall.1",
      "prompt": "请用自己的话解释 可靠性与实战。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.spring.topic-078-0195276a.recall.2",
      "prompt": "如果面试官追问 可靠性与实战 的应用场景或常见误区，你会怎么回答？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "定义准确",
      "关键机制",
      "适用场景",
      "常见误区",
      "面试表达结构"
    ],
    "goodToHave": [
      "结合项目经验",
      "能做对比",
      "能说明取舍"
    ],
    "commonMistakes": [
      "只背概念不讲原因",
      "忽略边界条件",
      "缺少面试化表达"
    ],
    "scoreWeights": {
      "coverage": 40,
      "accuracy": 25,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-05-27"
}
