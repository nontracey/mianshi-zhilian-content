{
  "id": "java.collections.topic-040-ba7e4b10",
  "domain": "java",
  "category": "collections",
  "group": "collections",
  "title": "SpringBoot启动流程",
  "summary": "SpringBoot启动流程",
  "tags": [
    "Java",
    "集合与 Java 基础",
    "SpringBoot启动流程"
  ],
  "difficulty": 2,
  "estimatedMinutes": 20,
  "order": 400,
  "recommendWeight": 100,
  "learningCards": [
    {
      "type": "explain",
      "title": "知识全景",
      "content": "# SpringBoot启动流程\n## 一、SpringApplication.run() 全流程概述\nSpringBoot的启动入口就是 `SpringApplication.run()` 方法，整个启动过程可以分为以下几个核心阶段：\n```\nSpringApplication.run()\n│\n├── 1. 创建SpringApplication实例\n│       ├── 推断应用类型（Servlet/Reactive/None）\n│       ├── 加载ApplicationContextInitializer\n│       └── 加载ApplicationListener\n│\n├── 2. 调用run(args)\n│       ├── 创建计时器StopWatch\n│       ├── 加载SpringApplicationRunListeners\n│       ├── 发布ApplicationStartingEvent\n│       │\n├── 3. 准备环境（prepareEnvironment）\n│       ├── 创建/配置Environment\n│       ├── 发布ApplicationEnvironmentPreparedEvent\n│       └── 绑定参数到Environment\n│\n├── 4. 创建ApplicationContext\n│       ├── 根据应用类型选择容器类\n│       └── 反射实例化\n│\n├── 5. 准备上下文（prepareContext）\n│       ├── 设置Environment\n│       ├── 应用ApplicationContextInitializer\n│       ├── 发布ApplicationContextInitializedEvent\n│       ├── 加载sources（主配置类）\n│       └── 发布ApplicationPreparedEvent\n│\n├── 6. 刷新上下文（refreshContext）  ← 核心步骤\n│       └── 调用AbstractApplicationContext.refresh()\n│\n├── 7. 发布ApplicationStartedEvent\n│\n├── 8. 调用Runner\n│       ├── ApplicationRunner.run()\n│       └── CommandLineRunner.run()\n│\n└── 9. 发布ApplicationReadyEvent\n```\n## 二、@SpringBootApplication 注解解析\n`@SpringBootApplication` 是一个组合注解，等价于同时使用 `@SpringBootConfiguration` + `@EnableAutoConfiguration` + `@ComponentScan`。\n### 2.1 注解结构\n```java\n@Target(ElementType.TYPE)\n@Retention(RetentionPolicy.RUNTIME)\n@Documented\n@Inherited\n@SpringBootConfiguration    // 1. 标识这是一个配置类（本质是@Configuration）\n@EnableAutoConfiguration    // 2. 开启自动装配（核心）\n@ComponentScan(             // 3. 包扫描\nexcludeFilters = {\n@Filter(type = FilterType.CUSTOM, classes = TypeExcludeFilter.class),\n@Filter(type = FilterType.CUSTOM, classes = AutoConfigurationExcludeFilter.class)\n}\n)\npublic @interface SpringBootApplication {\n@AliasFor(annotation = EnableAutoConfiguration.class)\nClass<?>[] exclude() default {};\n@AliasFor(annotation = EnableAutoConfiguration.class)\nString[] excludeName() default {};\n@AliasFor(annotation = ComponentScan.class, attribute = \"basePackages\")\nString[] scanBasePackages() default {};\n@AliasFor(annotation = ComponentScan.class, attribute = \"basePackageClasses\")\nClass<?>[] scanBasePackageClasses() default {};\n}\n```\n### 2.2 三个核心注解详解\n## 三、创建 SpringApplication 实例\n```java\npublic SpringApplication(ResourceLoader resourceLoader, Class<?>... primarySources) {\nthis.resourceLoader = resourceLoader;\nAssert.notNull(primarySources, \"PrimarySources must not be null\");\nthis.primarySources = new LinkedHashSet<>(Arrays.asList(primarySources));\n// 1. 推断应用类型\nthis.webApplicationType = WebApplicationType.deduceFromClasspath();\n// 2. 加载ApplicationContextInitializer（从META-INF/spring.factories）\nthis.initializers = getSpringFactoriesInstances(ApplicationContextInitializer.class);\n// 3. 加载ApplicationListener（从META-INF/spring.factories）\nthis.listeners = getSpringFactoriesInstances(ApplicationListener.class);\n// 4. 推断主类（通过获取调用栈中的main方法所在类）\nthis.mainApplicationClass = deduceMainApplicationClass();\n}\n```\n### 3.1 应用类型推断\n```java\n// WebApplicationType.deduceFromClasspath()\n// 通过classpath中是否存在特定类来判断\n// REACTIVE: 存在 DispatcherHandler 且不存在 Servlet 和 ConfigurableWebApplicationContext\n// SERVLET:  存在 Servlet 和 ConfigurableWebApplicationContext\n// NONE:     都不存在\nstatic WebApplicationType deduceFromClasspath() {\nif (ClassUtils.isPresent(WEBFLUX_INDICATOR_CLASS, null)\n&& !ClassUtils.isPresent(WEBMVC_INDICATOR_CLASS, null)\n&& !ClassUtils.isPresent(JERSEY_INDICATOR_CLASS, null)) {\nreturn WebApplicationType.REACTIVE;\n}\nfor (String className : SERVLET_INDICATOR_CLASSES) {\nif (!ClassUtils.isPresent(className, null)) {\nreturn WebApplicationType.NONE;\n}\n}\nreturn WebApplicationType.SERVLET;\n}\n```\n## 四、准备环境（prepareEnvironment）\n```java\nprivate ConfigurableEnvironment prepareEnvironment(SpringApplicationRunListeners listeners,\nApplicationArguments applicationArguments) {\n// 1. 根据应用类型创建Environment\n//    - SERVLET -> StandardServletEnvironment\n//    - REACTIVE -> StandardReactiveWebEnvironment\n//    - NONE -> StandardEnvironment\nConfigurableEnvironment environment = getOrCreateEnvironment();\n// 2. 配置Environment（加载配置文件、命令行参数等）\nconfigureEnvironment(environment, applicationArguments.getSourceArgs());\n// 3. 发布环境准备完成事件\nlisteners.environmentPrepared(environment);\n// 4. 绑定SpringApplication属性到Environment\nbindToSpringApplication(environment);\nreturn environment;\n}\n```\n**Environment 的属性源优先级**（从高到低）：\n1. 命令行参数（`--server.port=8080`）\n2. JNDI属性\n3. Java系统属性（`System.getProperties()`）\n4. 操作系统环境变量\n5. `application-{profile}.yml`\n6. `application.yml`\n7. `@PropertySource` 注解指定的配置\n8. 默认属性\n## 五、刷新上下文（refreshContext）\n这是整个启动过程中最核心的步骤，调用的是 `AbstractApplicationContext.refresh()` 方法：\n```java\npublic void refresh() throws BeansException, IllegalStateException {\nsynchronized (this.startupShutdownMonitor) {\n// 1. 准备刷新：设置启动时间、active标志，初始化属性源\nprepareRefresh();\n// 2. 获取BeanFactory：创建DefaultListableBeanFactory，加载BeanDefinition\nConfigurableListableBeanFactory beanFactory = obtainFreshBeanFactory();\n// 3. 准备BeanFactory：设置类加载器、SpEL解析器、属性编辑器等\nprepareBeanFactory(beanFactory);\ntry {\n// 4. BeanFactory后处理（子类扩展点）\npostProcessBeanFactory(beanFactory);\n// 5. 调用BeanFactoryPostProcessor ★ 自动装配在这里发生\ninvokeBeanFactoryPostProcessors(beanFactory);\n// 6. 注册BeanPostProcessor\nregisterBeanPostProcessors(beanFactory);\n// 7. 初始化国际化消息源\ninitMessageSource();\n// 8. 初始化事件广播器\ninitApplicationEventMulticaster();\n// 9. 子类扩展：内嵌Tomcat/Jetty在这里启动 ★\nonRefresh();\n// 10. 注册事件监听器\nregisterListeners();\n// 11. 实例化所有非懒加载的单例Bean ★\nfinishBeanFactoryInitialization(beanFactory);\n// 12. 完成刷新：发布ContextRefreshedEvent\nfinishRefresh();\n} catch (BeansException ex) {\ndestroyBeans();\ncancelRefresh(ex);\nthrow ex;\n} finally {\nresetCommonCaches();\n}\n}\n}\n```\n## 六、内嵌Tomcat启动\n在 `onRefresh()` 阶段，对于Servlet类型的Web应用，会启动内嵌Tomcat：\n```java\n// ServletWebServerApplicationContext.onRefresh()\n@Override\nprotected void onRefresh() {\nsuper.onRefresh();\ntry {\ncreateWebServer();  // 创建内嵌Web服务器\n} catch (Throwable ex) {\nthrow new ApplicationContextException(\"Unable to start web server\", ex);\n}\n}\nprivate void createWebServer() {\nWebServer webServer = this.webServer;\nServletContext servletContext = getServletContext();\nif (webServer == null && servletContext == null) {\n// 1. 获取ServletWebServerFactory（默认TomcatServletWebServerFactory）\nServletWebServerFactory factory = getWebServerFactory();\n// 2. 通过工厂创建WebServer（Tomcat/Jetty/Undertow）\nthis.webServer = factory.getWebServer(getSelfInitializer());\n// 3. 注册销毁回调\naddBeanFactoryPostProcessor(new WebServerPortFileWriterBeanFactoryPostProcessor(this));\n}\ninitPropertySources();\n}\n```\n## 七、调用 Runner\n容器刷新完成后，会调用所有注册的Runner：\n```java\nprivate void callRunners(ApplicationContext context, ApplicationArguments args) {\nList<Object> runners = new ArrayList<>();\n// 收集ApplicationRunner和CommandLineRunner\nrunners.addAll(context.getBeansOfType(ApplicationRunner.class).values());\nrunners.addAll(context.getBeansOfType(CommandLineRunner.class).values());\n// 排序（通过@Order注解控制）\nAnnotationAwareOrderComparator.sort(runners);\nfor (Object runner : new LinkedHashSet<>(runners)) {\nif (runner instanceof ApplicationRunner) {\ncallRunner((ApplicationRunner) runner, args);\n}\nif (runner instanceof CommandLineRunner) {\ncallRunner((CommandLineRunner) runner, args);\n}\n}\n}\n```\n### ApplicationRunner vs CommandLineRunner\n```java\n// ApplicationRunner：接收ApplicationArguments对象，可以获取选项参数和非选项参数\n@Component\n@Order(1)\npublic class MyApplicationRunner implements ApplicationRunner {\n@Override\npublic void run(ApplicationArguments args) {\nSystem.out.println(\"选项参数: \" + args.getOptionNames());     // --debug\nSystem.out.println(\"非选项参数: \" + args.getNonOptionArgs());  // 普通参数\n}\n}\n// CommandLineRunner：接收原始字符串数组\n@Component\n@Order(2)\npublic class MyCommandLineRunner implements CommandLineRunner {\n@Override\npublic void run(String... args) {\nSystem.out.println(\"命令行参数: \" + Arrays.toString(args));\n}\n}\n```\n## 八、面试高频问题\n### Q1: 请描述SpringBoot的启动流程？\n**答**：SpringBoot启动主要分为以下步骤：\n1. **创建SpringApplication**：推断应用类型、加载Initializers和Listeners、推断主类\n2. **准备环境**：创建Environment，加载配置文件，发布环境事件\n3. **创建ApplicationContext**：根据应用类型创建对应的容器\n4. **刷新上下文**：核心步骤，包括B"
    },
    {
      "type": "explain",
      "title": "关键机制拆解",
      "content": "先把 SpringBoot启动流程 当成一个可讲清楚的系统来理解：\n\n1. SpringBoot启动流程\n2. 一、SpringApplication.run() 全流程概述\n3. SpringBoot的启动入口就是 `SpringApplication.run()` 方法，整个启动过程可以分为以下几个核心阶段：\n4. SpringApplication.run()\n5. 二、@SpringBootApplication 注解解析\n\n学习时重点关注“为什么需要它、它如何工作、什么时候会出问题、面试时怎么结构化表达”。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | SpringBoot启动流程 | 相邻知识/常见误解 |\n| --- | --- | --- |\n| 解决的问题 | 说明核心目标和使用场景 | 只背定义，忽略为什么需要它 |\n| 核心机制 | 拆成关键角色、流程、边界条件 | 把实现细节和概念边界混在一起 |\n| 面试表达 | 先结论，再机制，再场景和取舍 | 直接罗列术语，缺少层次 |\n| 关联分类 | 集合与 Java 基础 | 需要能和同类知识做横向比较 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "SpringBoot启动流程 的学习路径图，用于替代原始 Markdown 中容易变形的 ASCII 图。",
      "fallback": "建议用“输入/触发 → 核心流程 → 关键状态 → 输出/风险”的流程图复述 SpringBoot启动流程，并标出它在 集合与 Java 基础 中的位置。",
      "items": [
        "定位：集合与 Java 基础",
        "输入：问题场景或触发条件",
        "机制：一、SpringApplication.run() 全流程概述",
        "输出：面试可讲的结论与取舍"
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "## 二、@SpringBootApplication 注解解析\n`@SpringBootApplication` 是一个组合注解，等价于同时使用 `@SpringBootConfiguration` + `@EnableAutoConfiguration` + `@ComponentScan`。\n### 2.1 注解结构\n```java\n@Target(ElementType.TYPE)\n@Retention(RetentionPolicy.RUNTIME)\n@Documented\n@Inherited\n@SpringBootConfiguration    // 1. 标识这是一个配置类（本质是@Configuration）\n@EnableAutoConfiguration    // 2. 开启自动装配（核心）\n@ComponentScan(             // 3. 包扫描\nexcludeFilters = {\n@Filter(type = FilterType.CUSTOM, classes = TypeExcludeFilter.class),\n@Filter(type = FilterType.CUSTOM, classes = AutoConfigurationExcludeFilter.class)\n}"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "可以按“四段式”回答 SpringBoot启动流程：先用一句话给定义和价值；再拆 集合与 Java 基础 里的关键机制；接着补充适用场景、性能或一致性等取舍；最后用项目排查、优化或设计经验收束。"
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "SpringBoot启动流程 的定义和边界",
        "SpringBoot启动流程 的关键机制",
        "SpringBoot启动流程 的典型场景和常见追问",
        "SpringBoot启动流程 在面试中的结构化表达方式"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "java.collections.topic-040-ba7e4b10.recall.1",
      "prompt": "请用自己的话解释 SpringBoot启动流程。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.collections.topic-040-ba7e4b10.recall.2",
      "prompt": "如果面试官追问 SpringBoot启动流程 的应用场景或常见误区，你会怎么回答？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "定义准确",
      "关键机制",
      "适用场景",
      "常见误区",
      "面试表达结构"
    ],
    "goodToHave": [
      "结合项目经验",
      "能做对比",
      "能说明取舍"
    ],
    "commonMistakes": [
      "只背概念不讲原因",
      "忽略边界条件",
      "缺少面试化表达"
    ],
    "scoreWeights": {
      "coverage": 40,
      "accuracy": 25,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-05-27"
}
