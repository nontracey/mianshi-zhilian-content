{
  "id": "java.spring.topic-059-a4e73804",
  "domain": "java",
  "category": "spring",
  "group": "spring",
  "title": "Gateway",
  "summary": "Spring Cloud Gateway",
  "tags": [
    "Java",
    "Spring 生态",
    "Gateway"
  ],
  "difficulty": 2,
  "estimatedMinutes": 20,
  "order": 590,
  "recommendWeight": 81,
  "learningCards": [
    {
      "type": "explain",
      "title": "知识全景",
      "content": "# Spring Cloud Gateway\n## 1. 网关作用\nSpring Cloud Gateway 是 Spring Cloud 生态的 API 网关，基于 WebFlux（Reactor Netty）构建，提供路由转发、负载均衡、限流、鉴权、跨域等统一入口功能。\n### 要点\n- **路由转发**：根据请求路径、Header、参数等条件将请求转发到后端服务\n- **负载均衡**：集成 LoadBalancer，对后端服务做负载均衡\n- **统一鉴权**：在网关层统一做身份认证和权限校验\n- **限流熔断**：在网关层做流量控制，保护后端服务\n- **跨域处理**：统一配置 CORS，避免每个服务单独配置\n- **日志监控**：统一记录请求日志，便于监控和排查\n- 基于 WebFlux 实现非阻塞、异步处理，性能远优于 Zuul 1.x\n### 微服务网关架构\n```\n客户端（浏览器/App）\n│\n▼\n┌──────────┐\n│  Gateway  │  ← 路由、鉴权、限流、跨域\n│  网关     │\n└────┬─────┘\n│\n┌────┼────────────┐\n│    │             │\n▼    ▼             ▼\n用户服务  订单服务  商品服务\n```\n### 示例/代码\n```xml\n<!-- pom.xml -->\n<dependency>\n<groupId>org.springframework.cloud</groupId>\n<artifactId>spring-cloud-starter-gateway</artifactId>\n</dependency>\n<dependency>\n<groupId>org.springframework.cloud</groupId>\n<artifactId>spring-cloud-starter-loadbalancer</artifactId>\n</dependency>\n```\n```java\n@SpringBootApplication\n@EnableDiscoveryClient\npublic class GatewayApplication {\n    public static void main(String[] args) {\n        SpringApplication.run(GatewayApplication.class, args);\n    }\n}\n```\n## 2. 路由配置\n### 要点\n- **路由（Route）**：网关的基本单元，由 ID、目标 URI、断言集合、过滤器集合组成\n- **断言（Predicate）**：匹配条件，决定请求是否匹配该路由\n- **过滤器（Filter）**：对请求/响应进行处理（如添加 Header、限流等）\n- 支持多种断言：Path、Method、Header、Query、Cookie、Host、After/Before/Between\n- 支持配置文件和 Java DSL 两种方式配置路由\n### 示例/代码\n```yaml\n# application.yml 路由配置\nspring:\ncloud:\ngateway:\nroutes:\n- id: user-service-route\nuri: lb://user-service   # lb:// 表示使用负载均衡\npredicates:\n- Path=/api/users/**\nfilters:\n- StripPrefix=1        # 去掉 /api 前缀\n- id: order-service-route\nuri: lb://order-service\npredicates:\n- Path=/api/orders/**\n- Method=GET,POST\nfilters:\n- StripPrefix=1\n- AddRequestHeader=X-Request-Source, gateway\n- id: product-service-route\nuri: lb://product-service\npredicates:\n- Path=/api/products/**\n- Header=X-Token, \\d+   # Header 匹配正则\n- Query=name            # 必须包含 name 参数\nfilters:\n- StripPrefix=1\n```\n```java\n// Java DSL 方式配置路由\n@Configuration\npublic class GatewayRouteConfig {\n    @Bean\n    public RouteLocator customRoutes(RouteLocatorBuilder builder) {\n        return builder.routes()\n        .route(\"user-route\", r -> r\n        .path(\"/api/users/**\")\n        .filters(f -> f\n        .stripPrefix(1)\n        .addRequestHeader(\"X-Source\", \"gateway\"))\n        .uri(\"lb://user-service\"))\n        .route(\"order-route\", r -> r\n        .path(\"/api/orders/**\")\n        .and()\n        .method(HttpMethod.GET, HttpMethod.POST)\n        .uri(\"lb://order-service\"))\n        .build();\n    }\n}\n```\n## 3. 过滤器\n### 要点\n- **GlobalFilter**：全局过滤器，对所有路由生效（如鉴权、日志）\n- **GatewayFilter**：局部过滤器，只对指定路由生效\n- **执行顺序**：通过 Order 值排序，值越小优先级越高\n- 过滤器分 Pre（请求前）和 Post（响应后）两种\n- 内置过滤器：AddRequestHeader、StripPrefix、Retry、Hystrix 等\n- 自定义过滤器实现 GlobalFilter 或 GatewayFilter 接口\n### 过滤器执行链\n```\n请求进入 → Pre过滤器链（按Order排序执行）→ 后端服务 → Post过滤器链（逆序执行）→ 响应返回\n```\n### 示例/代码\n```java\n// 自定义全局过滤器 - 鉴权\n@Component\n@Order(-1)  // 优先级最高\npublic class AuthGlobalFilter implements GlobalFilter, Ordered {\n    private static final Logger log = LoggerFactory.getLogger(AuthGlobalFilter.class);\n    @Override\n    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {\n        ServerHttpRequest request = exchange.getRequest();\n        String token = request.getHeaders().getFirst(\"Authorization\");\n        // 白名单路径放行\n        String path = request.getURI().getPath();\n        if (path.contains(\"/login\") || path.contains(\"/register\")) {\n            return chain.filter(exchange);\n        }\n    // 校验 Token\n    if (StringUtils.isBlank(token) || !token.startsWith(\"Bearer \")) {\n        log.warn(\"请求未携带有效Token: {}\", path);\n        exchange.getResponse().setStatusCode(HttpStatus.UNAUTHORIZED);\n        return exchange.getResponse().setComplete();\n    }\n// 解析 Token 并将用户信息传递给下游\nString userId = parseToken(token);\nServerHttpRequest newRequest = request.mutate()\n.header(\"X-User-Id\", userId)\n.build();\nreturn chain.filter(exchange.mutate().request(newRequest).build());\n}\n@Override\npublic int getOrder() {\n    return -1;\n}\nprivate String parseToken(String token) {\n    // JWT 解析逻辑\n    return \"12345\";\n}\n}\n// 自定义全局过滤器 - 请求日志\n@Component\n@Order(0)\npublic class AccessLogGlobalFilter implements GlobalFilter, Ordered {\n    private static final Logger log = LoggerFactory.getLogger(AccessLogGlobalFilter.class);\n    @Override\n    public Mono<Void> filter(ServerWebExchange exchange, GatewayFilterChain chain) {\n        long startTime = System.currentTimeMillis();\n        ServerHttpRequest request = exchange.getRequest();\n        return chain.filter(exchange).then(Mono.fromRunnable(() -> {\n            long duration = System.currentTimeMillis() - startTime;\n            HttpStatus statusCode = exchange.getResponse().getStatusCode();\n            log.info(\"[Gateway] {} {} -> {} ({}ms)\",\n            request.getMethod(), request.getURI().getPath(),\n            statusCode, duration);\n        }));\n}\n@Override\npublic int getOrder() { return 0; }\n}\n```\n```yaml\n# 内置 GatewayFilter 配置示例\nspring:\ncloud:\ngateway:\nroutes:\n- id: retry-route\nuri: lb://user-service\npredicates:\n- Path=/api/users/**\nfilters:\n- StripPrefix=1\n- name: Retry\nargs:\nretries: 3\nstatuses: BAD_GATEWAY,SERVICE_UNAVAILABLE\nbackoff:\nfirstBackoff: 100ms\nmaxBackoff: 500ms\n```\n## 4. 限流\n### 要点\n- Gateway 内置 **RequestRateLimiter** 过滤器，基于 Redis + Lua 脚本实现\n- 支持基于 **IP**、**用户**、**URI** 等维度限流\n- 常用限流算法：令牌桶（Token Bucket）、漏桶（Leaky Bucket）\n- 也可以通过自定义过滤器集成 Sentinel 限流\n- 限流策略：QPS 限制、并发数限制\n### 示例/代码\n```yaml\n# 基于 Redis 的限流配置\nspring:\ncloud:\ngateway:\nroutes:\n- id: rate-limit-route\nuri: lb://user-service\npredicates:\n- Path=/api/users/**\nfilters:\n- StripPrefix=1\n- name: RequestRateLimiter\nargs:\nredis-rate-limiter.replenishRate: 10    # 每秒填充令牌数\nredis-rate-limiter.burstCapacity: 20     # 令牌桶容量\nredis-rate-limiter.requestedTokens: 1    # 每次请求消耗令牌数\nkey-resolver: \"#{@ipKeyResolver}\"        # 限流key解析器\nredis:\nhost: localhost\nport: 6379\n```\n```java\n// 自定义限流 Key 解析器\n@Configuration\npublic class RateLimiterConfig {\n    // 基于 IP 限流\n    @Bean\n    public KeyResolver ipKeyResolver() {\n        return exchange -> Mono.just(\n        exchange.getRequest().getRemoteAddress().getAddress().getHostAddress());\n    }\n// 基于用户 ID 限流\n@Bean\npublic KeyResolver userKeyResolver() {\n    return exchange -> Mono.just(\n    exchange.getRequest().getHeaders().getFirst(\"X-User-Id\"));\n}\n// 基于 URI 限流\n@Bean\npublic KeyResolver uriKeyResolver() {\n    return exchange -> Mono.just(\n    exchange.getRequest().getPath().value());\n}\n}\n```\n## 5. 跨域配置\n### 要点\n- 前后端分离架构中，浏览器同源策略会阻止跨域请求\n- 网关层统一配置 CORS，避免每个微服务单独配置\n- 配置项：allowedOrigins、allowedMethods、allowedHeaders、allowCredentials、maxAge\n### 示例/代码\n```yaml\n# 配置文件方式\nspring:\ncloud:\ngateway:\nglobalcors:\ncors-configurations:\n'[/**]':\nallowed-origins: \"http://localhost:3000\"\nallowed-methods: \"*\"\nallowed-headers: \"*\"\nallow-credentials: true\nmax-age: 3600\n```\n```java\n// Java 配置方式\n@Configuration\npublic class CorsConfig {\n    @Bean\n    public CorsWebFilter corsWebFilter() {\n        CorsConfiguration config = new CorsConfiguration();\n        config.addAllowedOrigin(\"http://localhost:3000\");\n        config.addAllowedMethod(\"*\");\n        config.addAllowedHeader(\"*\");\n        config.setAllowCredentials(true);\n        config.setMaxAge(3600L);\n        UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();\n        source.registerCorsConfiguration(\"/**\", config);\n        return new CorsWebFilter(source);\n    }\n}\n```\n## 6. Gateway与Zuul对比\n### 要点\n## 7. 面试高频问题\n### Q1：Spring Cloud Gateway 的核心概念有哪些？\n**答**：三个核心概念：(1) Route（路由）：网关基本单元，由 ID、URI、断言集合、过滤器集合组成；(2) Predicate（断言）：匹配条件，基于 Java 8 Function Predicate，支持 Path/Method/Header/Query 等多种匹配方式；(3) Filter（过滤器）：对请求/响应进行处理，分为 GlobalFilter（全局）和 GatewayFilter（局部）。\n### Q2：Gateway 的过滤器执行顺序是怎样的？\n**答**：过滤器通过 Order 值排序执行。请求进入后，先执行所有 Pre 过滤器（按 Order 升序），再转发到后端服务，响应返回后执行 Post 过滤器（按 Order 降序）。自定义 GlobalFilter 通过实现 Ordered 接口或 @Order 注解指定顺序。Order 值越小，优先级越高。\n### Q3：Gateway 和 Zuul 有什么区别？\n**答**：(1) 编程模型：Gateway 基于 WebFlux 非阻塞，Zuul 1.x 基于 Servlet 阻塞；(2) 性能：Gateway 远优于 Zuul 1.x；(3) 功能：Gateway 内置限流、重试等过滤器，Zuul 需自行实现；(4) 维护：Zuul 已停止维护，Gateway 是 Spring Cloud 官方推荐。\n### Q4：Gateway 如何实现限流？\n**答**：Gateway 内置 RequestRateLimiter 过滤器，基于 Redis + Lua 脚本实现令牌桶算法。通过 key-resolver 指定限流维度（IP/用户/URI），通过 replenishRate 和 burstCapacity 控制速率。也可以集成 Sentinel 实现更丰富的限流策略（如热点参数限流、系统规则等）。\n### Q5：Gateway 如何实现统一鉴权？\n**答**：通过自定义 GlobalFilter 实现。在 Pre 阶段拦截请求，从 Header 中获取 Token，校验 Token 合法性。校验通过则解析用户信息并通过 Header 传递给下游服务；校验失败则直接返回 401。设置 Order 为较高优先级（如 -1），确保鉴权过滤器先于其他过滤器执行。"
    },
    {
      "type": "explain",
      "title": "关键机制拆解",
      "content": "先把 Gateway 当成一个可讲清楚的系统来理解：\n\n1. Spring Cloud Gateway\n2. Spring Cloud Gateway 是 Spring Cloud 生态的 API 网关，基于 WebFlux（Reactor Netty）构建，提供路由转发、负载均衡、限流、鉴权、跨域等统一入口功能。\n3. **路由转发**：根据请求路径、Header、参数等条件将请求转发到后端服务\n4. **负载均衡**：集成 LoadBalancer，对后端服务做负载均衡\n5. **统一鉴权**：在网关层统一做身份认证和权限校验\n\n学习时重点关注“为什么需要它、它如何工作、什么时候会出问题、面试时怎么结构化表达”。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | Gateway | 相邻知识/常见误解 |\n| --- | --- | --- |\n| 解决的问题 | 说明核心目标和使用场景 | 只背定义，忽略为什么需要它 |\n| 核心机制 | 拆成关键角色、流程、边界条件 | 把实现细节和概念边界混在一起 |\n| 面试表达 | 先结论，再机制，再场景和取舍 | 直接罗列术语，缺少层次 |\n| 关联分类 | Spring 生态 | 需要能和同类知识做横向比较 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "Gateway 的学习路径图，用于替代原始 Markdown 中容易变形的 ASCII 图。",
      "fallback": "建议用“输入/触发 → 核心流程 → 关键状态 → 输出/风险”的流程图复述 Gateway，并标出它在 Spring 生态 中的位置。",
      "items": [
        "定位：Spring 生态",
        "输入：问题场景或触发条件",
        "机制：**路由转发**：根据请求路径、Header、参数等条件将请求转发到后端服务",
        "输出：面试可讲的结论与取舍"
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "@SpringBootApplication\n@EnableDiscoveryClient\npublic class GatewayApplication {\n    public static void main(String[] args) {\n        SpringApplication.run(GatewayApplication.class, args);\n    }\n}\n```\n---\n## 2. 路由配置\n### 要点\n- **路由（Route）**：网关的基本单元，由 ID、目标 URI、断言集合、过滤器集合组成\n- **断言（Predicate）**：匹配条件，决定请求是否匹配该路由\n- **过滤器（Filter）**：对请求/响应进行处理（如添加 Header、限流等）"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "可以按“四段式”回答 Gateway：先用一句话给定义和价值；再拆 Spring 生态 里的关键机制；接着补充适用场景、性能或一致性等取舍；最后用项目排查、优化或设计经验收束。"
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "Gateway 的定义和边界",
        "Gateway 的关键机制",
        "Gateway 的典型场景和常见追问",
        "Gateway 在面试中的结构化表达方式"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "java.spring.topic-059-a4e73804.recall.1",
      "prompt": "请用自己的话解释 Gateway。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.spring.topic-059-a4e73804.recall.2",
      "prompt": "如果面试官追问 Gateway 的应用场景或常见误区，你会怎么回答？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "定义准确",
      "关键机制",
      "适用场景",
      "常见误区",
      "面试表达结构"
    ],
    "goodToHave": [
      "结合项目经验",
      "能做对比",
      "能说明取舍"
    ],
    "commonMistakes": [
      "只背概念不讲原因",
      "忽略边界条件",
      "缺少面试化表达"
    ],
    "scoreWeights": {
      "coverage": 40,
      "accuracy": 25,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-05-27"
}
