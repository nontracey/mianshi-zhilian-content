{
  "id": "java.database.topic-066-d9e7b897",
  "domain": "java",
  "category": "database",
  "group": "database",
  "title": "事务机制",
  "summary": "聚焦数据库事务的 ACID、隔离级别、锁边界和异常现象；MVCC 的 Undo Log/ReadView 细节放在 MySQL MVCC 专题中展开。",
  "tags": [
    "Java",
    "数据库",
    "事务机制"
  ],
  "difficulty": 4,
  "estimatedMinutes": 10,
  "order": 20,
  "recommendWeight": 85,
  "learningCards": [
    {
      "type": "explain",
      "title": "事务机制：ACID特性",
      "content": "## 1. ACID特性\n事务是数据库操作的最小逻辑单元，具有 ACID 四个特性，保证数据的可靠性和一致性。\n### 要点\n- **原子性（Atomicity）**：事务中的操作要么全部成功，要么全部回滚，不存在中间状态。通过 undo log 实现\n- **一致性（Consistency）**：事务执行前后，数据库从一个一致状态转换到另一个一致状态。是事务的最终目标\n- **隔离性（Isolation）**：多个并发事务之间互不干扰，如同串行执行。通过 MVCC + 锁机制实现\n- **持久性（Durability）**：事务一旦提交，数据变更永久保存，即使系统故障也不丢失。通过 redo log 实现\n### ACID 实现机制\n```\n原子性 ← undo log（回滚日志）\n事务回滚时，根据 undo log 恢复到事务前的状态\n一致性 ← 是目标，由 A + I + D 共同保证\n隔离性 ← MVCC（多版本并发控制）+ 锁\n读操作通过 MVCC 实现无锁读\n写操作通过锁机制保证互斥\n持久性 ← redo log（重做日志）\n事务提交时先写 redo log（WAL），即使宕机也能通过 redo log 恢复\n```\n### 示例/代码\n```sql\n-- 事务基本操作\nSTART TRANSACTION;  -- 或 BEGIN\nUPDATE account SET balance = balance - 100 WHERE user_id = 1;\nUPDATE account SET balance = balance + 100 WHERE user_id = 2;\n-- 如果成功\nCOMMIT;\n-- 如果失败\nROLLBACK;\n-- 设置事务隔离级别\nSET TRANSACTION ISOLATION LEVEL READ COMMITTED;\n```"
    },
    {
      "type": "explain",
      "title": "事务机制：隔离级别",
      "content": "## 2. 隔离级别\nSQL 标准定义了四种隔离级别，隔离级别越高，并发性能越低。\n### 要点\n> *注：MySQL InnoDB 在 RR 级别通过 MVCC + Next-Key Lock 在很大程度上解决了幻读问题\n### 各隔离级别问题说明\n```\n脏读（Dirty Read）：\n事务A读到了事务B尚未提交的数据，如果B回滚，A读到的就是无效数据\n不可重复读（Non-Repeatable Read）：\n事务A内两次读取同一行数据，中间事务B修改并提交了该行，导致两次读取结果不同\n幻读（Phantom Read）：\n事务A两次查询同一范围的数据，中间事务B插入了新行，导致第二次查询多了行\n```\n### 示例/代码\n```sql\n-- 脏读示例（READ UNCOMMITTED）\n-- 事务A\nSET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;\nSTART TRANSACTION;\nSELECT balance FROM account WHERE id = 1;  -- 读到 1000\n-- 此时事务B修改 balance 为 800 但未提交\nSELECT balance FROM account WHERE id = 1;  -- 读到 800（脏数据！）\nCOMMIT;\n-- 不可重复读示例（READ COMMITTED）\n-- 事务A\nSET TRANSACTION ISOLATION LEVEL READ COMMITTED;\nSTART TRANSACTION;\nSELECT balance FROM account WHERE id = 1;  -- 读到 1000\n-- 此时事务B修改 balance 为 800 并提交\nSELECT balance FROM account WHERE id = 1;  -- 读到 800（不可重复读！）\nCOMMIT;\n-- 可重复读示例（REPEATABLE READ）- MySQL默认\nSET TRANSACTION ISOLATION LEVEL REPEATABLE READ;\nSTART TRANSACTION;\nSELECT balance FROM account WHERE id = 1;  -- 读到 1000\n-- 此时事务B修改 balance 为 800 并提交\nSELECT balance FROM account WHERE id = 1;  -- 仍然读到 1000（可重复读）\nCOMMIT;\n```"
    },
    {
      "type": "explain",
      "title": "事务机制：隔离级别、锁与 MVCC 边界",
      "content": "事务机制这一题重点先讲 ACID、隔离级别和并发现象：脏读、不可重复读、幻读分别对应不同隔离级别下的读一致性问题。InnoDB 通过锁和 MVCC 共同处理并发读写：普通一致性读通常走快照，更新、加锁查询和唯一性检查会走当前读并加锁。这里需要把边界说清：事务 topic 只要求知道 MVCC 参与实现隔离，Undo Log 版本链、ReadView 可见性判断和 RC/RR 差异应到「MySQL MVCC」专题展开。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | READ UNCOMMITTED | READ COMMITTED | REPEATABLE READ | SERIALIZABLE |\n| --- | --- | --- | --- | --- |\n| 脏读 | 有 | 无 | 无 | 无 |\n| 不可重复读 | 有 | 有 | 无 | 无 |\n| 幻读 | 有 | 有 | 基本解决（MVCC+NKL） | 无 |\n| 实现方式 | 直接读最新数据 | 每次SELECT创建新ReadView | 事务首次SELECT创建ReadView | 加锁串行执行 |\n| 并发性能 | 最高 | 高 | 高（MySQL默认） | 最低 |\n| 适用场景 | 几乎不用 | Oracle默认，多数场景 | MySQL默认，推荐 | 强一致性要求 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "事务隔离级别与并发现象、ACID 实现机制的对应关系。",
      "fallback": "脏读、不可重复读、幻读随隔离级别升高被逐步消除；ACID 由 undo log（原子性）、redo log（持久性）、MVCC+锁（隔离性）共同保证。",
      "items": [
        "脏读：读到其他事务未提交的修改 → RU 有，RC 及以上消除",
        "不可重复读：两次读同一行结果不同 → RC 有，RR 及以上消除",
        "幻读：两次范围查询行数不同 → RR 用 Next-Key Lock 基本解决，SERIALIZABLE 彻底解决",
        "隔离性实现：一致性读走 MVCC 快照（无锁），更新/加锁读/唯一性检查走当前读加锁",
        "原子性 ← undo log 回滚；持久性 ← redo log（WAL，先写日志再提交）",
        "MVCC 的版本链、ReadView、RC/RR 创建时机等细节见《MySQL MVCC》专题"
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "原子性 ← undo log（回滚日志）\n  事务回滚时，根据 undo log 恢复到事务前的状态\n一致性 ← 是目标，由 A + I + D 共同保证\n隔离性 ← MVCC（多版本并发控制）+ 锁\n  读操作通过 MVCC 实现无锁读\n  写操作通过锁机制保证互斥\n持久性 ← redo log（重做日志）\n  事务提交时先写 redo log（WAL），即使宕机也能通过 redo log 恢复",
      "language": "java"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "**Q1：MySQL的四种隔离级别分别解决了什么问题？**\n\nA：从低到高依次是READ UNCOMMITTED、READ COMMITTED、REPEATABLE READ、SERIALIZABLE。READ UNCOMMITTED最低，存在脏读、不可重复读、幻读。READ COMMITTED解决了脏读，但存在不可重复读。REPEATABLE READ是MySQL默认级别，通过MVCC解决了不可重复读，配合Next-Key Lock基本解决幻读。SERIALIZABLE最高，通过加锁串行执行解决所有问题，但并发性能最差。生产环境推荐用MySQL默认的REPEATABLE READ。\n\n**Q2：MVCC 在事务里扮演什么角色？**\n\nA：隔离性靠 MVCC + 锁实现——一致性读走 MVCC 快照（无锁），更新和加锁读走当前读加锁。MVCC 的具体实现（DB_TRX_ID/DB_ROLL_PTR 隐藏列、undo log 版本链、ReadView 可见性判断、RC 每次 SELECT 建快照而 RR 复用首次快照）属于《MySQL MVCC》专题，这里只要说清 MVCC 参与隔离实现即可。\n\n**Q3：redo log和undo log有什么区别？**\n\nA：redo log保证持久性（Durability），undo log保证原子性（Atomicity）。redo log记录的是物理修改（某页某偏移写入了什么数据），采用WAL（Write-Ahead Logging）机制，事务提交前先写redo log，即使宕机也能通过redo log恢复已提交的数据。undo log记录的是逻辑修改的逆操作（如INSERT对应DELETE），用于事务回滚时恢复数据，同时也支持MVCC的版本链。两者配合：redo log保证已提交事务不丢失，undo log保证未提交事务可回滚。",
      "followUpQuestions": [
        {
          "question": "事务机制和 MVCC 两个 topic 的回答边界怎么分？",
          "answer": "事务机制先讲 ACID、隔离级别、并发现象和锁边界；MVCC 专题再展开 Undo Log 版本链、ReadView 可见性和 RC/RR 创建时机。面试中先用事务建立大框架，再按追问切到 MVCC 细节。"
        },
        {
          "question": "为什么有了 MVCC，更新语句仍然会加锁？",
          "answer": "MVCC 主要服务一致性读，让读写尽量不互相阻塞；更新、删除、唯一性检查和 select for update 属于当前读，必须读取最新版本并保护即将修改的记录或范围，所以仍需要行锁、间隙锁等机制。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "ACID四个特性各自由什么机制保证（undo log/redo log/MVCC+锁）",
        "四种隔离级别各自解决和存在的问题（脏读/不可重复读/幻读）",
        "隔离性由 MVCC（一致性读）+ 锁（当前读）共同实现",
        "RC 与 RR 在并发现象上的差异，以及为何 RR 能可重复读",
        "redo log的WAL机制和崩溃恢复原理",
        "undo log的回滚功能和MVCC版本链支持",
        "MySQL默认RR级别下Next-Key Lock如何解决幻读"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "java.database.topic-066-d9e7b897.recall.1",
      "prompt": "事务的 ACID 四个特性分别解决什么问题？隔离性为什么最容易被追问？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.database.topic-066-d9e7b897.recall.2",
      "prompt": "脏读、不可重复读、幻读分别是什么？MySQL 常见隔离级别如何处理它们？",
      "mode": "text",
      "expectedMinutes": 4,
      "difficulty": 3
    },
    {
      "id": "java.database.topic-066-d9e7b897.recall.3",
      "prompt": "快照读和当前读有什么区别？为什么 MVCC 细节要单独展开？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "能准确解释 ACID 四个特性、隔离级别关系和事务一致性边界",
      "说清要点和ACID 实现机制的关键机制",
      "能说清 事务 的核心机制和关键流程",
      "ACID四个特性各自由什么机制保证（undo log/redo log/MVCC+锁）"
    ],
    "goodToHave": [
      "能用 EXPLAIN 分析 事务 相关查询的执行计划",
      "能说出 事务 在不同存储引擎和数据规模下的表现差异"
    ],
    "commonMistakes": [
      "不清楚MVCC的Read View机制",
      "混淆RC和RR隔离级别下MVCC的行为差异",
      "不了解undo log版本链的作用"
    ],
    "scoreWeights": {
      "coverage": 35,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-06-11",
  "status": "production",
  "prerequisites": [],
  "interviewFrequency": "high",
  "interviewerFocus": "考察是否能围绕「事务机制」说清能准确解释 ACID 四个特性、隔离级别关系和事务一致性边界、说清要点和ACID 实现机制的关键机制，并放到Java 工程场景中解释关键流程、边界条件和出错后的处理思路。"
}
