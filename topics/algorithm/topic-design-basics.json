{
  "id": "algorithm.design.topic-design-basics",
  "domain": "algorithm",
  "category": "design",
  "group": "design",
  "title": "设计题基础",
  "summary": "设计题考察数据结构的综合运用能力，核心是 HashMap + 双向链表、Trie 树等经典组合。",
  "tags": [
    "算法与数据结构",
    "设计题",
    "LRU",
    "LFU",
    "Trie"
  ],
  "difficulty": 4,
  "estimatedMinutes": 20,
  "order": 10,
  "recommendWeight": 90,
  "learningCards": [
    {
      "type": "explain",
      "title": "核心概念",
      "content": "# 设计题基础\n\n## 1. 设计题考察重点\n设计题考察数据结构的综合运用能力，核心是：\n- **HashMap + 双向链表**：LRU/LFU 缓存\n- **Trie 树**：前缀树、自动补全\n- **栈/队列**：最小栈、最大频率栈\n\n## 2. LRU 缓存核心思想\n### 要点\n- 使用 HashMap 存储 key → Node 映射，O(1) 查找\n- 使用双向链表维护访问顺序，最近访问的放头部\n- get/put 操作都需要将节点移到头部\n- 容量满时删除尾部节点\n\n### 数据结构\n```java\nclass Node {\n    int key, val;\n    Node prev, next;\n    Node(int k, int v) { key = k; val = v; }\n}\n\nclass LRUCache {\n    int capacity;\n    Map<Integer, Node> map;\n    Node head, tail; // 哨兵节点\n}\n```\n\n## 3. LFU 缓存核心思想\n### 要点\n- 在 LRU 基础上增加频率维度\n- 使用 freqMap 存储频率 → 双向链表映射\n- 删除时优先删除频率最低的，同频率删除最久未使用的\n- 每次访问更新频率\n\n## 4. Trie 树核心思想\n### 要点\n- 每个节点包含 26 个子节点（字母表）\n- 插入：从根节点开始，按字符逐层创建节点\n- 查找：从根节点开始，按字符逐层查找\n- 前缀匹配：找到前缀最后一个节点，遍历其子树"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 数据结构 | 时间复杂度 | 空间复杂度 | 适用场景 |\n| --- | --- | --- | --- |\n| LRU 缓存 | get/put O(1) | O(capacity) | 缓存淘汰、页面置换 |\n| LFU 缓存 | get/put O(1) | O(capacity) | 频率敏感的缓存 |\n| Trie 树 | insert/search O(L) | O(N*L) | 前缀匹配、自动补全 |\n| 最小栈 | push/pop/getMin O(1) | O(n) | 需要快速获取最小值 |\n\n**关键点**：\n- LRU 核心是 HashMap + 双向链表\n- LFU 核心是 HashMap + 频率链表\n- Trie 核心是多叉树结构"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "**Q: 设计一个 LRU 缓存？**\n\nA: LRU 缓存需要支持 O(1) 的 get 和 put 操作。核心思路：\n1. 使用 HashMap 存储 key → Node 映射，实现 O(1) 查找\n2. 使用双向链表维护访问顺序，最近访问的放头部\n3. get 操作：从 HashMap 查找，找到后移到链表头部\n4. put 操作：如果 key 存在，更新值并移到头部；如果不存在，创建新节点放头部，容量满时删除尾部节点\n\n关键点：使用哨兵节点（head 和 tail）简化边界判断。\n\n**Q: LRU 和 LFU 的区别？**\n\nA: LRU（最近最少使用）淘汰最久未访问的；LFU（最不经常使用）淘汰访问频率最低的。LFU 在 LRU 基础上增加了频率维度，实现更复杂，需要维护频率映射。LFU 更适合访问模式稳定的场景，LRU 更适合时间局部性强的场景。\n\n**Q: Trie 树的应用场景？**\n\nA: Trie 树主要用于：1) 前缀匹配，如搜索提示、自动补全；2) 字符串统计，如词频统计；3) 字符串去重。优点是前缀查询 O(L)，缺点是空间开销大。可以使用压缩 Trie 或 Patricia 树优化空间。"
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能画出 LRU 缓存的数据结构图",
        "能解释 HashMap + 双向链表的组合使用",
        "能写出 LRU 缓存的核心代码",
        "能说明 LFU 缓存的频率管理机制",
        "能解释 Trie 树的插入和查找流程",
        "能分析各数据结构的时间空间复杂度"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "algorithm.design.topic-design-basics.recall.1",
      "prompt": "请画出 LRU 缓存的数据结构图，并解释 get/put 操作的流程",
      "mode": "text",
      "expectedMinutes": 5,
      "difficulty": 4
    },
    {
      "id": "algorithm.design.topic-design-basics.recall.2",
      "prompt": "请写出 LRU 缓存的核心代码框架（Node 类 + LRUCache 类）",
      "mode": "code",
      "expectedMinutes": 10,
      "difficulty": 4
    }
  ],
  "rubric": {
    "mustHave": [
      "理解 HashMap + 双向链表的组合",
      "能画出 LRU 缓存的数据结构图",
      "能写出核心代码框架"
    ],
    "goodToHave": [
      "理解 LFU 的频率管理机制",
      "理解 Trie 树的实现",
      "能分析时间空间复杂度"
    ],
    "commonMistakes": [
      "忘记使用哨兵节点导致边界判断复杂",
      "put 操作忘记更新已存在 key 的值",
      "删除节点时忘记同时更新 HashMap"
    ],
    "scoreWeights": {
      "coverage": 30,
      "accuracy": 35,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-05-29",
  "status": "production",
  "interviewFrequency": "high",
  "interviewerFocus": "考察数据结构综合运用能力，能否设计出 O(1) 复杂度的缓存系统"
}
