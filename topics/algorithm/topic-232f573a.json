{
  "id": "algorithm.tree-graph.topic-232f573a",
  "domain": "algorithm",
  "category": "tree-graph",
  "group": "tree-graph",
  "title": "拓扑排序",
  "summary": "理解有向无环图(DAG)的拓扑排序算法，掌握 Kahn 算法和 DFS 两种实现方式",
  "tags": [
    "algorithm",
    "tree-graph",
    "拓扑排序"
  ],
  "difficulty": 3,
  "estimatedMinutes": 20,
  "order": 999,
  "recommendWeight": 70,
  "status": "production",
  "learningCards": [
    {
      "type": "explain",
      "title": "知识全景",
      "content": "# 拓扑排序\n\n## 一、什么是拓扑排序\n对有向无环图(DAG)的所有顶点进行线性排序，使得对于每条边 (u, v)，u 在排序中出现在 v 的前面。\n\n## 二、Kahn 算法（BFS）\n**思路**：不断移除入度为 0 的节点\n```java\nList<Integer> topologicalSort(List<List<Integer>> graph, int n) {\n    int[] inDegree = new int[n];\n    for (int u = 0; u < n; u++)\n        for (int v : graph.get(u)) inDegree[v]++;\n    \n    Queue<Integer> queue = new LinkedList<>();\n    for (int i = 0; i < n; i++)\n        if (inDegree[i] == 0) queue.offer(i);\n    \n    List<Integer> result = new ArrayList<>();\n    while (!queue.isEmpty()) {\n        int node = queue.poll();\n        result.add(node);\n        for (int neighbor : graph.get(node)) {\n            if (--inDegree[neighbor] == 0) queue.offer(neighbor);\n        }\n    }\n    return result.size() == n ? result : new ArrayList<>(); // 空表示有环\n}\n```\n\n## 三、DFS 实现\n**思路**：后序遍历的结果反转\n```java\nList<Integer> topoSort = new ArrayList<>();\nboolean[] visited = new boolean[n];\nboolean[] inStack = new boolean[n];\n\nboolean dfs(int node) {\n    if (inStack[node]) return false; // 有环\n    if (visited[node]) return true;\n    visited[node] = true;\n    inStack[node] = true;\n    for (int neighbor : graph.get(node)) {\n        if (!dfs(neighbor)) return false;\n    }\n    inStack[node] = false;\n    topoSort.add(node);\n    return true;\n}\nCollections.reverse(topoSort);\n```\n\n## 四、应用场景\n- 课程安排（判断能否完成所有课程）\n- 编译顺序（依赖关系）\n- 任务调度"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | Kahn 算法 | DFS 实现 |\n| --- | --- | --- |\n| 思路 | 移除入度为0的节点 | 后序遍历反转 |\n| 数据结构 | 队列 + 入度数组 | 递归栈 |\n| 环检测 | result.size() < n | 栈内节点重复访问 |\n| 适用场景 | 需要同时检测环 | 代码更简洁 |"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "**Q1: 什么是拓扑排序？什么时候需要拓扑排序？**\n\n拓扑排序是对有向无环图(DAG)的线性排序，使得所有边从前指向后。典型场景：课程安排（先修课→后续课）、编译依赖（库A依赖库B）、任务调度。\n\n**Q2: 如何检测图中是否有环？**\n\nKahn 算法：如果排序结果的节点数不等于总节点数，说明有环（因为环上的节点入度永远不会变为0）。DFS：如果在当前递归栈中再次访问到同一节点，说明有环。\n\n**Q3: 拓扑排序的结果唯一吗？**\n\n不唯一。如果有多个入度为0的节点，选择不同节点会导致不同排序结果。但如果每次都选同一个（如编号最小的），结果就是唯一的。",
      "followUpQuestions": [
        {
          "question": "能深入说说拓扑排序的核心实现原理吗？",
          "answer": "从拓扑排序的底层实现来看，关键在于理解其核心数据结构和算法选择。建议通过画图和写代码的方式深入理解。"
        },
        {
          "question": "在实际项目中，拓扑排序有哪些应用场景？",
          "answer": "拓扑排序在实际项目中的应用需要结合具体业务场景，关键是理解其适用边界和性能特征。"
        },
        {
          "question": "使用拓扑排序时有哪些常见的坑？",
          "answer": "最大的坑是只背概念不理解原理。建议通过debug源码、写demo验证的方式深入理解。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能解释拓扑排序的定义和适用条件（DAG）",
        "能手写 Kahn 算法的实现",
        "能用拓扑排序检测有向图中是否有环",
        "能说出拓扑排序的实际应用场景"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "algorithm.tree-graph.topic-232f573a.recall.1",
      "prompt": "请用自己的话解释拓扑排序的核心概念。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "能解释拓扑排序的定义和适用条件（DAG）",
      "能手写 Kahn 算法的实现",
      "能用拓扑排序检测有向图中是否有环"
    ],
    "goodToHave": [
      "能说出拓扑排序的实际应用场景"
    ],
    "commonMistakes": [
      "对有环图执行拓扑排序没有检测到环",
      "忘记初始化入度数组",
      "混淆拓扑排序和 DFS 后序遍历"
    ],
    "scoreWeights": {
      "coverage": 30,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 20
    }
  },
  "updatedAt": "2026-05-29"
}
