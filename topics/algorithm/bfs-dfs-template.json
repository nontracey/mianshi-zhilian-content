{
  "id": "algorithm.graph.bfs-dfs-template",
  "domain": "algorithm",
  "category": "graph",
  "group": "graph",
  "title": "BFS 与 DFS 模板",
  "summary": "BFS 和 DFS 是图和树遍历的两种基本策略，BFS 用队列逐层扩展适合最短路径，DFS 用栈或递归深入探索适合路径枚举和连通性判断。",
  "tags": [
    "BFS",
    "DFS",
    "图",
    "树",
    "模板"
  ],
  "difficulty": 2,
  "estimatedMinutes": 20,
  "order": 20,
  "recommendWeight": 85,
  "learningCards": [
    {
      "type": "explain",
      "title": "核心区别",
      "content": "BFS（广度优先搜索）用队列逐层遍历，天然求最短路径；DFS（深度优先搜索）用栈或递归沿一条路走到底再回溯。BFS 空间 O(宽度)，DFS 空间 O(深度)。网格问题中 BFS 适合求最短距离，DFS 适合求连通分量。"
    },
    {
      "type": "code",
      "title": "BFS 模板",
      "content": "void bfs(int[][] grid, int sr, int sc) {\n    Queue<int[]> q = new LinkedList<>();\n    q.offer(new int[]{sr, sc});\n    boolean[][] visited = new boolean[grid.length][grid[0].length];\n    visited[sr][sc] = true;\n    int[][] dirs = {{0,1},{0,-1},{1,0},{-1,0}};\n    while (!q.isEmpty()) {\n        int[] cur = q.poll();\n        for (int[] d : dirs) {\n            int nr = cur[0]+d[0], nc = cur[1]+d[1];\n            if (nr>=0 && nr<grid.length && nc>=0 && nc<grid[0].length\n                && !visited[nr][nc] && grid[nr][nc]==1) {\n                visited[nr][nc] = true;\n                q.offer(new int[]{nr, nc});\n            }\n        }\n    }\n}",
      "language": "java"
    },
    {
      "type": "code",
      "title": "DFS 模板",
      "content": "void dfs(int[][] grid, int r, int c) {\n    if (r<0 || r>=grid.length || c<0 || c>=grid[0].length\n        || grid[r][c] == 0) return;\n    grid[r][c] = 0; // 标记访问\n    dfs(grid, r+1, c);\n    dfs(grid, r-1, c);\n    dfs(grid, r, c+1);\n    dfs(grid, r, c-1);\n}",
      "language": "java"
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "BFS 用队列逐层遍历的机制",
        "DFS 递归和迭代两种实现方式",
        "visited 数组的作用和去重策略",
        "网格问题中方向数组的使用"
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "回答 BFS 与 DFS 模板 时，先给出清晰结论：它解决什么问题、适合什么场景；再说明核心机制和关键流程；随后补充复杂度、边界条件、常见误区或工程取舍；最后结合一两个排查或优化点说明如何落地。",
      "followUpQuestions": [
        {
          "question": "如果题目要求最短步数，你为什么通常优先考虑 BFS，而不是 DFS？",
          "answer": "无权图按层扩展时，BFS 第一次到达目标就是最短步数；DFS 会沿单条路径走到底，需要额外回溯和全局最优比较，容易超时或写错剪枝。"
        },
        {
          "question": "网格 DFS 为什么要注意 visited 标记的时机？",
          "answer": "进入节点时标记可以避免同一轮搜索重复入栈；如果需要枚举所有路径，则要在回溯时撤销。面试时要先判断是连通性/染色问题，还是路径枚举问题。"
        },
        {
          "question": "BFS 队列里除了坐标，还可能需要存什么状态？",
          "answer": "需要存步数、钥匙集合、剩余可消除障碍次数、方向等额外状态。visited 也要从二维升级为包含状态维度，否则会错误剪枝。"
        }
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "algorithm.graph.bfs-dfs-template.recall.1",
      "prompt": "BFS 和 DFS 在时间和空间复杂度上各有什么特点？什么场景下选择哪种？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "algorithm.graph.bfs-dfs-template.recall.2",
      "prompt": "BFS 求最短路径的原理是什么？为什么 DFS 不能保证最短路径？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "algorithm.graph.bfs-dfs-template.recall.3",
      "prompt": "如何用 BFS 解决双向 BFS 优化的问题？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    }
  ],
  "rubric": {
    "mustHave": [
      "BFS 用队列逐层遍历的机制",
      "DFS 递归和迭代两种实现方式",
      "visited 数组的作用和去重策略",
      "网格问题中方向数组的使用"
    ],
    "goodToHave": [
      "能手写 BFS 和 DFS 模板",
      "能判断什么场景用 BFS 什么场景用 DFS",
      "能解释 visited 去重的作用"
    ],
    "commonMistakes": [
      "BFS 和 DFS 的适用场景搞混",
      "忘记标记 visited 导致死循环",
      "DFS 递归深度过大导致栈溢出"
    ],
    "scoreWeights": {
      "coverage": 40,
      "accuracy": 25,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-05-29",
  "interviewFrequency": "high",
  "status": "production",
  "interviewerFocus": "考察图/树遍历模板是否熟练，能否根据最短路、连通性、路径搜索、回溯枚举等场景选择 BFS 或 DFS 并说明复杂度。"
}
