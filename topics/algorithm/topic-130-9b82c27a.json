{
  "id": "algorithm.array-list.topic-130-9b82c27a",
  "domain": "algorithm",
  "category": "array-list",
  "group": "array-list",
  "title": "回溯基础高频题",
  "summary": "1. 全排列（LeetCode 46 - Medium）",
  "tags": [
    "算法与数据结构",
    "数组与链表",
    "回溯基础高频题"
  ],
  "difficulty": 3,
  "estimatedMinutes": 20,
  "order": 1300,
  "recommendWeight": 90,
  "learningCards": [
    {
      "type": "explain",
      "title": "知识全景",
      "content": "# 回溯高频题目\n## 1. 全排列（LeetCode 46 - Medium）\n### 题目描述\n给定一个不含重复数字的数组 nums，返回其所有可能的全排列。\n### 思路分析\n- 回溯：每个位置从所有未使用的数字中选择\n- 用 used 数组标记已使用的元素\n### 要点\n- 排列问题不使用 startIndex，每次从 0 开始\n- used 数组确保同一元素不重复使用\n### 代码\n```java\npublic List<List<Integer>> permute(int[] nums) {\n    List<List<Integer>> result = new ArrayList<>();\n    boolean[] used = new boolean[nums.length];\n    backtrack(nums, new ArrayList<>(), used, result);\n    return result;\n}\nprivate void backtrack(int[] nums, List<Integer> path, boolean[] used, List<List<Integer>> result) {\n    if (path.size() == nums.length) {\n        result.add(new ArrayList<>(path));\n        return;\n    }\nfor (int i = 0; i < nums.length; i++) {\n    if (used[i]) continue;\n    used[i] = true;\n    path.add(nums[i]);\n    backtrack(nums, path, used, result);\n    path.remove(path.size() - 1);\n    used[i] = false;\n}\n}\n```\n### 复杂度\n- 时间：O(n! * n)  空间：O(n)\n## 2. 子集（LeetCode 78 - Medium）\n### 题目描述\n给你一个整数数组 nums，数组中的元素互不相同，返回该数组所有可能的子集。\n### 思路分析\n- 回溯：每个节点都收集结果，不只是叶子节点\n- 使用 startIndex 避免重复\n### 要点\n- 子集问题的收集时机：每个节点都收集\n- startIndex 保证不重复\n### 代码\n```java\npublic List<List<Integer>> subsets(int[] nums) {\n    List<List<Integer>> result = new ArrayList<>();\n    backtrack(nums, 0, new ArrayList<>(), result);\n    return result;\n}\nprivate void backtrack(int[] nums, int start, List<Integer> path, List<List<Integer>> result) {\n    result.add(new ArrayList<>(path));\n    for (int i = start; i < nums.length; i++) {\n        path.add(nums[i]);\n        backtrack(nums, i + 1, path, result);\n        path.remove(path.size() - 1);\n    }\n}\n```\n### 复杂度\n- 时间：O(2^n * n)  空间：O(n)\n## 3. 组合总和（LeetCode 39 - Medium）\n### 题目描述\n给定一个无重复元素的数组 candidates 和一个目标数 target，找出所有可以使数字和为 target 的组合。同一个数字可以无限重复选取。\n### 思路分析\n- 回溯 + 剪枝\n- 排序后可以提前终止（当前数 > target 时 break）\n- 下一层从 i 开始（允许重复选）\n### 要点\n- 允许重复选：递归时传 i 而非 i+1\n- 排序 + 剪枝优化\n### 代码\n```java\npublic List<List<Integer>> combinationSum(int[] candidates, int target) {\n    List<List<Integer>> result = new ArrayList<>();\n    Arrays.sort(candidates);\n    backtrack(candidates, target, 0, new ArrayList<>(), result);\n    return result;\n}\nprivate void backtrack(int[] candidates, int remain, int start, List<Integer> path, List<List<Integer>> result) {\n    if (remain == 0) {\n        result.add(new ArrayList<>(path));\n        return;\n    }\nfor (int i = start; i < candidates.length; i++) {\n    if (candidates[i] > remain) break; // 剪枝\n    path.add(candidates[i]);\n    backtrack(candidates, remain - candidates[i], i, path, result); // i 不是 i+1\n    path.remove(path.size() - 1);\n}\n}\n```\n### 复杂度\n- 时间：O(S)，S 为所有可行解的长度之和  空间：O(target)\n## 4. N 皇后（LeetCode 51 - Hard）\n### 题目描述\n在 n x n 的棋盘上放置 n 个皇后，使得它们互不攻击（同行、同列、同对角线不能有两个皇后）。\n### 思路分析\n- 逐行放置皇后，每行选一个列\n- 用三个集合记录已占用的列、正对角线（row-col）、反对角线（row+col）\n### 要点\n- 对角线编码：正对角线 row-col 相同，反对角线 row+col 相同\n- 每行只放一个，不需要检查行冲突\n### 代码\n```java\npublic List<List<String>> solveNQueens(int n) {\n    List<List<String>> result = new ArrayList<>();\n    char[][] board = new char[n][n];\n    for (char[] row : board) Arrays.fill(row, '.');\n    Set<Integer> cols = new HashSet<>();\n    Set<Integer> diag1 = new HashSet<>(); // row - col\n    Set<Integer> diag2 = new HashSet<>(); // row + col\n    backtrack(board, 0, cols, diag1, diag2, result);\n    return result;\n}\nprivate void backtrack(char[][] board, int row, Set<Integer> cols,\nSet<Integer> diag1, Set<Integer> diag2, List<List<String>> result) {\n    int n = board.length;\n    if (row == n) {\n        List<String> solution = new ArrayList<>();\n        for (char[] r : board) solution.add(new String(r));\n        result.add(solution);\n        return;\n    }\nfor (int col = 0; col < n; col++) {\n    if (cols.contains(col) || diag1.contains(row - col) || diag2.contains(row + col)) continue;\n    board[row][col] = 'Q';\n    cols.add(col); diag1.add(row - col); diag2.add(row + col);\n    backtrack(board, row + 1, cols, diag1, diag2, result);\n    board[row][col] = '.';\n    cols.remove(col); diag1.remove(row - col); diag2.remove(row + col);\n}\n}\n```\n### 复杂度\n- 时间：O(n!)  空间：O(n^2)\n## 5. 单词搜索（LeetCode 79 - Medium）\n### 题目描述\n给定一个 m x n 二维字符网格 board 和一个字符串 word，判断 word 是否存在于网格中（相邻格子可以连接）。\n### 思路分析\n- DFS + 回溯：从每个格子开始搜索\n- 用 visited 数组或修改原数组标记已访问\n### 要点\n- 四个方向搜索\n- 回溯时恢复状态\n- 提前终止：找到一个就返回\n### 代码\n```java\npublic boolean exist(char[][] board, String word) {\n    int m = board.length, n = board[0].length;\n    for (int i = 0; i < m; i++) {\n        for (int j = 0; j < n; j++) {\n            if (dfs(board, word, i, j, 0)) return true;\n        }\n}\nreturn false;\n}\nprivate boolean dfs(char[][] board, String word, int i, int j, int k) {\n    if (k == word.length()) return true;\n    if (i < 0 || i >= board.length || j < 0 || j >= board[0].length) return false;\n    if (board[i][j] != word.charAt(k)) return false;\n    char temp = board[i][j];\n    board[i][j] = '#'; // 标记已访问\n    boolean found = dfs(board, word, i + 1, j, k + 1)\n    board[i][j] = temp; // 恢复\n    return found;\n}\n```\n### 复杂度\n- 时间：O(m * n * 4^L)，L 为 word 长度  空间：O(L)"
    },
    {
      "type": "explain",
      "title": "关键机制拆解",
      "content": "先把 回溯基础高频题 当成一个可讲清楚的系统来理解：\n\n1. 全排列（LeetCode 46 - Medium）\n2. 给定一个不含重复数字的数组 nums，返回其所有可能的全排列。\n3. 回溯：每个位置从所有未使用的数字中选择\n4. 用 used 数组标记已使用的元素\n5. 排列问题不使用 startIndex，每次从 0 开始\n\n学习时重点关注“为什么需要它、它如何工作、什么时候会出问题、面试时怎么结构化表达”。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | 回溯基础高频题 | 相邻知识/常见误解 |\n| --- | --- | --- |\n| 解决的问题 | 说明核心目标和使用场景 | 只背定义，忽略为什么需要它 |\n| 核心机制 | 拆成关键角色、流程、边界条件 | 把实现细节和概念边界混在一起 |\n| 面试表达 | 先结论，再机制，再场景和取舍 | 直接罗列术语，缺少层次 |\n| 关联分类 | 数组与链表 | 需要能和同类知识做横向比较 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "回溯基础高频题 的学习路径图，用于替代原始 Markdown 中容易变形的 ASCII 图。",
      "fallback": "建议用“输入/触发 → 核心流程 → 关键状态 → 输出/风险”的流程图复述 回溯基础高频题，并标出它在 数组与链表 中的位置。",
      "items": [
        "定位：数组与链表",
        "输入：问题场景或触发条件",
        "机制：全排列（LeetCode 46 - Medium）",
        "输出：面试可讲的结论与取舍"
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "public List<List<Integer>> permute(int[] nums) {\n    List<List<Integer>> result = new ArrayList<>();\n    boolean[] used = new boolean[nums.length];\n    backtrack(nums, new ArrayList<>(), used, result);\n    return result;\n}\nprivate void backtrack(int[] nums, List<Integer> path, boolean[] used, List<List<Integer>> result) {\n    if (path.size() == nums.length) {\n        result.add(new ArrayList<>(path));\n        return;\n    }\n    for (int i = 0; i < nums.length; i++) {\n        if (used[i]) continue;\n        used[i] = true;\n        path.add(nums[i]);\n        backtrack(nums, path, used, result);\n        path.remove(path.size() - 1);\n        used[i] = false;\n    }\n}"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "可以按“四段式”回答 回溯基础高频题：先用一句话给定义和价值；再拆 数组与链表 里的关键机制；接着补充适用场景、性能或一致性等取舍；最后用项目排查、优化或设计经验收束。"
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "回溯基础高频题 的定义和边界",
        "回溯基础高频题 的关键机制",
        "回溯基础高频题 的典型场景和常见追问",
        "回溯基础高频题 在面试中的结构化表达方式"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "algorithm.array-list.topic-130-9b82c27a.recall.1",
      "prompt": "请用自己的话解释 回溯基础高频题。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "algorithm.array-list.topic-130-9b82c27a.recall.2",
      "prompt": "如果面试官追问 回溯基础高频题 的应用场景或常见误区，你会怎么回答？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "定义准确",
      "关键机制",
      "适用场景",
      "常见误区",
      "面试表达结构"
    ],
    "goodToHave": [
      "结合项目经验",
      "能做对比",
      "能说明取舍"
    ],
    "commonMistakes": [
      "只背概念不讲原因",
      "忽略边界条件",
      "缺少面试化表达"
    ],
    "scoreWeights": {
      "coverage": 40,
      "accuracy": 25,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-05-27"
}
