{
  "id": "dotnet.dotnet-core.middleware-pipeline",
  "domain": "dotnet",
  "category": "dotnet-core",
  "group": "dotnet-core",
  "title": "中间件管道",
  "summary": "理解 RequestDelegate、Use/Map/Run 的区别以及中间件的执行流程",
  "tags": [
    ".NET",
    "中间件",
    "Middleware",
    "管道",
    "RequestDelegate"
  ],
  "difficulty": 2,
  "estimatedMinutes": 20,
  "order": 20,
  "recommendWeight": 90,
  "status": "draft",
  "learningCards": [
    {
      "type": "explain",
      "title": "知识全景",
      "content": "ASP.NET Core 的中间件管道是处理 HTTP 请求的核心机制，每个请求都会经过一系列中间件的处理。\n\n## RequestDelegate\n\nRequestDelegate 是中间件的核心委托类型：\n```csharp\npublic delegate Task RequestDelegate(HttpContext context);\n```\n\n每个中间件本质上就是一个处理 HttpContext 并返回 Task 的函数。\n\n## 中间件的执行流程\n\n中间件按照注册顺序形成管道，请求从前往后经过每个中间件，响应从后往前经过：\n```\n请求 → 中间件1 → 中间件2 → 中间件3 → 终端\n响应 ← 中间件1 ← 中间件2 ← 中间件3 ← 终端\n```\n\n每个中间件可以选择：\n1. 调用 `next()` 将请求传递给下一个中间件\n2. 短路管道，直接返回响应\n\n## Use/Map/Run 扩展方法\n\n**Use**：注册中间件，可以调用 next()\n```csharp\napp.Use(async (context, next) =>\n{\n    // 请求处理前\n    Console.WriteLine(\"Before\");\n    \n    await next(); // 调用下一个中间件\n    \n    // 响应处理后\n    Console.WriteLine(\"After\");\n});\n```\n\n**Map**：根据路径分支管道\n```csharp\napp.Map(\"/api\", apiApp =>\n{\n    apiApp.Use(async (context, next) =>\n    {\n        // 处理 /api 路径的请求\n        await next();\n    });\n});\n```\n\n**MapWhen**：根据条件分支管道\n```csharp\napp.MapWhen(context => context.Request.Path.StartsWithSegments(\"/admin\"), adminApp =>\n{\n    // 处理管理员路径\n});\n```\n\n**Run**：注册终端中间件，不调用 next()\n```csharp\napp.Run(async context =>\n{\n    // 终端中间件，不调用 next()\n    await context.Response.WriteAsync(\"Hello World!\");\n});\n```\n\n## 内置中间件\n\nASP.NET Core 提供了丰富的内置中间件：\n\n```csharp\napp.UseRouting();          // 路由\napp.UseAuthentication();   // 认证\napp.UseAuthorization();    // 授权\napp.UseCors();             // 跨域\napp.UseResponseCompression(); // 响应压缩\napp.UseStaticFiles();      // 静态文件\napp.UseEndpoints(endpoints =>\n{\n    endpoints.MapControllers();\n});\n```\n\n## 自定义中间件\n\n**内联中间件**：\n```csharp\napp.Use(async (context, next) =>\n{\n    var stopwatch = Stopwatch.StartNew();\n    \n    await next();\n    \n    stopwatch.Stop();\n    Console.WriteLine($\"请求耗时: {stopwatch.ElapsedMilliseconds}ms\");\n});\n```\n\n**基于类的中间件**：\n```csharp\npublic class RequestTimingMiddleware\n{\n    private readonly RequestDelegate _next;\n\n    public RequestTimingMiddleware(RequestDelegate next)\n    {\n        _next = next;\n    }\n\n    public async Task InvokeAsync(HttpContext context)\n    {\n        var stopwatch = Stopwatch.StartNew();\n        \n        await _next(context);\n        \n        stopwatch.Stop();\n        context.Response.Headers.Add(\"X-Request-Duration\", \n            $\"{stopwatch.ElapsedMilliseconds}ms\");\n    }\n}\n\n// 注册\napp.UseMiddleware<RequestTimingMiddleware>();\n```\n\n**扩展方法注册**：\n```csharp\npublic static class RequestTimingMiddlewareExtensions\n{\n    public static IApplicationBuilder UseRequestTiming(this IApplicationBuilder app)\n    {\n        return app.UseMiddleware<RequestTimingMiddleware>();\n    }\n}\n\n// 使用\napp.UseRequestTiming();\n```\n\n## 中间件的执行顺序\n\n中间件的注册顺序非常重要：\n```csharp\n// 正确顺序\napp.UseExceptionHandler(\"/Error\");\napp.UseHsts();\napp.UseHttpsRedirection();\napp.UseStaticFiles();\napp.UseRouting();\napp.UseCors();\napp.UseAuthentication();\napp.UseAuthorization();\napp.UseEndpoints(endpoints => endpoints.MapControllers());\n```\n\n## 短路与分支\n\n**短路**：中间件不调用 next()，直接返回响应\n```csharp\napp.Use(async (context, next) =>\n{\n    if (context.Request.Path == \"/health\")\n    {\n        await context.Response.WriteAsync(\"OK\");\n        return; // 短路\n    }\n    \n    await next();\n});\n```\n\n**分支**：根据条件执行不同的中间件管道\n```csharp\napp.MapWhen(context => context.Request.Method == \"GET\", getApp =>\n{\n    getApp.Run(async context => await context.Response.WriteAsync(\"GET Request\"));\n});\n```"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "using System;\nusing System.Diagnostics;\nusing System.Threading.Tasks;\nusing Microsoft.AspNetCore.Builder;\nusing Microsoft.AspNetCore.Http;\nusing Microsoft.Extensions.DependencyInjection;\n\nnamespace MiddlewarePipelineDemo\n{\n    // 基于类的中间件\n    public class RequestLoggingMiddleware\n    {\n        private readonly RequestDelegate _next;\n        private readonly ILogger<RequestLoggingMiddleware> _logger;\n\n        public RequestLoggingMiddleware(RequestDelegate next, ILogger<RequestLoggingMiddleware> logger)\n        {\n            _next = next;\n            _logger = logger;\n        }\n\n        public async Task InvokeAsync(HttpContext context)\n        {\n            var requestId = Guid.NewGuid().ToString();\n            context.Items[\"RequestId\"] = requestId;\n\n            _logger.LogInformation($\"[{requestId}] 开始处理 {context.Request.Method} {context.Request.Path}\");\n\n            try\n            {\n                await _next(context);\n                _logger.LogInformation($\"[{requestId}] 请求完成，状态码: {context.Response.StatusCode}\");\n            }\n            catch (Exception ex)\n            {\n                _logger.LogError(ex, $\"[{requestId}] 请求处理失败\");\n                throw;\n            }\n        }\n    }\n\n    public class RequestTimingMiddleware\n    {\n        private readonly RequestDelegate _next;\n\n        public RequestTimingMiddleware(RequestDelegate next)\n        {\n            _next = next;\n        }\n\n        public async Task InvokeAsync(HttpContext context)\n        {\n            var stopwatch = Stopwatch.StartNew();\n\n            await _next(context);\n\n            stopwatch.Stop();\n            var duration = stopwatch.ElapsedMilliseconds;\n\n            context.Response.Headers.Add(\"X-Request-Duration\", $\"{duration}ms\");\n\n            if (duration > 1000)\n            {\n                Console.WriteLine($\"警告: 请求 {context.Request.Path} 耗时 {duration}ms\");\n            }\n        }\n    }\n\n    // 中间件扩展方法\n    public static class MiddlewareExtensions\n    {\n        public static IApplicationBuilder UseRequestLogging(this IApplicationBuilder app)\n        {\n            return app.UseMiddleware<RequestLoggingMiddleware>();\n        }\n\n        public static IApplicationBuilder UseRequestTiming(this IApplicationBuilder app)\n        {\n            return app.UseMiddleware<RequestTimingMiddleware>();\n        }\n    }\n\n    class Program\n    {\n        static void Main(string[] args)\n        {\n            var builder = WebApplication.CreateBuilder(args);\n            var app = builder.Build();\n\n            // 注册中间件\n            app.UseRequestLogging();\n            app.UseRequestTiming();\n\n            // 内联中间件示例\n            app.Use(async (context, next) =>\n            {\n                // 添加自定义头\n                context.Response.Headers.Add(\"X-Custom-Header\", \"MiddlewareDemo\");\n                \n                await next();\n            });\n\n            // 路径分支\n            app.Map(\"/api\", apiApp =>\n            {\n                apiApp.Use(async (context, next) =>\n                {\n                    Console.WriteLine(\"API 中间件\");\n                    await next();\n                });\n\n                apiApp.Run(async context =>\n                {\n                    await context.Response.WriteAsync(\"API Response\");\n                });\n            });\n\n            // 条件分支\n            app.MapWhen(\n                context => context.Request.Path.StartsWithSegments(\"/admin\"),\n                adminApp =>\n                {\n                    adminApp.Use(async (context, next) =>\n                    {\n                        // 管理员认证检查\n                        if (!context.User.Identity.IsAuthenticated)\n                        {\n                            context.Response.StatusCode = 401;\n                            return;\n                        }\n                        await next();\n                    });\n\n                    adminApp.Run(async context =>\n                    {\n                        await context.Response.WriteAsync(\"Admin Area\");\n                    });\n                });\n\n            // 终端中间件\n            app.Run(async context =>\n            {\n                await context.Response.WriteAsync(\"Hello World!\");\n            });\n\n            app.Run();\n        }\n    }\n}",
      "language": "csharp"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "面试官您好，关于 ASP.NET Core 的中间件管道，我来详细介绍一下。\n\n中间件管道是 ASP.NET Core 处理 HTTP 请求的核心机制。每个中间件都是一个处理 HttpContext 的组件，它们按照注册顺序形成管道。请求从前往后经过每个中间件，响应从后往前经过。每个中间件可以选择调用 next() 将请求传递给下一个中间件，或者短路管道直接返回响应。\n\nASP.NET Core 提供了三个主要的扩展方法来注册中间件：Use 用于注册可以调用 next() 的中间件，Map 用于根据路径分支管道，Run 用于注册终端中间件。Use 和 Map 的区别是 Use 可以调用 next() 继续管道，而 Run 是终端中间件，不会调用 next()。\n\n中间件的执行顺序非常重要。比如认证中间件必须在授权中间件之前，静态文件中间件通常在路由之前。错误处理中间件应该在最前面，以捕获后续中间件的异常。\n\n自定义中间件可以基于类实现，通过构造函数注入 RequestDelegate，实现 InvokeAsync 方法。这种方式可以利用依赖注入，更灵活。也可以使用内联中间件，适合简单的逻辑。\n\n在实际开发中，我会注意中间件的执行顺序，合理使用短路和分支，避免在中间件中执行耗时操作。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 维度 | Use | Map | MapWhen | Run |\n| --- | --- | --- | --- | --- |\n| 用途 | 注册中间件 | 路径分支 | 条件分支 | 终端中间件 |\n| 调用 next() | 可以 | 可以 | 可以 | 不可以 |\n| 参数 | (context, next) | path, action | predicate, action | context => |\n| 管道行为 | 继续管道 | 创建新管道 | 创建新管道 | 结束管道 |\n| 典型应用 | 日志、认证、CORS | /api 路径 | 管理员路径 | 最终响应 |"
    },
    {
      "type": "compareTable",
      "title": "维度对比",
      "content": "| 维度 | 内联中间件 | 基于类的中间件 |\n| --- | --- | --- |\n| 定义方式 | lambda 表达式 | 独立类 |\n| 依赖注入 | 不支持 | 支持（构造函数） |\n| 可重用性 | 低（仅当前应用） | 高（可封装为库） |\n| 复杂度 | 适合简单逻辑 | 适合复杂逻辑 |\n| 测试 | 难以单元测试 | 易于单元测试 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "中间件管道执行流程：\n\n请求 → 中间件1 → 中间件2 → 中间件3 → 终端\n        ↓ next()   ↓ next()   ↓ next()\n        ↑ 响应     ↑ 响应     ↑ 响应\n响应 ← 中间件1 ← 中间件2 ← 中间件3 ← 终端\n\n短路场景：\n请求 → 中间件1 → 中间件2（短路）→ 直接返回响应\n        ↓ next()\n响应 ← 中间件1\n\n分支场景：\n请求 → /api → API中间件 → 终端\n请求 → /admin → 管理中间件 → 终端",
      "fallback": "中间件管道的请求/响应流程图",
      "items": [
        "请求从前往后经过中间件",
        "响应从后往前返回",
        "中间件可调用 next() 继续管道",
        "中间件可短路管道直接返回"
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "中间件管道的工作原理和执行流程",
        "Use、Map、MapWhen、Run 的区别和使用场景",
        "中间件的注册顺序为什么重要",
        "如何自定义中间件（基于类和内联）",
        "短路和分支的概念及实现",
        "常见内置中间件的作用和顺序"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "dotnet.dotnet-core.middleware-pipeline.recall.1",
      "prompt": "请解释 ASP.NET Core 中间件管道的工作原理，包括请求和响应的处理流程。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "dotnet.dotnet-core.middleware-pipeline.recall.2",
      "prompt": "Use、Map、MapWhen 和 Run 这四个扩展方法有什么区别？分别在什么场景下使用？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    }
  ],
  "rubric": {
    "mustHave": [
      "理解中间件管道的执行流程",
      "掌握 Use、Map、Run 的区别",
      "知道中间件注册顺序的重要性",
      "能够自定义中间件",
      "了解短路和分支的概念"
    ],
    "goodToHave": [
      "理解 RequestDelegate 的作用",
      "知道如何编写可重用的中间件库",
      "了解中间件的性能考虑"
    ],
    "commonMistakes": [
      "中间件注册顺序错误",
      "在 Use 中忘记调用 next()",
      "在 Run 中调用 next()",
      "在中间件中执行耗时阻塞操作"
    ],
    "scoreWeights": {
      "coverage": 40,
      "accuracy": 25,
      "interviewExpression": 20,
      "depth": 15
    }
  }
}
