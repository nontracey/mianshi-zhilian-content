{
  "id": "dotnet.aspnet.web-api-design",
  "domain": "dotnet",
  "category": "aspnet",
  "group": "aspnet",
  "title": "Web API 设计",
  "summary": "讲清 RESTful Web API 的资源/方法/状态码语义、幂等性、模型绑定与验证，以及 PUT/PATCH 区别等实践要点。",
  "tags": [
    ".NET",
    "ASP.NET",
    "Web API",
    "RESTful",
    "路由",
    "模型绑定"
  ],
  "difficulty": 3,
  "estimatedMinutes": 40,
  "order": 10,
  "recommendWeight": 90,
  "status": "production",
  "learningCards": [
    {
      "type": "explain",
      "title": "Web API 设计：RESTful 设计原则、路由配置",
      "content": "ASP.NET Core Web API 是构建 RESTful 服务的主流框架，遵循 REST 架构风格设计 API。\n\n## RESTful 设计原则\n\n**资源导向**：每个 URL 代表一个资源\n```\nGET    /api/users          - 获取用户列表\nGET    /api/users/123      - 获取特定用户\nPOST   /api/users          - 创建用户\nPUT    /api/users/123      - 更新用户\nDELETE /api/users/123      - 删除用户\n```\n\n**HTTP 方法语义**：\n- GET：获取资源，幂等\n- POST：创建资源，非幂等\n- PUT：更新资源（全量），幂等\n- PATCH：更新资源（部分），非幂等\n- DELETE：删除资源，幂等\n\n**状态码规范**：\n- 200 OK：成功\n- 201 Created：创建成功\n- 204 No Content：删除成功\n- 400 Bad Request：请求错误\n- 401 Unauthorized：未认证\n- 403 Forbidden：无权限\n- 404 Not Found：资源不存在\n- 500 Internal Server Error：服务器错误\n\n## 路由配置\n\n**属性路由**（推荐）：\n```csharp\n[ApiController]\n[Route(\"api/[controller]\")]\npublic class UsersController : ControllerBase\n{\n    [HttpGet]\n    public IActionResult GetAll() { }\n\n    [HttpGet(\"{id}\")]\n    public IActionResult GetById(int id) { }\n\n    [HttpPost]\n    public IActionResult Create([FromBody] UserDto user) { }\n\n    [HttpPut(\"{id}\")]\n    public IActionResult Update(int id, [FromBody] UserDto user) { }\n\n    [HttpDelete(\"{id}\")]\n    public IActionResult Delete(int id) { }\n}\n```\n\n**路由约束**：\n```csharp\n[HttpGet(\"{id:int}\")]\n[HttpGet(\"{id:int:min(1)}\")]\n[HttpGet(\"{name:alpha}\")]\n[HttpGet(\"{date:datetime}\")]\n```\n\n**路由参数绑定**：\n- `[FromRoute]`：从 URL 路径获取\n- `[FromQuery]`：从查询字符串获取\n- `[FromBody]`：从请求体获取\n- `[FromHeader]`：从请求头获取\n- `[FromForm]`：从表单数据获取\n\n## 模型绑定\n\nASP.NET Core 自动将请求数据绑定到操作参数：\n```csharp\n// JSON 请求体\n[HttpPost]\npublic IActionResult Create([FromBody] CreateUserRequest request)\n{\n    // 自动反序列化 JSON\n}\n\n// 查询参数\n[HttpGet]\npublic IActionResult Search([FromQuery] string keyword, [FromQuery] int page = 1)\n{\n    // 从查询字符串绑定\n}\n\n// 路由参数\n[HttpGet(\"{id}\")]\npublic IActionResult GetById([FromRoute] int id)\n{\n    // 从 URL 路径绑定\n}\n```\n\n## 模型验证\n\n使用数据注解进行验证：\n```csharp\npublic class CreateUserRequest\n{\n    [Required(ErrorMessage = \"用户名不能为空\")]\n    [StringLength(50, MinimumLength = 3)]\n    public string Username { get; set; }\n\n    [Required]\n    [EmailAddress]\n    public string Email { get; set; }\n\n    [Required]\n    [StringLength(100, MinimumLength = 6)]\n    public string Password { get; set; }\n\n    [Range(1, 150)]\n    public int Age { get; set; }\n}\n\n// 控制器自动验证\n[ApiController] // 自动验证模型\npublic class UsersController : ControllerBase\n{\n    [HttpPost]\n    public IActionResult Create([FromBody] CreateUserRequest request)\n    {\n        // 如果验证失败，自动返回 400\n        if (!ModelState.IsValid)\n        {\n            return BadRequest(ModelState);\n        }\n        // 处理请求\n    }\n}\n```\n\n**自定义验证**：\n```csharp\npublic class UserValidator : AbstractValidator<CreateUserRequest>\n{\n    public UserValidator()\n    {\n        RuleFor(x => x.Username)\n            .NotEmpty()\n            .MinimumLength(3)\n            .MustAsync(async (username, ct) => await IsUsernameUnique(username))\n            .WithMessage(\"用户名已存在\");\n    }\n}\n```"
    },
    {
      "type": "explain",
      "title": "Web API 设计：响应契约、错误模型与演进策略",
      "content": "## 响应契约先尊重 HTTP 语义\n\nAPI 不建议所有结果都包成 HTTP 200 + 自定义 Code。成功、客户端错误和服务端错误应使用清晰状态码：201 Created 搭配 Location 表达资源创建，400/422 表达校验失败，401 表示未认证，403 表示无权限，404 表示资源不存在，409 表示并发冲突或唯一约束冲突，429 表示限流，5xx 表示服务端故障。\n\n## 错误模型要稳定可机器处理\n\n错误响应可以采用 ProblemDetails 风格：type、title、status、detail、instance、traceId，再按业务需要扩展 errorCode 和 fieldErrors。traceId 帮排查，errorCode 帮前端和调用方做稳定分支，detail 不要泄露 SQL、堆栈和内部服务名。\n\n## 演进策略比“统一格式”更重要\n\n分页要明确 cursor/offset 语义和排序稳定性；过滤和排序要白名单化；版本策略可以用 URL、Header 或媒体类型，但必须说明兼容窗口；限流要返回 Retry-After 或明确的错误码；缓存要用 ETag、Last-Modified、Cache-Control 表达可重用边界。\n\n## 什么时候还需要响应壳\n\n内部 BFF 或强前端约定场景可以保留响应壳，但不要吞掉 HTTP 状态码。更稳的做法是 HTTP 状态表达协议级结果，body 中的业务码表达领域级失败。"
    },
    {
      "type": "diagram",
      "format": "mermaid",
      "title": "Web API 设计链路：资源建模、状态码、错误模型、版本与兼容演进",
      "content": "flowchart TD\nA[\"业务能力\"] --> B[\"资源和操作建模\"]\nB --> C[\"HTTP 方法和路由\"]\nC --> D[\"请求校验\nDTO/绑定/验证\"]\nD --> E[\"业务处理\"]\nE --> F{\"结果类型\"}\nF -->|成功| G[\"2xx + 资源表示/Location\"]\nF -->|客户端错误| H[\"4xx + ProblemDetails\"]\nF -->|服务错误| I[\"5xx + traceId\"]\nG --> J[\"版本、分页、过滤、缓存和限流\"]\nH --> J\nI --> J",
      "caption": "Web API 设计链路：资源建模、状态码、错误模型、版本与兼容演进",
      "fallback": "图展示 Web API 设计要从资源建模到状态码、错误模型、版本兼容、分页缓存和限流，而不是只套统一响应壳。",
      "sources": [
        {
          "kind": "mermaid",
          "content": "flowchart TD\nA[\"业务能力\"] --> B[\"资源和操作建模\"]\nB --> C[\"HTTP 方法和路由\"]\nC --> D[\"请求校验\nDTO/绑定/验证\"]\nD --> E[\"业务处理\"]\nE --> F{\"结果类型\"}\nF -->|成功| G[\"2xx + 资源表示/Location\"]\nF -->|客户端错误| H[\"4xx + ProblemDetails\"]\nF -->|服务错误| I[\"5xx + traceId\"]\nG --> J[\"版本、分页、过滤、缓存和限流\"]\nH --> J\nI --> J"
        },
        {
          "kind": "text",
          "content": "图展示 Web API 设计要从资源建模到状态码、错误模型、版本兼容、分页缓存和限流，而不是只套统一响应壳。"
        }
      ]
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 维度 | 属性路由 | 传统路由 |\n| --- | --- | --- |\n| 定义位置 | 控制器/操作方法上 | Startup.Configure |\n| 可读性 | 高（直接看到路由） | 低（需要查看配置） |\n| 灵活性 | 高（每个操作独立） | 低（统一模式） |\n| 适用场景 | RESTful API | MVC 应用 |\n| 版本控制 | 容易实现 | 相对复杂 |"
    },
    {
      "type": "compareTable",
      "title": "维度对比",
      "content": "| 维度 | [FromBody] | [FromQuery] | [FromRoute] |\n| --- | --- | --- | --- |\n| 数据来源 | 请求体 | 查询字符串 | URL 路径 |\n| HTTP方法 | POST/PUT/PATCH | GET | 所有 |\n| 数据格式 | JSON/XML | 键值对 | 路径段 |\n| 适用场景 | 复杂对象 | 简单参数 | 资源标识 |\n| 示例 | {\"name\":\"test\"} | ?page=1 | /users/123 |"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "using System;\nusing System.Collections.Generic;\nusing System.ComponentModel.DataAnnotations;\nusing System.Linq;\nusing Microsoft.AspNetCore.Mvc;\n\nnamespace WebApiDesignDemo\n{\n    // 请求模型\n    public class CreateProductRequest\n    {\n        [Required(ErrorMessage = \"产品名称不能为空\")]\n        [StringLength(100, MinimumLength = 2)]\n        public string Name { get; set; }\n\n        [StringLength(500)]\n        public string Description { get; set; }\n\n        [Required]\n        [Range(0.01, 100000)]\n        public decimal Price { get; set; }\n\n        [Required]\n        [Range(0, 10000)]\n        public int Stock { get; set; }\n\n        [Required]\n        [StringLength(50)]\n        public string Category { get; set; }\n    }\n\n    public class UpdateProductRequest\n    {\n        [StringLength(100, MinimumLength = 2)]\n        public string Name { get; set; }\n\n        [StringLength(500)]\n        public string Description { get; set; }\n\n        [Range(0.01, 100000)]\n        public decimal? Price { get; set; }\n\n        [Range(0, 10000)]\n        public int? Stock { get; set; }\n    }\n\n    // 响应模型\n    public class ProductResponse\n    {\n        public int Id { get; set; }\n        public string Name { get; set; }\n        public string Description { get; set; }\n        public decimal Price { get; set; }\n        public int Stock { get; set; }\n        public string Category { get; set; }\n        public DateTime CreatedAt { get; set; }\n        public DateTime? UpdatedAt { get; set; }\n    }\n\n    public class ApiResponse<T>\n    {\n        public int Code { get; set; }\n        public string Message { get; set; }\n        public T Data { get; set; }\n        public object Errors { get; set; }\n\n        public static ApiResponse<T> Success(T data, string message = \"成功\")\n        {\n            return new ApiResponse<T>\n            {\n                Code = 200,\n                Message = message,\n                Data = data\n            };\n        }\n\n        public static ApiResponse<T> Error(int code, string message, object errors = null)\n        {\n            return new ApiResponse<T>\n            {\n                Code = code,\n                Message = message,\n                Errors = errors\n            };\n        }\n    }\n\n    // 查询参数\n    public class ProductQueryParameters\n    {\n        public string Category { get; set; }\n        public decimal? MinPrice { get; set; }\n        public decimal? MaxPrice { get; set; }\n        public string Search { get; set; }\n        public string SortBy { get; set; } = \"Name\";\n        public bool Descending { get; set; }\n        public int Page { get; set; } = 1;\n        public int PageSize { get; set; } = 10;\n    }\n\n    // 控制器\n    [ApiController]\n    [Route(\"api/[controller]\")]\n    [Produces(\"application/json\")]\n    public class ProductsController : ControllerBase\n    {\n        private static readonly List<ProductResponse> Products = new();\n        private static int _nextId = 1;\n\n        /// <summary>\n        /// 获取产品列表\n        /// </summary>\n        [HttpGet]\n        [ProducesResponseType(typeof(ApiResponse<IEnumerable<ProductResponse>>), 200)]\n        public IActionResult GetAll([FromQuery] ProductQueryParameters query)\n        {\n            var filtered = Products.AsQueryable();\n\n            // 过滤\n            if (!string.IsNullOrEmpty(query.Category))\n                filtered = filtered.Where(p => p.Category == query.Category);\n\n            if (query.MinPrice.HasValue)\n                filtered = filtered.Where(p => p.Price >= query.MinPrice.Value);\n\n            if (query.MaxPrice.HasValue)\n                filtered = filtered.Where(p => p.Price <= query.MaxPrice.Value);\n\n            if (!string.IsNullOrEmpty(query.Search))\n                filtered = filtered.Where(p => p.Name.Contains(query.Search) ||\n                                               p.Description.Contains(query.Search));\n\n            // 排序\n            filtered = query.SortBy?.ToLower() switch\n            {\n                \"price\" => query.Descending\n                    ? filtered.OrderByDescending(p => p.Price)\n                    : filtered.OrderBy(p => p.Price),\n                \"name\" => query.Descending\n                    ? filtered.OrderByDescending(p => p.Name)\n                    : filtered.OrderBy(p => p.Name),\n                _ => filtered.OrderBy(p => p.Name)\n            };\n\n            // 分页\n            var paged = filtered\n                .Skip((query.Page - 1) * query.PageSize)\n                .Take(query.PageSize)\n                .ToList();\n\n            return Ok(ApiResponse<IEnumerable<ProductResponse>>.Success(paged));\n        }\n\n        /// <summary>\n        /// 获取特定产品\n        /// </summary>\n        [HttpGet(\"{id:int:min(1)}\")]\n        [ProducesResponseType(typeof(ApiResponse<ProductResponse>), 200)]\n        [ProducesResponseType(404)]\n        public IActionResult GetById([FromRoute] int id)\n        {\n            var product = Products.FirstOrDefault(p => p.Id == id);\n            if (product == null)\n            {\n                return NotFound(ApiResponse<ProductResponse>.Error(404, \"产品不存在\"));\n            }\n            return Ok(ApiResponse<ProductResponse>.Success(product));\n        }\n\n        /// <summary>\n        /// 创建产品\n        /// </summary>\n        [HttpPost]\n        [ProducesResponseType(typeof(ApiResponse<ProductResponse>), 201)]\n        [ProducesResponseType(400)]\n        public IActionResult Create([FromBody] CreateProductRequest request)\n        {\n            if (!ModelState.IsValid)\n            {\n                return BadRequest(ApiResponse<ProductResponse>.Error(400, \"验证失败\",\n                    ModelState.ToDictionary(\n                        kvp => kvp.Key,\n                        kvp => kvp.Value.Errors.Select(e => e.ErrorMessage))));\n            }\n\n            var product = new ProductResponse\n            {\n                Id = _nextId++,\n                Name = request.Name,\n                Description = request.Description,\n                Price = request.Price,\n                Stock = request.Stock,\n                Category = request.Category,\n                CreatedAt = DateTime.UtcNow\n            };\n\n            Products.Add(product);\n\n            return CreatedAtAction(\n                nameof(GetById),\n                new { id = product.Id },\n                ApiResponse<ProductResponse>.Success(product, \"创建成功\"));\n        }\n\n        /// <summary>\n        /// 更新产品\n        /// </summary>\n        [HttpPut(\"{id}\")]\n        [ProducesResponseType(typeof(ApiResponse<ProductResponse>), 200)]\n        [ProducesResponseType(404)]\n        public IActionResult Update([FromRoute] int id, [FromBody] UpdateProductRequest request)\n        {\n            var product = Products.FirstOrDefault(p => p.Id == id);\n            if (product == null)\n            {\n                return NotFound(ApiResponse<ProductResponse>.Error(404, \"产品不存在\"));\n            }\n\n            if (request.Name != null) product.Name = request.Name;\n            if (request.Description != null) product.Description = request.Description;\n            if (request.Price.HasValue) product.Price = request.Price.Value;\n            if (request.Stock.HasValue) product.Stock = request.Stock.Value;\n            product.UpdatedAt = DateTime.UtcNow;\n\n            return Ok(ApiResponse<ProductResponse>.Success(product, \"更新成功\"));\n        }\n\n        /// <summary>\n        /// 删除产品\n        /// </summary>\n        [HttpDelete(\"{id}\")]\n        [ProducesResponseType(204)]\n        [ProducesResponseType(404)]\n        public IActionResult Delete([FromRoute] int id)\n        {\n            var product = Products.FirstOrDefault(p => p.Id == id);\n            if (product == null)\n            {\n                return NotFound(ApiResponse<object>.Error(404, \"产品不存在\"));\n            }\n\n            Products.Remove(product);\n            return NoContent();\n        }\n    }\n\n    // 全局异常处理\n    public class GlobalExceptionHandler\n    {\n        public static IActionResult HandleException(Exception ex)\n        {\n            return new ObjectResult(ApiResponse<object>.Error(500, \"服务器内部错误\"))\n            {\n                StatusCode = 500\n            };\n        }\n    }\n}",
      "language": "csharp",
      "highlights": [
        {
          "line": 10,
          "note": "`public class CreateProductRequest` 定义了 Web API 设计 示例中承载状态和操作的代码位置，复述时从参数和状态字段讲起。"
        },
        {
          "line": 41,
          "note": "`public decimal? Price { get; set; }` 是分支判断，决定正常路径、边界输入或失败路径走向。"
        },
        {
          "line": 14,
          "note": "`public string Name { get; set; }` 更新核心数据结构，是解释状态如何推进的抓手。"
        },
        {
          "line": 107,
          "note": "`private static readonly List<ProductResponse> Products = new();` 体现运行时或系统调用边界，适合用来说明阻塞、并发或资源释放语义。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "结论：ASP.NET Core Web API 的核心是 RESTful 风格——资源用 URL 表示，用 HTTP 方法表达操作语义（GET 查、POST 增、PUT/PATCH 改、DELETE 删），用状态码表达结果，配合模型绑定、验证和统一响应。\n\n要点：\n\n1. REST 设计：URL 是名词（资源），方法是动词；GET/PUT/DELETE 幂等、POST/PATCH 非幂等；状态码要语义化（201 创建、204 无内容、400 参数错、401 未认证、403 无权限、404 不存在）。\n2. 路由与绑定：推荐属性路由；参数用 FromRoute、FromQuery、FromBody、FromHeader 明确来源；用数据注解加 ModelState 做模型验证。\n3. 工程实践：版本控制（URL 或 Header）、列表分页、过滤排序、限流、统一响应包装。\n\n边界和坑。① 幂等性要落实：PUT 重复提交结果一致、POST 不可重复，重要写操作要做幂等键防重复提交。② 别用 200 包一切，错误也返回 200 里塞 code 是反 REST 的，应该用对应状态码。③ PATCH 是部分更新，要处理「字段没传」和「字段传 null」的区别，常用 JSON Merge Patch 或 JSON Patch。",
      "followUpQuestions": [
        {
          "question": "RESTful 里幂等性是什么意思？哪些方法幂等？为什么重要？",
          "answer": "幂等是「同一请求执行一次和执行多次，对服务器状态的影响相同」。GET、PUT、DELETE 幂等：GET 不改状态；PUT 是全量替换，重复提交结果一样；DELETE 删了再删还是删除状态。POST 非幂等，每次都新建资源；PATCH 一般也非幂等。它重要是因为网络会重试——客户端超时重发、网关重试时，幂等方法可以安全重试不出错，非幂等的 POST 重试会产生重复订单或重复扣款，所以重要写操作要用幂等键（业务唯一号）去重。"
        },
        {
          "question": "PUT 和 PATCH 有什么区别？部分更新该用哪个，要注意什么？",
          "answer": "PUT 是全量替换整个资源，没传的字段会被当成清空，所以客户端要带完整对象；PATCH 是部分更新，只改传来的字段。部分更新该用 PATCH。注意点：要区分「字段没传」（保持原值）和「字段传了 null」（清空），普通 DTO 分不清这两者，所以规范做法是用 JSON Merge Patch 或 JSON Patch（用操作列表描述改动），或在 DTO 里用可空加是否赋值的标记区分。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "REST 资源建模、HTTP 状态码、ProblemDetails 和版本演进",
        "属性路由的配置和使用",
        "模型绑定的各种来源",
        "数据注解验证和 FluentValidation",
        "统一响应格式的设计",
        "API 版本控制和分页实现"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "dotnet.aspnet.web-api-design.recall.1",
      "prompt": "请说明 Web API 设计 的核心机制、典型使用场景和常见坑。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "dotnet.aspnet.web-api-design.recall.2",
      "prompt": "ASP.NET Core 中如何实现模型验证？[ApiController] 特性有什么作用？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "dotnet.aspnet.web-api-design.recall.3",
      "prompt": "RESTful 设计中 PUT 和 PATCH 怎么区分？API 版本化你会选哪种方案、为什么？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "能说出 RESTful 用 URL 表示资源、HTTP 方法表达操作语义",
      "能解释 GET/PUT/DELETE 幂等、POST/PATCH 非幂等",
      "能说出常见状态码的语义",
      "能讲清模型绑定来源和模型验证"
    ],
    "goodToHave": [
      "能说明版本控制、分页、限流等实践",
      "能区分 PUT 全量更新与 PATCH 部分更新",
      "能指出统一响应和错误处理方式"
    ],
    "commonMistakes": [
      "所有响应都返回 200、错误也塞进 200 里",
      "混淆 PUT 与 PATCH 的语义",
      "忽略幂等性导致重复提交产生重复数据"
    ],
    "scoreWeights": {
      "coverage": 35,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "interviewFrequency": "high",
  "interviewerFocus": "考察能否解释 REST 的资源与方法语义、幂等性及其重要性、状态码使用，并区分 PUT 与 PATCH、说清模型绑定验证。",
  "updatedAt": "2026-06-18"
}
