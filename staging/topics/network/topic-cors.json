{
  "id": "network.http-https.topic-cors",
  "domain": "network",
  "category": "http-https",
  "group": "http-https",
  "title": "跨域与 CORS",
  "summary": "理解浏览器同源策略的原理和限制，掌握CORS预检请求机制和简单请求/复杂请求的区别，能正确配置跨域解决方案。",
  "tags": [
    "CORS",
    "跨域",
    "同源策略",
    "OPTIONS"
  ],
  "difficulty": 3,
  "estimatedMinutes": 15,
  "order": 40,
  "recommendWeight": 88,
  "learningCards": [
    {
      "type": "explain",
      "title": "跨域与 CORS：同源先看三元组、CORS 是服务端授权浏览器放行",
      "content": "## 同源先看三元组\n同源要求协议、域名、端口都相同。浏览器允许页面发出很多跨源请求，但会限制脚本读取响应内容，这样可以减少恶意站点读取用户在其他站点登录态数据的风险。\n\n## CORS 是服务端授权浏览器放行\nCORS 通过响应头表达“哪些 Origin 可以读我的响应”。常见头包括 `Access-Control-Allow-Origin`、`Access-Control-Allow-Credentials`、`Access-Control-Allow-Methods`、`Access-Control-Allow-Headers`。它不是绕过浏览器安全，而是服务端显式授权。"
    },
    {
      "type": "explain",
      "title": "跨域与 CORS：简单请求与预检、常见坑",
      "content": "## 简单请求与预检\n简单请求满足方法、头部和 Content-Type 限制，浏览器直接发送真实请求。复杂请求会先发 `OPTIONS` 预检，询问服务端是否允许对应方法和头部，预检通过后才发真实请求。\n\n## 常见坑\n带 Cookie 时不能使用 `Access-Control-Allow-Origin: *`，必须回显具体 Origin 并设置 `Access-Control-Allow-Credentials: true`。预检失败常见原因是服务端没有处理 OPTIONS、漏了自定义请求头，或 CDN/网关没有透传 CORS 响应头。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | 简单请求 | 复杂请求 |\n| --- | --- | --- |\n| 方法 | GET/HEAD/POST | PUT/DELETE/PATCH等 |\n| Content-Type | text/plain, multipart/form-data, application/x-www-form-urlencoded | application/json等 |\n| 自定义头部 | 无 | 有(如Authorization) |\n| 预检请求 | 不需要 | 需要(OPTIONS) |\n| 交互次数 | 1次请求 | 2次请求(预检+实际) |\n| 缓存 | 无 | 预检结果可缓存(max-age) |"
    },
    {
      "type": "diagram",
      "title": "CORS 简单请求与预检流程",
      "format": "mermaid",
      "content": "flowchart TD\nA[浏览器发起跨源请求] --> B{是否简单请求}\nB -->|是| C[发送真实请求]\nB -->|否| D[发送 OPTIONS 预检]\nD --> E{服务端允许方法和头?}\nE -->|否| F[浏览器拦截并报 CORS 错误]\nE -->|是| C\nC --> G{响应头允许当前 Origin?}\nG -->|是| H[脚本可读取响应]\nG -->|否| F",
      "caption": "CORS 的成败由浏览器根据服务端响应头判定。",
      "fallback": "CORS 流程：跨源请求 -> 简单请求直发，复杂请求预检 -> 校验响应头 -> 放行或拦截。"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "// Express.js CORS中间件配置\nconst express = require('express');\nconst cors = require('cors');\nconst app = express();\n\n// 方式1: 允许所有来源(开发环境)\napp.use(cors());\n\n// 方式2: 精细配置(生产环境推荐)\napp.use(cors({\n    origin: ['https://app.example.com', 'https://admin.example.com'],\n    methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],\n    allowedHeaders: ['Content-Type', 'Authorization', 'X-Requested-With'],\n    exposedHeaders: ['X-Total-Count', 'X-Request-Id'],\n    credentials: true,  // 允许携带Cookie\n    maxAge: 86400,      // 预检结果缓存24小时\n}));\n\n// 方式3: 动态origin\napp.use(cors({\n    origin: function(origin, callback) {\n        const allowedOrigins = ['https://app.example.com'];\n        if (!origin || allowedOrigins.includes(origin)) {\n            callback(null, true);\n        } else {\n            callback(new Error('Not allowed by CORS'));\n        }\n    },\n    credentials: true,\n}));\n\n// 方式4: 手动设置CORS头\napp.use((req, res, next) => {\n    res.header('Access-Control-Allow-Origin', req.headers.origin);\n    res.header('Access-Control-Allow-Credentials', 'true');\n    res.header('Access-Control-Allow-Methods', 'GET,POST,PUT,DELETE,OPTIONS');\n    res.header('Access-Control-Allow-Headers', 'Content-Type,Authorization');\n    res.header('Access-Control-Max-Age', '86400');\n    if (req.method === 'OPTIONS') {\n        return res.sendStatus(204);\n    }\n    next();\n});\n\napp.get('/api/data', (req, res) => {\n    res.json({ message: 'CORS configured!' });\n});\n\napp.listen(3000);",
      "language": "javascript"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "**高频问题1：什么是跨域？为什么会有跨域限制？**\n\n跨域是指浏览器的同源策略限制了不同源(协议、域名、端口不同)之间的资源访问。同源策略是浏览器的安全机制，防止恶意网站窃取其他网站的数据。比如a.com的脚本不能读取b.com的Cookie或发起AJAX请求。但`<script>`、`<img>`标签不受此限制，JSONP就是利用这一点实现跨域的。\n\n**高频问题2：CORS的预检请求是什么？什么时候会触发？**\n\n预检请求(Preflight)是浏览器在发送复杂请求前自动发起的OPTIONS请求，用于询问服务器是否允许该跨域请求。触发条件：\n\n1. 请求方法不是GET/HEAD/POST\n2. 使用了自定义头部(如Authorization)\n3. Content-Type不是text/plain/multipart/form-data/urlencoded。预检请求会携带Origin、Access-Control-Request-Method、Access-Control-Request-Headers，服务器响应Access-Control-Allow-*后，浏览器才发送实际请求。预检结果可以通过max-age缓存减少请求次数。\n\n**高频问题3：有哪些跨域解决方案？各自的优缺点？**\n\n主要有四种方案：\n\n1. CORS：标准方案，服务端设置Access-Control-Allow-Origin，支持所有HTTP方法和自定义头部，是最推荐的方案\n2. Nginx反向代理：配置proxy_pass将请求代理到后端，对前端透明，适合生产环境\n3. JSONP：利用script标签不受同源限制，只支持GET，安全性差，已不推荐\n4. WebSocket：不受同源策略限制，适合实时通信场景。实际项目中推荐CORS或Nginx反向代理。",
      "followUpQuestions": [
        {
          "question": "面试官追问「跨域与 CORS」时，最应该展开哪条机制链？",
          "answer": "回答「跨域与 CORS」时先抓住 能解释同源策略的含义和限制范围，再展开 能区分简单请求和复杂请求的判断条件。不要停在定义层，要把输入条件、关键状态变化、输出结果和失败边界串起来，这样才能体现对网络协议里真实机制的理解。"
        },
        {
          "question": "如果「跨域与 CORS」在线上或编码中出问题，你会优先检查什么？",
          "answer": "我会先确认问题是否落在「跨域与 CORS」的核心边界上，再看 抓包结果、状态码/错误码、连接状态、RTT、重传率和 DNS/TLS 指标。排查时特别避免“不清楚简单请求和非简单请求的区别”这个误区，先复现触发条件，再用日志、指标或最小用例验证修复是否真的命中原因。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能解释同源策略的三个判断条件(协议/域名/端口)",
        "能区分简单请求和复杂请求的判断标准",
        "能描述CORS预检请求的触发条件和交互流程",
        "能用代码配置Express/Nginx的CORS",
        "能说明Access-Control-Allow-Origin/Credentials/Max-Age等头部的作用",
        "能列举四种跨域解决方案及其适用场景"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "network.http-https.topic-cors.recall.1",
      "prompt": "什么是同源策略？什么是CORS？简单请求和复杂请求有什么区别？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "network.http-https.topic-cors.recall.2",
      "prompt": "CORS预检请求在什么情况下触发？如何减少预检请求的次数？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "能解释同源策略的含义和限制范围",
      "能区分简单请求和复杂请求的判断条件",
      "能描述CORS预检请求(OPTIONS)的交互流程",
      "能说明Access-Control-Allow-*各头部字段的作用"
    ],
    "goodToHave": [
      "了解CORS预检结果的缓存机制(max-age)",
      "能配置Nginx反向代理解决跨域",
      "能说明withCredentials和Access-Control-Allow-Credentials的关系"
    ],
    "commonMistakes": [
      "不清楚简单请求和非简单请求的区别",
      "不了解OPTIONS预检请求的作用",
      "Access-Control-Allow-Origin配置不当"
    ],
    "scoreWeights": {
      "coverage": 35,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-06-11",
  "status": "staging",
  "prerequisites": [
    "network.http-https.topic-http-evolution"
  ],
  "interviewFrequency": "medium",
  "interviewerFocus": "考察是否能围绕「跨域与 CORS」说清能解释同源策略的含义和限制范围、能区分简单请求和复杂请求的判断条件，并放到网络协议场景中解释关键流程、边界条件和出错后的处理思路。"
}
