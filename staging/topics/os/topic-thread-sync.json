{
  "id": "os.process-thread.topic-thread-sync",
  "domain": "os",
  "category": "process-thread",
  "group": "process-thread",
  "title": "线程同步机制",
  "summary": "掌握互斥锁、条件变量、读写锁、自旋锁等同步原语的原理和适用场景",
  "tags": [
    "操作系统",
    "线程同步",
    "互斥锁",
    "条件变量"
  ],
  "difficulty": 3,
  "estimatedMinutes": 20,
  "order": 20,
  "recommendWeight": 88,
  "learningCards": [
    {
      "type": "explain",
      "title": "为什么要同步，三种锁怎么选",
      "content": "## 为什么需要同步\n多个线程共享地址空间，并发访问同一资源时会出现**竞态条件**（结果取决于线程调度顺序），导致数据不一致。同步机制用来协调访问顺序、保护临界区。\n\n## 互斥锁 Mutex —— 抢不到就睡\n保证同一时刻只有一个线程进临界区；抢不到锁的线程**阻塞挂起、让出 CPU**。适合临界区执行时间较长的场景（睡眠/唤醒有上下文切换成本，但不空耗 CPU）。\n\n## 自旋锁 Spinlock —— 抢不到就忙等\n抢不到时**不睡、原地循环检查**锁状态。省了上下文切换，但持续占着 CPU 空转。只适合临界区**极短**的场景（典型是内核里的短临界区）。\n\n## 读写锁 RWLock —— 读共享、写排他\n多个读线程可同时持读锁，写锁排他（写时不许读也不许写）。适合**读多写少**。注意写饥饿问题——大量读可能让写一直等不到。\n\n**选择口诀**：临界区长→互斥锁；临界区极短→自旋锁；读多写少→读写锁。"
    },
    {
      "type": "explain",
      "title": "条件变量、信号量与原子操作",
      "content": "## 条件变量 —— 等“条件成立”而不忙等\n和互斥锁配合：`wait()` **释放锁并阻塞**，等别的线程把条件凑齐后 `signal()`/`broadcast()` 唤醒；被唤醒后重新拿锁继续。它解决的是“等某个条件成立”这件事（如队列非空），避免反复加锁轮询的忙等。**注意**：wait 要放在 `while(条件不满足)` 循环里，防止虚假唤醒。\n\n## 信号量 Semaphore —— 带计数的许可\n维护一个计数：P 操作（wait）计数减 1、减到负数就阻塞；V 操作（signal）计数加 1 并唤醒等待者。**计数信号量**允许至多 N 个线程同时访问（如限制连接池大小）；**二值信号量**（0/1）退化成互斥锁，但语义上信号量可由不同线程 P/V，互斥锁强调“谁加锁谁解锁”。\n\n## 原子操作 —— 无锁同步的基础\nCAS（Compare-And-Swap）：比较内存值与期望值，相等才更新，是无锁算法的基石；适合简单计数器、标志位。优点是无锁无切换、性能最高，但只适合单变量的简单更新，复杂临界区还得用锁。"
    },
    {
      "type": "diagram",
      "title": "线程同步机制 学习主线图",
      "content": "flowchart LR\nA[线程同步机制] --> B[操作系统]\nB --> C[线程同步]\nC --> D[互斥锁]\nD --> E[条件变量]\nE --> F[互斥锁阻塞等待 vs 自旋锁忙等的区别]",
      "fallback": "线程同步机制 -> 操作系统 -> 线程同步 -> 互斥锁 -> 条件变量 -> 互斥锁阻塞等待 vs 自旋锁忙等的区别",
      "items": [
        "简单计数器/标志位 -> 原子操作（CAS）",
        "短临界区（内核态） -> 自旋锁",
        "长临界区（用户态） -> 互斥锁",
        "读多写少 -> 读写锁",
        "等待某个条件 -> 条件变量 + 互斥锁",
        "多资源计数 -> 信号量"
      ],
      "format": "mermaid",
      "caption": "从 操作系统 到 互斥锁阻塞等待 vs 自旋锁忙等的区别，这张图帮助区分 线程同步机制 的核心机制、边界条件和常见误区。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 同步机制 | 等待方式 | 适用场景 | 性能 |\n| --- | --- | --- | --- |\n| 互斥锁 | 阻塞（睡眠） | 临界区较长 | 中（有上下文切换） |\n| 自旋锁 | 忙等待 | 临界区极短 | 高（无切换，浪费 CPU） |\n| 读写锁 | 阻塞 | 读多写少 | 高 |\n| 条件变量 | 阻塞 | 等待条件成立 | 高 |\n| 信号量 | 阻塞 | 多资源计数 | 高 |\n| 原子操作 | 无锁 | 简单计数/标志 | 最高 |"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "#include <pthread.h>\n#include <stdio.h>\n\npthread_mutex_t mutex = PTHREAD_MUTEX_INITIALIZER;\npthread_cond_t cond = PTHREAD_COND_INITIALIZER;\nint ready = 0;\nint data = 0;\n\n// 生产者\nvoid* producer(void* arg) {\n    for (int i = 0; i < 5; i++) {\n        pthread_mutex_lock(&mutex);\n        data = i;\n        ready = 1;\n        pthread_cond_signal(&cond);  // 通知消费者\n        pthread_mutex_unlock(&mutex);\n    }\n    return NULL;\n}\n\n// 消费者\nvoid* consumer(void* arg) {\n    for (int i = 0; i < 5; i++) {\n        pthread_mutex_lock(&mutex);\n        while (!ready) {\n            pthread_cond_wait(&cond, &mutex);  // 释放锁并等待\n        }\n        printf(\"Consumed: %d\\n\", data);\n        ready = 0;\n        pthread_mutex_unlock(&mutex);\n    }\n    return NULL;\n}\n\n// 读写锁示例\npthread_rwlock_t rwlock = PTHREAD_RWLOCK_INITIALIZER;\n\n// 读者\nvoid* reader(void* arg) {\n    pthread_rwlock_rdlock(&rwlock);\n    // 读操作（多个读者可同时执行）\n    pthread_rwlock_unlock(&rwlock);\n    return NULL;\n}\n\n// 写者\nvoid* writer(void* arg) {\n    pthread_rwlock_wrlock(&rwlock);\n    // 写操作（独占）\n    pthread_rwlock_unlock(&rwlock);\n    return NULL;\n}",
      "language": "c",
      "highlights": [
        {
          "line": 11,
          "note": "这里体现状态推进或遍历过程，需要说明终止条件、复杂度和异常输入。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "结论：线程同步机制 要先说清它解决什么问题，再展开机制、边界和验证方式。掌握互斥锁、条件变量、读写锁、自旋锁等同步原语的原理和适用场景\n\n我会这样回答：\n1. 先定位核心问题：互斥锁阻塞等待 vs 自旋锁忙等的区别。\n2. 再串起关键机制：读写锁允许多读单写的特性，并说明它和 条件变量必须与互斥锁配合使用 的关系。\n3. 接着补充边界和风险：只给出结论，不能说明互斥锁阻塞等待 vs 自旋锁忙等的区。\n4. 验证 线程同步机制 时，我会把 操作系统、线程同步 的关键现象、日志指标、测试结果和失败样本放在一起交叉确认。\n\n如果继续追问 线程同步机制，我会用 操作系统、线程同步、互斥锁 的真实场景串起输入、调度状态、失败表现和恢复动作。",
      "followUpQuestions": [
        {
          "question": "用户态的互斥锁底层是怎么实现的？为什么说它‘无竞争时很快’？",
          "answer": "Linux 上 pthread_mutex 底层基于 futex（Fast Userspace muTEX）。无竞争时，加锁/解锁只是用户态的一条原子 CAS 操作改一个内存值，根本不进内核，所以非常快；只有当 CAS 发现锁已被占用、确实需要阻塞时，才通过 futex 系统调用陷入内核把线程挂起，等解锁方再 futex 唤醒。这种‘快路径走用户态、慢路径才进内核’的设计，避免了每次加解锁都付出系统调用开销，这也是为什么轻度竞争下互斥锁开销很低。"
        },
        {
          "question": "自旋锁在单核 CPU 上为什么基本没意义，甚至有害？",
          "answer": "自旋锁的前提是‘持锁线程正在另一个核上跑、很快会释放’，所以原地忙等比睡眠唤醒划算。单核上只有一个执行流：当前线程在自旋等锁，持锁线程根本得不到 CPU 去释放锁，自旋就是纯浪费——它会一直空转直到时间片耗尽被抢占，反而拖慢了持锁线程的执行。所以单核（或持锁时间不确定、可能较长）的场景应该用会让出 CPU 的互斥锁，而不是自旋锁。内核里用自旋锁也通常配合关抢占/关中断来保证持锁段极短。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能解释互斥锁的阻塞等待机制",
        "能说明自旋锁的忙等原理和适用场景",
        "能解释读写锁在读多写少场景下的优势",
        "能说明条件变量与互斥锁配合使用的模式",
        "能解释为什么条件等待要用 while 而不是 if",
        "能说明信号量的 P/V 操作",
        "能区分原子操作和锁的适用场景"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "os.process-thread.topic-thread-sync.recall.1",
      "prompt": "请比较互斥锁、读写锁、条件变量、信号量和原子操作的使用场景。",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "os.process-thread.topic-thread-sync.recall.2",
      "prompt": "条件变量为什么要和互斥锁配合使用？wait 操作内部做了什么？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "os.process-thread.topic-thread-sync.recall.3",
      "prompt": "线程同步机制 和相邻概念或常见替代方案有什么关键区别？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "互斥锁阻塞等待 vs 自旋锁忙等的区别",
      "读写锁允许多读单写的特性",
      "条件变量必须与互斥锁配合使用",
      "条件等待必须用 while 循环防止虚假唤醒"
    ],
    "goodToHave": [
      "CAS 原子操作的实现原理",
      "读写锁的写者饥饿问题",
      "Linux futex 的实现原理"
    ],
    "commonMistakes": [
      "只列出 线程同步、互斥锁、条件变量，但说不出触发条件、状态变化和验证证据",
      "把「线程同步机制」的适用场景、失效条件和相邻概念混在一起",
      "遇到操作系统与 Linux 排查里的真实问题时，不能说明「线程同步机制」应该观察哪些状态、日志或指标"
    ],
    "scoreWeights": {
      "coverage": 35,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "updatedAt": "2026-06-12",
  "status": "staging",
  "prerequisites": [
    "os.process-thread.topic-process-vs-thread"
  ],
  "interviewFrequency": "medium",
  "interviewerFocus": "考察是否能围绕「线程同步机制」说清互斥锁阻塞等待 vs 自旋锁忙等的区别、读写锁允许多读单写的特性，并放到操作系统与 Linux 排查场景中解释关键流程、边界条件和出错后的处理思路。"
}
