{
  "id": "java.spring.topic-041-d2d1cd02",
  "domain": "java",
  "category": "spring",
  "group": "spring",
  "title": "自动装配原理",
  "summary": "自动装配（Auto-Configuration）是SpringBoot的核心特性之一。它的本质是：**根据classpath中的依赖、已有的Bean定义、配置属性等条件，自动配置Spring应",
  "tags": [
    "Java",
    "Spring生态",
    "自动装配原理"
  ],
  "difficulty": 4,
  "estimatedMinutes": 30,
  "order": 50,
  "recommendWeight": 99,
  "learningCards": [
    {
      "type": "explain",
      "title": "条件装配与自动配置",
      "content": "# 自动装配原理\n\n## 一、什么是自动装配\n自动装配（Auto-Configuration）是SpringBoot的核心特性之一。它的本质是：**根据classpath中的依赖、已有的Bean定义、配置属性等条件，自动配置Spring应用所需的Bean**。开发者只需要引入Starter依赖，SpringBoot就能自动完成大量配置工作，真正做到\"约定优于配置\"。"
    },
    {
      "type": "explain",
      "title": "spring.factories与配置加载",
      "content": "## 二、@EnableAutoConfiguration 原理\n\n### 2.1 注解结构\n```java\n@Target(ElementType.TYPE)\n@Retention(RetentionPolicy.RUNTIME)\n@Documented\n@Inherited\n@AutoConfigurationPackage                    // 1. 自动配置包\n@Import(AutoConfigurationImportSelector.class) // 2. 导入自动配置选择器\npublic @interface EnableAutoConfiguration {\n    Class<?>[] exclude() default {};\n    String[] excludeName() default {};\n}\n```\n\n### 2.2 @AutoConfigurationPackage\n`AutoConfigurationPackages.Registrar` 会将主类所在的包路径注册到Spring容器中，后续MyBatis-Plus等框架可以通过 `AutoConfigurationPackages.get()` 获取到扫描包路径。\n\n### 2.3 AutoConfigurationImportSelector\n这是自动装配的核心类，实现了 `DeferredImportSelector` 接口：\n```java\npublic class AutoConfigurationImportSelector implements DeferredImportSelector {\n    @Override\n    public String[] selectImports(AnnotationMetadata annotationMetadata) {\n        if (!isEnabled(annotationMetadata)) {\n            return NO_IMPORTS;\n        }\n    AutoConfigurationEntry autoConfigurationEntry = getAutoConfigurationEntry(annotationMetadata);\n    return StringUtils.toStringArray(autoConfigurationEntry.getConfigurations());\n}\nprotected AutoConfigurationEntry getAutoConfigurationEntry(AnnotationMetadata annotationMetadata) {\n    // 1. 获取@EnableAutoConfiguration注解的属性（exclude等）\n    AnnotationAttributes attributes = getAttributes(annotationMetadata);\n    // 2. 从META-INF/spring.factories获取所有自动配置类 ★\n    List<String> configurations = getCandidateConfigurations(annotationMetadata, attributes);\n    // 3. 去重\n    configurations = removeDuplicates(configurations);\n    // 4. 获取需要排除的配置类\n    Set<String> exclusions = getExclusions(annotationMetadata, attributes);\n    configurations.removeAll(exclusions);\n    // 5. 根据条件注解过滤 ★\n    configurations = getConfigurationClassFilter().filter(configurations);\n    // 6. 发布事件并返回\n    fireAutoConfigurationImportEvents(configurations, exclusions);\n    return new AutoConfigurationEntry(configurations, exclusions);\n}\n}\n```\n\n## 三、spring.factories 机制\n\n### 3.1 什么是spring.factories\n`spring.factories` 是Spring SPI机制的实现文件，位于每个jar包的 `META-INF/` 目录下。\n```properties\n# META-INF/spring.factories 示例（spring-boot-autoconfigure包中）\norg.springframework.boot.autoconfigure.EnableAutoConfiguration=\\\norg.springframework.boot.autoconfigure.aop.AopAutoConfiguration,\\\norg.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\\\norg.springframework.boot.autoconfigure.web.servlet.WebMvcAutoConfiguration,\\\norg.springframework.boot.autoconfigure.redis.RedisAutoConfiguration\n```\n\n### 3.2 加载过程\n```java\n// SpringFactoriesLoader.loadFactoryNames()\npublic static List<String> loadFactoryNames(Class<?> factoryType, ClassLoader classLoader) {\n    String factoryTypeName = factoryType.getName();\n    return loadSpringFactories(classLoader).getOrDefault(factoryTypeName, Collections.emptyList());\n}\nprivate static Map<String, List<String>> loadSpringFactories(ClassLoader classLoader) {\n    // 从所有jar的META-INF/spring.factories中加载\n    Enumeration<URL> urls = classLoader.getResources(\"META-INF/spring.factories\");\n    Map<String, List<String>> result = new LinkedHashMap<>();\n    while (urls.hasMoreElements()) {\n        URL url = urls.nextElement();\n        Properties properties = PropertiesLoaderUtils.loadProperties(new UrlResource(url));\n        for (Map.Entry<?, ?> entry : properties.entrySet()) {\n            String factoryTypeName = ((String) entry.getKey()).trim();\n            String[] factoryImplementationNames =\n            StringUtils.commaDelimitedListToStringArray((String) entry.getValue());\n            for (String name : factoryImplementationNames) {\n                result.computeIfAbsent(factoryTypeName, key -> new ArrayList<>())\n                .add(name.trim());\n            }\n    }\n}\nreturn result;\n}\n```\n\n### 3.3 SpringBoot 2.7+ 的新变化\n从SpringBoot 2.7开始，推荐使用新文件替代 `spring.factories`：\n```\n# META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports\norg.springframework.boot.autoconfigure.aop.AopAutoConfiguration\norg.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration\norg.springframework.boot.autoconfigure.web.servlet.WebMvcAutoConfiguration\n```\n> **注意**：`spring.factories` 中的 `EnableAutoConfiguration` 在SpringBoot 3.0中已被移除，必须使用新的imports文件。"
    },
    {
      "type": "explain",
      "title": "条件注解（@Conditional系列）",
      "content": "## 四、条件注解（@Conditional系列）\n\n### 4.2 条件注解使用示例\n```java\n@Configuration\n@ConditionalOnClass(DataSource.class)  // classpath中有DataSource类才生效\n@EnableConfigurationProperties(DataSourceProperties.class)\n@AutoConfigureBefore(DataSourceTransactionManagerAutoConfiguration.class)\npublic class DataSourceAutoConfiguration {\n    @Configuration\n    @ConditionalOnMissingBean({DataSource.class, XADataSource.class})\n    @Import({DataSourceConfiguration.Hikari.class, DataSourceConfiguration.Tomcat.class})\n    protected static class PooledDataSourceConfiguration {\n    }\n}\n```\n\n### 4.3 自定义条件注解\n```java\n// 1. 定义条件类\npublic class MyCondition implements Condition {\n    @Override\n    public boolean matches(ConditionContext context, AnnotatedTypeMetadata metadata) {\n        Environment environment = context.getEnvironment();\n        return environment.containsProperty(\"my.custom.enabled\")\n        && \"true\".equals(environment.getProperty(\"my.custom.enabled\"));\n    }\n}\n// 2. 使用自定义条件\n@Configuration\n@Conditional(MyCondition.class)\npublic class MyCustomConfiguration {\n    @Bean\n    public MyService myService() {\n        return new MyService();\n    }\n}\n```\n\n### 4.4 @ConditionalOnMissingBean 的作用\n这是自动装配最重要的条件注解，保证了**用户的配置优先于自动配置**：\n```java\n// SpringBoot自动配置的DataSource\n@Configuration\n@ConditionalOnClass(DataSource.class)\npublic class DataSourceAutoConfiguration {\n    @Bean\n    @ConditionalOnMissingBean  // ★ 如果用户已定义DataSource Bean，则不创建\n    public DataSource dataSource(DataSourceProperties properties) {\n        return DataSourceBuilder.create()\n        .url(properties.getUrl())\n        .username(properties.getUsername())\n        .password(properties.getPassword())\n        .build();\n    }\n}\n// 用户自定义的DataSource（优先级更高）\n@Configuration\npublic class MyDataSourceConfig {\n    @Bean\n    public DataSource dataSource() {\n        HikariDataSource ds = new HikariDataSource();\n        ds.setJdbcUrl(\"jdbc:mysql://localhost:3306/mydb\");\n        return ds;\n    }\n}\n```\n\n## 五、自定义Starter\n\n### 5.1 Starter命名规范\n- 官方Starter：`spring-boot-starter-{name}`（如 `spring-boot-starter-web`）\n- 自定义Starter：`{name}-spring-boot-starter`（如 `mybatis-spring-boot-starter`）\n\n### 5.2 自定义Starter的组成\n一个完整的Starter通常由两个模块组成：\n- `my-spring-boot-starter`：starter模块（空的，只引入autoconfigure依赖）\n- `my-spring-boot-starter-autoconfigure`：自动配置模块（核心逻辑）\n\n### 5.3 实现步骤\n**第一步：定义配置属性类**\n```java\n@ConfigurationProperties(prefix = \"my.service\")\npublic class MyServiceProperties {\n    private String name = \"default\";\n    private int timeout = 3000;\n    private boolean enabled = true;\n    // getter/setter\n}\n```\n**第二步：定义核心服务类**\n```java\npublic class MyService {\n    private final MyServiceProperties properties;\n    public MyService(MyServiceProperties properties) {\n        this.properties = properties;\n    }\npublic String execute() {\n    return \"Hello from MyService, name=\" + properties.getName();\n}\n}\n```\n**第三步：定义自动配置类**\n```java\n@AutoConfiguration  // SpringBoot 2.7+ 推荐\n@ConditionalOnClass(MyService.class)\n@EnableConfigurationProperties(MyServiceProperties.class)\npublic class MyServiceAutoConfiguration {\n    @Bean\n    @ConditionalOnMissingBean  // 用户可以覆盖\n    public MyService myService(MyServiceProperties properties) {\n        return new MyService(properties);\n    }\n}\n```\n**第四步：注册自动配置类**\n```\n# META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports\ncom.example.starter.MyServiceAutoConfiguration\n```\n**第五步：使用自定义Starter**\n```yaml\n# application.yml\nmy:\nservice:\nname: myApp\ntimeout: 5000\nenabled: true\n```\n```java\n@RestController\npublic class MyController {\n    @Autowired\n    private MyService myService;\n    @GetMapping(\"/test\")\n    public String test() {\n        return myService.execute();\n    }\n}\n```\n\n## 六、面试高频问题\n\n### Q1: SpringBoot自动装配的原理是什么？\n**答**：核心是 `@EnableAutoConfiguration` 注解，它通过 `@Import(AutoConfigurationImportSelector.class)` 导入配置选择器。`AutoConfigurationImportSelector` 通过 `SpringFactoriesLoader` 从所有jar包的 `META-INF/spring.factories` 文件中加载 `EnableAutoConfiguration` 对应的配置类，然后通过 `@Conditional` 系列条件注解过滤，只保留满足条件的配置类注册到容器中。\n\n### Q2: @ConditionalOnMissingBean的作用是什么？\n**答**：保证**用户的配置优先于自动配置**。当容器中已经存在指定类型的Bean时，带有 `@ConditionalOnMissingBean` 的Bean不会被创建。这是SpringBoot\"约定优于配置\"理念的重要体现。\n\n### Q3: spring.factories和AutoConfiguration.imports有什么区别？\n**答**：\n- `META-INF/spring.factories`：SpringBoot 2.6及之前使用的SPI机制\n- `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`：SpringBoot 2.7引入的新方式\n- SpringBoot 3.0移除了对 `spring.factories` 中 `EnableAutoConfiguration` 的支持，必须使用新的imports文件\n\n### Q4: 如何自定义一个Starter？\n**答**：需要以下步骤：\n1. 创建自动配置模块：定义配置属性类（`@ConfigurationProperties`）、核心服务类、自动配置类（`@AutoConfiguration` + `@Conditional`）\n2. 注册配置类：在 `AutoConfiguration.imports` 中声明\n3. 创建Starter模块：引入autoconfigure依赖\n4. 命名规范：`{name}-spring-boot-starter`\n\n### Q5: 自动装配和手动配置的区别？什么时候需要手动配置？\n**答**：\n- **自动装配**：SpringBoot根据classpath和条件注解自动配置，无需手动编写配置代码\n- **手动配置**：开发者通过 `@Configuration` + `@Bean` 显式定义Bean\n- **需要手动配置的场景**：需要高度定制化的Bean（如自定义数据源、自定义线程池）；自动配置不满足需求；集成第三方库没有提供Starter"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | SpringBoot自动装配 | Spring手动配置 |\n| --- | --- | --- |\n| 配置方式 | 引入starter + 条件注解自动生效 | 手动编写@Configuration + @Bean |\n| 条件控制 | @ConditionalOnClass/OnBean/OnProperty等 | 无条件控制，全部加载 |\n| 配置来源 | META-INF/spring.factories（2.x）/ META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports（3.x） | 开发者自己编写的配置类 |\n| 学习成本 | 引入依赖即可，几乎零配置 | 需要了解每个框架的配置细节 |\n| 灵活性 | 可通过exclude/属性覆盖自定义 | 完全可控但代码量大 |\n| 调试难度 | 需要理解条件注解匹配过程 | 配置一目了然 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "format": "mermaid",
      "content": "flowchart TD\nA[EnableAutoConfiguration] --> B[AutoConfigurationImportSelector]\nB --> C[从 META-INF 加载候选配置类]\nC --> D[条件注解过滤 OnClass/OnBean/OnProperty]\nD --> E[Spring Boot 3.x imports 文件替代 spring.factories]",
      "caption": "自动装配：@EnableAutoConfiguration 经 Selector 加载候选类，按条件注解过滤。",
      "fallback": "自动装配：@EnableAutoConfiguration → Selector → META-INF 候选类 → 条件注解过滤。",
      "items": [
        "@EnableAutoConfiguration触发AutoConfigurationImportSelector",
        "AutoConfigurationImportSelector从META-INF加载候选配置类列表",
        "条件注解过滤：@ConditionalOnClass判断classpath有无指定类",
        "@ConditionalOnBean/OnMissingBean判断容器中是否已有指定Bean",
        "@ConditionalOnProperty通过配置属性控制是否生效",
        "配置类排序：@AutoConfigureBefore/@AutoConfigureAfter控制顺序",
        "Spring Boot 3.x变化：imports文件替代spring.factories"
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "@Target(ElementType.TYPE)\n@Retention(RetentionPolicy.RUNTIME)\n@Documented\n@Inherited\n@AutoConfigurationPackage                    // 1. 自动配置包\n@Import(AutoConfigurationImportSelector.class) // 2. 导入自动配置选择器\npublic @interface EnableAutoConfiguration {\n    Class<?>[] exclude() default {};\n    String[] excludeName() default {};\n}",
      "language": "java"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "**Q1：SpringBoot自动装配的原理是什么？**\n自动装配的核心入口是`@EnableAutoConfiguration`，它通过`@Import(AutoConfigurationImportSelector.class)`导入选择器。`AutoConfigurationImportSelector`会从`META-INF/spring.factories`（2.x）或`META-INF/spring/...AutoConfiguration.imports`（3.x）加载所有候选自动配置类，然后通过**条件注解**（@ConditionalOnClass、@ConditionalOnBean等）过滤，只保留当前classpath和配置条件下需要的配置类。这些配置类中定义的@Bean就会被注册到容器中。\n\n**Q2：常用的条件注解有哪些？**\n核心条件注解：①`@ConditionalOnClass`——classpath中存在指定类时生效，比如有DataSource类才配置数据源；②`@ConditionalOnMissingBean`——容器中没有指定Bean时才创建，允许用户自定义覆盖默认配置；③`@ConditionalOnProperty`——配置文件中有指定属性时生效；④`@ConditionalOnBean`——容器中有指定Bean时生效。组合使用可以实现\"有依赖才配置、没自定义才用默认\"的智能装配。\n\n**Q3：如何自定义一个Starter？**\n步骤：①创建`xxx-spring-boot-autoconfigure`模块，编写自动配置类（加`@Configuration`和条件注解）；②创建`xxx-spring-boot-starter`模块，只引入autoconfigure模块的依赖；③在autoconfigure模块的`META-INF/spring.factories`中注册自动配置类（2.x）或在`META-INF/spring/...imports`文件中注册（3.x）；④可选：定义`@ConfigurationProperties`类绑定配置属性，让用户可以通过application.yml自定义参数。",
      "followUpQuestions": [
        {
          "question": "Spring Boot自动装配的原理是怎样的？",
          "answer": "自动装配流程：1. @EnableAutoConfiguration导入AutoConfigurationImportSelector；2. 读取META-INF/spring.factories中的自动配置类；3. 通过@Conditional系列注解过滤；4. 注册符合条件的Bean。核心注解：@ConditionalOnClass、@ConditionalOnMissingBean、@ConditionalOnProperty。"
        },
        {
          "question": "如何自定义一个Starter？",
          "answer": "自定义Starter步骤：1. 创建autoconfigure模块，编写配置类和@Conditional条件；2. 创建starter模块，依赖autoconfigure；3. 在META-INF/spring.factories中注册配置类；4. 提供配置元数据（spring-configuration-metadata.json）。命名规范：第三方xxx-spring-boot-starter，官方spring-boot-starter-xxx。"
        },
        {
          "question": "@Conditional系列注解有哪些？工作原理是怎样的？",
          "answer": "常用@Conditional：OnClass（类路径存在）、OnMissingBean（Bean不存在）、OnProperty（配置属性）、OnWebApplication（Web环境）、OnExpression（SpEL表达式）。工作原理：实现Condition接口，重写matches方法，Spring容器在解析BeanDefinition时调用条件判断。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能否说清自动装配从@EnableAutoConfiguration到Bean注册的完整链路",
        "能否列举4个以上常用条件注解及其作用",
        "能否解释@ConditionalOnMissingBean为什么能让用户配置覆盖默认配置",
        "能否说清spring.factories文件的作用和格式",
        "能否说明SpringBoot 3.x自动装配的变化（imports文件替代factories）",
        "能否解释自动装配的配置类排序机制",
        "能否说明如何排查自动装配不生效的问题（debug日志、条件评估报告）"
      ]
    }
  ],
  "recallPrompts": [
    {
      "prompt": "@EnableAutoConfiguration 注解的加载流程是什么？spring.factories 和 AutoConfiguration.imports 有什么区别？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2,
      "id": "java.spring.topic-041-d2d1cd02.recall.1"
    },
    {
      "prompt": "如何自定义一个Starter？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2,
      "id": "java.spring.topic-041-d2d1cd02.recall.2"
    }
  ],
  "rubric": {
    "mustHave": [
      "能准确解释自动装配的核心概念、作用和适用边界",
      "能说清 自动装配 的核心机制和关键流程",
      "能否说清自动装配从@EnableAutoConfiguration到Bean注册的完整链路"
    ],
    "goodToHave": [
      "能说出 自动装配 的源码关键类和扩展点",
      "能解释 自动装配 与 Spring 生态其他组件的协作关系"
    ],
    "commonMistakes": [
      "不清楚@Autowired的注入顺序（先类型再名称）",
      "不了解@Conditional系列注解的作用",
      "混淆BeanFactory和ApplicationContext的区别"
    ],
    "scoreWeights": {
      "coverage": 25,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 25
    }
  },
  "updatedAt": "2026-06-11",
  "status": "staging",
  "prerequisites": [],
  "interviewFrequency": "high",
  "interviewerFocus": "考察是否能围绕「自动装配原理」说清能准确解释自动装配的核心概念、作用和适用边界、能说清 自动装配 的核心机制和关键流程，并放到Java 工程场景中解释关键流程、边界条件和出错后的处理思路。"
}
