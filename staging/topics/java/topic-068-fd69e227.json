{
  "id": "java.database.topic-068-fd69e227",
  "domain": "java",
  "category": "database",
  "group": "database",
  "title": "锁机制",
  "summary": "MySQL 中的锁用于控制并发访问，保证数据一致性。不同存储引擎支持不同的锁粒度。",
  "tags": [
    "Java",
    "数据库",
    "锁机制"
  ],
  "difficulty": 4,
  "estimatedMinutes": 15,
  "order": 30,
  "recommendWeight": 85,
  "learningCards": [
    {
      "type": "explain",
      "title": "知识全景",
      "content": "# 锁机制\n## 1. 锁的分类\nMySQL 中的锁用于控制并发访问，保证数据一致性。不同存储引擎支持不同的锁粒度。\n### 要点\n- **按粒度**：表锁、页锁（BDB引擎）、行锁（InnoDB）\n- **按类型**：共享锁（S锁/读锁）、排他锁（X锁/写锁）、意向锁（IS/IX）\n- **行锁细分**：Record Lock（记录锁）、Gap Lock（间隙锁）、Next-Key Lock（临键锁）\n- **按思想**：乐观锁（CAS/版本号）、悲观锁（数据库锁）\n- MyISAM 只支持表锁，InnoDB 支持行锁（默认）+ 表锁\n### 锁分类总览\n```\nMySQL 锁\n├── 按粒度\n│   ├── 表锁：锁整张表，并发度低，开销小\n│   ├── 页锁：锁一页数据（BDB引擎，已淘汰）\n│   └── 行锁：锁一行数据，并发度高，开销大\n├── 按类型\n│   ├── 共享锁（S）：读锁，多个事务可同时持有\n│   ├── 排他锁（X）：写锁，独占，其他事务不能读写\n│   └── 意向锁（IS/IX）：表级锁，标识事务打算加行级S/X锁\n└── 行锁细分（InnoDB）\n├── Record Lock：锁住索引记录\n├── Gap Lock：锁住索引记录之间的间隙\n└── Next-Key Lock：Record Lock + Gap Lock\n```\n## 2. InnoDB行锁实现\n### 要点\n- InnoDB 行锁是通过给**索引记录**加锁实现的，不是锁行数据\n- 如果 SQL 没有使用索引，InnoDB 会退化为表锁（因为需要扫描所有行）\n- **Record Lock（记录锁）**：锁住索引记录本身，阻止其他事务修改/删除\n- **Gap Lock（间隙锁）**：锁住索引记录之间的间隙，阻止其他事务在间隙中插入\n- **Next-Key Lock（临键锁）**：Record Lock + Gap Lock，锁住记录及其前面的间隙\n- InnoDB 默认使用 Next-Key Lock，在可重复读级别下防止幻读\n### 加锁规则（RR级别）\n```\nNext-Key Lock = Record Lock + Gap Lock\n加锁规则：\n1. 等值查询（唯一索引）：命中 → Record Lock；未命中 → Gap Lock\n2. 等值查询（非唯一索引）：命中 → Next-Key Lock + 后一个 Gap Lock\n3. 范围查询（唯一索引）：对范围内的记录加 Next-Key Lock\n4. 范围查询（非唯一索引）：对范围内的记录加 Next-Key Lock\n```\n### 示例/代码\n```sql\n-- 表结构和数据\nCREATE TABLE t (\nid INT PRIMARY KEY,\nage INT,\nname VARCHAR(10),\nINDEX idx_age (age)\n) ENGINE=InnoDB;\n-- 数据: id=[1,5,10,15,20], age=[10,20,30,40,50]\n-- 1. Record Lock 示例\n-- 事务A\nSELECT * FROM t WHERE id = 5 FOR UPDATE;\n-- 加 Record Lock: 锁住 id=5 的记录\n-- 事务B 无法修改/删除 id=5，但可以插入 id=6\n-- 2. Gap Lock 示例\n-- 事务A\nSELECT * FROM t WHERE id = 8 FOR UPDATE;\n-- id=8 不存在，加 Gap Lock: 锁住 (5, 10) 的间隙\n-- 事务B 无法插入 id=6,7,8,9 的记录\n-- 3. Next-Key Lock 示例\n-- 事务A\nSELECT * FROM t WHERE age >= 20 AND age < 40 FOR UPDATE;\n-- 加 Next-Key Lock: (10,20], (20,30], (30,40]\n-- 事务B 无法在这些范围内插入新记录\n-- 4. 非唯一索引的加锁\n-- 事务A\nSELECT * FROM t WHERE age = 30 FOR UPDATE;\n-- 在 idx_age 上加 Next-Key Lock: (20, 30]\n-- 加 Gap Lock: (30, 40]\n-- 在主键上加 Record Lock: id=10\n```"
    },
    {
      "type": "explain",
      "title": "深入理解",
      "content": "## 3. 表锁\n### 要点\n- **表锁**：锁住整张表，开销小但并发度低\n- **表锁类型**：表读锁（LOCK TABLES t READ）、表写锁（LOCK TABLES t WRITE）\n- **意向锁（IS/IX）**：表级锁，标识事务打算在表中加行级S锁或X锁\n- 意向锁之间不冲突，意向锁与表锁的兼容性需要单独判断\n- MDL（Metadata Lock）：DDL操作自动加MDL读锁，DML操作自动加MDL写锁\n### 意向锁作用\n```\n场景：事务A想加表锁，如何快速判断表中是否有行锁？\n没有意向锁：需要逐行扫描检查 → 效率低\n有意向锁：事务B加行锁前先加意向锁 → 事务A检查意向锁即可 → O(1)\n```\n## 4. 乐观锁与悲观锁\n### 要点\n- **悲观锁**：假设会冲突，先加锁再操作。数据库的S锁/X锁都是悲观锁\n- **乐观锁**：假设不冲突，提交时检查是否被修改。通过版本号或CAS实现\n- 乐观锁适用于读多写少场景，悲观锁适用于写多冲突多场景\n### 乐观锁实现\n```sql\n-- 版本号方式\n-- 1. 查询时获取版本号\nSELECT id, name, version FROM product WHERE id = 1;  -- version = 1\n-- 2. 更新时检查版本号\nUPDATE product SET name = 'new_name', version = version + 1\nWHERE id = 1 AND version = 1;\n-- 3. 如果 affected_rows = 0，说明被其他事务修改，需要重试\n```"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 对比项 | Record Lock | Gap Lock | Next-Key Lock |\n| --- | --- | --- | --- |\n| 锁定对象 | 索引记录本身 | 索引记录之间的间隙 | 记录 + 前面的间隙 |\n| 锁定范围 | 精确一行 | 开区间（不包含记录） | 左开右闭区间 |\n| 防止操作 | 修改/删除该记录 | 在间隙中插入 | 修改/删除/插入 |\n| 触发条件 | 等值查询命中唯一索引 | 等值查询未命中记录 | 范围查询、非唯一索引等值查询 |\n| 并发影响 | 最小（只锁一行） | 较大（锁住间隙范围） | 较大 |\n| RR级别默认 | 否 | 否 | 是（默认锁类型） |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "InnoDB行锁的三种类型和加锁规则。",
      "fallback": "InnoDB行锁通过给索引记录加锁实现。Record Lock锁记录本身，Gap Lock锁间隙防插入，Next-Key Lock=Record+Gap。无索引时退化为表锁。",
      "items": [
        "行锁本质：给索引记录加锁，不是锁行数据 → SQL不走索引时退化为表锁",
        "Record Lock：锁住id=5这一条记录 → 其他事务不能UPDATE/DELETE id=5 → 可以INSERT id=6",
        "Gap Lock：id=8不存在 → 锁住(5,10)间隙 → 其他事务不能INSERT id=6,7,8,9",
        "Next-Key Lock：范围查询age>=20 AND age<40 → 锁住(10,20],(20,30],(30,40] → 防止幻读",
        "非唯一索引加锁：WHERE age=30 → idx_age加Next-Key Lock(20,30] + Gap Lock(30,40] + 主键Record Lock",
        "意向锁：事务加行级X锁前先加IX表锁 → 其他事务想加表锁时检查IX即可（O(1)）",
        "乐观锁：UPDATE时检查version → affected_rows=0则说明被修改需重试 → 适用于读多写少"
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "SELECT * FROM t WHERE id = 5 FOR UPDATE;\n-- 加 Record Lock: 锁住 id=5 的记录\n-- 事务B 无法修改/删除 id=5，但可以插入 id=6\n-- 2. Gap Lock 示例\n-- 事务A\nSELECT * FROM t WHERE id = 8 FOR UPDATE;\n-- id=8 不存在，加 Gap Lock: 锁住 (5, 10) 的间隙\n-- 事务B 无法插入 id=6,7,8,9 的记录\n-- 3. Next-Key Lock 示例\n-- 事务A\nSELECT * FROM t WHERE age >= 20 AND age < 40 FOR UPDATE;\n-- 加 Next-Key Lock: (10,20], (20,30], (30,40]\n-- 事务B 无法在这些范围内插入新记录\n-- 4. 非唯一索引的加锁\n-- 事务A",
      "language": "sql"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "**Q1：InnoDB的行锁有哪几种？分别什么作用？**\n\nA：InnoDB有三种行锁。Record Lock锁住索引记录本身，阻止其他事务修改或删除这条记录。Gap Lock锁住索引记录之间的间隙，阻止其他事务在间隙中插入新记录，主要用来防止幻读。Next-Key Lock是Record Lock和Gap Lock的组合，锁住记录及其前面的间隙，左开右闭区间。InnoDB在可重复读级别下默认使用Next-Key Lock。行锁是通过给索引记录加锁实现的，如果SQL没走索引，会退化为表锁。\n\n**Q2：什么情况下行锁会退化为表锁？**\n\nA：当SQL没有使用索引时，InnoDB无法确定要锁哪些行，只能对所有行加锁，相当于表锁。比如`UPDATE t SET name='x' WHERE age=30`，如果age列没有索引，InnoDB会扫描全表，对每一行都加锁。所以加锁的列一定要有索引，否则并发性能急剧下降。另外即使有索引，如果MySQL优化器判断全表扫描更快（比如数据量很小），也可能不走索引导致表锁。\n\n**Q3：乐观锁和悲观锁怎么选？**\n\nA：悲观锁假设会冲突，先加锁再操作，适合写多冲突多的场景，比如`SELECT ... FOR UPDATE`。乐观锁假设不冲突，提交时检查版本号或CAS，适合读多写少的场景。实现上悲观锁用数据库的`FOR UPDATE`或`LOCK IN SHARE MODE`，乐观锁在表中加version字段，UPDATE时带`WHERE version=old_version`，如果affected_rows=0说明被修改了需要重试。高并发秒杀场景推荐乐观锁，因为悲观锁会长时间持有锁导致其他事务等待。",
      "followUpQuestions": [
        {
          "question": "在实际项目中，你是怎么应用锁机制来优化查询性能的？能举一个具体的优化案例吗？",
          "answer": "MySQL锁层次：全局锁→表锁→行锁。InnoDB行锁：Record Lock（记录锁）、Gap Lock（间隙锁）、Next-Key Lock。加锁规则：等值查询唯一索引命中→Record Lock；未命中→Gap Lock；范围查询→Next-Key Lock。死锁排查：SHOW ENGINE INNODB STATUS查看死锁日志。"
        },
        {
          "question": "锁 在不同存储引擎（InnoDB vs MyISAM）下的实现有什么区别？",
          "answer": "InnoDB 和 MyISAM 在 锁 上有本质区别：InnoDB 支持事务、行级锁、MVCC，适合高并发写入；MyISAM 只有表级锁但读性能好。当前主流都是 InnoDB，MyISAM 主要用于只读或读多写少的场景。"
        },
        {
          "question": "如果 锁 相关的查询变慢了，你会从哪些方面排查和优化？",
          "answer": "排查慢查询的思路：先用 EXPLAIN 分析执行计划，关注 type、key、rows、Extra 等字段。常见原因包括索引失效（函数/隐式转换/前导%）、数据量过大、锁等待等。优化手段有加索引、改写 SQL、分页优化、读写分离等。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "行锁三种类型（Record/Gap/Next-Key Lock）的锁定范围和作用",
        "行锁基于索引记录加锁，无索引时退化为表锁",
        "RR级别下Next-Key Lock防止幻读的机制",
        "意向锁（IS/IX）的作用：快速判断表中是否有行锁",
        "乐观锁（版本号/CAS）和悲观锁（FOR UPDATE）的适用场景",
        "非唯一索引等值查询的加锁规则（Next-Key Lock + Gap Lock + 主键Record Lock）",
        "MDL锁的作用和DDL操作可能引起的锁等待问题"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "java.database.topic-068-fd69e227.recall.1",
      "prompt": "锁机制 的核心原理是什么？如何通过执行计划或日志来分析问题？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "java.database.topic-068-fd69e227.recall.2",
      "prompt": "锁机制 在高并发场景下有哪些优化策略？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    }
  ],
  "rubric": {
    "mustHave": [
      "能准确解释乐观/悲观、公平/非公平、可重入、读写锁等分类和适用场景",
      "说清要点和锁分类总览的关键机制",
      "能说清 锁 的核心机制和关键流程",
      "行锁三种类型（Record/Gap/Next-Key Lock）的锁定范围和作用"
    ],
    "goodToHave": [
      "能用 EXPLAIN 分析 锁 相关查询的执行计划",
      "能说出 锁 在不同存储引擎和数据规模下的表现差异"
    ],
    "commonMistakes": [
      "不清楚Record Lock、Gap Lock、Next-Key Lock的区别",
      "不了解加锁规则（等值查询 vs 范围查询）",
      "不知道如何排查死锁（SHOW ENGINE INNODB STATUS）"
    ],
    "scoreWeights": {
      "coverage": 25,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 25
    }
  },
  "updatedAt": "2026-05-27",
  "status": "staging",
  "prerequisites": [],
  "interviewFrequency": "high",
  "interviewerFocus": "考察数据库索引、事务、锁、MVCC 和 SQL/存储层性能排查能力。"
}
