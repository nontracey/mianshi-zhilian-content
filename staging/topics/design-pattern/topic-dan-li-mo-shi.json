{
  "id": "design-pattern.creational.topic-dan-li-mo-shi",
  "domain": "design-pattern",
  "category": "creational",
  "group": "creational",
  "title": "单例模式",
  "summary": "确保一个类只有一个实例，并提供全局访问点，是Java中最基础的设计模式之一。",
  "tags": [
    "设计模式",
    "创建型模式",
    "单例模式"
  ],
  "difficulty": 3,
  "estimatedMinutes": 10,
  "order": 10,
  "recommendWeight": 95,
  "status": "staging",
  "learningCards": [
    {
      "type": "explain",
      "title": "五种写法：线程安全与懒加载的取舍",
      "content": "单例靠三件套实现：私有构造（堵外部 `new`）、静态变量持有唯一实例、`getInstance()` 提供全局入口。五种写法本质是在权衡两件事——**何时创建**（启动建/用时建）和**多线程下如何保证只建一个**：\n\n- **饿汉式**：类加载就 `new`，JVM 保证类只加载一次，天然线程安全；代价是没用到也占资源、无法延迟。\n- **懒汉式**：用时才建，省资源，但 `if(null) new` 在多线程下会各建一个，**线程不安全**。\n- **加 synchronized**：给整个方法加锁能修复，但每次获取都抢锁，而锁只在第一次创建有意义，高并发下浪费、慢。\n- **双重检查锁 DCL**：判空→进同步块→再判空，只在创建那刻同步。`instance` **必须加 `volatile`**：`new` 分“分配内存→初始化→赋引用”三步可能被重排，不加 volatile 别的线程可能拿到引用非空但**没初始化完的半成品**。\n- **静态内部类**：实例放进 `Holder` 静态内部类，第一次调 `getInstance()` 才触发加载——懒加载 + 类加载机制保证线程安全 + 无锁开销，最常用。\n- **枚举**：JVM 保证实例唯一，天然防反射和序列化破坏，《Effective Java》首推。"
    },
    {
      "type": "explain",
      "title": "选型、防反射/序列化与 Spring 单例的区别",
      "content": "**怎么选**：创建开销小且一定会用→饿汉式或枚举；需要懒加载→静态内部类（最省心）或枚举；DCL 更多是面试考点（考 volatile 与指令重排），实际项目少用。\n\n**为什么普通写法会被破坏**：\n- 反射：`setAccessible(true)` 能调用私有构造造出新实例。\n- 序列化：反序列化绕过构造函数生成新对象（可写 `readResolve()` 返回已有实例防护）。\n- **枚举对这两种攻击天然免疫**，这就是它“最安全”的根本原因。\n\n**和 Spring 单例的区别（高频混淆）**：Spring 的“单例 Bean”是**容器级**单例——一个容器一个、靠 `ConcurrentHashMap` 缓存；GoF 单例是**类级**单例——一个 JVM 一个。不是一回事。\n\n**何时别用**：单例是全局状态，过度使用会隐藏依赖、难以单测（无法 mock）；分布式下“单例”只在单 JVM 成立，跨节点并不唯一。"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "// 1. 饿汉式\npublic class SingletonHungry {\n    private static final SingletonHungry INSTANCE = new SingletonHungry();\n    private SingletonHungry() {}\n    public static SingletonHungry getInstance() {\n        return INSTANCE;\n    }\n}\n\n// 2. 懒汉式（线程不安全）\npublic class SingletonLazyUnsafe {\n    private static SingletonLazyUnsafe instance;\n    private SingletonLazyUnsafe() {}\n    public static SingletonLazyUnsafe getInstance() {\n        if (instance == null) {\n            instance = new SingletonLazyUnsafe();\n        }\n        return instance;\n    }\n}\n\n// 3. 双重检查锁（DCL）\npublic class SingletonDCL {\n    private volatile static SingletonDCL instance;\n    private SingletonDCL() {}\n    public static SingletonDCL getInstance() {\n        if (instance == null) {\n            synchronized (SingletonDCL.class) {\n                if (instance == null) {\n                    instance = new SingletonDCL();\n                }\n            }\n        }\n        return instance;\n    }\n}\n\n// 4. 静态内部类\npublic class SingletonInner {\n    private SingletonInner() {}\n    private static class Holder {\n        private static final SingletonInner INSTANCE = new SingletonInner();\n    }\n    public static SingletonInner getInstance() {\n        return Holder.INSTANCE;\n    }\n}\n\n// 5. 枚举\npublic enum SingletonEnum {\n    INSTANCE;\n    public void doSomething() {\n        // 业务方法\n    }\n}",
      "language": "java"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "单例模式确保一个类只有一个实例。在Java中有5种实现方式：\n\n第一种是饿汉式，类加载时就创建实例，优点是简单线程安全，缺点是可能浪费资源。\n\n第二种是懒汉式，第一次使用时才创建，但需要加synchronized保证线程安全，性能较差。\n\n第三种是双重检查锁DCL，通过volatile禁止指令重排序，两次null检查减少同步开销，是常用的懒加载方案。\n\n第四种是静态内部类，利用JVM类加载机制，内部类在第一次被访问时才加载，既实现了懒加载又保证了线程安全。\n\n第五种是枚举，这是《Effective Java》推荐的方式，天然防止反射攻击和序列化破坏，是最安全的实现。\n\n在实际项目中，如果单例创建开销不大，推荐使用饿汉式；如果需要懒加载，推荐静态内部类或枚举。Spring框架中Bean默认就是单例的，通过容器管理生命周期。",
      "followUpQuestions": [
        {
          "question": "在Spring框架中，单例模式有哪些经典应用？能结合源码说说吗？",
          "answer": "单例在Spring中的应用：Bean默认单例，通过BeanFactory缓存。DCL需要volatile的原因：new对象分三步（分配→初始化→赋值），JVM可能重排。枚举单例最安全：天然防反射和序列化破坏。Effective Java推荐枚举方式。"
        },
        {
          "question": "Spring 的单例 Bean 和 GoF 的单例模式是一回事吗？",
          "answer": "不是。GoF 单例是类级单例——靠私有构造把“一个 JVM 一个实例”写死在类内部；Spring 单例是容器级单例——Bean 本身是普通类，由容器用 ConcurrentHashMap 缓存，保证“一个容器一个实例”，换个容器或自己 new 仍能有多个。所以 Spring 单例是管理策略，不是 GoF 那种语言层面的强制唯一。"
        },
        {
          "question": "枚举单例为什么能防住反射和序列化攻击，而其他写法不行？",
          "answer": "反射方面：Constructor.newInstance 对枚举类型会直接抛 IllegalArgumentException，JVM 禁止反射创建枚举实例；普通单例的私有构造则能被 setAccessible(true) 调用造出新对象。序列化方面：枚举序列化只写枚举名，反序列化按名字返回已有实例，不会 new；普通单例反序列化会绕过构造函数生成新对象，需要自己写 readResolve 返回已有实例才能防住。"
        }
      ]
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 维度 | 饿汉式 | 懒汉式 | 双重检查锁 | 静态内部类 | 枚举 |\n| ------ | -------- | -------- | ------------ | ------------ | ------ |\n| 线程安全 | 是 | 否（需加锁） | 是（volatile） | 是 | 是 |\n| 懒加载 | 否 | 是 | 是 | 是 | 否 |\n| 防反射攻击 | 否 | 否 | 否 | 否 | 是 |\n| 防序列化破坏 | 否 | 否 | 否 | 否 | 是 |\n| 实现复杂度 | 简单 | 简单 | 中等 | 中等 | 简单 |\n| 性能 | 高 | 低 | 中 | 高 | 高 |\n| 推荐场景 | 资源开销小 | 不推荐 | 需要懒加载 | 需要懒加载 | 最佳实践 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "单例模式的核心结构包含三个要素：私有构造函数、静态实例变量、静态获取方法。",
      "fallback": "Singleton类图：-Singleton类，-instance: Singleton，-Singleton()，+getInstance(): Singleton",
      "items": [
        "1. 私有构造函数防止外部new",
        "2. 静态变量持有唯一实例",
        "3. 公有静态方法提供全局访问点",
        "4. 双重检查锁中volatile防止指令重排序",
        "5. 静态内部类利用类加载机制"
      ],
      "asset": "assets/diagrams/22-singleton-pattern.svg"
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "5种单例实现方式的区别和适用场景",
        "为什么双重检查锁需要volatile关键字",
        "枚举单例为什么是最安全的实现方式",
        "如何防护反射攻击和序列化破坏",
        "JDK中哪些类使用了单例模式"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "design-pattern.creational.topic-dan-li-mo-shi.recall.1",
      "prompt": "如果面试官问你：为什么双重检查锁中instance变量需要volatile修饰？不加volatile会有什么问题？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "design-pattern.creational.topic-dan-li-mo-shi.recall.2",
      "prompt": "假设你在一个高并发系统中需要实现一个配置管理器，要求全局唯一且支持懒加载，你会选择哪种单例实现方式？为什么？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "5种单例写法及区别",
      "双重检查锁为什么要volatile",
      "枚举单例为什么最安全",
      "反射攻击如何防护",
      "JDK中的单例实例"
    ],
    "goodToHave": [
      "Spring中单例Bean的实现原理",
      "单例模式在分布式环境下的考虑"
    ],
    "commonMistakes": [
      "DCL单例忘记volatile导致指令重排问题",
      "以为Spring的单例和GoF单例实现方式相同",
      "枚举单例为什么最安全说不清楚"
    ],
    "scoreWeights": {
      "coverage": 35,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "prerequisites": [
    "design-pattern.principles.topic-solid-yuan-ze"
  ],
  "interviewFrequency": "high",
  "interviewerFocus": "考察是否能围绕「单例模式」说清5种单例写法及区别、双重检查锁为什么要volatile，并放到设计模式场景中解释关键流程、边界条件和出错后的处理思路。",
  "updatedAt": "2026-06-11"
}
