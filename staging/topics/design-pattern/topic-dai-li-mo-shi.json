{
  "id": "design-pattern.structural.topic-dai-li-mo-shi",
  "domain": "design-pattern",
  "category": "structural",
  "group": "structural",
  "title": "代理模式",
  "summary": "讲清代理模式的访问控制目的、静态/JDK/CGLIB 三种实现及限制、Spring AOP 选型，以及自调用失效和与装饰器的区别。",
  "tags": [
    "设计模式",
    "结构型模式",
    "代理模式",
    "动态代理"
  ],
  "difficulty": 3,
  "estimatedMinutes": 20,
  "order": 10,
  "recommendWeight": 95,
  "status": "staging",
  "learningCards": [
    {
      "type": "explain",
      "title": "三种实现：静态 → JDK 动态 → CGLIB",
      "content": "代理的目的：在**不改原对象代码**的前提下控制/增强对它的访问（权限、事务、缓存、延迟加载、日志、远程调用）。做法是造一个和真实对象同接口的代理，客户端面向接口调用，代理在调用前后插逻辑再转发——这正是 Spring AOP 的底层机制。三种实现是逐步解决“代理类太多”的过程：\n\n- **静态代理**：手写一个实现相同接口的代理类，方法里包裹增强再调真实对象。问题：每个要代理的类都得手写一个，方法多了逐个包裹，重复难维护。\n- **JDK 动态代理**：`Proxy.newProxyInstance` + `InvocationHandler`，运行时生成实现目标接口的代理类，所有调用统一进 `invoke()`。不用手写。**限制：目标必须有接口**。\n- **CGLIB**：用字节码生成**目标类的子类**重写方法插逻辑，**不需要接口**；但靠继承，**无法代理 final 类和 final/private 方法**。"
    },
    {
      "type": "explain",
      "title": "Spring AOP 选型、自调用坑与和装饰器的区别",
      "content": "## Spring AOP 怎么选\n- 目标有接口 → 默认 JDK 动态代理；无接口 → CGLIB。\n- Spring Boot 2.x 起默认把 `proxyTargetClass` 设为 true，**统一用 CGLIB**，避免“注入接口类型却拿到 CGLIB 代理”之类的困惑。\n\n## 自调用失效的坑（高频）\n同一个类里方法 A 直接调用本类的方法 B，走的是 `this` 而不是代理对象，**B 上的 @Transactional/@Cacheable 不生效**。解法：注入自身代理、或拆到另一个 Bean。\n\n## 和装饰器模式的区别\n结构相似（都包一层、同接口），但意图不同：**代理**重在“**控制访问**”（要不要让你调、调之前做什么），代理通常自己管理真实对象的生命周期；**装饰器**重在“**增强功能**”，由外部把被装饰对象传进来、可层层叠加。\n\n## 边界\n代理会带来一层间接调用，增加调试难度和少量性能开销；只在确实需要横切增强或访问控制时使用。"
    },
    {
      "type": "diagram",
      "format": "mermaid",
      "title": "代理模式三种实现",
      "content": "flowchart TD\nA[\"客户端面向接口调用\"] --> B[\"代理对象：调用前后插增强\"]\nB --> C[\"转发给真实对象\"]\nB --> D[\"静态代理：手写，每类一个\"]\nB --> E[\"JDK 动态代理：基于接口，invoke 统一处理\"]\nB --> F[\"CGLIB：生成子类，无需接口但不能代理 final\"]",
      "caption": "代理模式：代理与真实对象同接口，客户端调代理、代理在前后插增强再转发；三种实现是静态代理（手写）、JDK 动态代理（基于接口）、CGLIB（生成子类、无需接口）。",
      "fallback": "客户端面向接口调用代理对象，代理在调用前后插增强逻辑再转发给真实对象；三种实现：静态代理（手写、每类一个）、JDK 动态代理（基于接口、invoke 统一处理）、CGLIB（生成子类、无需接口但不能代理 final）。"
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 维度 | 静态代理 | JDK动态代理 | CGLIB动态代理 |\n| ------ | ---------- | ------------ | --------------- |\n| 实现方式 | 编译时 | 运行时反射 | 运行时字节码 |\n| 代理基础 | 接口 | 接口 | 继承 |\n| 性能 | 高 | 中 | 中 |\n| 灵活性 | 低 | 高 | 高 |\n| 是否需要接口 | 是 | 是 | 否 |\n| 能否代理final | 能 | 能 | 不能 |\n| Spring应用 | 手动实现 | AOP默认 | 无接口时 |\n| 典型场景 | 事务管理 | AOP切面 | 无接口类代理 |"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "// 1. 静态代理\npublic interface UserService {\n    void save();\n}\npublic class UserServiceImpl implements UserService {\n    @Override\n    public void save() {\n        System.out.println(\"保存用户\");\n    }\n}\npublic class UserServiceProxy implements UserService {\n    private UserService target;\n    \n    public UserServiceProxy(UserService target) {\n        this.target = target;\n    }\n    \n    @Override\n    public void save() {\n        System.out.println(\"开启事务\");\n        target.save();\n        System.out.println(\"提交事务\");\n    }\n}\n\n// 2. JDK动态代理\npublic class JdkProxyHandler implements InvocationHandler {\n    private Object target;\n    \n    public JdkProxyHandler(Object target) {\n        this.target = target;\n    }\n    \n    @Override\n    public Object invoke(Object proxy, Method method, Object[] args) throws Throwable {\n        System.out.println(\"JDK代理前置处理\");\n        Object result = method.invoke(target, args);\n        System.out.println(\"JDK代理后置处理\");\n        return result;\n    }\n}\n// 使用\nUserService target = new UserServiceImpl();\nUserService proxy = (UserService) Proxy.newProxyInstance(\n    target.getClass().getClassLoader(),\n    target.getClass().getInterfaces(),\n    new JdkProxyHandler(target)\n);\n\n// 3. CGLIB动态代理\npublic class CglibInterceptor implements MethodInterceptor {\n    @Override\n    public Object intercept(Object obj, Method method, Object[] args, MethodProxy methodProxy) throws Throwable {\n        System.out.println(\"CGLIB代理前置处理\");\n        Object result = methodProxy.invokeSuper(obj, args);\n        System.out.println(\"CGLIB代理后置处理\");\n        return result;\n    }\n}\n// 使用\nEnhancer enhancer = new Enhancer();\nenhancer.setSuperclass(UserServiceImpl.class);\nenhancer.setCallback(new CglibInterceptor());\nUserServiceImpl proxy = (UserServiceImpl) enhancer.create();",
      "language": "java",
      "highlights": [
        {
          "line": 39,
          "note": "这里给出最终结果或提前退出条件，需要补充空值、失败和边界场景。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "结论：代理模式是在不改原对象代码的前提下，造一个和真实对象同接口的代理，客户端面向接口调用，代理在调用前后插逻辑再转发——用来做权限、事务、缓存、延迟加载、日志、远程调用等访问控制和增强。这正是 Spring AOP 的底层机制。\n\n三种实现是逐步解决「代理类太多」的过程：\n\n1. 静态代理：手写一个实现相同接口的代理类，方法里包裹增强再调真实对象。问题是每个类都得手写一个、重复难维护。\n2. JDK 动态代理：Proxy.newProxyInstance 加 InvocationHandler，运行时生成实现目标接口的代理类，所有调用统一进 invoke()。不用手写，但目标必须有接口。\n3. CGLIB：用字节码生成目标类的子类、重写方法插逻辑，不需要接口；但靠继承，无法代理 final 类和 final/private 方法。\n\nSpring AOP 选型：目标有接口默认 JDK 动态代理、无接口用 CGLIB；Spring Boot 2.x 起默认 proxyTargetClass=true 统一用 CGLIB，避免「注入接口类型却拿到 CGLIB 代理」的困惑。\n\n高频坑——自调用失效：同一个类里 A 直接调本类的 B，走的是 this 不是代理对象，B 上的 @Transactional 或 @Cacheable 不生效。解法是注入自身代理或拆到另一个 Bean。\n\n和装饰器模式的区别（常考）：结构相似（都包一层、同接口），但意图不同——代理重在「控制访问」（要不要让你调、调之前做什么）、通常自己管理真实对象；装饰器重在「增强功能」、由外部把被装饰对象传进来、可层层叠加。",
      "followUpQuestions": [
        {
          "question": "在Spring框架中，代理模式有哪些经典应用？能结合源码说说吗？",
          "answer": "代理模式在Spring AOP中的应用：JDK动态代理（基于接口）和CGLIB（基于继承）。Spring Boot 2.x后默认全用CGLIB。MyBatis的Mapper也是通过JDK动态代理实现。性能差异：JDK代理调用快但创建慢，CGLIB创建快但调用略慢。"
        },
        {
          "question": "为什么同一个类里方法 A 调方法 B，B 上的 @Transactional 会失效？怎么解决？",
          "answer": "因为 Spring 的事务是基于代理实现的：外部调用先进代理、再转发给真实对象，代理才有机会开启事务。而 A 内部直接调 B 走的是 this 引用，绕过了代理对象，B 上的事务/缓存注解自然不生效。解决办法：把 B 拆到另一个 Bean 再注入调用；或注入自身代理（AopContext.currentProxy() 或自注入）来调 B；本质都是让调用重新经过代理。"
        },
        {
          "question": "JDK 动态代理和 CGLIB 在原理和限制上有什么区别，Spring 怎么选？",
          "answer": "JDK 动态代理基于接口，运行时生成实现目标接口的代理类，要求目标必须有接口；CGLIB 基于继承，生成目标类的子类来重写方法，不需要接口，但无法代理 final 类和 final/private 方法。性能上 JDK 代理创建快、早期调用稍慢，CGLIB 反之，现代 JVM 下差距很小。Spring 默认：有接口用 JDK、无接口用 CGLIB；Spring Boot 2.x 起默认 proxyTargetClass=true 统一用 CGLIB。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "JDK动态代理的实现原理",
        "CGLIB动态代理的实现原理",
        "两种动态代理的区别及选择标准",
        "Spring AOP如何选择代理方式",
        "代理模式与装饰器模式的区别"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "design-pattern.structural.topic-dai-li-mo-shi.recall.1",
      "prompt": "面试官问你：Spring AOP是如何选择使用JDK动态代理还是CGLIB的？在什么情况下会强制使用CGLIB？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "design-pattern.structural.topic-dai-li-mo-shi.recall.2",
      "prompt": "假设你需要为一个没有接口的第三方类添加日志功能，你会选择哪种代理方式？如何实现？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "design-pattern.structural.topic-dai-li-mo-shi.recall.3",
      "prompt": "JDK 动态代理和 CGLIB 的实现机制差异是什么？Spring 在什么情况下分别选择哪一个？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "能说出代理模式的目的：不改原对象做访问控制和增强",
      "能区分静态代理、JDK 动态代理、CGLIB 及各自限制",
      "能说明 Spring AOP 的代理选型",
      "能讲清自调用导致 @Transactional 失效"
    ],
    "goodToHave": [
      "能说明 JDK 动态代理要求接口、CGLIB 不能代理 final",
      "能解释 Spring Boot 2.x 默认 CGLIB",
      "能区分代理模式与装饰器模式的意图"
    ],
    "commonMistakes": [
      "分不清静态、JDK、CGLIB 三种代理的限制",
      "自调用导致事务/缓存注解失效",
      "把代理模式和装饰器模式混为一谈"
    ],
    "scoreWeights": {
      "coverage": 35,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "prerequisites": [
    "design-pattern.principles.topic-solid-yuan-ze"
  ],
  "interviewFrequency": "high",
  "interviewerFocus": "考察能否说清代理的目的、区分三种实现及限制、说明 Spring AOP 选型，并解释自调用失效和代理与装饰器的区别。",
  "updatedAt": "2026-06-12"
}
