{
  "id": "python.web-python.flask-fastapi",
  "domain": "python",
  "category": "web-python",
  "group": "web-python",
  "title": "Flask 与 FastAPI",
  "summary": "讲清 Flask（WSGI 同步）与 FastAPI（ASGI 异步）的本质区别、FastAPI 的 Pydantic/Depends/自动文档，以及选型和 async 路由的坑。",
  "tags": [
    "Python",
    "Flask",
    "FastAPI",
    "Web框架",
    "ASGI",
    "Pydantic"
  ],
  "difficulty": 3,
  "estimatedMinutes": 20,
  "order": 190,
  "recommendWeight": 85,
  "learningCards": [
    {
      "type": "explain",
      "title": "Flask 与 FastAPI：WSGI vs ASGI、Flask 核心机制",
      "content": "## 一、WSGI vs ASGI\n\n| 协议 | 说明 | 框架 |\n| --- | --- | --- |\n| WSGI | 同步 Web 网关接口（标准） | Flask、Django |\n| ASGI | 异步 Web 网关接口（Python 3.5+） | FastAPI、Starlette |\n\nASGI 支持 WebSocket、SSE、HTTP/2 等长连接，WSGI 只能处理 HTTP 请求-响应。\n\n## 二、Flask 核心机制\n\n### 路由与视图\n```python\nfrom flask import Flask, request, jsonify\n\napp = Flask(__name__)\n\n@app.route(\"/api/users/<int:user_id>\", methods=[\"GET\"])\ndef get_user(user_id):\n    return jsonify({\"id\": user_id, \"name\": \"Alice\"})\n```\n\n### 请求上下文\nFlask 通过上下文（Context）实现线程安全的全局访问：\n- `request`：当前请求对象\n- `current_app`：当前应用实例\n- `g`：请求级别的全局变量\n- `session`：用户会话\n\n### 蓝图（Blueprint）\n模块化路由组织：\n```python\nfrom flask import Blueprint\nuser_bp = Blueprint(\"user\", __name__)\n\n@user_bp.route(\"/users\")\ndef list_users():\n    ...\n\napp.register_blueprint(user_bp, url_prefix=\"/api\")\n```"
    },
    {
      "type": "explain",
      "title": "Flask 与 FastAPI：自动 API 文档、Pydantic 模型",
      "content": "### 自动 API 文档\nFastAPI 基于 OpenAPI 规范，自动生成 Swagger UI 和 ReDoc 文档。\n\n### Pydantic 模型\n```python\nfrom fastapi import FastAPI\nfrom pydantic import BaseModel\n\nclass UserCreate(BaseModel):\n    name: str\n    email: str\n    age: int | None = None\n\napp = FastAPI()\n\n@app.post(\"/users\")\nasync def create_user(user: UserCreate):\n    # 自动验证请求体\n    return {\"id\": 1, **user.model_dump()}\n```\n\n### 依赖注入\n```python\nfrom fastapi import Depends, HTTPException\n\ndef get_current_user(token: str = Header(...)):\n    user = verify_token(token)\n    if user is None:\n        raise HTTPException(status_code=401)\n    return user\n\n@app.get(\"/profile\")\nasync def profile(user: User = Depends(get_current_user)):\n    return user\n```\n\n### 异步原生\nFastAPI 基于 Starlette，原生支持 async/await，但同步函数也能正常工作（在独立线程池中运行）。\n\n## 四、性能对比\n\n| 对比项 | Flask | FastAPI |\n| --- | --- | --- |\n| 协议 | WSGI（同步） | ASGI（异步） |\n| 自动文档 | 需安装 flasgger | ✅ 内置 Swagger/ReDoc |\n| 请求验证 | 手动 marshmallow | ✅ Pydantic 自动验证 |\n| 依赖注入 | 不支持 | ✅ 内置 Depends |\n| WebSocket | 需安装扩展 | ✅ 原生支持 |\n| 性能 | 中 | 高（Uvicorn 异步） |\n| 学习曲线 | 低 | 中 |\n| 适用场景 | 传统 Web 应用 | API 服务、高并发 |"
    },
    {
      "type": "diagram",
      "format": "mermaid",
      "title": "Flask 与 FastAPI：机制分支与风险路径",
      "content": "flowchart TD\nNomnh4tS[Flask 与 FastAPI 触发入口] --> Nomnh4tM[核心对象或状态: Python]\nNomnh4tM --> Nomnh4tC{Flask 与 FastAPI 是否走正常路径}\nNomnh4tC -->|正常| Nomnh4tN[执行 Flask 与 FastAPI 主流程]\nNomnh4tC -->|异常| Nomnh4tF[处理 Flask 与 FastAPI 边界风险]\nNomnh4tN --> Nomnh4tO[输出结果或交给下一组件]\nNomnh4tF --> Nomnh4tR[按 Flask 与 FastAPI 语义降级/重试/报错]\nNomnh4tO --> Nomnh4tV[验证 Flask 与 FastAPI 日志、指标或测试]\nNomnh4tR --> Nomnh4tV",
      "caption": "Flask 与 FastAPI 图中的分支用于区分本题正常机制、异常边界和工程验证路径。",
      "fallback": "Flask 与 FastAPI 的图示按本题触发入口、核心状态、正常路径、失败路径和验证手段展开，重点看分支关系。",
      "sources": [
        {
          "kind": "mermaid",
          "content": "flowchart TD\nNomnh4tS[Flask 与 FastAPI 触发入口] --> Nomnh4tM[核心对象或状态: Python]\nNomnh4tM --> Nomnh4tC{Flask 与 FastAPI 是否走正常路径}\nNomnh4tC -->|正常| Nomnh4tN[执行 Flask 与 FastAPI 主流程]\nNomnh4tC -->|异常| Nomnh4tF[处理 Flask 与 FastAPI 边界风险]\nNomnh4tN --> Nomnh4tO[输出结果或交给下一组件]\nNomnh4tF --> Nomnh4tR[按 Flask 与 FastAPI 语义降级/重试/报错]\nNomnh4tO --> Nomnh4tV[验证 Flask 与 FastAPI 日志、指标或测试]\nNomnh4tR --> Nomnh4tV"
        },
        {
          "kind": "text",
          "content": "Flask 与 FastAPI 的图示按本题触发入口、核心状态、正常路径、失败路径和验证手段展开，重点看分支关系。"
        }
      ]
    },
    {
      "type": "code",
      "title": "代码/伪代码抓手",
      "content": "# 1. FastAPI CRUD 示例\nfrom fastapi import FastAPI, HTTPException, Depends\nfrom pydantic import BaseModel\nfrom typing import List\n\napp = FastAPI(title=\"User API\")\n\ndb: List[dict] = []\n\nclass UserCreate(BaseModel):\n    name: str\n    email: str\n\nclass UserResponse(BaseModel):\n    id: int\n    name: str\n    email: str\n\n@app.get(\"/users\", response_model=List[UserResponse])\nasync def list_users():\n    return db\n\n@app.post(\"/users\", response_model=UserResponse, status_code=201)\nasync def create_user(user: UserCreate):\n    new_user = {\"id\": len(db) + 1, **user.model_dump()}\n    db.append(new_user)\n    return new_user\n\n@app.get(\"/users/{user_id}\", response_model=UserResponse)\nasync def get_user(user_id: int):\n    for user in db:\n        if user[\"id\"] == user_id:\n            return user\n    raise HTTPException(status_code=404)\n\n# 2. Flask 蓝图示例\nfrom flask import Flask, Blueprint, jsonify\n\napp = Flask(__name__)\napi = Blueprint(\"api\", __name__, url_prefix=\"/api\")\n\n@api.route(\"/health\")\ndef health():\n    return jsonify({\"status\": \"ok\"})\n\napp.register_blueprint(api)\n\n# 3. FastAPI 依赖注入 + 数据库会话\nfrom sqlalchemy.ext.asyncio import create_async_engine\n\nasync def get_db():\n    async with async_session() as session:\n        yield session\n\n@app.get(\"/items\")\nasync def get_items(db=Depends(get_db)):\n    result = await db.execute(...)\n    return result.scalars().all()",
      "language": "python",
      "highlights": [
        {
          "line": 10,
          "note": "`class UserCreate(BaseModel):` 定义了 Flask 与 FastAPI 示例中承载状态和操作的代码位置，复述时从参数和状态字段讲起。"
        },
        {
          "line": 31,
          "note": "`for user in db:` 承担主要遍历过程，时间复杂度通常由这层循环和内部状态更新共同决定。"
        },
        {
          "line": 32,
          "note": "`if user[\"id\"] == user_id:` 是分支判断，决定正常路径、边界输入或失败路径走向。"
        },
        {
          "line": 26,
          "note": "`db.append(new_user)` 更新核心数据结构，是解释状态如何推进的抓手。"
        }
      ]
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答思路",
      "content": "结论：Flask 和 FastAPI 最本质的区别是底层协议——Flask 是 WSGI（同步），FastAPI 是 ASGI（异步、原生 async/await）。FastAPI 还自带 Pydantic 数据校验、依赖注入和自动 OpenAPI 文档，更适合做现代 API；Flask 轻量灵活，适合传统 Web 应用和小项目。\n\n讲清几点：\n\n1. WSGI vs ASGI：WSGI 只能处理同步的请求-响应；ASGI 支持 async、WebSocket、SSE、长连接，这是 FastAPI 高并发能力的根。\n2. Flask 核心：用上下文（request、current_app、g、session）实现线程内的全局访问，用蓝图 Blueprint 做模块化路由。\n3. FastAPI 核心：基于 Starlette 加 Pydantic，类型注解直接用于请求校验和序列化，Depends 做依赖注入，自动生成 Swagger 和 ReDoc 文档。\n\n边界和坑。① FastAPI 里写了 async def 路由就不能在里面调阻塞函数（同步 DB、requests），否则卡住事件循环，要么用异步库、要么把路由写成普通 def（FastAPI 会丢到线程池跑）。② Flask 默认单线程同步，高并发靠 gunicorn 多 worker；FastAPI 配 uvicorn 异步 worker。③ 选型：重 IO、高并发、要自动文档和校验选 FastAPI；生态成熟、团队熟悉、偏传统渲染选 Flask。",
      "followUpQuestions": [
        {
          "question": "Flask 的请求上下文（request context）和 FastAPI 的依赖注入在测试时有什么不同？",
          "answer": "Flask 测试需要 `with app.test_request_context()` 手动推送上下文，否则 request 对象不可用。FastAPI 使用 Depends，可以用 `app.dependency_overrides` 直接替换所有依赖，不需要模拟上下文。这使得 FastAPI 的测试更加直接和可预测。"
        },
        {
          "question": "FastAPI 性能瓶颈通常在哪里？怎么优化？",
          "answer": "常见瓶颈：1) CPU 密集型处理（任务会阻塞事件循环——用 BackgroundTasks 或 Celery 分流）；2) 数据库查询慢（加索引、用连接池）；3) 序列化开销大（用 `response_model` 让 FastAPI 优化序列化）。可以用 `gunicorn -k uvicorn.workers.UvicornWorker` 多 worker 提升吞吐。"
        }
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "能解释 WSGI 和 ASGI 的区别",
        "能写出 Flask 蓝图的基本用法",
        "能写出 FastAPI 路由和 Pydantic 模型",
        "能使用 FastAPI 依赖注入处理认证和数据库",
        "能利用 FastAPI 自动文档调试 API",
        "能对比 Flask 和 FastAPI 的适用场景",
        "能理解 FastAPI 同步/异步路由的处理差异"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "python.web-python.flask-fastapi.recall.1",
      "prompt": "Flask 和 FastAPI 的核心差异有哪些？什么场景选 Flask，什么场景选 FastAPI？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    },
    {
      "id": "python.web-python.flask-fastapi.recall.2",
      "prompt": "用 FastAPI 实现一个简单的用户 CRUD API，包括请求验证和自动文档。",
      "mode": "code",
      "expectedMinutes": 8,
      "difficulty": 3
    },
    {
      "id": "python.web-python.flask-fastapi.recall.3",
      "prompt": "FastAPI 的高性能来自哪里？Pydantic 在请求校验链路里扮演什么角色？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 3
    }
  ],
  "rubric": {
    "mustHave": [
      "能说出 Flask=WSGI 同步、FastAPI=ASGI 异步的本质区别",
      "能讲清 FastAPI 的 Pydantic 校验、Depends 依赖注入、自动文档",
      "能说明 Flask 的上下文（request/g）和蓝图",
      "能给出两者的选型场景"
    ],
    "goodToHave": [
      "能说明 ASGI 支持 WebSocket/SSE 等长连接",
      "能指出 async 路由里调阻塞函数会卡事件循环",
      "能说出 gunicorn/uvicorn 的部署差异"
    ],
    "commonMistakes": [
      "在 FastAPI 的 async 路由里调用同步阻塞 IO",
      "以为 Flask 也能原生处理 WebSocket",
      "只比性能、忽略生态和团队熟悉度"
    ],
    "scoreWeights": {
      "coverage": 25,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 25
    }
  },
  "status": "staging",
  "interviewFrequency": "high",
  "interviewerFocus": "考察能否从 WSGI/ASGI 讲到两者核心能力差异、说清 FastAPI 的校验和依赖注入、给出选型，并知道 async 路由调阻塞 IO 的坑。",
  "updatedAt": "2026-06-18",
  "prerequisites": [
    "python.engineering-python.memory-gc"
  ]
}
