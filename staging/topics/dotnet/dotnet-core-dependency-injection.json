{
  "id": "dotnet.dotnet-core.dependency-injection",
  "domain": "dotnet",
  "category": "dotnet-core",
  "group": "dotnet-core",
  "title": "依赖注入",
  "summary": "依赖注入（Dependency Injection, DI）是 .NET Core 的核心特性之一，它实现了控制反转（IoC）原则，让对象的创建和依赖管理由容器负责。",
  "tags": [
    ".NET",
    "依赖注入",
    "DI",
    "IoC",
    "生命周期"
  ],
  "difficulty": 3,
  "estimatedMinutes": 45,
  "order": 10,
  "recommendWeight": 95,
  "status": "production",
  "learningCards": [
    {
      "type": "explain",
      "title": "核心概念",
      "content": "依赖注入（Dependency Injection, DI）是 .NET Core 的核心特性之一，它实现了控制反转（IoC）原则，让对象的创建和依赖管理由容器负责。\n\n\n## 依赖注入的基本概念\n\n**依赖注入的三种方式**：\n1. **构造函数注入**（推荐）：通过构造函数传入依赖\n2. **属性注入**：通过公共属性设置依赖\n3. **方法注入**：通过方法参数传入依赖\n\n```csharp\n// 构造函数注入（推荐）\npublic class OrderService\n{\n    private readonly IOrderRepository _repository;\n    \n    public OrderService(IOrderRepository repository)\n    {\n        _repository = repository;\n    }\n}\n```"
    },
    {
      "type": "explain",
      "title": "深入理解",
      "content": "## .NET Core 内置 DI 容器\n\n.NET Core 提供了内置的 DI 容器 `IServiceCollection` 和 `IServiceProvider`：\n\n```csharp\n// 注册服务\nservices.AddScoped<IOrderRepository, OrderRepository>();\nservices.AddScoped<IOrderService, OrderService>();\n\n// 使用服务\nvar serviceProvider = services.BuildServiceProvider();\nvar orderService = serviceProvider.GetRequiredService<IOrderService>();\n```\n\n\n## 生命周期\n\n.NET Core DI 容器支持三种生命周期：\n\n**Transient（瞬态）**：每次请求都创建新实例\n```csharp\nservices.AddTransient<IService, Service>();\n```\n- 适用于轻量级、无状态的服务\n- 每次注入都是新实例\n\n**Scoped（作用域）**：每个请求/作用域内共享同一实例\n```csharp\nservices.AddScoped<IService, Service>();\n```\n- 适用于需要在同一请求内共享状态的服务\n- ASP.NET Core 中每个 HTTP 请求一个作用域\n\n**Singleton（单例）**：整个应用程序生命周期内共享同一实例\n```csharp\nservices.AddSingleton<IService, Service>();\n```\n- 适用于全局共享的服务（如配置、缓存）\n- 注意线程安全问题\n\n\n## 工厂模式注册\n\n使用工厂方法注册服务：\n```csharp\n// 简单工厂\nservices.AddTransient<IService>(provider =>\n{\n    var config = provider.GetRequiredService<IConfiguration>();\n    return new Service(config);\n});\n\n// 工厂类\nservices.AddSingleton<IServiceFactory, ServiceFactory>();\n```"
    },
    {
      "type": "explain",
      "title": "深入理解（续）",
      "content": "## 多实现注册\n\n注册同一接口的多个实现：\n```csharp\n// 注册多个实现\nservices.AddTransient<INotification, EmailNotification>();\nservices.AddTransient<INotification, SmsNotification>();\nservices.AddTransient<INotification, PushNotification>();\n\n// 注入所有实现\npublic class NotificationService\n{\n    private readonly IEnumerable<INotification> _notifications;\n    \n    public NotificationService(IEnumerable<INotification> notifications)\n    {\n        _notifications = notifications;\n    }\n    \n    public void Notify(string message)\n    {\n        foreach (var notification in _notifications)\n        {\n            notification.Send(message);\n        }\n    }\n}\n\n// 注入特定实现\nservices.AddKeyedTransient<INotification, EmailNotification>(\"email\");\nservices.AddKeyedTransient<INotification, SmsNotification>(\"sms\");\n```\n\n\n## 服务解析\n\n**GetService vs GetRequiredService**：\n```csharp\n// GetService：找不到返回 null\nvar service = serviceProvider.GetService<IService>();\n\n// GetRequiredService：找不到抛出异常\nvar service = serviceProvider.GetRequiredService<IService>();\n```\n\n**注入 IEnumerable<T>**：\n```csharp\nvar services = serviceProvider.GetServices<INotification>();\n```\n\n\n## 最佳实践\n\n1. **优先使用构造函数注入**：明确依赖关系\n2. **避免在服务定位器模式中使用**：直接注入依赖\n3. **正确选择生命周期**：根据服务特性选择 Transient/Scoped/Singleton\n4. **避免循环依赖**：重构代码消除循环依赖\n5. **使用接口而非具体类**：提高可测试性和灵活性\n\n\n## 与第三方容器集成\n\n.NET Core 支持与第三方 DI 容器集成：\n- Autofac\n- Ninject\n- StructureMap\n- Unity"
    },
    {
      "type": "code",
      "title": "代码示例",
      "content": "using System;\nusing System.Collections.Generic;\nusing Microsoft.Extensions.DependencyInjection;\n\nnamespace DependencyInjectionDemo\n{\n    // 接口定义\n    public interface IEmailService\n    {\n        void SendEmail(string to, string subject, string body);\n    }\n\n    public interface ISmsService\n    {\n        void SendSms(string phoneNumber, string message);\n    }\n\n    public interface INotificationService\n    {\n        void Notify(string message);\n    }\n\n    // 实现类\n    public class SmtpEmailService : IEmailService\n    {\n        public void SendEmail(string to, string subject, string body)\n        {\n            Console.WriteLine($\"发送邮件到 {to}: {subject}\");\n        }\n    }\n\n    public class TwilioSmsService : ISmsService\n    {\n        public void SendSms(string phoneNumber, string message)\n        {\n            Console.WriteLine($\"发送短信到 {phoneNumber}: {message}\");\n        }\n    }\n\n    // 聚合服务\n    public class NotificationService : INotificationService\n    {\n        private readonly IEmailService _emailService;\n        private readonly ISmsService _smsService;\n\n        // 构造函数注入\n        public NotificationService(IEmailService emailService, ISmsService smsService)\n        {\n            _emailService = emailService;\n            _smsService = smsService;\n        }\n\n        public void Notify(string message)\n        {\n            _emailService.SendEmail(\"user@example.com\", \"通知\", message);\n            _smsService.SendSms(\"+8613800138000\", message);\n        }\n    }\n\n    // 多实现示例\n    public interface IRepository<T>\n    {\n        T GetById(int id);\n        void Add(T entity);\n    }\n\n    public class CachedRepository<T> : IRepository<T> where T : class\n    {\n        private readonly IRepository<T> _inner;\n        private readonly Dictionary<int, T> _cache = new();\n\n        public CachedRepository(IRepository<T> inner)\n        {\n            _inner = inner;\n        }\n\n        public T GetById(int id)\n        {\n            if (!_cache.TryGetValue(id, out var entity))\n            {\n                entity = _inner.GetById(id);\n                _cache[id] = entity;\n            }\n            return entity;\n        }\n\n        public void Add(T entity)\n        {\n            _inner.Add(entity);\n        }\n    }\n\n    public class SqlRepository<T> : IRepository<T> where T : class\n    {\n        public T GetById(int id)\n        {\n            Console.WriteLine($\"从数据库获取 {typeof(T).Name} {id}\");\n            return null; // 简化示例\n        }\n\n        public void Add(T entity)\n        {\n            Console.WriteLine($\"添加 {typeof(T).Name} 到数据库\");\n        }\n    }\n\n    // 工厂模式\n    public interface IConnectionFactory\n    {\n        string CreateConnection();\n    }\n\n    public class SqlConnectionFactory : IConnectionFactory\n    {\n        private readonly string _connectionString;\n\n        public SqlConnectionFactory(string connectionString)\n        {\n            _connectionString = connectionString;\n        }\n\n        public string CreateConnection() => _connectionString;\n    }\n\n    // 生命周期演示\n    public interface IOperation\n    {\n        Guid OperationId { get; }\n    }\n\n    public interface ITransientOperation : IOperation { }\n    public interface IScopedOperation : IOperation { }\n    public interface ISingletonOperation : IOperation { }\n\n    public class Operation : ITransientOperation, IScopedOperation, ISingletonOperation\n    {\n        public Guid OperationId { get; } = Guid.NewGuid();\n    }\n\n    // 服务使用\n    public class MyService\n    {\n        private readonly ITransientOperation _transient;\n        private readonly IScopedOperation _scoped;\n        private readonly ISingletonOperation _singleton;\n\n        public MyService(\n            ITransientOperation transient,\n            IScopedOperation scoped,\n            ISingletonOperation singleton)\n        {\n            _transient = transient;\n            _scoped = scoped;\n            _singleton = singleton;\n        }\n\n        public void ShowIds()\n        {\n            Console.WriteLine($\"Transient: {_transient.OperationId}\");\n            Console.WriteLine($\"Scoped: {_scoped.OperationId}\");\n            Console.WriteLine($\"Singleton: {_singleton.OperationId}\");\n        }\n    }\n\n    class Program\n    {\n        static void Main()\n        {\n            // 配置 DI 容器\n            var services = new ServiceCollection();\n\n            // 注册服务\n            services.AddTransient<IEmailService, SmtpEmailService>();\n            services.AddTransient<ISmsService, TwilioSmsService>();\n            services.AddTransient<INotificationService, NotificationService>();\n\n            // 工厂模式注册\n            services.AddSingleton<IConnectionFactory>(provider =>\n            {\n                return new SqlConnectionFactory(\"Server=localhost;Database=MyDb\");\n            });\n\n            // 生命周期注册\n            services.AddTransient<ITransientOperation, Operation>();\n            services.AddScoped<IScopedOperation, Operation>();\n            services.AddSingleton<ISingletonOperation, Operation>();\n            services.AddTransient<MyService>();\n\n            // 构建服务提供者\n            var serviceProvider = services.BuildServiceProvider();\n\n            // 使用服务\n            var notificationService = serviceProvider.GetRequiredService<INotificationService>();\n            notificationService.Notify(\"Hello DI!\");\n\n            // 生命周期演示\n            Console.WriteLine(\"\\n=== 生命周期演示 ===\");\n            using (var scope1 = serviceProvider.CreateScope())\n            {\n                var service1 = scope1.ServiceProvider.GetRequiredService<MyService>();\n                service1.ShowIds();\n\n                Console.WriteLine(\"\\n同一作用域内：\");\n                var service2 = scope1.ServiceProvider.GetRequiredService<MyService>();\n                service2.ShowIds();\n            }\n\n            Console.WriteLine(\"\\n新作用域：\");\n            using (var scope2 = serviceProvider.CreateScope())\n            {\n                var service3 = scope2.ServiceProvider.GetRequiredService<MyService>();\n                service3.ShowIds();\n            }\n\n            // 获取所有实现\n            Console.WriteLine(\"\\n=== 多实现 ===\");\n            var services2 = new ServiceCollection();\n            services2.AddTransient<IEmailService, SmtpEmailService>();\n            // 可以注册更多实现\n            var provider2 = services2.BuildServiceProvider();\n            var emailService = provider2.GetRequiredService<IEmailService>();\n            emailService.SendEmail(\"test@test.com\", \"Test\", \"Hello\");\n        }\n    }\n}",
      "language": "csharp"
    },
    {
      "type": "interviewAnswer",
      "title": "面试回答模板",
      "content": "面试官您好，关于依赖注入，我来详细介绍一下。\n\n依赖注入是 .NET Core 的核心特性，它实现了控制反转原则。在 .NET Core 中，我们通过 IServiceCollection 注册服务，通过 IServiceProvider 解析服务。服务注册时需要指定生命周期：Transient 每次请求创建新实例，Scoped 在同一请求/作用域内共享实例，Singleton 在整个应用生命周期内共享实例。\n\nASP.NET Core 中，每个 HTTP 请求会创建一个 Scoped 作用域，所以 Scoped 服务在同一请求内是单例的。Transient 适合轻量级无状态服务，Scoped 适合需要在同一请求内共享状态的服务，Singleton 适合全局共享的服务如配置、缓存。\n\n构造函数注入是最推荐的方式，它能明确依赖关系，便于测试。属性注入和方法注入使用较少。注册服务时，可以通过工厂方法创建实例，也可以注册同一接口的多个实现，使用 IEnumerable<T> 注入所有实现。\n\n与第三方 DI 容器集成时，需要替换默认的 IServiceProvider。常用的第三方容器包括 Autofac、Ninject 等，它们提供了更丰富的功能如属性注入、拦截器等。\n\n在实际开发中，我会遵循最佳实践：优先使用构造函数注入，正确选择生命周期，避免循环依赖，使用接口而非具体类提高可测试性。",
      "followUpQuestions": [
        {
          "question": "在.NET Core/8+中，依赖注入的实现和Java有什么异同？",
          "answer": ".NET Core DI容器内置在框架中。三种生命周期：Transient、Scoped、Singleton。和Spring区别：.NET Core以构造函数注入为主，不支持属性注入（需Autofac）。Host.CreateDefaultBuilder自动注册大量框架服务。"
        },
        {
          "question": "依赖注入 在 .NET 6/7/8 的演进中有什么变化？跨平台部署时需要注意什么？",
          "answer": ".NET 版本演进带来了显著改进：.NET 6 引入了 Minimal API 和 Hot Reload，.NET 7 优化了正则和 GC，.NET 8 引入了 AOT 编译和 FrozenDictionary。跨平台部署需要注意 Windows 特有 API 的替代、文件路径差异、时区处理、原生依赖等。"
        },
        {
          "question": "如果 依赖注入 在 .NET 应用中出现了性能问题或内存泄漏，你会怎么排查？",
          "answer": "排查 .NET 性能问题的思路：用 dotnet-trace 收集运行时跟踪数据，用 dotnet-dump 分析内存转储，用 BenchmarkDotNet 做微基准测试。常见问题包括大对象堆碎片、Finalizer 队列积压、线程池饥饿、GC 暂停过长等。PerfView 和 Visual Studio Diagnostic Tools 也很有用。"
        }
      ]
    },
    {
      "type": "compareTable",
      "title": "对比与边界",
      "content": "| 维度 | Transient | Scoped | Singleton |\n| --- | --- | --- | --- |\n| 实例创建 | 每次请求 | 每个作用域 | 应用启动 |\n| 内存占用 | 高（频繁创建） | 中 | 低 |\n| 线程安全 | 无需考虑 | 请求内安全 | 需要线程安全 |\n| 典型应用 | 轻量级无状态服务 | 数据库上下文、工作单元 | 配置、缓存、日志 |\n| 作用域内共享 | 否 | 是 | 是 |\n| 跨作用域共享 | 否 | 否 | 是 |"
    },
    {
      "type": "compareTable",
      "title": "维度对比",
      "content": "| 维度 | 构造函数注入 | 属性注入 | 方法注入 |\n| --- | --- | --- | --- |\n| 依赖可见性 | 高（明确列出） | 中（可能遗漏） | 低（方法参数） |\n| 测试友好性 | 高（易于 Mock） | 中 | 低 |\n| 不可变性 | 支持（readonly） | 不支持 | 不支持 |\n| 推荐程度 | 强烈推荐 | 谨慎使用 | 特殊场景 |\n| 框架支持 | ASP.NET Core 默认 | 需要容器支持 | 需要容器支持 |"
    },
    {
      "type": "diagram",
      "title": "结构图解",
      "content": "依赖注入工作流程：\n\n1. 配置阶段\n   IServiceCollection services = new ServiceCollection();\n   services.AddScoped<IService, Service>();\n   ↓\n2. 构建容器\n   IServiceProvider provider = services.BuildServiceProvider();\n   ↓\n3. 解析服务\n   var service = provider.GetRequiredService<IService>();\n   ↓\n4. 容器创建实例\n   new Service(dependency1, dependency2)\n\n生命周期示例：\nTransient: 每次 GetService 都是新实例\nScoped: 同一作用域内 GetService 返回同一实例\nSingleton: 始终返回同一实例",
      "fallback": "依赖注入的配置、构建、解析流程图",
      "items": [
        "IServiceCollection 注册服务",
        "IServiceProvider 解析服务",
        "三种生命周期：Transient/Scoped/Singleton",
        "构造函数注入是最推荐的方式"
      ]
    },
    {
      "type": "checklist",
      "title": "学完后应能说清楚",
      "items": [
        "依赖注入的三种方式及推荐做法",
        ".NET Core 内置 DI 容器的使用方法",
        "三种生命周期的区别和适用场景",
        "如何注册和使用多实现服务",
        "工厂模式注册服务的方法",
        "依赖注入的最佳实践"
      ]
    }
  ],
  "recallPrompts": [
    {
      "id": "dotnet.dotnet-core.dependency-injection.recall.1",
      "prompt": "依赖注入 的内部实现机制是什么？和 Java 对应技术有什么异同？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    },
    {
      "id": "dotnet.dotnet-core.dependency-injection.recall.2",
      "prompt": "如何注册同一接口的多个实现？如何注入所有实现或特定实现？",
      "mode": "text",
      "expectedMinutes": 3,
      "difficulty": 2
    }
  ],
  "rubric": {
    "mustHave": [
      "理解依赖注入的概念和优势",
      "掌握 .NET Core 内置 DI 容器的使用",
      "区分三种生命周期的差异",
      "知道构造函数注入的最佳实践",
      "了解服务注册和解析的方法"
    ],
    "goodToHave": [
      "了解第三方 DI 容器（如 Autofac）",
      "理解服务定位器模式及其缺点",
      "知道如何处理循环依赖"
    ],
    "commonMistakes": [
      "不清楚三种生命周期（Transient/Scoped/Singleton）的区别",
      "Scoped服务在Singleton中使用导致问题",
      "不了解构造函数注入和属性注入的区别"
    ],
    "scoreWeights": {
      "coverage": 35,
      "accuracy": 30,
      "interviewExpression": 20,
      "depth": 15
    }
  },
  "interviewFrequency": "high",
  "interviewerFocus": "考察.NET技术栈的深度，是否理解运行时机制和框架设计，能否和Java生态做对比"
}
